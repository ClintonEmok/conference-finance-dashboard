# Phase 21 Plan 03: Remove Scope Reach Modal and Deprecated Linking

**Goal:** Clean up the inventory page by removing the Scope Reach Management modal and deprecated `attachHotelToEventByProviderId` mutation.

## Prerequisites

- Read `21-CONTEXT.md` § "Inventory Page", "Hotel Linking Architecture"
- Read `app/dashboard/accommodation/inventory/page.tsx` lines 675-748 (Scope Reach modal)
- Ensure Plans 21-01 and 21-02 are complete (new linking path exists)

## Tasks

### 1. Remove Scope Reach Management Modal

**1.1** Read inventory page:

- Locate Scope Reach Management modal code (lines 675-748)
- Understand what it does: links hotels to events via checkboxes
- Note state management and handlers

**1.2** Remove modal component:

- Delete `ScopeReachModal` or equivalent component
- Remove modal state (isOpen, selected hotels, etc.)
- Remove modal open button from UI

**1.3** Update imports:

- Remove unused imports related to Scope Reach
- Clean up any orphaned helper functions

### 2. Remove Deprecated Mutation Usage

**2.1** Find all usages of `attachHotelToEventByProviderId`:

- `app/dashboard/accommodation/inventory/page.tsx`
- `lib/convex/hooks/accommodation.ts`
- Any other files

**2.2** Remove from inventory page:

- Delete calls to `attachHotelToEventByProviderId`
- Delete event handler functions
- Delete state related to this mutation

**2.3** Update hooks file:

- Mark `useAttachHotelToEventByProviderId` as deprecated (if kept temporarily)
- Or remove entirely if no other usages

### 3. Update Inventory Page UI

**3.1** Remove "Manage Scope Reach" button:

- Find button that opens Scope Reach modal
- Remove button from hotel cards/actions

**3.2** Update hotel card actions:

- Remove or replace scope-related actions
- Keep other actions (edit, delete if available)
- Add note: "Link hotels in Event Settings"

**3.3** Update empty states:

- Remove references to Scope Reach Management
- Point users to Event Settings for linking

### 4. Mark Mutation as Deprecated in Convex

**4.1** Add deprecation notice:

```typescript
/**
 * @deprecated Use linkHotelToEvent instead.
 * This mutation will be removed in a future release.
 */
export const attachHotelToEventByProviderId = internalMutation({
  // ...
})
```

**4.2** Add runtime warning (optional):

```typescript
console.warn(
  "[DEPRECATED] attachHotelToEventByProviderId is deprecated. Use linkHotelToEvent."
)
```

### 5. Update Documentation

**5.1** Update any internal docs:

- Remove references to Scope Reach Management
- Update flow diagrams
- Note the new canonical path: Event Settings

**5.2** Update comments in code:

- Remove TODOs related to Scope Reach
- Update any misleading comments

### 6. Verify No Broken References

**6.1** Search codebase:

```bash
grep -r "attachHotelToEventByProviderId" --include="*.ts" --include="*.tsx"
grep -r "ScopeReach" --include="*.ts" --include="*.tsx"
grep -r "scope.*reach" --include="*.ts" --include="*.tsx" -i
```

**6.2** Fix any remaining references:

- Update to use new mutation
- Or remove if dead code

### 7. Regression Testing

**7.1** Test inventory page:

- Hotels load correctly
- Can still create hotels
- Can still create room types
- Can still provision rooms
- No console errors

**7.2** Test event settings:

- Can still link/unlink hotels
- All 21-02 features work
- No references to old mutation

**7.3** Test workspace:

- Room assignment still works
- Slots generated correctly
- No broken hotel links

## Success Criteria

- [ ] Scope Reach Management modal completely removed
- [ ] No references to `attachHotelToEventByProviderId` in UI code
- [ ] Inventory page works without Scope Reach features
- [ ] Event settings is the only place to link hotels
- [ ] Mutation marked deprecated in Convex
- [ ] No broken references or console errors
- [ ] All existing tests pass

## Files to Modify

- `app/dashboard/accommodation/inventory/page.tsx` - Remove modal
- `lib/convex/hooks/accommodation.ts` - Deprecate/remove hook
- `convex/accommodation.ts` - Mark mutation deprecated

## Dependencies

- Plan 21-01 complete (new mutation working)
- Plan 21-02 complete (event settings UI ready)
- Users migrated to new flow (or okay with breaking change)

## Breaking Changes

⚠️ **This is a breaking change** - users can no longer link hotels from inventory page. Ensure:

- Plan 21-02 is complete and tested
- Team is aware of the change
- Documentation is updated

## Estimated Effort

- 1 developer day

## Notes

- This is cleanup work - the heavy lifting is in 21-01 and 21-02
- Can be done incrementally: deprecate first, remove later
- Consider keeping mutation for one release cycle with warnings
