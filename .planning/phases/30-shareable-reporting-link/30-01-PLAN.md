---
phase: 30-shareable-reporting-link
plan: "01"
type: tdd
wave: 1
depends_on: []
requirements: [RPT-03]
files_modified:
  - convex/schema.ts
  - convex/reportShares.ts
  - convex/reports.ts
  - lib/domain/finance/reporting.ts
  - tests/reporting/share-link.test.ts
autonomous: true
must_haves:
  truths:
    - "A share token resolves to one read-only reporting scope and can be revoked without exposing dashboard auth."
    - "The reporting data contract contains aggregates only, with location, gender, and balance/overpayment slices."
    - "Invalid or revoked tokens fail closed."
    - "No public payload includes row-level attendee, order, or payment details."
  artifacts:
    - path: convex/schema.ts
      provides: report share token storage and lifecycle fields
    - path: convex/reportShares.ts
      provides: token creation, lookup, and revocation helpers
    - path: convex/reports.ts
      provides: read-only aggregated report query by token
    - path: lib/domain/finance/reporting.ts
      provides: canonical aggregation helpers for stakeholder slices
    - path: tests/reporting/share-link.test.ts
      provides: regression coverage for token validity and payload shape
  key_links:
    - from: convex/reportShares.ts
      to: convex/reports.ts
      via: token lookup and scope resolution
      pattern: token
    - from: convex/reports.ts
      to: lib/domain/finance/reporting.ts
      via: canonical aggregate builder
      pattern: build.*Report
    - from: lib/domain/finance/reporting.ts
      to: existing finance readers
      via: reused canonical totals and balance math
      pattern: amountDueMinor
---

<objective>
Define the share-token and reporting data contract before the public page exists so the public surface can stay read-only and aggregate-only.

Purpose: if the access model is vague, the public report risks leaking raw operational data.
Output: token storage plus a strict report payload shape that is safe to expose.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-shareable-reporting-link/30-CONTEXT.md
@convex/schema.ts
@convex/publicTracking.ts
@convex/finance.ts
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/reconciliation.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add report token storage and aggregate query</name>
  <files>convex/reportShares.ts, convex/reports.ts, convex/schema.ts</files>
  <read_first>
    @convex/publicTracking.ts
    @convex/finance.ts
    @convex/schema.ts
    @lib/domain/finance/order-ledger.ts
    @lib/domain/finance/reconciliation.ts
  </read_first>
  <action>Create a minimal share-token model that supports generating, looking up, and revoking a report link. Add a read-only report query keyed by token that returns only aggregated slices needed for stakeholder reporting: totals, grouped summaries by location and gender, and balance state / overpayment visibility. The query must not return raw rows or dashboard-only fields.</action>
  <acceptance_criteria>
    <criterion>Invalid or revoked tokens return a null/failed-closed result.</criterion>
    <criterion>The returned payload contains only aggregates and safe metadata.</criterion>
    <criterion>No helper exposes attendee/order/payment rows directly to the public report surface.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/reporting/share-link.test.ts`</verify>
  <done>The reporting contract is explicit, token-backed, and safe for a public surface.</done>
</task>

<task type="auto">
  <name>Task 2: Build the canonical reporting helper</name>
  <files>lib/domain/finance/reporting.ts</files>
  <read_first>
    @convex/reports.ts
    @lib/domain/finance/order-ledger.ts
    @lib/domain/finance/reconciliation.ts
    @app/dashboard/events/[slug]/overview/page.tsx
  </read_first>
  <action>Implement the smallest aggregation helpers needed to power the public report. Reuse canonical amount math and existing summary concepts so the report slices stay aligned with internal finance truth. Keep the output stable enough for both the public page and dashboard share action.</action>
  <acceptance_criteria>
    <criterion>The helper derives totals and grouped slices from canonical data only.</criterion>
    <criterion>Overpayments and negative balance states stay explicit in the aggregation output.</criterion>
    <criterion>The helper can be consumed without requiring authenticated dashboard state.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/reporting/share-link.test.ts && npm run typecheck`</verify>
  <done>The reporting helper can feed a public read-only page without leaking non-aggregated data.</done>
</task>

</tasks>

<verification>
1. The report token resolves or fails closed as expected.
2. The payload shape is aggregate-only.
3. Canonical amount math matches the existing finance readers.
</verification>

<success_criteria>
Stakeholders can be given a stable, revocable link that shows only aggregated reporting slices and no raw finance rows.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shareable-reporting-link/30-01-SUMMARY.md`
</output>
