---
phase: 30-shareable-reporting-link
plan: "02"
type: implementation
wave: 2
depends_on:
  - 30-01
requirements: [RPT-03]
files_modified:
  - app/reports/[token]/page.tsx
  - app/dashboard/events/[slug]/overview/page.tsx
  - components/reporting/report-summary.tsx
  - components/reporting/report-slices.tsx
autonomous: true
must_haves:
  truths:
    - "The public page is read-only and does not expose row-level data."
    - "The report is understandable on mobile and desktop."
    - "The dashboard has a simple share action that produces the tokenized link."
    - "Location, gender, and balance/overpayment slices are easy to scan."
  artifacts:
    - path: app/reports/[token]/page.tsx
      provides: public stakeholder report entrypoint
    - path: app/dashboard/events/[slug]/overview/page.tsx
      provides: share-link launch point for the current event
    - path: components/reporting/report-summary.tsx
      provides: summary cards for totals and balances
    - path: components/reporting/report-slices.tsx
      provides: grouped location/gender/balance tables
  key_links:
    - from: app/reports/[token]/page.tsx
      to: convex/reports.ts
      via: token-backed read-only fetch
      pattern: token
    - from: app/dashboard/events/[slug]/overview/page.tsx
      to: app/reports/[token]/page.tsx
      via: share-link CTA
      pattern: share
    - from: components/reporting/report-slices.tsx
      to: lib/domain/finance/reporting.ts
      via: aggregate slices renderer
      pattern: slice
---

<objective>
Turn the reporting contract into a stakeholder-friendly page and add a dashboard entry point to generate or copy the share link.

Purpose: the feature only matters if a non-admin can actually open it.
Output: a public report page with clear aggregate slices and a dashboard action that creates the link.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-shareable-reporting-link/30-CONTEXT.md
@app/dashboard/events/[slug]/overview/page.tsx
@app/dashboard/financial/page.tsx
@convex/reports.ts
@lib/domain/finance/reporting.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Build the public report page</name>
  <files>app/reports/[token]/page.tsx, components/reporting/report-summary.tsx, components/reporting/report-slices.tsx</files>
  <read_first>
    @convex/reports.ts
    @lib/domain/finance/reporting.ts
    @app/dashboard/financial/page.tsx
    @app/dashboard/events/[slug]/overview/page.tsx
  </read_first>
  <action>Build the public reporting page around the aggregate payload. Include summary cards for total collected / outstanding / overpaid, then grouped slices for location, gender, and balance state. Provide clear empty, expired, revoked, and no-data states. Keep the styling aligned with the existing dashboard but make it lighter and shareable.</action>
  <acceptance_criteria>
    <criterion>The page is accessible without dashboard authentication.</criterion>
    <criterion>The page presents only aggregate slices and no hidden row detail.</criterion>
    <criterion>Mobile layout remains readable and forwarding-friendly.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/reporting/public-report.test.ts`</verify>
  <done>The stakeholder-facing report is usable as a standalone link.</done>
</task>

<task type="auto">
  <name>Task 2: Add the share action to the dashboard</name>
  <files>app/dashboard/events/[slug]/overview/page.tsx, app/dashboard/financial/page.tsx</files>
  <read_first>
    @app/dashboard/events/[slug]/overview/page.tsx
    @app/dashboard/financial/page.tsx
    @convex/reports.ts
  </read_first>
  <action>Add a compact share/report CTA in the event-scoped dashboard so the operator can generate or copy the tokenized URL from the current event context. Keep the change minimal and reuse the existing event header/surface patterns rather than adding a new dashboard section.</action>
  <acceptance_criteria>
    <criterion>The share action is obvious from the event overview surface.</criterion>
    <criterion>The action uses the current event context and does not require manual URL construction.</criterion>
    <criterion>The dashboard still feels like an operator tool, not a public report editor.</criterion>
  </acceptance_criteria>
  <verify>`npm run typecheck && npm test -- tests/reporting/public-report.test.ts`</verify>
  <done>The dashboard can launch the public report without duplicating the reporting UI.</done>
</task>

</tasks>

<verification>
1. The public page works from a token URL.
2. The dashboard share action produces the same URL.
3. No authenticated dashboard-only controls leak into the public page.
</verification>

<success_criteria>
Stakeholders can open a shared link and quickly understand the financial picture by location, gender, and balance state without seeing operational rows.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shareable-reporting-link/30-02-SUMMARY.md`
</output>
