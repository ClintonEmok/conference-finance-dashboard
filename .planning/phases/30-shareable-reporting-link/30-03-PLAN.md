---
phase: 30-shareable-reporting-link
plan: "03"
type: verification
wave: 3
depends_on:
  - 30-02
requirements: [RPT-03]
files_modified:
  - tests/reporting/public-report.test.ts
  - tests/reporting/share-link.test.ts
autonomous: true
must_haves:
  truths:
    - "The report fails closed for invalid, missing, or revoked tokens."
    - "The public page never exposes row-level data in text or structure."
    - "The share flow does not regress existing dashboard finance surfaces."
  artifacts:
    - path: tests/reporting/public-report.test.ts
      provides: public-page behavior and privacy regression coverage
    - path: tests/reporting/share-link.test.ts
      provides: token lifecycle and payload-shape coverage
  key_links:
    - from: tests/reporting/public-report.test.ts
      to: app/reports/[token]/page.tsx
      via: privacy and accessibility checks
      pattern: report
    - from: tests/reporting/share-link.test.ts
      to: convex/reports.ts
      via: token lifecycle assertions
      pattern: token
---

<objective>
Prove the reporting link is safe to share and still behaves like a finance summary, not a data dump.

Purpose: a public URL is only acceptable if privacy and failure behavior are tight.
Output: regression coverage that protects the public report from leaking rows or auth-only actions.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-shareable-reporting-link/30-CONTEXT.md
@app/reports/[token]/page.tsx
@convex/reports.ts
@tests/reporting/share-link.test.ts
@tests/reporting/public-report.test.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add privacy and token-path tests</name>
  <files>tests/reporting/share-link.test.ts, tests/reporting/public-report.test.ts</files>
  <read_first>
    @app/reports/[token]/page.tsx
    @convex/reports.ts
    @lib/domain/finance/reporting.ts
  </read_first>
  <action>Write regression tests that validate the share-link lifecycle and public page behavior: valid token loads a report, revoked or invalid token fails closed, and the rendered output does not contain raw attendee/order rows. Include a small check for the location/gender/balance slices so the report contract stays stable.</action>
  <acceptance_criteria>
    <criterion>The tests fail if the page starts leaking operational rows.</criterion>
    <criterion>The tests fail if revoked tokens still resolve.</criterion>
    <criterion>The tests cover the main stakeholder slices.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/reporting/share-link.test.ts tests/reporting/public-report.test.ts`</verify>
  <done>The public report is covered by privacy-first regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Human-verify the share link flow</name>
  <files>app/reports/[token]/page.tsx, app/dashboard/events/[slug]/overview/page.tsx</files>
  <read_first>
    @app/reports/[token]/page.tsx
    @app/dashboard/events/[slug]/overview/page.tsx
  </read_first>
  <action>Manually verify the end-to-end flow: create a share link from an event overview, open it in a clean browser session, confirm the summary is readable, confirm the grouping slices make sense, and confirm no raw operational rows or edit controls appear. Verify that invalid or revoked links show a safe failure state.</action>
  <acceptance_criteria>
    <criterion>The link is understandable and shareable by a stakeholder.</criterion>
    <criterion>The report reads as an aggregate summary rather than a dashboard table.</criterion>
    <criterion>No dashboard-only controls appear on the public page.</criterion>
  </acceptance_criteria>
  <verify>Browser inspection of the public report and dashboard share action.</verify>
  <done>The reporting link is confirmed safe and usable.</done>
</task>

</tasks>

<verification>
1. Token failure states are safe.
2. Public output stays aggregate-only.
3. Share flow works end to end from the dashboard.
</verification>

<success_criteria>
The shareable reporting link is test-covered, human-verified, and safe to hand to a stakeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shareable-reporting-link/30-03-SUMMARY.md`
</output>
