---
phase: 12-use-clerk-as-only-auth-remove-stale-better-auth
plan: "02"
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app/dashboard/integrations/page.tsx
  - app/dashboard/layout.tsx
  - app/dashboard/logout-button.tsx
  - app/login/login-form.tsx
  - app/login/page.tsx
  - proxy.ts
autonomous: true
must_haves:
  truths:
    - "Signed-out visitors are blocked from dashboard pages by Clerk."
    - "Signed-in operators still see their email in the dashboard shell and can log out."
    - "No live dashboard redirect depends on the stale `/login` Better Auth flow."
  artifacts:
    - path: "proxy.ts"
      provides: "Middleware protection for dashboard routes"
      contains: "createRouteMatcher"
    - path: "app/dashboard/layout.tsx"
      provides: "Clerk-authenticated dashboard shell"
      contains: "requirePageUser"
    - path: "app/dashboard/logout-button.tsx"
      provides: "Clerk logout control"
      contains: "SignOutButton"
  key_links:
    - from: "proxy.ts"
      to: "/dashboard(.*)"
      via: "createRouteMatcher + auth.protect"
      pattern: "dashboard\(\.\*\)"
    - from: "app/dashboard/layout.tsx"
      to: "lib/auth/server.ts"
      via: "requirePageUser"
      pattern: "requirePageUser"
---

<objective>
Move dashboard route protection and signed-in UX from Better Auth to Clerk without changing the operator-facing dashboard shell.

Purpose: The app already renders Clerk on the landing page, but the dashboard still depends on Better Auth sessions and `/login` redirects.
Output: Protected dashboard routes via Clerk middleware, Clerk-based server page guards, Clerk logout UI, and removal of the old Better Auth login flow from active navigation.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md
@.planning/quick/260326-163-add-clerk-to-next-js-app-router/260326-163-SUMMARY.md
@proxy.ts
@app/dashboard/layout.tsx
@app/dashboard/integrations/page.tsx
@app/dashboard/logout-button.tsx
@app/login/page.tsx
@app/login/login-form.tsx
@lib/auth/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protect dashboard routes in Clerk middleware</name>
  <read_first>proxy.ts, .planning/quick/260326-163-add-clerk-to-next-js-app-router/260326-163-SUMMARY.md, .planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md</read_first>
  <files>proxy.ts</files>
  <action>Replace the current no-op `clerkMiddleware()` export in `proxy.ts` with Clerk's protected-route pattern. Import `createRouteMatcher` alongside `clerkMiddleware` from `@clerk/nextjs/server`. Declare `const isDashboardRoute = createRouteMatcher(["/dashboard(.*)"])`. Export `clerkMiddleware(async (auth, req) => { if (isDashboardRoute(req)) await auth.protect() })`. Keep the existing matcher array unchanged so static assets and public files remain excluded. Do not protect `/api/webhooks/*`, and do not introduce any Better Auth compatibility logic.</action>
  <acceptance_criteria>`proxy.ts` contains `createRouteMatcher`; `proxy.ts` contains `"/dashboard(.*)"`; `proxy.ts` contains `await auth.protect()`; `proxy.ts` still contains the existing `matcher` array entries for `_next` and `(api|trpc)`.</acceptance_criteria>
  <verify>Read `proxy.ts` and run `npm run typecheck`.</verify>
  <done>Dashboard pages are protected by Clerk at the middleware layer before server rendering begins.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard page guards and logout with Clerk</name>
  <read_first>app/dashboard/layout.tsx, app/dashboard/integrations/page.tsx, app/dashboard/logout-button.tsx, app/login/page.tsx, app/login/login-form.tsx, lib/auth/server.ts</read_first>
  <files>app/dashboard/layout.tsx, app/dashboard/integrations/page.tsx, app/dashboard/logout-button.tsx, app/login/page.tsx, app/login/login-form.tsx</files>
  <action>Update `app/dashboard/layout.tsx` and `app/dashboard/integrations/page.tsx` to use `requirePageUser(returnBackUrl)` from `@/lib/auth/server` instead of `auth.api.getSession(...)` and `redirect("/login?...`)`. Preserve the existing shell props so `DashboardShell` still receives `userEmail`. Replace `app/dashboard/logout-button.tsx` with Clerk logout UX using `SignOutButton` from `@clerk/nextjs`; after sign-out, send the user to `/`. Retire the Better Auth login UI by making `app/login/page.tsx` an immediate Clerk sign-in handoff using `redirectToSignIn({ returnBackUrl })` from Clerk server auth or by deleting `app/login/login-form.tsx` and replacing its page usage with a redirect-only compatibility page. The finished app must have no live sign-in/sign-up logic calling `authClient.signIn.email`, `authClient.signUp.email`, or `authClient.signOut`.</action>
  <acceptance_criteria>`app/dashboard/layout.tsx` contains `requirePageUser`; `app/dashboard/integrations/page.tsx` contains `requirePageUser`; `app/dashboard/logout-button.tsx` contains `SignOutButton`; `app/dashboard/logout-button.tsx` does not contain `authClient.signOut`; `app/login/page.tsx` contains `redirectToSignIn`; `app/login/login-form.tsx` is deleted or no longer imported; `grep -R "redirect(\"/login" app/dashboard app/login` returns no results.</acceptance_criteria>
  <verify>Run `npm run typecheck` and read the modified files; run the grep command from acceptance criteria.</verify>
  <done>Dashboard pages, settings page, and logout all use Clerk, and the old Better Auth login form is no longer part of the active auth flow.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm `proxy.ts` protects `/dashboard(.*)`.
- Confirm dashboard files no longer redirect to `/login` manually.
</verification>

<success_criteria>

- `proxy.ts` uses `createRouteMatcher(["/dashboard(.*)"])` with `auth.protect()`.
- `app/dashboard/layout.tsx` and `app/dashboard/integrations/page.tsx` use the shared Clerk helper.
- `app/dashboard/logout-button.tsx` no longer calls Better Auth client methods.
- The old Better Auth login form is removed from the live route flow.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-02-SUMMARY.md`
</output>
