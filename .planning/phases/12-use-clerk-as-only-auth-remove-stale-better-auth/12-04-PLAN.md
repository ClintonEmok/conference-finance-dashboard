---
phase: 12-use-clerk-as-only-auth-remove-stale-better-auth
plan: "04"
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - app/api/auth/[...all]/route.ts
  - lib/auth-client.ts
  - lib/auth.ts
  - lib/prisma.ts
  - package-lock.json
  - package.json
autonomous: false
must_haves:
  truths:
    - "The app no longer ships Better Auth runtime code."
    - "The dependency graph no longer includes stale Better Auth packages once code references are gone."
    - "A human can sign in, open the dashboard, and sign out using Clerk only."
  artifacts:
    - path: "package.json"
      provides: "Clerk-only auth dependency set"
      contains: "@clerk/nextjs"
    - path: "app/api/auth/[...all]/route.ts"
      provides: "Removed Better Auth App Router endpoint"
      contains: "DELETED"
    - path: "lib/auth.ts"
      provides: "Removed Better Auth server config"
      contains: "DELETED"
  key_links:
    - from: "package.json"
      to: "runtime imports"
      via: "dependency cleanup after code search"
      pattern: "@clerk/nextjs"
---

<objective>
Remove the stale Better Auth runtime after the Clerk migration is complete, then perform a real browser-level verification of the sign-in/dashboard/logout flow.

Purpose: The migration is not complete until the old auth system is gone and the operator-facing flow is proven end-to-end.
Output: Better Auth files and packages removed, plus a final human verification checkpoint for Clerk-only sign-in and sign-out behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md
@package.json
@app/api/auth/[...all]/route.ts
@lib/auth.ts
@lib/auth-client.ts
@lib/prisma.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Better Auth runtime files and packages</name>
  <read_first>package.json, app/api/auth/[...all]/route.ts, lib/auth.ts, lib/auth-client.ts, lib/prisma.ts, .planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md</read_first>
  <files>package.json, package-lock.json, app/api/auth/[...all]/route.ts, lib/auth.ts, lib/auth-client.ts, lib/prisma.ts</files>
  <action>Delete `app/api/auth/[...all]/route.ts`, `lib/auth.ts`, and `lib/auth-client.ts` after confirming plans 12-02 and 12-03 removed all imports. Remove `lib/prisma.ts` only if `grep -R "@/lib/prisma\|PrismaClient\|@prisma/client" app lib tests` returns no runtime references. In `package.json` and `package-lock.json`, remove `better-auth` and `@better-auth/prisma-adapter`; also remove `prisma` and `@prisma/client` only if no code imports remain after the grep check. Do not edit historical `prisma/migrations/*` files in this phase. The end state must leave `@clerk/nextjs` as the only app auth dependency.</action>
  <acceptance_criteria>`package.json` does not contain `better-auth`; `package.json` does not contain `@better-auth/prisma-adapter`; `app/api/auth/[...all]/route.ts` is deleted; `lib/auth.ts` is deleted; `lib/auth-client.ts` is deleted; `grep -R "better-auth\|@/lib/auth-client\|auth.api.getSession" app lib tests package.json` returns no results; `npm run build` exits 0; `npm run typecheck` exits 0.</acceptance_criteria>
  <verify>Run `npm run build`, `npm run typecheck`, and `grep -R "better-auth\|@/lib/auth-client\|auth.api.getSession" app lib tests package.json`.</verify>
  <done>The repo no longer ships Better Auth runtime code, and dependency cleanup matches actual remaining imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Clerk-only auth flow in the browser</name>
  <what-built>Clerk-only auth routing, dashboard protection, protected APIs, and Better Auth cleanup</what-built>
  <how-to-verify>
    1. Start the app and open `/` while signed out.
    2. Click the Clerk sign-in/sign-up affordance and confirm Clerk opens correctly.
    3. Visit `/dashboard` while signed out and confirm Clerk requires sign-in before the dashboard renders.
    4. Complete sign-in, confirm the dashboard shell loads, and verify the signed-in email still appears in the shell.
    5. Click the logout control, then revisit `/dashboard` and confirm you are blocked again.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the auth flow issue that failed.</resume-signal>
</task>

</tasks>

<verification>
- Run `npm run build`.
- Run `npm run typecheck`.
- Run `grep -R "better-auth\|@/lib/auth-client\|auth.api.getSession" app lib tests package.json`.
- Complete the human verification checkpoint.
</verification>

<success_criteria>

- Better Auth runtime files are deleted.
- `package.json` no longer includes Better Auth dependencies.
- No source file still references `auth.api.getSession` or `@/lib/auth-client`.
- Human verification confirms Clerk-only sign-in, dashboard access, and logout all work.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-04-SUMMARY.md`
</output>
