---
phase: 12-use-clerk-as-only-auth-remove-stale-better-auth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - convex/auth.config.ts
  - lib/auth/server.ts
  - lib/convex/client.tsx
autonomous: true
user_setup:
  - service: clerk
    why: "Clerk becomes the only auth provider and must issue Convex-compatible JWTs"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> API Keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> API Keys"
      - name: CLERK_JWT_ISSUER_DOMAIN
        source: "Clerk Dashboard -> JWT Templates / issuer domain; also set in Convex deployment env"
    dashboard_config:
      - task: "Create or verify a Clerk JWT template/audience named 'convex'"
        location: "Clerk Dashboard -> JWT Templates"
      - task: "Set CLERK_JWT_ISSUER_DOMAIN in the Convex deployment"
        location: "Convex Dashboard -> Environment Variables"
must_haves:
  truths:
    - "Authenticated Clerk sessions can be forwarded to Convex."
    - "Server code has one shared Clerk auth helper instead of scattered Better Auth assumptions."
    - "Convex is configured to trust Clerk-issued tokens for audience 'convex'."
  artifacts:
    - path: "lib/auth/server.ts"
      provides: "Shared Clerk server auth helpers for pages and API routes"
      contains: "auth("
    - path: "lib/convex/client.tsx"
      provides: "Authenticated Convex provider wired to Clerk"
      contains: "ConvexProviderWithClerk"
    - path: "convex/auth.config.ts"
      provides: "Convex Clerk JWT issuer config"
      contains: 'applicationID: "convex"'
  key_links:
    - from: "lib/convex/client.tsx"
      to: "@clerk/nextjs"
      via: "useAuth passed into ConvexProviderWithClerk"
      pattern: "useAuth"
    - from: "convex/auth.config.ts"
      to: "Clerk"
      via: "issuer domain env var"
      pattern: "CLERK_JWT_ISSUER_DOMAIN"
---

<objective>
Create the shared Clerk auth foundation the rest of the phase can depend on, including an authenticated Convex client bridge and one reusable server-side auth helper module.

Purpose: The current app shell shows Clerk UI, but protected code still depends on Better Auth and Convex still runs anonymously. This plan makes Clerk the real source of auth state.
Output: A new `lib/auth/server.ts` helper module plus updated Convex client/provider wiring and validated Convex Clerk auth config.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md
@.planning/quick/260326-163-add-clerk-to-next-js-app-router/260326-163-SUMMARY.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@app/layout.tsx
@lib/convex/client.tsx
@convex/auth.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Clerk server auth helpers</name>
  <read_first>lib/auth.ts, app/dashboard/layout.tsx, app/api/protected/ping/route.ts, .planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md</read_first>
  <files>lib/auth/server.ts</files>
  <action>Create `lib/auth/server.ts` as the new shared server-side auth module. Import `auth` and `currentUser` from `@clerk/nextjs/server`. Export helpers that cover the existing app needs exactly: (1) `unauthorizedJson()` returning `NextResponse.json({ error: { code: "UNAUTHORIZED", message: "Authentication required" } }, { status: 401 })`; (2) `requireApiUser()` that calls `await auth()`, returns `{ userId }` when authenticated, and returns the `unauthorizedJson()` response when not; (3) `requirePageUser(returnBackUrl: string)` that calls `await auth()`, invokes `redirectToSignIn({ returnBackUrl })` when signed out, then loads `currentUser()` and returns `{ userId, email }` where email is resolved from `primaryEmailAddress?.emailAddress` with fallback to the first email address. Keep these helpers Clerk-native; do not re-export or wrap any Better Auth objects.</action>
  <acceptance_criteria>`lib/auth/server.ts` contains `from "@clerk/nextjs/server"`; `lib/auth/server.ts` contains `code: "UNAUTHORIZED"`; `lib/auth/server.ts` contains `redirectToSignIn({ returnBackUrl })`; `lib/auth/server.ts` contains `currentUser()`.</acceptance_criteria>
  <verify>Read `lib/auth/server.ts` and confirm all four helper behaviors are present exactly; run `npm run typecheck`.</verify>
  <done>A single Clerk server helper module exists and can replace Better Auth session checks in both pages and route handlers without changing the 401 JSON contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Convex to Clerk-authenticated requests</name>
  <read_first>lib/convex/client.tsx, app/layout.tsx, convex/auth.config.ts, .planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-RESEARCH.md, convex/_generated/ai/guidelines.md</read_first>
  <files>lib/convex/client.tsx, app/layout.tsx, convex/auth.config.ts</files>
  <action>Update `lib/convex/client.tsx` to replace plain `ConvexProvider` with `ConvexProviderWithClerk` from `convex/react-clerk`, passing Clerk's `useAuth` from `@clerk/nextjs` and preserving the exported singleton `convex`. Keep the file as a client component. Update `app/layout.tsx` only as needed so the provider order remains `ClerkProvider` outside `ConvexClientProvider`, then `QueryProvider`, then `ThemeProvider`. Normalize `convex/auth.config.ts` formatting to project style while keeping `domain: process.env.CLERK_JWT_ISSUER_DOMAIN!` and `applicationID: "convex"` exactly. Do not add Better Auth shims, and do not fall back to anonymous Convex requests.</action>
  <acceptance_criteria>`lib/convex/client.tsx` contains `ConvexProviderWithClerk`; `lib/convex/client.tsx` contains `useAuth`; `app/layout.tsx` contains `ClerkProvider`; `convex/auth.config.ts` contains `CLERK_JWT_ISSUER_DOMAIN`; `convex/auth.config.ts` contains `applicationID: "convex"`.</acceptance_criteria>
  <verify>Run `npm run typecheck` and read the three files to confirm the provider order and Clerk/Convex integration strings are present.</verify>
  <done>Signed-in Clerk users will send auth context to Convex, and Convex is configured to validate Clerk-issued `convex` JWTs.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm `lib/auth/server.ts` contains the preserved `UNAUTHORIZED` JSON contract.
- Confirm `lib/convex/client.tsx` uses `ConvexProviderWithClerk` instead of `ConvexProvider`.
</verification>

<success_criteria>

- `lib/auth/server.ts` exists and exports Clerk-native helpers for pages and API routes.
- `lib/convex/client.tsx` uses `ConvexProviderWithClerk` with Clerk `useAuth`.
- `app/layout.tsx` still wraps the app in `ClerkProvider` before Convex/query/theme providers.
- `convex/auth.config.ts` still targets Clerk issuer env `CLERK_JWT_ISSUER_DOMAIN` and audience `convex`.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-01-SUMMARY.md`
</output>
