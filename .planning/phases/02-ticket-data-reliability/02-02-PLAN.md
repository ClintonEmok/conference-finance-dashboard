---
phase: 02-ticket-data-reliability
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - lib/integrations/ticket-tailor/sync.ts
  - app/api/ticket-tailor/sync/route.ts
  - app/dashboard/ticket-tailor/sync/page.tsx
autonomous: false
must_haves:
  truths:
    - "Admin can run manual re-sync scoped to selected event/date range"
    - "Scoped re-sync returns clear success/failure breakdown to operator"
    - "Manual re-sync visibly refreshes stale/missing records without introducing duplicates"
  artifacts:
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Scoped sync execution (event/date filters) with deterministic reruns"
      exports: ["runTicketTailorSync"]
    - path: "app/api/ticket-tailor/sync/route.ts"
      provides: "Manual re-sync input contract and scoped execution wiring"
      exports: ["POST"]
    - path: "app/dashboard/ticket-tailor/sync/page.tsx"
      provides: "Protected operator UI for manual re-sync trigger and result display"
      contains: "event/date"
  key_links:
    - from: "app/dashboard/ticket-tailor/sync/page.tsx"
      to: "/api/ticket-tailor/sync"
      via: "form submit/manual trigger"
      pattern: "fetch.*api/ticket-tailor/sync"
    - from: "app/api/ticket-tailor/sync/route.ts"
      to: "lib/integrations/ticket-tailor/sync.ts"
      via: "scope translation and execution"
      pattern: "runTicketTailorSync"
---

<objective>
Add operator-controlled manual re-sync for selected event/date ranges so stale or missing Ticket Tailor data can be corrected on demand.

Purpose: Complete recovery workflows for Ticket Tailor ingestion reliability (TT-02) while preserving idempotent behavior from Plan 02-01.
Output: Scoped sync contract, protected manual trigger UI, and operator-visible run outcomes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ticket-data-reliability/02-CONTEXT.md
@.planning/phases/02-ticket-data-reliability/02-01-SUMMARY.md
@app/dashboard/layout.tsx
@app/dashboard/integrations/page.tsx
@app/api/ticket-tailor/sync/route.ts
@lib/integrations/ticket-tailor/sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sync service and API for scoped manual re-sync</name>
  <files>lib/integrations/ticket-tailor/sync.ts, app/api/ticket-tailor/sync/route.ts</files>
  <action>Extend `runTicketTailorSync` to accept optional manual scope input (`eventId`, `from`, `to`) and enforce deterministic filtering in provider fetch logic. Update sync API `POST` contract to parse/validate scope fields, reject invalid date ranges with 400 `{ error: { code: "BAD_REQUEST", message } }`, and pass validated scope into sync service. Response must include scope echo + counts so operators can confirm exactly what was synced.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Manual API callers can request targeted re-sync by event/date range and receive explicit scoped summary without breaking global sync behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build protected manual re-sync dashboard page</name>
  <files>app/dashboard/ticket-tailor/sync/page.tsx</files>
  <action>Create `/dashboard/ticket-tailor/sync` protected page with form controls for optional event id, from date, and to date; submit to `/api/ticket-tailor/sync` via server action or fetch POST. Render run result card showing status, scope, imported/upserted counts, and any non-sensitive diagnostics. Include clear validation messaging for invalid date window and network failures. Keep styling aligned with existing dashboard + shadcn patterns.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Admin can trigger and observe scoped manual re-sync from dashboard UI without using curl/CLI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Scoped manual Ticket Tailor re-sync flow (API + dashboard UI) with result summaries</what-built>
  <how-to-verify>
    1) Start app and sign in as admin.
    2) Open `/dashboard/ticket-tailor/sync`.
    3) Submit one valid scoped run (event id + date range) and confirm success response includes matching scope + counts.
    4) Submit an invalid date range (from > to) and confirm inline/API validation error appears.
    5) Re-run same valid scope and confirm counts reflect update/idempotent behavior (no obvious duplicate explosion).
  </how-to-verify>
  <resume-signal>Type "approved" if behavior matches, or report observed mismatch/errors.</resume-signal>
</task>

</tasks>

<verification>
- Manual scoped run succeeds for valid inputs and fails clearly for invalid ranges.
- API and UI both show the executed scope and per-run outcome counts.
- Repeated scoped sync remains rerun-safe.
</verification>

<success_criteria>
- TT-02 satisfied: admin has manual recovery flow for targeted event/date backfill.
- Operators can verify what scope was synced and whether it succeeded.
- No regression to unauthorized access rules for sync operations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ticket-data-reliability/02-02-SUMMARY.md`
</output>
