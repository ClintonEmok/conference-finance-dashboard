---
phase: 02-ticket-data-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/domain/finance/ticket-tailor-status.ts
  - lib/integrations/ticket-tailor/client.ts
  - lib/integrations/ticket-tailor/sync.ts
  - app/api/ticket-tailor/sync/route.ts
autonomous: true
user_setup:
  - service: ticket-tailor
    why: "Provider API access for real sync execution"
    env_vars:
      - name: TICKET_TAILOR_API_KEY
        source: "Ticket Tailor dashboard -> Account/API keys"
must_haves:
  truths:
    - "Admin can trigger a Ticket Tailor sync and receive a run summary"
    - "Ticket Tailor events and orders are persisted without duplicates on rerun"
    - "Synced orders expose canonical status values (paid/refunded/cancelled/pending)"
    - "Sync failures are captured with actionable operator-safe diagnostics"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Durable Ticket Tailor event/order/sync-run models"
      contains: "model TicketTailorOrder"
    - path: "lib/domain/finance/ticket-tailor-status.ts"
      provides: "Central provider-to-canonical status mapping"
      exports: ["normalizeTicketTailorStatus"]
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Idempotent event+order import orchestration and run summary"
      exports: ["runTicketTailorSync"]
    - path: "app/api/ticket-tailor/sync/route.ts"
      provides: "Protected manual sync trigger endpoint"
      exports: ["POST"]
  key_links:
    - from: "app/api/ticket-tailor/sync/route.ts"
      to: "lib/integrations/ticket-tailor/sync.ts"
      via: "authenticated sync execution"
      pattern: "runTicketTailorSync"
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "lib/domain/finance/ticket-tailor-status.ts"
      via: "per-order normalization during upsert"
      pattern: "normalizeTicketTailorStatus"
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "prisma.ticketTailor(Order|Event|SyncRun)"
      via: "transactional upserts"
      pattern: "prisma\\.ticketTailor"
---

<objective>
Build the core Ticket Tailor sync pipeline so event/order data lands in durable app storage with canonical statuses and operator-safe run summaries.

Purpose: Deliver trustworthy reporting input (TT-01, TT-03) before dashboard analytics are built in later phases.
Output: New Ticket Tailor persistence models, centralized normalization module, and a protected sync API trigger.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ticket-data-reliability/02-CONTEXT.md
@.planning/phases/01-foundation-secure-access/01-01-SUMMARY.md
@.planning/phases/01-foundation-secure-access/01-02-SUMMARY.md
@prisma/schema.prisma
@lib/integrations/ticket-tailor/client.ts
@lib/integrations/ticket-tailor/webhook.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable Ticket Tailor sync storage models</name>
  <files>prisma/schema.prisma</files>
  <action>Extend Prisma schema with canonical finance ingestion entities: `TicketTailorEvent`, `TicketTailorOrder`, and `TicketTailorSyncRun` (plus enum(s) for canonical order status and sync outcome). Ensure provider identifiers are unique (`providerEventId`, `providerOrderId`), orders relate to events, and sync runs store counts + error summary for operational visibility. Keep raw provider payload fields as JSON for traceability. Add indexes for common lookups (`eventId`, `orderedAt`, `normalizedStatus`, `startedAt`).</action>
  <verify>npx prisma migrate dev --name ticket_tailor_sync_models && npx prisma generate</verify>
  <done>Database schema supports idempotent Ticket Tailor event/order upserts and run-level observability needed for sync operations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement centralized normalization + idempotent sync orchestrator</name>
  <files>lib/domain/finance/ticket-tailor-status.ts, lib/integrations/ticket-tailor/client.ts, lib/integrations/ticket-tailor/sync.ts</files>
  <action>Create `normalizeTicketTailorStatus` in `lib/domain/finance` that maps provider status signals into `paid | refunded | cancelled | pending`; unknown/unmapped values must safely default to `pending` with a diagnostic note. Extend Ticket Tailor client helpers to support deterministic pagination for events/orders fetches. Implement `runTicketTailorSync` to fetch events and related orders, normalize each order status, and upsert into Prisma with rerun-safe unique keys (no duplicates on repeated sync). Record per-run counts (events scanned, orders upserted, normalized fallback count, failed items) into `TicketTailorSyncRun`.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Running sync orchestration repeatedly with same upstream data updates existing records instead of duplicating and always produces canonical status values for stored orders.</done>
</task>

<task type="auto">
  <name>Task 3: Add protected sync trigger endpoint with explicit summary contract</name>
  <files>app/api/ticket-tailor/sync/route.ts</files>
  <action>Create authenticated `POST /api/ticket-tailor/sync` route using `auth.api.getSession({ headers })`; unauthenticated requests must return 401 `{ error: { code: "UNAUTHORIZED", message } }`. For authenticated requests, call `runTicketTailorSync` and return run metadata plus counts using operational JSON shape (`{ ok: true, runId, status, counts, diagnostics }`). Handle provider/config/runtime failures with explicit non-sensitive error response (`SYNC_FAILED`) and HTTP 500.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin can trigger sync via protected endpoint and receive structured run summary suitable for operator workflows.</done>
</task>

</tasks>

<verification>
1. Call `POST /api/ticket-tailor/sync` without session; confirm 401 + `UNAUTHORIZED` JSON contract.
2. Call same route with valid session and configured Ticket Tailor key; confirm response includes `runId`, `status`, and count fields.
3. Re-run sync on unchanged upstream data; confirm no duplicate order/event records are created (idempotent upsert behavior).
4. Validate persisted orders contain only canonical statuses: `paid | refunded | cancelled | pending`.
</verification>

<success_criteria>
- TT-01 satisfied baseline: system can import Ticket Tailor events/orders into durable storage through an authenticated sync flow.
- TT-03 satisfied baseline: canonical normalized statuses are persisted centrally for downstream reporting use.
- Sync execution produces operator-safe diagnostics and run-level counts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ticket-data-reliability/02-01-SUMMARY.md`
</output>
