---
phase: 06-tikkie-integration
plan: 01
title: Harden Tikkie backend contracts, status trust, and attendee-detail link projection
type: execute
wave: 1
depends_on: [05-02]
requirements: [TK-02]
files_modified:
  - lib/domain/finance/tikkie-links.ts
  - app/api/dashboard/tikkie-links/route.ts
  - lib/domain/finance/attendee-detail.ts
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - lib/integrations/tikkie/webhook.ts
  - app/api/webhooks/tikkie/route.ts
  - app/api/jobs/tikkie/status-sync/route.ts
  - tests/tikkie/tikkie-links.test.ts
  - tests/tikkie/webhook-route.test.ts
autonomous: true
user_setup:
  - service: tikkie
    why: "End-to-end status verification requires real provider credentials and the app callback configured for notifications"
    env_vars:
      - name: TIKKIE_API_KEY
        source: "ABN AMRO Developer Portal -> My Apps -> API Key"
      - name: TIKKIE_APP_TOKEN
        source: "Tikkie Business Portal or sandbox app setup"
    dashboard_config:
      - task: "Point the single payment-request notification subscription at the active deployment's /api/webhooks/tikkie endpoint"
        location: "ABN AMRO / Tikkie notification subscription settings"
must_haves:
  truths:
    - "Only valid Tikkie link payloads are accepted for creation"
    - "Open Tikkie links can converge to paid or expired through webhook and poll refresh without regressing paid links"
    - "Attendee detail data includes latest-link-first Tikkie summary and history for the attendee's order"
  artifacts:
    - path: "lib/domain/finance/tikkie-links.ts"
      provides: "Validated create/list/refresh domain logic with stale-aware latest-link projection"
      exports: ["createTikkiePaymentLink", "listTikkiePaymentLinksByOrder", "refreshTikkiePaymentLinkStatus", "syncPendingTikkiePaymentLinks"]
    - path: "app/api/dashboard/tikkie-links/route.ts"
      provides: "Protected create/list/refresh API contract for dashboard Tikkie workflows"
      exports: ["GET", "POST"]
    - path: "lib/domain/finance/attendee-detail.ts"
      provides: "Attendee detail projection with order-level Tikkie latest summary and history"
      exports: ["getAttendeeDetail"]
    - path: "app/api/webhooks/tikkie/route.ts"
      provides: "Webhook ingestion that deduplicates notifications and refreshes payment status safely"
      exports: ["POST"]
    - path: "app/api/jobs/tikkie/status-sync/route.ts"
      provides: "Protected fallback poll endpoint for open links"
      exports: ["POST"]
  key_links:
    - from: "app/api/dashboard/tikkie-links/route.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "validated protected route handlers"
      pattern: "createTikkiePaymentLink|listTikkiePaymentLinksByOrder|refreshTikkiePaymentLinkStatus"
    - from: "app/api/webhooks/tikkie/route.ts"
      to: "lib/integrations/tikkie/webhook.ts"
      via: "notification parsing and dedupe"
      pattern: "processTikkieWebhookNotification|verifyTikkieWebhook"
    - from: "lib/domain/finance/attendee-detail.ts"
      to: "prisma.tikkiePaymentLink"
      via: "latest-first order payment-link projection"
      pattern: "tikkiePaymentLinks"
---

<objective>
Make Tikkie payment-link data trustworthy before UI polish depends on it.

Purpose: Satisfy TK-02 by tightening provider validation, stale-aware status projection, webhook/poll recovery, and attendee-detail-ready data contracts.
Output: Hardened Tikkie domain and API contracts, verified webhook/poll behavior, and attendee detail data shaped for latest-link-first UI.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tikkie-integration/06-CONTEXT.md
@.planning/phases/06-tikkie-integration/06-RESEARCH.md
@.planning/phases/05-room-allocation-operator-flow-polish/05-02-SUMMARY.md
@app/api/dashboard/tikkie-links/route.ts
@lib/domain/finance/tikkie-links.ts
@lib/domain/finance/attendee-detail.ts
@app/api/webhooks/tikkie/route.ts
@app/api/jobs/tikkie/status-sync/route.ts
@tests/tikkie/webhook-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten Tikkie create/list contracts around provider-safe inputs and latest-link summaries</name>
  <files>lib/domain/finance/tikkie-links.ts, app/api/dashboard/tikkie-links/route.ts, tests/tikkie/tikkie-links.test.ts</files>
  <action>Refactor the Tikkie domain and route validation so both creation entry points can rely on the same server-safe rules: `amountMinor` must stay a positive integer, `description` and `referenceId` must respect the Tikkie 35-character limit, `expiryDate` must parse to a real future date, and invalid provider-order or event identifiers must fail with the existing `BAD_REQUEST` contract. Update the list/query shape so consumers get latest-link-first data with explicit `latestLink`, prior-history entries, `providerLastCheckedAt`, and a stale/fresh signal derived for open links only. Keep the canonical app statuses `created | paid | expired`; do not leak raw provider enums into operator-facing fields.</action>
  <verify>npm run test -- tests/tikkie/tikkie-links.test.ts && npm run typecheck</verify>
  <done>Protected Tikkie APIs reject invalid payloads consistently and return latest-link-first order data with recency metadata that UI tasks can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify webhook and fallback polling keep payment state trustworthy</name>
  <files>lib/domain/finance/tikkie-links.ts, lib/integrations/tikkie/webhook.ts, app/api/webhooks/tikkie/route.ts, app/api/jobs/tikkie/status-sync/route.ts, tests/tikkie/webhook-route.test.ts, tests/tikkie/tikkie-links.test.ts</files>
  <action>Harden the Tikkie sync path so webhook notifications and manual/job polling both converge open links without duplicating transitions or regressing terminal `paid` links. Preserve the optional webhook-secret behavior already in place, but make duplicate-notification handling, missing-link behavior, and poll-only refresh of `created` links explicit and test-covered. Keep the sync route protected with the established `UNAUTHORIZED` contract and return counts operators can trust when they trigger a manual status sync.</action>
  <verify>npm run test -- tests/tikkie/webhook-route.test.ts tests/tikkie/tikkie-links.test.ts && npm run typecheck</verify>
  <done>Webhook and poll paths can refresh open links safely, duplicate notifications do not create duplicate transitions, and manual status sync remains a trustworthy recovery path.</done>
</task>

<task type="auto">
  <name>Task 3: Shape attendee detail data for latest link, history, and follow-up context</name>
  <files>lib/domain/finance/attendee-detail.ts, app/api/dashboard/attendees/[attendeeId]/route.ts</files>
  <action>Extend attendee-detail projection so the page no longer has to infer Tikkie follow-up from a flat ledger alone. Return the order-level Tikkie context needed by the next plan: latest link summary, whether the latest open link is stale, ordered prior-link history, refresh/share-safe URLs, and generation defaults derived from the attendee's outstanding balance and order identifiers. Preserve the current finance totals, room-status payload, and 401/404/400 response contracts.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>Attendee detail API returns a stable latest-link-first Tikkie payload alongside the existing finance and accommodation data.</done>
</task>

</tasks>

<verification>
1. Call `POST /api/dashboard/tikkie-links` with too-long description/reference and past expiry date; confirm `400 BAD_REQUEST` responses stay explicit.
2. Trigger or simulate webhook plus manual `POST /api/jobs/tikkie/status-sync` refreshes and confirm open links converge without duplicate transition rows.
3. Load `/api/dashboard/attendees/[attendeeId]` for an attendee with Tikkie history and confirm the payload includes latest-link summary, recency, stale signal, and prior history in descending order.
4. Confirm unauthenticated calls to the Tikkie dashboard and job endpoints still return the established `401 UNAUTHORIZED` contract.
</verification>

<success_criteria>
- TK-02 backend foundation is satisfied: payment-link metadata and state updates are durable, monotonic, and recoverable via webhook or polling.
- The next UI plan can consume one server-owned latest-link-first contract instead of rebuilding Tikkie rules in the client.
- Operators can trust attendee-detail Tikkie state once the UI renders it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tikkie-integration/06-01-SUMMARY.md`
</output>
