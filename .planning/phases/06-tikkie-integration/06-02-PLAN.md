---
phase: 06-tikkie-integration
plan: 02
title: Add latest-first Tikkie modal and follow-up actions to outstanding balances and attendee detail
type: execute
wave: 2
depends_on: [06-01]
requirements: [TK-01, TK-03]
files_modified:
  - components/dashboard/tikkie-link-dialog.tsx
  - components/dashboard/tikkie-link-summary.tsx
  - app/dashboard/reconciliation/page.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
autonomous: false
must_haves:
  truths:
    - "Admin can open a lightweight confirmation modal from outstanding balances and attendee detail before creating a Tikkie link"
    - "Admin can see the latest Tikkie link first with copy, open, refresh, and recency/stale state in both follow-up surfaces"
    - "Prior links are available through expandable history instead of an inline full dump"
  artifacts:
    - path: "components/dashboard/tikkie-link-dialog.tsx"
      provides: "Reusable confirmation modal with provider-safe defaults and inline validation"
      contains: "amount"
    - path: "components/dashboard/tikkie-link-summary.tsx"
      provides: "Reusable latest-link-first summary with stale badge, recency, and expandable history"
      contains: "last checked"
    - path: "app/dashboard/reconciliation/page.tsx"
      provides: "Primary outstanding-balance row action for generate/copy/open/refresh"
      contains: "Generate Tikkie link"
    - path: "app/dashboard/attendees/[attendeeId]/page.tsx"
      provides: "Secondary attendee-detail Tikkie follow-up surface with latest link and history"
      contains: "Tikkie"
  key_links:
    - from: "app/dashboard/reconciliation/page.tsx"
      to: "components/dashboard/tikkie-link-dialog.tsx"
      via: "direct row action opens prefilled confirmation modal"
      pattern: "Generate Tikkie link"
    - from: "app/dashboard/attendees/[attendeeId]/page.tsx"
      to: "/api/dashboard/tikkie-links"
      via: "latest-link load, create, copy/open, and refresh actions"
      pattern: "api/dashboard/tikkie-links"
    - from: "components/dashboard/tikkie-link-summary.tsx"
      to: "latestLink/history payload from 06-01"
      via: "shared latest-first display logic"
      pattern: "latestLink|history|providerLastCheckedAt"
---

<objective>
Put the Tikkie workflow into the existing operator loop without creating a separate collection flow.

Purpose: Satisfy TK-01 and TK-03 by adding a lightweight confirmation modal, latest-link-first visibility, and copy/share/open follow-up actions in the two locked entry points.
Output: Shared Tikkie UI components plus polished outstanding-balances and attendee-detail experiences aligned with the phase context decisions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tikkie-integration/06-CONTEXT.md
@.planning/phases/06-tikkie-integration/06-RESEARCH.md
@.planning/phases/06-tikkie-integration/06-01-SUMMARY.md
@app/dashboard/reconciliation/page.tsx
@app/dashboard/attendees/[attendeeId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared Tikkie dialog and latest-link summary components</name>
  <files>components/dashboard/tikkie-link-dialog.tsx, components/dashboard/tikkie-link-summary.tsx</files>
  <action>Create reusable client components for both entry points. The dialog must feel lightweight and operational: prefill `amount`, `expiryDate`, `description`, and `referenceId`, allow editing before submit, mirror the provider-safe validation from Plan 06-01, and avoid turning into a multi-step wizard. The summary component must default to the newest link, show status, last checked time, stale badge for open links only, copy/open/refresh affordances, and keep older links behind an expand/collapse history affordance.</action>
  <verify>npm run typecheck</verify>
  <done>Both Tikkie entry points can share one confirmation modal and one latest-link-first presentation pattern instead of duplicating UI logic.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the Tikkie workflow into outstanding balances and attendee detail</name>
  <files>app/dashboard/reconciliation/page.tsx, app/dashboard/attendees/[attendeeId]/page.tsx, components/dashboard/tikkie-link-dialog.tsx, components/dashboard/tikkie-link-summary.tsx</files>
  <action>Replace the current straight-through create action in `Outstanding balances` with a direct row action that opens the shared confirmation modal while keeping the trigger visible on the row. Use latest-link-first summaries in each row so operators can copy the newest link, open it directly, refresh status, and see recency without leaving the page. Add the same generation and follow-up workflow to attendee detail as the secondary surface for order history and operator context. Keep attendee detail anchored in finance and accommodation context; do not add a separate collection page or dump full history inline by default.</action>
  <verify>npm run lint && npm run typecheck && npm run build</verify>
  <done>Operators can generate, open, copy, refresh, and inspect latest-link-first Tikkie state from both outstanding balances and attendee detail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Tikkie operator workflow across outstanding balances and attendee detail</what-built>
  <how-to-verify>
    1. Open `/dashboard/reconciliation` and choose a row with an outstanding amount.
    2. Click `Generate Tikkie link`; confirm the modal opens prefilled, allows edits, and creates the link after submit.
    3. Confirm the row now shows the newest link first with status, last checked time, copy/open actions, and history hidden behind disclosure.
    4. Open attendee detail for the same order and confirm the same latest link appears there with matching status and history.
    5. Trigger refresh from one surface and confirm the updated status/recency appears consistently on both surfaces after reload.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the UI/flow issues that need correction.</resume-signal>
</task>

</tasks>

<verification>
1. Reconciliation keeps `Generate Tikkie link` as a direct row action and no longer hides the primary workflow behind navigation.
2. Attendee detail exposes latest-link-first Tikkie context, copy/open/refresh actions, and expandable prior history.
3. Last-checked recency and stale wording are visible for open links, while terminal links do not show noisy stale warnings.
4. The full path outstanding balances -> attendee detail stays consistent for the same order after create and refresh actions.
</verification>

<success_criteria>
- TK-01 satisfied: admins can generate a Tikkie payment link from the current finance workflow with a lightweight confirmation step.
- TK-03 satisfied: admins can copy/share the latest link, open it directly, and inspect its current status without leaving the dashboard flow.
- Phase 6 finishes as an operator-loop enhancement, not a new standalone collection product area.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tikkie-integration/06-02-SUMMARY.md`
</output>
