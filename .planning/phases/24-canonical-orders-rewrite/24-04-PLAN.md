---
phase: 24-canonical-orders-rewrite
plan: 04
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - convex/payments.ts
autonomous: true

must_haves:
  truths:
    - "Payment matching reads orders from core orders table"
    - "Payment-to-order assignment uses Id<"orders"> types"
    - "Auto-match queries read orders by status from core table"
  artifacts:
    - path: convex/payments.ts
      provides: "Updated payment matching reading from core orders table"
  key_links:
    - from: convex/payments.ts
      to: orders table
      via: "ctx.db.query('orders') for payment matching"
      pattern: "ctx\\.db\\.query\\(\"orders\""
---

<objective>
Update payment matching to read orders from core orders table
</objective>

Purpose: Payment matching reads order data to match payments against orders. Must read from the canonical orders table instead of ticketTailorOrders.

Output: Updated payments.ts with core-table reads.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md
@convex/payments.ts
@convex/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update payments.ts to read orders from core table</name>
  <files>convex/payments.ts</files>
  <read_first>
    - convex/payments.ts (full file — 563 lines)
    - convex/schema.ts (orders table definition)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-23, D-27)
  </read_first>
  <action>
    Update convex/payments.ts:

    1. Replace all ctx.db.query("ticketTailorOrders") with ctx.db.query("orders")
    2. Replace all ctx.db.query("submissions") references with ctx.db.query("orders")
    3. Update all Id<"ticketTailorOrders"> to Id<"orders">
    4. Update all Id<"submissions"> to Id<"orders">
    5. For autoMatchPayments:
       - Query orders with .withIndex("by_status", q.eq("status", "paid")) for paid orders
       - Read bookerName from orders.bookerName (moved from ticketTailorOrders.buyerName per D-11)
       - Read totalAmountMinor from orders.totalAmountMinor (moved from ticketTailorOrders per D-11)
    6. For internalGetPaidOrders (in sync.ts, referenced by autoSync.ts):
       - Already handled in Plan 02 Task 3 — ensure consistency
    7. For internalAssignPaymentToOrder:
       - Update orderId arg type to v.id("orders")
    8. For internalUpsertTikkiePayment:
       - Update any orderId references to use v.id("orders") or v.optional(v.id("orders"))
    9. Ensure all queries use .withIndex() — no full table scans (D-34)
    10. Keep requireIdentity(ctx) as first statement in public mutations (D-29)

  </action>
  <verify>npx convex dev compiles; payments.ts contains no references to "ticketTailorOrders" or "submissions" in query calls</verify>
  <done>Payment matching reads from core orders table; all types updated to Id<"orders">; queries use indexes</done>
</task>

</tasks>

<verification>
- Run `npx convex dev` — no compilation errors
- Grep payments.ts: no "ticketTailorOrders" or "submissions" in query calls
- Grep payments.ts: contains Id<"orders"> type references
- All queries use .withIndex()
</verification>

<success_criteria>

- payments.ts reads order data from core orders table
- All type references updated to Id<"orders">
- Payment matching works with core table data
- All queries use .withIndex() with proper indexes
  </success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-orders-rewrite/24-04-SUMMARY.md`
</output>
