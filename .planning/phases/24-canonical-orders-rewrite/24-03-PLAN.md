---
phase: 24-canonical-orders-rewrite
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - convex/orders.ts
  - convex/tikkie.ts
autonomous: true

must_haves:
  truths:
    - "Order list queries read from orders table, not ticketTailorOrders"
    - "Order detail queries join orders + ticketTailorOrders for full data"
    - "Tikkie link creation reads from orders table"
    - "All queries use .withIndex() — no full table scans"
  artifacts:
    - path: convex/orders.ts
      provides: "Updated order queries/mutations reading from core tables"
    - path: convex/tikkie.ts
      provides: "Updated Tikkie mutations reading orders from core table"
  key_links:
    - from: convex/orders.ts
      to: orders table
      via: "ctx.db.query('orders') with .withIndex()"
      pattern: "ctx\\.db\\.query\\(\"orders\""
    - from: convex/tikkie.ts
      to: orders table
      via: "ctx.db.query('orders') for order lookup in Tikkie flow"
      pattern: "ctx\\.db\\.query\\(\"orders\""
---

<objective>
Update order management and Tikkie link creation to read from core orders table
</objective>

Purpose: Orders.ts and tikkie.ts are primary consumers of order data. They must read from the canonical orders table instead of ticketTailorOrders, joining with TT extension data when provider-specific fields are needed.

Output: Updated orders.ts and tikkie.ts with core-table reads.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md
@convex/orders.ts
@convex/tikkie.ts
@convex/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orders.ts queries and mutations to use core tables</name>
  <files>convex/orders.ts</files>
  <read_first>
    - convex/orders.ts (full file — 752 lines)
    - convex/schema.ts (orders, orderAttendees, orderTicketSelections, orderAssignments tables)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-23, D-27, D-30-D-34)
  </read_first>
  <action>
    Update all queries and mutations in convex/orders.ts:

    1. Replace all ctx.db.query("ticketTailorOrders") with ctx.db.query("orders")
    2. Replace all ctx.db.query("ticketTailorAttendees") with ctx.db.query("orderAttendees")
    3. Replace all ctx.db.query("submissionAttendees") with ctx.db.query("orderAttendees")
    4. Replace all ctx.db.query("submissionTicketSelections") with ctx.db.query("orderTicketSelections")
    5. Replace all ctx.db.query("submissionAssignments") with ctx.db.query("orderAssignments")
    6. Update all Id<"ticketTailorOrders"> to Id<"orders">
    7. Update all Id<"submissionAttendees"> to Id<"orderAttendees">
    8. Update all Id<"submissionTicketSelections"> to Id<"orderTicketSelections">
    9. Update all Id<"submissionAssignments"> to Id<"orderAssignments">
    10. For queries that need both core + TT data: first query orders table, then join with ticketTailorOrders via orderId FK
    11. For queries that need attendee data: query orderAttendees by orderId, then optionally join ticketTailorAttendees by attendeeId FK
    12. Ensure all list queries use .paginate(paginationOpts) with .withIndex() (D-30, D-34)
    13. Ensure all single-result lookups use .first() on indexed queries (D-32)
    14. Keep requireIdentity(ctx) as first statement in public mutations (D-29)
    15. Keep internalMutation for cron/internal calls (D-28)

  </action>
  <verify>npx convex dev compiles without errors; orders.ts contains no references to "submissions" or "submissionAttendees" or "submissionTicketSelections" or "submissionAssignments"</verify>
  <done>All order queries read from core tables; types updated to use Id<"orders">, Id<"orderAttendees">, etc.; pagination uses .withIndex()</done>
</task>

<task type="auto">
  <name>Task 2: Update tikkie.ts to read orders from core table</name>
  <files>convex/tikkie.ts</files>
  <read_first>
    - convex/tikkie.ts (full file — 633 lines)
    - convex/schema.ts (orders table definition)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-23, D-27)
  </read_first>
  <action>
    Update convex/tikkie.ts:

    1. Replace all ctx.db.query("ticketTailorOrders") with ctx.db.query("orders")
    2. Update Id<"ticketTailorOrders"> to Id<"orders">
    3. For order lookups in Tikkie flow:
       - Query orders table by .withIndex("by_providerOrderId", q.eq("providerOrderId", ...)) for integration orders
       - Query orders table by .withIndex("by_bookingRef", q.eq("bookingRef", ...)) for internal orders
    4. When reading order amounts: read totalAmountMinor from orders table (moved from ticketTailorOrders per D-11)
    5. When reading order email: read bookerEmail from orders table (moved from ticketTailorOrders per D-17)
    6. When reading order name: read bookerName from orders table
    7. Ensure all queries use .withIndex() — no full table scans (D-34)
    8. Keep requireIdentity(ctx) as first statement in public mutations (D-29)

  </action>
  <verify>npx convex dev compiles; tikkie.ts contains no references to "ticketTailorOrders" for data reads (only for provider-specific extension data if needed)</verify>
  <done>Tikkie mutations read order data from core orders table; all queries use indexed lookups</done>
</task>

</tasks>

<verification>
- Run `npx convex dev` — no compilation errors
- Grep orders.ts: no "ticketTailorOrders" in query calls (only in join contexts)
- Grep orders.ts: no "submissionAttendees" or "submissionTicketSelections" or "submissionAssignments"
- Grep tikkie.ts: no "ticketTailorOrders" for primary data reads
- All queries use .withIndex() — grep for ".collect()" should show 0 results or only bounded internal helpers
</verification>

<success_criteria>

- orders.ts reads from orders, orderAttendees, orderTicketSelections, orderAssignments tables
- tikkie.ts reads order data from orders table
- All type references updated to Id<"orders">, Id<"orderAttendees">, etc.
- All queries use .withIndex() with proper indexes
- No .collect() without bounds
  </success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-orders-rewrite/24-03-SUMMARY.md`
</output>
