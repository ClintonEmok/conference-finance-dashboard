---
phase: 24-canonical-orders-rewrite
plan: 05
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03", "24-04"]
files_modified:
  - convex/accommodation.ts
autonomous: true

must_haves:
  truths:
    - "Room allocation board reads attendees from orderAttendees (core) + ticketTailorAttendees (extension)"
    - "Room allocation board reads orders from orders (core) table"
    - "Attendee assignment mutations update orderAttendees table"
    - "Gender normalization works with lowercase core gender field"
  artifacts:
    - path: convex/accommodation.ts
      provides: "Updated room allocation board reading from core + extension tables"
  key_links:
    - from: convex/accommodation.ts
      to: orderAttendees table
      via: "ctx.db.query('orderAttendees') for attendee data"
      pattern: "ctx\\.db\\.query\\(\"orderAttendees\""
    - from: convex/accommodation.ts
      to: orders table
      via: "ctx.db.query('orders') for order data"
      pattern: "ctx\\.db\\.query\\(\"orders\""
---

<objective>
Update accommodation module — most complex consumer — to read from core tables
</objective>

Purpose: accommodation.ts is the largest and most complex consumer (1847 lines). It reads both TT and submission data for the room allocation board. Must be updated last since it depends on all other consumers being updated.

Output: Updated accommodation.ts with core-table reads for orders and attendees.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md
@convex/accommodation.ts
@convex/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update accommodation.ts table references and helper functions</name>
  <files>convex/accommodation.ts</files>
  <read_first>
    - convex/accommodation.ts (lines 1-100: DocTables, helper functions)
    - convex/accommodation.ts (lines 94-110: mapSubmissionGender)
    - convex/schema.ts (orderAttendees table definition)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-14, D-15, D-23)
  </read_first>
  <action>
    In convex/accommodation.ts, update the foundational types and helpers:

    1. Update DocTables type (line 6-11):
       - Change ticketTailorAttendees: Doc<"ticketTailorAttendees"> to include both:
         orderAttendees: Doc<"orderAttendees">
         ticketTailorAttendees: Doc<"ticketTailorAttendees"> (keep for extension data)

    2. Update getAttendeeByStringId (lines 62-70):
       - Change from "ticketTailorAttendees" to "orderAttendees" for the primary lookup
       - This function is used to resolve attendee IDs — should resolve against core table

    3. Update mapSubmissionGender (lines 94-105):
       - Rename to normalizeGender (more accurate name)
       - The function maps lowercase to uppercase — this is still needed for display
       - Core orderAttendees.gender is lowercase (D-14), TT ticketTailorAttendees.genderType is uppercase
       - Keep the mapping but add a comment explaining the direction

    4. Update getAttendeeLocation (lines 79-86):
       - This reads from customAnswers on TT attendees — keep as-is since customAnswers stays in TT table (D-18)

  </action>
  <verify>npx convex dev compiles; DocTables includes orderAttendees; getAttendeeByStringId queries orderAttendees</verify>
  <done>DocTables and helper functions reference orderAttendees; gender normalization preserved; getAttendeeLocation unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update getRoomAllocationBoard and assignment mutations</name>
  <files>convex/accommodation.ts</files>
  <read_first>
    - convex/accommodation.ts (lines 200-700: getRoomAllocationBoard — main consumer)
    - convex/accommodation.ts (lines 1060-1250: assignment mutations)
    - convex/accommodation.ts (lines 1460-1530: additional attendee queries)
    - convex/accommodation.ts (lines 1825-1847: submissions count query)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-23, D-27, D-30-D-34)
  </read_first>
  <action>
    Update all data queries and mutations in accommodation.ts:

    1. Replace ctx.db.query("submissions") with ctx.db.query("orders"):
       - Line 307, 312: query orders instead of submissions
       - Line 1831-1832: query orders instead of submissions
       - Update Id<"submissions"> to Id<"orders">

    2. Replace ctx.db.query("submissionAttendees") with ctx.db.query("orderAttendees"):
       - Line 487: query orderAttendees instead of submissionAttendees
       - Update Id<"submissionAttendees"> to Id<"orderAttendees">
       - Update submissionId references to orderId

    3. Replace ctx.db.query("submissionAssignments") with ctx.db.query("orderAssignments"):
       - Line 495: query orderAssignments instead of submissionAssignments
       - Update Id<"submissionAssignments"> to Id<"orderAssignments">
       - Update submissionId references to orderId

    4. For ticketTailorAttendees queries (lines 208, 302, 704, 742, 1106, 1182, 1463, 1524):
       - These read TT-specific data (genderType, ticketTypeLabel, customAnswers, etc.)
       - Keep querying ticketTailorAttendees for provider-specific fields
       - BUT also query orderAttendees for core fields (name, email, assignedRoomId, allocationPriority)
       - Join the two by ticketTailorAttendees.attendeeId = orderAttendees._id

    5. For attendee assignment mutations (lines 1065-1240):
       - updateAttendeeRoom: patch orderAttendees.assignedRoomId (moved from ticketTailorAttendees per D-15)
       - updateAttendeePriority: patch orderAttendees.allocationPriority and orderAttendees.priorityReason (moved per D-15)
       - removeAttendeeAssignment: patch orderAttendees.assignedRoomId to undefined
       - clearAttendeePriority: patch orderAttendees.allocationPriority and orderAttendees.priorityReason to undefined

    6. For getRoomAllocationBoard (lines 220-662):
       - Build attendee queue from BOTH sources:
         a. orderAttendees (core) — has name, assignedRoomId, allocationPriority, priorityReason
         b. ticketTailorAttendees (extension) — has genderType, ticketTypeLabel, customAnswers, tikkieAmountOverrideMinor
       - Join on ticketTailorAttendees.attendeeId = orderAttendees._id
       - For internal orders (source="internal"): attendees are only in orderAttendees
       - For integration orders (source="integration"): attendees exist in both tables

    7. Ensure all queries use .withIndex() — no full table scans (D-34)
    8. For large attendee reads, use .take(N) with reasonable bounds (D-31)
    9. Keep requireIdentity(ctx) as first statement in public mutations (D-29)

  </action>
  <verify>npx convex dev compiles; accommodation.ts contains ctx.db.query("orderAttendees") and ctx.db.query("orders"); no references to "submissions" or "submissionAttendees" or "submissionAssignments"</verify>
  <done>Room allocation board reads from core + extension tables; assignment mutations update orderAttendees; all queries use indexes</done>
</task>

</tasks>

<verification>
- Run `npx convex dev` — no compilation errors
- Grep accommodation.ts: no "submissions" or "submissionAttendees" or "submissionAssignments" or "submissionTicketSelections"
- Grep accommodation.ts: contains ctx.db.query("orderAttendees") and ctx.db.query("orders")
- Grep accommodation.ts: assignment mutations patch orderAttendees (not ticketTailorAttendees) for assignedRoomId, allocationPriority
- getRoomAllocationBoard joins orderAttendees + ticketTailorAttendees for complete attendee data
</verification>

<success_criteria>

- accommodation.ts reads from core orders and orderAttendees tables
- TT extension data joined via attendeeId FK
- Assignment mutations update orderAttendees for domain fields
- All queries use .withIndex() or .take(N)
- Room allocation board works for both internal and integration orders
  </success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-orders-rewrite/24-05-SUMMARY.md`
</output>
