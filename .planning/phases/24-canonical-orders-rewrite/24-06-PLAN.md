---
phase: 24-canonical-orders-rewrite
plan: 06
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03", "24-04"]
files_modified:
  - convex/signupSubmission.ts
autonomous: true

must_haves:
  truths:
    - "Signup submission writes to orders, orderAttendees, orderTicketSelections, orderAssignments, orderIdempotency tables"
    - "Idempotency check queries orderIdempotency table"
    - "Booking ref generation uses orders table"
    - "Restore payload reads from orders + child tables"
  artifacts:
    - path: convex/signupSubmission.ts
      provides: "Updated signup submission writing to core tables"
  key_links:
    - from: convex/signupSubmission.ts
      to: orders table
      via: "ctx.db.insert('orders')"
      pattern: "ctx\\.db\\.insert\\(\"orders\""
    - from: convex/signupSubmission.ts
      to: orderIdempotency table
      via: "ctx.db.query('orderIdempotency') for idempotency check"
      pattern: "ctx\\.db\\.query\\(\"orderIdempotency\""
---

<objective>
Update signup submission to write to core orders tables
</objective>

Purpose: signupSubmission.ts is the public-facing submission endpoint. It writes to the submissions* tables which are being renamed to orders*. Must update all table references to use the new canonical names.

Output: Updated signupSubmission.ts writing to orders, orderAttendees, orderTicketSelections, orderAssignments, orderIdempotency.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md
@convex/signupSubmission.ts
@convex/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update signupSubmission.ts to write to core orders tables</name>
  <files>convex/signupSubmission.ts</files>
  <read_first>
    - convex/signupSubmission.ts (full file — 871 lines)
    - convex/schema.ts (orders, orderAttendees, orderTicketSelections, orderAssignments, orderIdempotency tables)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-04 through D-08)
  </read_first>
  <action>
    Update convex/signupSubmission.ts — systematic table reference replacement:

    1. Replace ALL "submissions" table references with "orders":
       - Line 115: Id<"submissions"> → Id<"orders">
       - Line 250: v.id("submissions") → v.id("orders")
       - Line 482: .query("submissions") → .query("orders")
       - Line 534: ctx.db.insert("submissions", ...) → ctx.db.insert("orders", ...)
       - Line 669: v.id("submissions") → v.id("orders")
       - Line 705: .query("submissions") → .query("orders")

    2. Replace ALL "submissionAttendees" table references with "orderAttendees":
       - Line 153: Id<"submissionAttendees"> → Id<"orderAttendees">
       - Line 164: .query("submissionAttendees") → .query("orderAttendees")
       - Line 489: .query("submissionAttendees") → .query("orderAttendees")
       - Line 547: Id<"submissionAttendees"> → Id<"orderAttendees">
       - Line 550: ctx.db.insert("submissionAttendees", ...) → ctx.db.insert("orderAttendees", ...)
       - Line 719: .query("submissionAttendees") → .query("orderAttendees")

    3. Replace ALL "submissionTicketSelections" table references with "orderTicketSelections":
       - Line 172: .query("submissionTicketSelections") → .query("orderTicketSelections")
       - Line 489: .query("submissionTicketSelections") → .query("orderTicketSelections")
       - Line 576: ctx.db.insert("submissionTicketSelections", ...) → ctx.db.insert("orderTicketSelections", ...)
       - Line 725: .query("submissionTicketSelections") → .query("orderTicketSelections")

    4. Replace ALL "submissionAssignments" table references with "orderAssignments":
       - Line 177: .query("submissionAssignments") → .query("orderAssignments")
       - Line 512: .query("submissionAssignments") → .query("orderAssignments")
       - Line 594: ctx.db.insert("submissionAssignments", ...) → ctx.db.insert("orderAssignments", ...)
       - Line 775: .query("submissionAssignments") → .query("orderAssignments")

    5. Replace ALL "submissionIdempotency" table references with "orderIdempotency":
       - Line 275: .query("submissionIdempotency") → .query("orderIdempotency")
       - Line 302: .query("submissionIdempotency") → .query("orderIdempotency")
       - Line 614: ctx.db.insert("submissionIdempotency", ...) → ctx.db.insert("orderIdempotency", ...)

    6. Update buildRestorePayload function (lines 113-200):
       - Change submissionId param type from Id<"submissions"> to Id<"orders">
       - Change ctx.db.get(submissionId) to ctx.db.get("orders", submissionId)
       - Change attendees query from "submissionAttendees" to "orderAttendees"
       - Change selections query from "submissionTicketSelections" to "orderTicketSelections"
       - Change assignments query from "submissionAssignments" to "orderAssignments"
       - Update attendee type from Id<"submissionAttendees"> to Id<"orderAttendees">

    7. Update the main submission mutation (lines 480-620):
       - Change submission existence check to query "orders" table
       - Change ctx.db.insert("orders", {...}) with the correct field names:
         eventId, source: "internal", idempotencyKey, bookingRef, honeypotSeen, notes, bookerName, bookerEmail, bookerPhone, submittedAt
       - Change attendee inserts to ctx.db.insert("orderAttendees", {...}) with:
         orderId (FK to orders._id), attendeeKey, name, email, phone, gender, location, dietaryRestrictions, roommatePreference, roommateAvoid, sortOrder
       - Change ticket selection inserts to ctx.db.insert("orderTicketSelections", {...}) with:
         orderId, attendeeId, ticketTypeId, quantity, sortOrder
       - Change assignment inserts to ctx.db.insert("orderAssignments", {...}) with:
         orderId, attendeeId, slotId, assignmentIntent, sortOrder
       - Change idempotency insert to ctx.db.insert("orderIdempotency", {...}) with:
         eventId, idempotencyKey, fingerprint, orderId (FK to orders._id), expiresAt

    8. Update the getSubmissionRestore query (lines 665-780):
       - Query "orders" table instead of "submissions"
       - Query "orderAttendees" instead of "submissionAttendees"
       - Query "orderTicketSelections" instead of "submissionTicketSelections"
       - Query "orderAssignments" instead of "submissionAssignments"

    9. Keep requireIdentity(ctx) as first statement in public mutations (D-29)
    10. All queries use .withIndex() — no full table scans (D-34)

  </action>
  <verify>npx convex dev compiles; signupSubmission.ts contains no references to "submissions", "submissionAttendees", "submissionTicketSelections", "submissionAssignments", or "submissionIdempotency"</verify>
  <done>All submission writes go to orders, orderAttendees, orderTicketSelections, orderAssignments, orderIdempotency tables; idempotency checks work with new table names</done>
</task>

</tasks>

<verification>
- Run `npx convex dev` — no compilation errors
- Grep signupSubmission.ts: no "submissions" or "submissionAttendees" or "submissionTicketSelections" or "submissionAssignments" or "submissionIdempotency"
- Grep signupSubmission.ts: contains ctx.db.insert("orders"), ctx.db.insert("orderAttendees"), ctx.db.insert("orderTicketSelections"), ctx.db.insert("orderAssignments"), ctx.db.insert("orderIdempotency")
- Idempotency check queries orderIdempotency table with correct index
</verification>

<success_criteria>

- signupSubmission.ts writes to all 5 core tables
- All Id<"orders">, Id<"orderAttendees">, etc. types updated
- Idempotency checks work with orderIdempotency table
- Restore payload reads from correct tables
- All queries use .withIndex() with proper indexes
  </success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-orders-rewrite/24-06-SUMMARY.md`
</output>
