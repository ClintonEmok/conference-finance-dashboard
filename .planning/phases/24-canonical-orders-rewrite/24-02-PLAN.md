---
phase: 24-canonical-orders-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - convex/sync.ts
  - convex/autoSync.ts
autonomous: true

must_haves:
  truths:
    - "Sync pipeline writes to orders (core) + ticketTailorOrders (extension) simultaneously"
    - "Sync pipeline writes to orderAttendees (core) + ticketTailorAttendees (extension) simultaneously"
    - "Archive logic patches both orders and ticketTailorOrders tables"
    - "Family linking references orderAttendees IDs"
  artifacts:
    - path: convex/sync.ts
      provides: "Rewritten TT sync mutations reading/writing core + extension tables"
      exports:
        [
          "internalUpsertTicketTailorOrder",
          "internalUpsertTicketTailorAttendee",
          "internalArchiveMissingOrdersForEvent",
        ]
    - path: convex/autoSync.ts
      provides: "Updated auto-sync action using rewritten mutations"
  key_links:
    - from: convex/sync.ts
      to: orders table
      via: "ctx.db.query('orders') and ctx.db.insert('orders')"
      pattern: "ctx\\.db\\.(query|insert)\\(\"orders\""
    - from: convex/sync.ts
      to: orderAttendees table
      via: "ctx.db.query('orderAttendees') and ctx.db.insert('orderAttendees')"
      pattern: "ctx\\.db\\.(query|insert)\\(\"orderAttendees\""
    - from: convex/autoSync.ts
      to: internalUpsertTicketTailorOrder
      via: "ctx.runMutation(internal.sync.internalUpsertTicketTailorOrder)"
      pattern: "internal\\.sync\\.internalUpsertTicketTailorOrder"
---

<objective>
Rewrite TT sync pipeline to write to core + extension tables simultaneously
</objective>

Purpose: The sync pipeline is the primary data ingestion path. It must write to both the new core tables (orders/orderAttendees) and the slimmed TT extension tables. This keeps the dual-write pattern during transition.

Output: Rewritten sync.ts mutations and updated autoSync.ts action.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md
@convex/sync.ts
@convex/autoSync.ts
@convex/schema.ts
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite internalUpsertTicketTailorOrder to write orders + ticketTailorOrders</name>
  <files>convex/sync.ts</files>
  <read_first>
    - convex/sync.ts (lines 736-783: current internalUpsertTicketTailorOrder)
    - convex/schema.ts (new orders + ticketTailorOrders table definitions)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-20, D-21)
  </read_first>
  <action>
    Rewrite internalUpsertTicketTailorOrder in convex/sync.ts:

    1. Change args: REMOVE eventId (v.id("events")), buyerEmail, buyerName, currency, totalAmountMinor, orderedAt, refundedAt, cancelledAt from args. These now go to core orders table.
    2. New args: providerOrderId (v.string()), providerEventId (v.string()), normalizedStatus (optional), providerStatus (optional), normalizationNote (optional), rawPayload (v.any()), isArchived (optional, default false).
    3. Handler logic:
       a. Look up existing ticketTailorOrders by providerOrderId index
       b. Look up or create the canonical orders record:
          - Query orders table by .withIndex("by_providerOrderId", q.eq("providerOrderId", args.providerOrderId))
          - If exists: patch the orders record with {providerEventId, normalizedStatus, providerStatus, normalizationNote, rawPayload, isArchived: false, archiveReason: undefined}
          - If not exists: insert into orders table with {eventId: (resolve from ticketTailorOrders or skip if not yet available), source: "integration", providerOrderId, providerEventId, normalizedStatus, providerStatus, normalizationNote, rawPayload, isArchived: false}
       c. Upsert ticketTailorOrders (extension) with slimmed fields: providerOrderId, providerEventId, orderId (FK to orders._id), providerStatus, normalizedStatus, normalizationNote, isArchived, archivedAt, archiveReason, removedAt, removedReason, refundedAt, cancelledAt, rawPayload
    4. Return type: v.object({ orderId: v.id("orders"), ticketTailorOrderId: v.id("ticketTailorOrders") })
    5. Use ctx.db directly (not ctx.runQuery/ctx.runMutation) — single transaction (D-26)
    6. Use .first() for indexed lookups, not .collect() (D-27, D-32)

  </action>
  <verify>npx convex dev compiles; sync.ts contains ctx.db.query("orders") and ctx.db.query("ticketTailorOrders") in internalUpsertTicketTailorOrder</verify>
  <done>internalUpsertTicketTailorOrder writes to both orders and ticketTailorOrders tables in a single transaction; returns both IDs</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite internalUpsertTicketTailorAttendee to write orderAttendees + ticketTailorAttendees</name>
  <files>convex/sync.ts</files>
  <read_first>
    - convex/sync.ts (lines 785-842: current internalUpsertTicketTailorAttendee)
    - convex/schema.ts (new orderAttendees + ticketTailorAttendees table definitions)
    - .planning/phases/24-canonical-orders-rewrite/24-CONTEXT.md (decisions D-22)
  </read_first>
  <action>
    Rewrite internalUpsertTicketTailorAttendee in convex/sync.ts:

    1. Change args: REMOVE name, email, eventId, orderId (as ticketTailorOrders), assignedRoomId, allocationPriority, priorityReason. These now go to core orderAttendees table.
    2. New args: providerAttendeeId (optional), providerIssuedTicketId (optional), providerTicketTypeId (optional), providerEventId (v.string()), providerOrderId (v.string()), orderId (v.id("orders")), ticketTypeLabel (optional), ticketStatus (optional), checkedInAt (optional), rawPayload (v.any()), customAnswers (optional), genderType (optional, uppercase), ageGroup (optional), ticketCategory (optional), tikkieAmountOverrideMinor (optional).
    3. Handler logic:
       a. Look up existing ticketTailorAttendees by .withIndex("providerEventOrder", q.eq("providerEventId", args.providerEventId).eq("providerOrderId", args.providerOrderId)) then filter by providerAttendeeId
       b. Look up or create the canonical orderAttendees record:
          - Need to map providerAttendeeId to orderAttendees. Query orderAttendees table with a lookup mechanism (e.g., by a combination of orderId + attendeeKey or providerAttendeeId if stored)
          - For now: insert into orderAttendees with {orderId: args.orderId, attendeeKey: args.providerAttendeeId ?? args.providerIssuedTicketId ?? "auto", name: (extracted from rawPayload if available), email: (optional), phone: (optional), gender: "unknown" (TT gender is uppercase, normalize later), location: "", dietaryRestrictions: "", roommatePreference: "", roommateAvoid: "", sortOrder: 0}
          - NOTE: TT attendees don't have all the fields that orderAttendees requires. For TT-sourced attendees, use defaults: location="", dietaryRestrictions="", roommatePreference="", roommateAvoid=""
       c. Upsert ticketTailorAttendees (extension) with slimmed fields + attendeeId FK to orderAttendees._id
    4. Return type: v.object({ attendeeId: v.id("orderAttendees"), ticketTailorAttendeeId: v.id("ticketTailorAttendees") })
    5. Use ctx.db directly — single transaction (D-26)

  </action>
  <verify>npx convex dev compiles; sync.ts contains ctx.db.query("orderAttendees") and ctx.db.query("ticketTailorAttendees") in internalUpsertTicketTailorAttendee</verify>
  <done>internalUpsertTicketTailorAttendee writes to both orderAttendees and ticketTailorAttendees; attendeeId FK correctly set</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite internalArchiveMissingOrdersForEvent and update helper queries</name>
  <files>convex/sync.ts</files>
  <read_first>
    - convex/sync.ts (lines 844-893: current internalArchiveMissingOrdersForEvent)
    - convex/sync.ts (lines 959-1009: internal helper queries)
    - convex/schema.ts (orders table indexes)
  </read_first>
  <action>
    1. Rewrite internalArchiveMissingOrdersForEvent:
       - Query ticketTailorOrders by providerEventId index
       - For each missing order (not in seenProviderOrderIds, not already removed):
         a. Patch ticketTailorOrders with isArchived=true, archivedAt, archiveReason, normalizedStatus="cancelled", cancelledAt, normalizationNote
         b. Also patch the linked orders record (via orderId FK) with status="cancelled" if it exists
       - Query orders table by .withIndex("by_providerEventId", q.eq("providerEventId", args.providerEventId)) to find any orders without corresponding TT records and mark them
       - Return {scanned: number, archived: number}

    2. Update internalGetPaidOrders:
       - Change from querying ticketTailorOrders to querying orders table
       - Query orders with .withIndex("by_status", q.eq("status", "paid"))
       - Return {_id, bookerName, totalAmountMinor} from orders table

    3. Update internalGetAttendeesByOrder:
       - Change from querying ticketTailorAttendees to querying orderAttendees table
       - Query orderAttendees by .withIndex("by_orderId", q.eq("orderId", ...))
       - Return map of orderId -> attendee names

    4. Update internalGetTicketTailorAttendeesByOrderId:
       - Change orderId arg type from v.id("ticketTailorOrders") to v.id("orders")
       - Query ticketTailorAttendees via .withIndex("attendeeId", ...) joined with orderAttendees, OR query orderAttendees directly by orderId and join

    5. Update linkAttendeesAsFamily in autoSync.ts:
       - Change to reference orderAttendees IDs instead of ticketTailorAttendees IDs
       - Update internalGetTicketTailorAttendeesByOrderId calls to use orders ID

  </action>
  <verify>npx convex dev compiles; internalArchiveMissingOrdersForEvent patches both orders and ticketTailorOrders; internalGetPaidOrders queries orders table</verify>
  <done>Archive logic updates both tables; helper queries read from correct tables; family linking uses orderAttendees IDs</done>
</task>

</tasks>

<verification>
- Run `npx convex dev` — no compilation errors
- Grep sync.ts: internalUpsertTicketTailorOrder contains ctx.db.insert("orders")
- Grep sync.ts: internalUpsertTicketTailorAttendee contains ctx.db.insert("orderAttendees")
- Grep sync.ts: internalArchiveMissingOrdersForEvent patches both tables
- Grep autoSync.ts: family linking references orderAttendees
- All queries use .first() or .take(N) — no .collect() except in internal helpers that are bounded
</verification>

<success_criteria>

- TT sync writes to both core + extension tables in single transactions
- Archive logic maintains consistency across both tables
- Helper queries read from correct (core) tables
- Family linking uses orderAttendees IDs
- No .collect() without bounds; all queries use indexes
  </success_criteria>

<output>
After completion, create `.planning/phases/24-canonical-orders-rewrite/24-02-SUMMARY.md`
</output>
