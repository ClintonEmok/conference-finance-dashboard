---
phase: 18-dual-source-event-signup-platform
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [USF-01, USF-02, USF-03, DOM-01]
files_modified:
  - convex/schema.ts
  - convex/signupCatalog.ts
  - lib/types/signup.ts
  - lib/convex/hooks/signup.ts
  - lib/domain/signup/catalog.ts
autonomous: true
must_haves:
  truths:
    - "Public signup can load a single source-aware event catalog contract."
    - "Ticket types include selectable/unavailable state with explicit machine-readable reason."
    - "Accommodation readiness is explicit (eligible boolean + reason code + bed-slot readiness metadata)."
  artifacts:
    - path: "convex/schema.ts"
      provides: "Additive canonical signup entities and indexes without breaking existing ticketTailor tables"
      contains: "canonical table definitions (no required signup prefix)"
    - path: "convex/signupCatalog.ts"
      provides: "Bounded, source-aware public signup catalog query"
      exports: ["getPublicSignupCatalog"]
    - path: "lib/types/signup.ts"
      provides: "Shared contract types for public signup catalog"
    - path: "lib/convex/hooks/signup.ts"
      provides: "Client hook to consume new Convex catalog query"
  key_links:
    - from: "convex/signupCatalog.ts"
      to: "convex/schema.ts"
      via: "indexed reads over canonical and provider-backed tables"
      pattern: "ctx\.db\.query\(.*\)\.withIndex"
    - from: "lib/convex/hooks/signup.ts"
      to: "api.signupCatalog.getPublicSignupCatalog"
      via: "useQuery"
      pattern: "useQuery\(api\.signupCatalog\.getPublicSignupCatalog"
---

<objective>
Establish the canonical read foundation for public signup: one source-aware contract for event, tickets, and accommodation readiness.

Purpose: Satisfies DOM-01 and enables Phase 19 UI to consume one stable contract instead of provider-specific shapes.
Output: Canonical schema entities + public catalog query + typed hook/domain adapter.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md
@.planning/phases/18-dual-source-event-signup-platform/18-RESEARCH.md
@convex/_generated/ai/guidelines.md
@convex/schema.ts
@convex/events.ts
@lib/convex/hooks/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical signup domain schema (additive only)</name>
  <files>convex/schema.ts, lib/types/signup.ts</files>
  <read_first>convex/_generated/ai/guidelines.md, convex/schema.ts, .planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md</read_first>
  <action>
    Add canonical tables to `convex/schema.ts` without modifying or deleting existing `ticketTailor*` tables (do not require a `signup*` table-name prefix):
    - `events` with product-facing fields only: `slug`, `title`, `startsAt`, `endsAt`, `timezone`, `currency`, `isPublished`, `isSignupOpen`, `accommodationEnabled`, `primarySourceKind` (`integration|internal`), optional `primarySourceProvider`, `updatedAt`.
    - `eventSources` for integration mapping with fields: `eventId` (`v.id("events")`), `provider`, `externalEventId`, `syncStatus` (`active|paused|error`), `lastSyncedAt`, optional `providerSnapshotRef`, `updatedAt`.
    - `ticketTypes` with `eventId` (`v.id("events")`), `label`, `priceMinor`, `isActive`, `visibility`, `availabilityState` (`selectable|unavailable`), `unavailableReason`, `updatedAt`.
    - `accommodationSlots` with `eventId` (`v.id("events")`), `hotelId`, `roomId`, `slotLabel`, `genderPolicy`, `isAssignable`, `ineligibilityReason`, `updatedAt`.
    Add indexes for lookup and bounded reads:
    - `events`: `by_slug`, `by_startsAt`, `by_signup_visibility` (`isPublished`, `isSignupOpen`).
    - `eventSources`: `by_provider_and_externalEventId`, `by_eventId`, `by_eventId_and_provider`.
    - `ticketTypes`: `by_eventId`, `by_eventId_and_availabilityState`.
    - `accommodationSlots`: `by_eventId`, `by_eventId_and_isAssignable`.
    Create `lib/types/signup.ts` with exported stable reason-code unions:
    - Ticket unavailable reasons: `sold_out | disabled | hidden | not_on_sale`.
    - Accommodation ineligibility reasons: `accommodation_disabled | no_assignable_inventory | event_closed`.
    Keep all values as lower_snake_case strings for machine-readable contracts.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `convex/schema.ts` contains `events`, `eventSources`, `ticketTypes`, and `accommodationSlots` table definitions.
    - `convex/schema.ts` includes indexes `by_provider_and_externalEventId`, `by_eventId_and_provider`, `by_eventId_and_availabilityState`, and `by_eventId_and_isAssignable`.
    - Canonical relations use typed Convex ids where applicable (`v.id("events")` for child `eventId` columns).
    - Upsert/seed paths enforce uniqueness invariants for `events.slug` and `eventSources(provider, externalEventId)`.
    - `lib/types/signup.ts` exports both ticket and accommodation reason-code unions with exact literal values.
  </acceptance_criteria>
  <done>Canonical additive tables and reason-code types exist and compile without altering legacy provider tables.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded public signup catalog contract</name>
  <files>convex/signupCatalog.ts, lib/convex/hooks/signup.ts, lib/domain/signup/catalog.ts</files>
  <read_first>convex/_generated/ai/guidelines.md, convex/events.ts, lib/convex/hooks/events.ts, lib/domain/accommodation/inventory.ts, .planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md</read_first>
  <action>
    Create `convex/signupCatalog.ts` exporting `getPublicSignupCatalog` query with full args/returns validators.
    Contract details:
    - Return only published + open events.
    - Sort by `startsAt` ascending, then title.
    - Include ticket array with `selectable` boolean and `reason` string|null (from reason-code union).
    - Include accommodation object: `eligible` boolean, `reason` string|null, and assignable slot summaries (`slotId`, `roomLabel`, `roomTypeLabel`, `assignable`).
    - Use bounded/index-first reads (`withIndex + take`) and do not use unbounded `.collect()`.
    Create `lib/convex/hooks/signup.ts` with `usePublicSignupCatalog()` hook bound to `api.signupCatalog.getPublicSignupCatalog`.
    Create `lib/domain/signup/catalog.ts` adapter that normalizes null/undefined optional values for UI-safe usage (no shape mutation).
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `convex/signupCatalog.ts` exports `getPublicSignupCatalog` and defines both `args` and `returns` validators.
    - Query code contains bounded access (`take(` and/or pagination) and no new unbounded `.collect()` in this file.
    - `lib/convex/hooks/signup.ts` contains `useQuery(api.signupCatalog.getPublicSignupCatalog`.
    - `lib/domain/signup/catalog.ts` exports a normalization function consumed by signup catalog callers.
  </acceptance_criteria>
  <done>Public canonical read contract is callable, typed, and bounded for both integration/internal-source events.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Confirm generated Convex API references include `signupCatalog.getPublicSignupCatalog` after codegen/dev refresh.
</verification>

<success_criteria>

- DOM-01 satisfied by one source-aware public contract.
- USF-02/USF-03 contract support exists (ticket availability + accommodation eligibility reasons).
- No legacy ticketTailor schema/table behavior regresses.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-dual-source-event-signup-platform/18-01-SUMMARY.md`
</output>
