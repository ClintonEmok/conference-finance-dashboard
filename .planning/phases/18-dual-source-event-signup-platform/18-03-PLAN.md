---
phase: 18-dual-source-event-signup-platform
plan: 03
type: execute
wave: 3
depends_on: [18-02]
requirements: [USF-06, DOM-03]
files_modified:
  - convex/signupSubmission.ts
  - app/api/signup/submit/route.ts
  - app/api/signup/submit/route.test.ts
  - lib/types/signup.ts
autonomous: true
must_haves:
  truths:
    - "Capacity checks and duplicate/idempotent retry checks execute in the same write transaction as submission persistence."
    - "Accidental retry of same payload returns previously created submission reference instead of creating duplicate row(s)."
    - "Public submit endpoint applies abuse controls (rate limit + honeypot) before write execution."
  artifacts:
    - path: "convex/signupSubmission.ts"
      provides: "Transactional guard logic and idempotent replay behavior"
      contains: "capacity + duplicate guard paths"
    - path: "app/api/signup/submit/route.ts"
      provides: "Rate-limit and honeypot enforcement on public submission"
      exports: ["POST"]
    - path: "app/api/signup/submit/route.test.ts"
      provides: "Regression tests for abuse and idempotency behavior"
  key_links:
    - from: "app/api/signup/submit/route.ts"
      to: "lib/rate-limit.ts"
      via: "enforceRateLimit"
      pattern: "enforceRateLimit\(request, \"signup-submit\""
    - from: "convex/signupSubmission.ts"
      to: "submission/idempotency tables"
      via: "same-handler read-before-write guard"
      pattern: "idempotency"
---

<objective>
Add transactional safety and public abuse protections to make signup submission safe under concurrency and public traffic.

Purpose: Completes DOM-03 and USF-06 for Phase 18.
Output: Guarded mutation behavior + abuse-protected public route + retry-safe tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md
@.planning/phases/18-dual-source-event-signup-platform/18-RESEARCH.md
@.planning/phases/18-dual-source-event-signup-platform/18-02-SUMMARY.md
@lib/rate-limit.ts
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transactional capacity + duplicate/idempotency guards</name>
  <files>convex/signupSubmission.ts, lib/types/signup.ts</files>
  <read_first>convex/signupSubmission.ts, convex/schema.ts, .planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>
    Update `submitSignupEnvelope` to enforce in-handler guard sequence before final insert commit:
    - Validate selected ticket types are selectable at write time.
    - Validate assignable accommodation slots are still assignable.
    - Enforce capacity checks for selected tickets and targeted room slots within the same mutation.
    - Enforce uniqueness invariants in mutation logic for submission writes: idempotency key/fingerprint uniqueness within retry window and no duplicate insertion for the same replay context.
    - Implement idempotent replay window using request key/fingerprint + event context; if same payload is retried in window, return prior `{submissionId, bookingRef, submittedAt}` plus restore payload sufficient for prefill/continue-or-update flow, instead of creating new rows.
    - Do not block legitimate new booking when payload differs (same booker/event allowed).
    Add clear error codes in `lib/types/signup.ts` for: `CAPACITY_EXCEEDED`, `TICKET_UNAVAILABLE`, `ASSIGNMENT_UNAVAILABLE`, `SUBMISSION_CONFLICT`.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Guard checks occur in `convex/signupSubmission.ts` before persistence completion.
    - Idempotent retry path returns previous `submissionId`/`bookingRef` plus restore payload for prefill.
    - Duplicate prevention and idempotency checks use index-backed reads (no scan/filter patterns).
    - Mutation enforces submission/idempotency uniqueness invariants under concurrent writes.
    - New typed error codes are exported in `lib/types/signup.ts`.
  </acceptance_criteria>
  <done>Mutation now enforces transactional consistency and deterministic retry behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce public abuse controls in submit route</name>
  <files>app/api/signup/submit/route.ts, app/api/signup/submit/route.test.ts</files>
  <read_first>app/api/signup/submit/route.ts, lib/rate-limit.ts, app/api/webhooks/ticket-tailor/route.ts, .planning/phases/18-dual-source-event-signup-platform/18-CONTEXT.md</read_first>
  <action>
    Update POST handler in `app/api/signup/submit/route.ts` to add abuse gates before domain submit:
    - `enforceRateLimit(request, "signup-submit", { maxRequests: 20, windowMs: 60_000 })`.
    - Honeypot check: reject when hidden field `website` is non-empty with `400` and code `HONEYPOT_TRIGGERED`.
    - Forward idempotency key from `x-idempotency-key` header to submission domain call; generate deterministic fallback from payload hash when absent.
    - Return `429` with existing rate-limit contract when throttled.
    - Ensure idempotency context persistence remains PII-minimized (no raw contact fields in idempotency records).
    Add/extend `app/api/signup/submit/route.test.ts` cases for rate limit, honeypot rejection, and idempotent retry (same reference + restore payload returned, with no user-facing `reused` marker).
  </action>
  <verify>npm run test -- signup</verify>
  <acceptance_criteria>
    - Route contains `enforceRateLimit(request, "signup-submit"` with configured limit/window.
    - Honeypot field `website` non-empty path returns `400` + `HONEYPOT_TRIGGERED`.
    - Test file covers: throttled request (`429`), honeypot (`400`), idempotent retry returns same booking/submission reference plus restore payload and no `reused` marker.
  </acceptance_criteria>
  <done>Public submit route now blocks abuse patterns and preserves safe retries.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- signup`
- Simulate duplicate submit with same payload/key and confirm same `submissionId` is returned.
</verification>

<success_criteria>

- DOM-03 satisfied: capacity + duplicate checks are transactional with writes.
- USF-06 satisfied: rate limit + honeypot + idempotent retry controls are active.
- Legitimate repeated booking attempts remain possible when payload is intentionally different.
- Idempotency/duplicate logic remains deterministic under concurrency while keeping idempotency storage PII-minimized.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-dual-source-event-signup-platform/18-03-SUMMARY.md`
</output>
