---
phase: 03-finance-visibility-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/finance/order-ledger.ts
  - lib/domain/finance/reconciliation.ts
  - app/api/dashboard/orders/route.ts
  - app/api/dashboard/orders/export/route.ts
  - app/api/dashboard/reconciliation/route.ts
  - app/dashboard/orders/page.tsx
  - app/dashboard/reconciliation/page.tsx
autonomous: false
must_haves:
  truths:
    - "Admin can inspect order-level records filtered by event/date/status"
    - "Admin can export filtered order records to CSV"
    - "Admin can view reconciliation mismatches and outstanding balances requiring follow-up"
  artifacts:
    - path: "lib/domain/finance/order-ledger.ts"
      provides: "Filtered order-list query and CSV projection for dashboard exports"
      exports: ["getOrderLedger", "buildOrderLedgerCsv"]
    - path: "lib/domain/finance/reconciliation.ts"
      provides: "Mismatch/outstanding-balance computation from Ticket Tailor orders"
      exports: ["getReconciliationRows"]
    - path: "app/api/dashboard/orders/route.ts"
      provides: "Protected JSON API for order drilldown"
      exports: ["GET"]
    - path: "app/api/dashboard/orders/export/route.ts"
      provides: "Protected CSV export API honoring order filters"
      exports: ["GET"]
    - path: "app/api/dashboard/reconciliation/route.ts"
      provides: "Protected reconciliation API for mismatch/outstanding rows"
      exports: ["GET"]
    - path: "app/dashboard/orders/page.tsx"
      provides: "Order drilldown UI with filters and CSV export trigger"
      contains: "Export CSV"
    - path: "app/dashboard/reconciliation/page.tsx"
      provides: "Reconciliation list UI with mismatch indicators"
      contains: "Outstanding"
  key_links:
    - from: "app/dashboard/orders/page.tsx"
      to: "/api/dashboard/orders"
      via: "filter fetch"
      pattern: "fetch.*api/dashboard/orders"
    - from: "app/dashboard/orders/page.tsx"
      to: "/api/dashboard/orders/export"
      via: "CSV download action"
      pattern: "orders/export"
    - from: "app/dashboard/reconciliation/page.tsx"
      to: "/api/dashboard/reconciliation"
      via: "data fetch on filter updates"
      pattern: "fetch.*api/dashboard/reconciliation"
    - from: "app/api/dashboard/orders/route.ts"
      to: "lib/domain/finance/order-ledger.ts"
      via: "validated filter translation"
      pattern: "getOrderLedger"
    - from: "app/api/dashboard/reconciliation/route.ts"
      to: "lib/domain/finance/reconciliation.ts"
      via: "validated filter translation"
      pattern: "getReconciliationRows"
---

<objective>
Deliver order drilldown/export and reconciliation surfaces so finance admins can identify and action mismatches.

Purpose: Satisfy DASH-02 and DASH-03 by making synced records operationally usable, not just summarized.
Output: Protected order/reconciliation APIs, CSV export contract, and dashboard pages for drilldown + mismatch visibility.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ticket-data-reliability/02-01-SUMMARY.md
@app/dashboard/layout.tsx
@app/dashboard/ticket-tailor/sync/page.tsx
@lib/domain/finance/ticket-tailor-status.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain query modules for order ledger and reconciliation</name>
  <files>lib/domain/finance/order-ledger.ts, lib/domain/finance/reconciliation.ts</files>
  <action>Create two server-side finance modules. `getOrderLedger(filters)` returns paginated order-level rows with event metadata, canonical status, amount, and date fields for dashboard table consumption. `buildOrderLedgerCsv(rows)` returns deterministic CSV output with stable headers/order and escaped values. `getReconciliationRows(filters)` computes outstanding/mismatch candidates from canonical order status + known payment linkage fields (use conservative heuristics where external payment state is unavailable, e.g., flagged when normalized status indicates unpaid/pending or amount gaps). Keep all business logic in domain modules, not API/page components.</action>
  <verify>npm run typecheck && npm run test</verify>
  <done>Reusable domain functions provide filtered ledger rows, CSV payloads, and reconciliation candidates suitable for protected API routes.</done>
</task>

<task type="auto">
  <name>Task 2: Add protected dashboard APIs for orders, CSV export, and reconciliation</name>
  <files>app/api/dashboard/orders/route.ts, app/api/dashboard/orders/export/route.ts, app/api/dashboard/reconciliation/route.ts</files>
  <action>Create authenticated GET endpoints for orders JSON, orders CSV export, and reconciliation JSON. Parse shared query params (`eventId`, `from`, `to`, `status`, pagination) with strict validation and 400 `BAD_REQUEST` contracts for invalid values. Return 401 `UNAUTHORIZED` for missing sessions. For export route, set `Content-Type: text/csv` and `Content-Disposition` filename containing date stamp + filter scope. Ensure all endpoints echo applied filters so UI and operators can verify scope.</action>
  <verify>npm run typecheck && npm run lint && npm run test</verify>
  <done>Dashboard can securely fetch filtered order/reconciliation data and download CSV exports with predictable contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Build order drilldown + reconciliation dashboard pages</name>
  <files>app/dashboard/orders/page.tsx, app/dashboard/reconciliation/page.tsx</files>
  <action>Create `/dashboard/orders` page with filter controls (event/date/status), table of order rows, pagination controls, and an `Export CSV` action targeting `/api/dashboard/orders/export` with current filters. Create `/dashboard/reconciliation` page with filters and a list/table of outstanding or mismatched records including reason labels and key amounts/statuses. Include loading, empty, and error states; keep styling aligned with existing dashboard card/table patterns and non-sensitive operator copy.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin can drill into filtered orders, export current view to CSV, and inspect reconciliation mismatches in dedicated dashboard pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Order drilldown, CSV export, and reconciliation pages wired to protected APIs</what-built>
  <how-to-verify>
    1) Sign in and open `/dashboard/orders`; apply filters and confirm rows update.
    2) Click `Export CSV`; confirm a CSV downloads and row values match current filtered table scope.
    3) Open `/dashboard/reconciliation`; confirm outstanding/mismatch rows appear with clear reason labels.
    4) Hit one dashboard API unauthenticated (new private browser) and confirm 401 `UNAUTHORIZED` JSON contract.
  </how-to-verify>
  <resume-signal>Type "approved" if behavior matches, or report mismatches to fix.</resume-signal>
</task>

</tasks>

<verification>
- Filter changes update order and reconciliation datasets without full-page auth regressions.
- CSV export respects active filters and returns valid `text/csv` response headers.
- Reconciliation output highlights actionable mismatches/outstanding balances.
</verification>

<success_criteria>
- DASH-02 satisfied: admin can inspect order-level records and export filtered CSV.
- DASH-03 satisfied: admin can see reconciliation mismatches and outstanding balances requiring follow-up.
- All new finance APIs remain protected and use explicit operational error contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finance-visibility-reconciliation/03-02-SUMMARY.md`
</output>
