---
phase: 03-finance-visibility-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/finance/reporting.ts
  - app/api/dashboard/revenue/route.ts
  - app/dashboard/page.tsx
  - app/dashboard/layout.tsx
autonomous: true
must_haves:
  truths:
    - "Admin can view revenue totals from synced Ticket Tailor data for selected event/date range"
    - "Admin can view trend breakdown for the selected period"
    - "Dashboard metrics update when filter values change"
  artifacts:
    - path: "lib/domain/finance/reporting.ts"
      provides: "Server-side revenue aggregation and trend queries with event/date filtering"
      exports: ["getRevenueOverview"]
    - path: "app/api/dashboard/revenue/route.ts"
      provides: "Protected revenue overview API contract for dashboard filters"
      exports: ["GET"]
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard home with interactive revenue filters and metrics/trend display"
      contains: "Revenue"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "/api/dashboard/revenue"
      via: "filter-driven client fetch"
      pattern: "fetch.*api/dashboard/revenue"
    - from: "app/api/dashboard/revenue/route.ts"
      to: "lib/domain/finance/reporting.ts"
      via: "normalized query parameter parsing + aggregation"
      pattern: "getRevenueOverview"
    - from: "lib/domain/finance/reporting.ts"
      to: "prisma.ticketTailorOrder"
      via: "aggregate/group queries on normalized statuses"
      pattern: "prisma\\.ticketTailorOrder"
---

<objective>
Deliver the first finance visibility surface: filterable revenue totals and trends from synced Ticket Tailor records.

Purpose: Satisfy DASH-01 so finance admins can trust topline performance before drilldown/reconciliation work.
Output: Shared reporting query module, protected revenue API, and updated dashboard home with filters and metrics/trend display.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ticket-data-reliability/02-01-SUMMARY.md
@app/dashboard/layout.tsx
@app/dashboard/page.tsx
@app/api/ticket-tailor/sync/route.ts
@lib/integrations/ticket-tailor/sync.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add finance reporting aggregation module for revenue overview</name>
  <files>lib/domain/finance/reporting.ts</files>
  <action>Create a server-side query module that computes revenue overview from `TicketTailorOrder` + `TicketTailorEvent` using canonical normalized statuses already stored in Phase 2. Implement `getRevenueOverview({ eventId, from, to, trendGranularity })` that returns: totals (`grossMinor`, `paidMinor`, `refundedMinor`, `netMinor`), order counts by canonical status, and trend buckets grouped by day for the selected range. Use explicit, deterministic date/event filters; default to all events + last 30 days when filters are omitted. Keep money values in minor units (integers) to avoid floating-point drift and keep mapping logic in this domain module (not UI/API route).</action>
  <verify>npm run typecheck && npm run test</verify>
  <done>Revenue overview data can be produced from durable sync tables with consistent filtering and canonical-status semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Expose protected revenue overview API</name>
  <files>app/api/dashboard/revenue/route.ts</files>
  <action>Create authenticated `GET /api/dashboard/revenue` endpoint using `auth.api.getSession({ headers })`. Parse query params (`eventId`, `from`, `to`) with strict validation; invalid dates or inverted ranges return 400 `{ error: { code: "BAD_REQUEST", message } }`. For valid requests, call `getRevenueOverview` and return stable JSON payload including applied filters + generated timestamp. Keep unauthorized contract consistent with existing operational APIs (401 + `UNAUTHORIZED`).</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Dashboard clients can request filterable revenue totals/trends through a protected, explicit contract.</done>
</task>

<task type="auto">
  <name>Task 3: Replace dashboard home with filterable metrics/trends surface</name>
  <files>app/dashboard/page.tsx, app/dashboard/layout.tsx</files>
  <action>Replace placeholder dashboard home content with a revenue visibility surface that includes filter controls (event selector input and from/to range), summary cards (gross/paid/refunded/net + status counts), and a trend table/chart-friendly list. Wire filters to `/api/dashboard/revenue` and show loading, empty, and error states with neutral operational copy. Update dashboard header navigation to include links for upcoming Phase 3 surfaces (`/dashboard/orders`, `/dashboard/reconciliation`) while preserving existing integrations/sync access patterns and logout behavior.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin sees meaningful finance metrics on `/dashboard`, and changing filters visibly refreshes totals/trends without auth regressions.</done>
</task>

</tasks>

<verification>
1. Sign in and open `/dashboard`; confirm revenue cards and trend section render instead of placeholder text.
2. Apply at least one date filter and confirm API-backed values update.
3. Hit `/api/dashboard/revenue` unauthenticated and confirm 401 `UNAUTHORIZED` JSON contract.
4. Hit `/api/dashboard/revenue?from=invalid` and confirm 400 `BAD_REQUEST` contract.
</verification>

<success_criteria>
- DASH-01 satisfied: admin can inspect revenue totals and trends by event/date range from synced data.
- Revenue calculations use canonical normalized statuses from Phase 2, not ad-hoc UI mapping.
- Dashboard home becomes actionable for finance monitoring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finance-visibility-reconciliation/03-01-SUMMARY.md`
</output>
