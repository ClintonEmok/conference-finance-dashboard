---
phase: 46-event-owned-setup-schema-generalized-pricing-shared-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - lib/domain/finance/accommodation-pricing.ts
  - lib/domain/finance/accommodation-amounts.ts
  - tests/finance/accommodation-pricing.test.ts
  - tests/finance/accommodation-amounts.test.ts
autonomous: true
must_haves:
  truths:
    - "Existing catalog, event configuration, selection, and confirmed-snapshot rows remain schema-valid without a data migration."
    - "Event-owned commercial setup has typed rows for categories, options, age bands, rates, ticket rules, and generic option selections while physical hotel/room/room-type inventory remains referenced."
    - "The pure engine prices arbitrary resolved options by the supported units per_night, per_person, and per_person_per_night, and emits self-contained receipt lines."
    - "Both complete legacy boolean snapshots and complete v6 optionLines snapshots are accepted, while incomplete snapshots remain invalid."
  artifacts:
    - path: "convex/schema.ts"
      provides: "Additive event-owned setup tables, child selection rows, typed unit validators, setup modes, indexes, and dual-shape snapshot validator."
      contains: "eventAccommodationSetup"
    - path: "lib/domain/finance/accommodation-pricing.ts"
      provides: "Pure typed pricing-unit registry for per_night, per_person, and per_person_per_night."
      exports: ["PricingUnit", "deriveOptionChargeMinor"]
    - path: "lib/domain/finance/accommodation-amounts.ts"
      provides: "Generalized accommodation line-item derivation and fail-closed snapshot guard."
      exports: ["deriveAccommodationAmount", "buildAccommodationPriceSnapshot", "isCompleteAccommodationPriceSnapshot"]
    - path: "tests/finance/accommodation-pricing.test.ts"
      provides: "Per-unit tests including an arbitrary third option and unsupported-unit rejection."
  key_links:
    - from: "convex/schema.ts"
      to: "lib/domain/finance/accommodation-amounts.ts"
      via: "optionLines validator and shared typed unit vocabulary"
      pattern: "optionLines"
    - from: "lib/domain/finance/accommodation-amounts.ts"
      to: "lib/domain/finance/accommodation-pricing.ts"
      via: "unit-keyed handler registry"
      pattern: "deriveOptionChargeMinor"
---

<objective>
Establish the additive v6 contract and the pure pricing tracer that every later server surface will consume.

Purpose: Make event-owned commercial data and generic money math possible without rewriting legacy rows, orders, snapshots, or the preserved physical inventory model.
Output: Schema/types and a tested, pure line-item pricing core. This plan intentionally does not add UI, copy/template mutations, eligibility enforcement, archive/delete behavior, or a global backfill.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-CONTEXT.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-RESEARCH.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-PATTERNS.md
@convex/_generated/ai/guidelines.md
@convex/schema.ts
@lib/domain/finance/accommodation-amounts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the additive event-owned schema and generated contract surface</name>
  <files>convex/schema.ts</files>
  <action>
     Extend the existing accommodation section additively. Widen legacy naming keys (`accommodationCategories.code`, `accommodationOptions.code`, and legacy age-band code fields) so existing literal rows remain readable, but keep `occupancy`, `rateType`, resource kind, option kind, and the locked option units typed. Add `eventAccommodationSetup` as the one-row-per-event setup-mode/version boundary with `legacy_global | event_owned | uninitialized`, monotonic `updatedAt`, and nullable provenance placeholders that do not require a Phase 47 template table. Add event-owned category rows with `key`, label/description, `isSuperior`, ordering, enable/archive-ready state; event-owned age-band rows with key, bounds, and ordering; and make `eventAccommodationOptions` support an event-owned definition (`key`, label, description, typed kind/unit including `per_person_per_night`, price, eligibility/category-application metadata, ordering, enabled) while retaining its optional legacy global `optionId` reference for dual-read. Add an optional event-owned category key/id to rates while retaining the legacy global category reference, event age-pricing key support, and `eventTicketAccommodationRules` keyed by event and ticket type with allowed category keys, allowed occupancies, accommodation inclusion, included nights, and included option keys for covered base nights. Add `orderAccommodationOptionSelections` as child rows keyed by selection/order/attendee plus event option key, quantity, and sort order. Extend selection rows with optional event category identity and validated stay-choice fields, and extend the confirmation snapshot validator with fully resolved `optionLines` and stay/inclusion basis while retaining the complete v5 boolean shape. Add indexes named with all fields and typed `v.id` foreign keys; do not store option selections as an unbounded array. Keep hotels, rooms, room types, capacity, event-hotel links, slots, and assignment tables unchanged. Do not add a named template table, copy mutation, archive/delete mutation, or eligibility-enforcement logic in this task.
  </action>
  <verify>npx convex codegen && npx convex dev --once</verify>
  <done>Schema accepts the new typed event-owned rows and child selections, generated data-model types include them, legacy documents remain valid, every new index is event-scoped/index-first, and no physical-inventory table or legacy snapshot field was removed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace named accommodation pricing branches with the typed line-item engine</name>
  <files>lib/domain/finance/accommodation-pricing.ts, lib/domain/finance/accommodation-amounts.ts</files>
  <action>
     Create a pure `PricingUnit` registry supporting only `per_night`, `per_person`, and `per_person_per_night`. Normalize minor units, quantities, and night bases to safe non-negative finite values; charge `per_night` as price × applicable nights × quantity, `per_person` as price × quantity, and `per_person_per_night` as price × applicable nights × eligible-person quantity. Reject unsupported units rather than silently pricing them as zero. Preserve the typed age-pricing behaviors (`free`, `full`, `percent`, `flat`) and apply percent rounding using the existing `allocateMinorAmountByWeight` convention, with age-band selection resolved by the server context rather than literal age-band codes. Refactor `deriveAccommodationAmount` and `buildAccommodationPriceSnapshot` to consume resolved option lines (key, label, typed unit, price per unit, quantity, resolved nights, and inclusion basis) and produce data-driven receipt lines with label, unit, quantity, nights, unit price, and charge. Base-room covered nights and option inclusion are separate inputs: a non-included per-person-per-night upgrade remains chargeable on covered nights and extra nights. Remove literal category-code comparisons and named `superior_upgrade`/`cot` money branches from the pure module; legacy boolean fields remain represented only for the compatibility snapshot shape and are mapped by the server loader later. Extend `AccommodationPriceSnapshot` with optional complete `optionLines` and make `isCompleteAccommodationPriceSnapshot` return true for either a complete legacy v5 shape or a complete v6 shape whose every line has key/label/unit/price/quantity/nights/charge. Preserve the existing confirmed-row fail-closed semantics and snapshot self-containment.
  </action>
  <verify>npx vitest run tests/finance/accommodation-pricing.test.ts tests/finance/accommodation-amounts.test.ts</verify>
  <done>All supported unit math is centralized in the typed registry, the pure module contains no hardcoded category/option-code pricing branches, receipt lines are generic and snapshot-ready, and malformed or incomplete snapshots cannot pass the guard.</done>
</task>

<task type="auto">
  <name>Task 3: Prove unit semantics and snapshot compatibility before server wiring</name>
  <files>tests/finance/accommodation-pricing.test.ts, tests/finance/accommodation-amounts.test.ts</files>
  <action>
     Extend the existing pure fixtures with a third arbitrary option such as `bike_rack` and a non-default age-band key. Test `per_night`, `per_person`, and `per_person_per_night` separately, including quantity, covered-night, extra-night, and option-inclusion behavior, data-driven labels, zero-rate omission, deterministic line totals, and each supported age-pricing behavior including percent rounding. Prove a non-included higher-tier option charges across ticket-covered base nights plus a selected extra night, while an explicitly included option is free on covered base nights but charges on a selected extra night. Add assertions that future/unhandled units fail explicitly. Add a valid legacy boolean snapshot fixture, a valid complete v6 `optionLines` snapshot fixture, and incomplete v6 cases missing a required resolved field; assert the legacy total remains identical and incomplete confirmed data is rejected. Keep the tests pure: no Convex imports, database reads, UI assumptions, or client pricing.
  </action>
  <verify>npm test -- --run tests/finance/accommodation-pricing.test.ts tests/finance/accommodation-amounts.test.ts</verify>
  <done>Pure tests lock the two unit formulas, arbitrary-option behavior, non-default age-band fixture, legacy snapshot parity, v6 line completeness, and fail-closed behavior for incomplete snapshots.</done>
</task>

</tasks>

<verification>
Run `npx convex codegen`, `npx convex dev --once`, and the two targeted pure test files. Search `lib/domain/finance/accommodation-amounts.ts` for literal category/option-code comparisons and confirm none remain.
</verification>

<success_criteria>
The additive schema and pure pricing tracer are type-safe, generated successfully, unit-tested, and backward-compatible before any consumer is rewired.
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-01-SUMMARY.md`.
</output>
