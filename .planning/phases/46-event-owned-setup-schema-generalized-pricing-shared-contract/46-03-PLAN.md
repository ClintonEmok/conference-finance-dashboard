---
phase: 46-event-owned-setup-schema-generalized-pricing-shared-contract
plan: 03
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - convex/finance.ts
  - convex/accommodation.ts
  - convex/signupCatalog.ts
  - lib/domain/signup/catalog.ts
  - convex/signup-catalog.test.ts
  - tests/convex/finance-accommodation.test.ts
autonomous: true
must_haves:
  truths:
    - "Canonical amount-due uses the shared event-owned context for live selections and the immutable snapshot only for confirmed selections."
    - "New confirmations persist a complete resolved optionLines snapshot with eventAccommodationSetup.updatedAt, while historical configVersion values remain opaque and valid."
    - "Public catalog and quote contracts expose event-owned categories, arbitrary options, units, age bands, and receipt lines without code-specific branches, while the legacy slots contract remains readable."
    - "The allocation board reads the same event-owned setup projection and ticket-rule rows for its contract data while preserving physical inventory behavior; eligibility blocking remains Phase 50."
    - "Existing legacy-global events produce the same finance and public quote inputs after both duplicate resolvers are collapsed."
  artifacts:
    - path: "convex/finance.ts"
      provides: "Canonical `loadOrderAmountDueBreakdowns` wired to the shared event context and child option selections."
      exports: ["loadOrderAmountDueBreakdowns"]
    - path: "convex/accommodation.ts"
      provides: "Confirmation resolver/writer using child option rows, one setup version token, and v6 snapshots."
      contains: "resolveOrderAccommodationConfirmation"
    - path: "convex/signupCatalog.ts"
      provides: "Data-driven public catalog and quote projection from the shared loader."
      contains: "getPublicSignupAccommodationQuote"
    - path: "lib/domain/signup/catalog.ts"
      provides: "Shared public catalog option/category types without closed option-code assumptions."
  key_links:
    - from: "convex/finance.ts"
      to: "convex/accommodationContext.ts"
      via: "loadOrderAmountDueBreakdowns calls the sole event context loader"
      pattern: "loadEventOwnedAccommodationContext"
    - from: "convex/accommodation.ts"
      to: "lib/domain/finance/accommodation-amounts.ts"
      via: "confirmation and live quote use the same generalized line-item engine"
      pattern: "buildAccommodationPriceSnapshot"
    - from: "convex/signupCatalog.ts"
      to: "convex/accommodationContext.ts"
      via: "public catalog/quote projection"
      pattern: "loadEventOwnedAccommodationContext"
    - from: "convex/accommodation.ts"
      to: "convex/accommodationContext.ts"
      via: "allocation board reads event-owned setup and ticket-rule contract"
      pattern: "loadEventOwnedAccommodationContext"
---

<objective>
Wire the generalized pricing core and shared projection into canonical finance, confirmation, and the public server contract.

Purpose: Make every unconfirmed amount and public quote event-owned while preserving the confirmed snapshot lock and legacy public signup compatibility.
Output: Rewired finance/confirmation/public catalog/quote paths plus contract tests for arbitrary options, age bands, version tokens, and legacy parity. No UI changes are allowed.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-CONTEXT.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-RESEARCH.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-PATTERNS.md
@convex/_generated/ai/guidelines.md
@convex/finance.ts
@convex/accommodation.ts
@convex/signupCatalog.ts
@lib/domain/finance/accommodation-amounts.ts
@lib/domain/signup/catalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire canonical finance to shared live context and generic child option lines</name>
  <files>convex/finance.ts, tests/convex/finance-accommodation.test.ts</files>
  <action>
     Remove the duplicate `loadEventAccommodationContexts` implementation and have `loadOrderAmountDueBreakdowns` obtain every event context through `loadEventOwnedAccommodationContext`. Extend the local selection shape and indexed reads to include event category, validated stay choice, and `orderAccommodationOptionSelections` child rows. Build resolved option inputs from event-owned option keys/units/prices/labels and the loader's legacy boolean bridge; apply the resolved age-band rate behavior (`free`, `full`, `percent`, `flat`), base covered-night rule, option-specific inclusion keys, and extra selected nights once in the server-derived input. Support `per_night`, `per_person`, and `per_person_per_night`; a non-included per-person-per-night upgrade is charged across covered base nights and extra nights. Do not accept prices, computed nights, eligibility decisions, or snapshot data from the order payload. Preserve the existing canonical amount-due signature, per-attendee due/paid maps, missing-config legacy €0 behavior for unconfirmed rows, bounded async iteration, and fail-closed throw for confirmed rows missing finite confirmedAt/configVersion/complete snapshot. Confirmed rows must use only their persisted snapshot, with no equality check against the current setup token so historical v5 configVersion values remain valid. Extend finance tests for all three units, included versus non-included upgrades, selected extra nights, event-owned category rate keys, legacy-global parity, confirmed immutability after config edits, and incomplete-snapshot failure.
  </action>
   <verify>npx convex codegen && npx convex dev --once && npx vitest run --config vitest.convex.config.ts tests/convex/finance-accommodation.test.ts && npx vitest run tests/finance/phase45-money-integrity.test.ts</verify>
  <done>All canonical finance amounts derive from one shared context and pure line engine, dynamic child selections appear in receipt lines, confirmed history never re-prices, and legacy finance fixtures remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize confirmation resolution and persist self-contained v6 snapshots</name>
  <files>convex/accommodation.ts</files>
  <action>
     Update `resolveOrderAccommodationConfirmation` and `persistOrderAccommodationConfirmation` to load the event context through `loadEventOwnedAccommodationContext`, read the attendee's validated stay choice and option child rows, resolve generic option labels/units/prices/quantities/inclusion server-side, and call the generalized pure snapshot builder. New confirmations must record `configVersion` from `eventAccommodationSetup.updatedAt` and persist complete `optionLines` plus the resolved stay/inclusion basis containing covered base nights, selected extra nights, option inclusion decision, resolved nights, price, quantity, and charge for every line. An option explicitly included by the ticket is free only on covered base nights and is charged on selected extra nights unless the rule says otherwise; a non-included per-person-per-night upgrade is charged across all selected nights. Keep the existing `confirmedAt` lock, atomic patch behavior, and requirement that stay configuration exists. Existing v5 boolean snapshots and historical configVersion values remain readable; do not backfill or rewrite them. Preserve the physical room/assignment confirmation flow, and do not add Phase 47 eligibility enforcement, archive/delete, copy/template, or UI behavior.
  </action>
   <verify>npx convex codegen && npx convex dev --once && npx vitest run --config vitest.convex.config.ts convex/accommodation-admin.handlers.test.ts convex/accommodation-paid-priority.handlers.test.ts</verify>
  <done>Newly confirmed selections are immutable, self-contained, option-line snapshots with the sole setup version token; legacy confirmation fixtures still resolve and paid-priority/assignment confirmation behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Make the public server catalog and quote contract data-driven</name>
  <files>convex/signupCatalog.ts, lib/domain/signup/catalog.ts, convex/signup-catalog.test.ts</files>
   <action>
     Replace `loadPublicSignupAccommodationContext` internals with the shared loader while keeping the public function names and legacy slots block. Widen public category/option/age-band contract types to event-owned keys and labels; include `isSuperior`/relationship data, typed `kind` and `unit`, descriptions, enablement, age eligibility metadata, option inclusion metadata, allowed extension nights, setupMode, and versionToken. Change receipt-line validators and quote results to carry generic kind/key, label, unit, quantity, nights, unit price, inclusion basis, and charge. Keep `resolvePublicSignupSelection` as the public validation choke point and preserve existing no-accommodation and legacy ticket/slot behavior; Phase 47 will add the centralized rejection resolver and archive enforcement. Accept only event-scoped option keys, quantities, and bounded stay choices in the server contract, with prices/computed nights/eligibility resolved from the shared context. Add handler tests for a third arbitrary option, a non-default age band, event-owned Standard/Superior data without literal code branching, a ticket that covers base nights but not a per-person-per-night upgrade, a selected extra night, quote/finance parity, and an uninitialized/legacy event. In event-owned mode, use `eventTicketAccommodationRules` as the entitlement source; `ticketTypes.roomTypeId` and `accommodationIncluded` are seed/legacy inputs only.
   </action>
   <verify>npx convex codegen && npx convex dev --once && npx vitest run --config vitest.convex.config.ts convex/signup-catalog.test.ts && npm run typecheck</verify>
   <done>Public catalog and quote responses expose every configured event-owned option generically, third-option and age-band fixtures price through the same engine as finance, event-owned ticket rules are authoritative, and no public server resolver reads global catalog rows for event-owned configuration.</done>
   </task>

   <task type="auto">
     <name>Task 4: Wire allocation to the shared event-owned contract without adding Phase 50 enforcement</name>
     <files>convex/accommodation.ts, convex/accommodation-paid-priority.handlers.test.ts</files>
     <action>
       Update `getRoomAllocationBoard` to obtain event-owned setup mode, version token, and event ticket accommodation rules through `loadEventOwnedAccommodationContext` while preserving the existing physical hotel/room/room-type/capacity projection, paid-priority state, and assignment behavior. In event-owned mode, `eventTicketAccommodationRules` is the contract source; `ticketTypes.roomTypeId` and `accommodationIncluded` remain seed/legacy inputs only. Attach the resolved rule/version contract data needed by later allocation work, but do not block mismatches, implement the shared rejection resolver, or change the allocation UI; Phase 50 owns enforcement and presentation. Add handler coverage proving the board reads event-owned rules and that physical inventory and paid-priority output remain unchanged.
     </action>
     <verify>npx convex codegen && npx convex dev --once && npx vitest run --config vitest.convex.config.ts convex/accommodation-paid-priority.handlers.test.ts</verify>
     <done>Allocation reads the same event-owned projection and ticket-rule rows as other server consumers without duplicating configuration resolution or changing physical placement/enforcement behavior.</done>
   </task>

</tasks>

<verification>
  Run `npx convex codegen`, `npx convex dev --once`, the targeted finance, accommodation-admin/paid-priority, allocation, and signup-catalog handler suites, plus `npm run typecheck`. Run a static audit over modified backend/domain paths for `categoryCode === "superior"`, `optionCode === "superior_upgrade"`, `optionCode === "cot"`, and fixed age-band comparisons; remaining occurrences must be confined to explicitly documented legacy seed/compatibility branches and not runtime money/selection logic.
</verification>

<success_criteria>
Canonical finance, confirmation snapshots, and public catalog/quote all consume the same event-owned projection and generalized pricing engine, with legacy finance and signup behavior preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-03-SUMMARY.md`.
</output>
