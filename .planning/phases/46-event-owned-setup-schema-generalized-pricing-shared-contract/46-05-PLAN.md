---
phase: 46-event-owned-setup-schema-generalized-pricing-shared-contract
plan: 05
type: execute
wave: 5
depends_on: [46-04]
files_modified:
  - tests/convex/finance-accommodation.test.ts
  - tests/convex/phase45-money-integrity.test.ts
  - tests/finance/phase45-money-integrity.test.ts
  - convex/accommodation-admin.handlers.test.ts
  - convex/accommodation-paid-priority.handlers.test.ts
  - convex/signup-catalog.test.ts
  - convex/signup-submission.test.ts
  - convex/track-payment-edit.handlers.test.ts
autonomous: true
must_haves:
  truths:
    - "The complete handler, pure-domain, typecheck, build, codegen, and development-deploy gates pass after the backend contract generalization."
    - "Legacy global events, legacy boolean selections/snapshots, confirmed immutability, generic per-unit lines, child selections, and canonical finance consumers remain green together."
    - "A static source audit finds no new UI changes and no hardcoded option/category/age-band pricing branches in the new backend/data paths."
    - "Phase 47 work is not accidentally implemented: no copy/template mutation, shared eligibility enforcement, archive/delete, or named-template behavior is required for this phase gate."
  artifacts:
    - path: "tests/convex/phase45-money-integrity.test.ts"
      provides: "Regression matrix proving canonical money consumers remain aligned after the shared-loader/generalized-pricing change."
    - path: "tests/finance/phase45-money-integrity.test.ts"
      provides: "Pure/static finance authority audit extended for generic lines and no client-side money formulas."
    - path: "convex/track-payment-edit.handlers.test.ts"
      provides: "Final protected edit regression suite."
  key_links:
    - from: "tests/convex/phase45-money-integrity.test.ts"
      to: "convex/finance.ts"
      via: "all canonical consumer projections use loadOrderAmountDueBreakdowns"
      pattern: "loadOrderAmountDueBreakdowns"
    - from: "tests/finance/phase45-money-integrity.test.ts"
      to: "lib/domain/finance/accommodation-amounts.ts"
      via: "static audit for server-only generic money math"
      pattern: "accommodation-amounts"
---

<objective>
Close Phase 46 with the full backend/data verification matrix and an explicit no-UI/no-Phase-47 scope audit.

Purpose: Prove the event-owned contract is safe to hand to Phase 47 and the later UI phases without hiding legacy finance regressions or server-authority gaps.
Output: Extended regression suites and a green Convex/node/build/typecheck gate. This plan fixes only defects exposed by these Phase 46 tests; it does not add new Phase 47 or UI features.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-CONTEXT.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-RESEARCH.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-PATTERNS.md
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the cross-surface handler and finance regression matrix</name>
  <files>tests/convex/finance-accommodation.test.ts, tests/convex/phase45-money-integrity.test.ts, tests/finance/phase45-money-integrity.test.ts, convex/accommodation-admin.handlers.test.ts, convex/accommodation-paid-priority.handlers.test.ts, convex/signup-catalog.test.ts, convex/signup-submission.test.ts, convex/track-payment-edit.handlers.test.ts</files>
  <action>
     Extend the existing Phase 45 and accommodation handler fixtures rather than creating alternate test contracts. Cover a legacy-global event and an event-owned event through admin setup read, public catalog/quote, signup submission, track-payment edit, confirmation, canonical finance, reporting/payment-facing amount projections, and allocation-facing confirmed state. Assert identical canonical amounts where the inputs are equivalent; dynamic per_night, per_person, and per_person_per_night option lines carry correct labels/units/quantities/charges; a ticket covering base nights does not cover a non-included upgrade; an explicitly included option is free on covered base nights but charged on a selected extra night; selected extra nights add base accommodation charges; the snapshot stores the resolved covered/extra-night basis, inclusion decision, resolved nights, price, quantity, and charge; later ticket-rule, stay, or option edits cannot alter confirmed totals; config edits change only unconfirmed live totals; confirmed v5 and v6 snapshots remain immutable; child selections survive restore and digest replay; incomplete snapshots fail closed; event-owned reads do not follow global catalog edits; physical room/capacity derivation remains separate; setup version is the only new version token. Keep existing payment, paid-priority, authorization, idempotency, and legacy hotel workflow tests green. Add static assertions that no new backend/public contract path introduces literal option/category/age-band pricing branches. Do not add copy/template, eligibility-enforcement, archive/delete, or UI tests here; those belong to Phase 47/48/49/51.
  </action>
  <verify>npm test && npx vitest run --config vitest.convex.config.ts && npx vitest run --config vitest.components.config.ts</verify>
  <done>The full existing handler and pure suites pass with explicit coverage for event-owned/legacy parity, generic units, snapshot safety, selection authority, versioning, and canonical finance alignment; no component behavior was changed.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Convex, type, build, and no-UI scope gates</name>
  <files>verification-only; no production files modified</files>
  <action>
     Run the required final sequence after all Convex edits: `npx convex codegen`, `npx convex dev --once`, `npm run typecheck`, `npm run build`, `npm test`, `npx vitest run --config vitest.convex.config.ts`, and `npx vitest run --config vitest.components.config.ts`. Confirm the generated data model is current and the development deployment accepts the additive schema. Run a runnable source audit such as `rg -n 'categoryCode\s*===\s*["'"']superior|option(Code|Key)\s*===\s*["'"'](superior_upgrade|cot)|ageBand(Code|Key)\s*===\s*["'"'](under_3|3_11|12_17|18_plus)' convex lib --glob '*.ts' --glob '*.tsx'` and review every match against an explicit legacy seed/compatibility allowlist; runtime money/selection branches and new consumer reads of global catalog rows are not allowed. Run `git diff --name-only -- components app` and require no UI/application-route files changed in Phase 46. If the repository-wide lint remains red from the documented pre-existing findings, record it as non-gating and do not broaden scope to lint cleanup; all requested Phase 46 gates above must be green.
  </action>
  <verify>npx convex codegen && npx convex dev --once && npm run typecheck && npm run build && npm test && npx vitest run --config vitest.convex.config.ts && npx vitest run --config vitest.components.config.ts</verify>
  <done>Codegen, Convex dev validation, typecheck, build, pure tests, Convex handler tests, and component regression tests pass; the diff contains no UI changes and Phase 47 concerns are explicitly deferred.</done>
</task>

</tasks>

<verification>
  The final gate is the exact command sequence in Task 2. Record command output and any pre-existing lint exception in the summary. Verify the Phase 46 requirement matrix covers SET-01..05, STAY-01, CFG-01..03/05, TKT-01, TKT-04..05, SEL-01..03, and FIN-01..05; do not mark any requirement or phase complete until this gate passes.
</verification>

<success_criteria>
Phase 46 is implementation-ready for the next phase: all 21 owned requirements have executable backend/data proof, legacy finance remains safe, no UI changed, and Phase 47's copy/template/eligibility/archive work remains deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-05-SUMMARY.md`.
</output>
