---
phase: 46-event-owned-setup-schema-generalized-pricing-shared-contract
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - convex/accommodationContext.ts
  - convex/accommodation.ts
  - convex/init.ts
  - convex/accommodation-setup.handlers.test.ts
autonomous: true
must_haves:
  truths:
    - "There is exactly one event-owned accommodation projection with setup mode and one effective version token for all later consumers."
    - "A legacy event resolves through the old global-reference path with parity, while an event-owned event never reads global catalog rows as live commercial configuration."
    - "First explicit setup initialization materializes one event's commercial rows and ticket rules without a global backfill or changes to orders, payments, assignments, or historical snapshots."
    - "A fresh event remains disabled or unconfigured until an admin explicitly enables accommodation and sets a base stay; no automatic night-before default is created."
    - "Every event-owned setup write, including stay-window and extension-policy writes, advances eventAccommodationSetup.updatedAt atomically, while eventAccommodationConfig.updatedAt remains only non-authoritative stay metadata."
  artifacts:
    - path: "convex/accommodationContext.ts"
      provides: "The single `loadEventOwnedAccommodationContext` projection used by finance, signup, confirmation, and future allocation/admin consumers."
      exports: ["loadEventOwnedAccommodationContext"]
    - path: "convex/accommodation.ts"
      provides: "Setup initialization/materialization, event-owned upserts, setup-version advancement, and admin setup read contract."
      contains: "touchEventAccommodationSetupVersion"
    - path: "convex/accommodation-setup.handlers.test.ts"
      provides: "Convex handler proof for setup modes, materialization, ticket-rule seeding, version ownership, and legacy parity."
  key_links:
    - from: "convex/accommodationContext.ts"
      to: "convex/schema.ts"
      via: "indexed event-owned rows and setup singleton"
      pattern: "eventAccommodationSetup"
    - from: "convex/accommodation.ts"
      to: "convex/accommodationContext.ts"
      via: "effective setup/version contract used by setup writes and reads"
      pattern: "loadEventOwnedAccommodationContext"
    - from: "convex/accommodation.ts"
      to: "ticketTypes.roomTypeId"
      via: "eventTicketAccommodationRules seeding"
      pattern: "eventTicketAccommodationRules"
---

<objective>
Build the server-owned event context and per-event materialization/version boundary.

Purpose: Collapse the existing finance/signup resolver split and make event-owned setup authoritative without a risky global migration.
Output: One bounded loader, admin-only setup initialization/upserts, seeded event ticket rules, and handler-level proof of legacy parity and single-token behavior. Copy/template actions, eligibility rejection, and archive/delete remain Phase 47.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-CONTEXT.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-RESEARCH.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-PATTERNS.md
@convex/_generated/ai/guidelines.md
@convex/schema.ts
@convex/accommodation.ts
@convex/finance.ts
@convex/signupCatalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the single bounded event-owned accommodation context loader</name>
  <files>convex/accommodationContext.ts</files>
  <action>
     Create the leaf module `loadEventOwnedAccommodationContext(ctx, eventId)` with `QueryCtx`/`MutationCtx`-compatible database typing and no imports from `accommodation.ts` or `signupCatalog.ts`. Return setupMode, effective versionToken, stay configuration, event-owned categories, rates, options, age bands, age pricing, resources, and event ticket rules in a self-contained contract. Use `.unique()` for singleton setup/stay rows, indexed `for await` iteration for authoritative per-event collections, and batched referenced-ID reads only where legacy inventory/global seed references are required. `legacy_global` is an explicit compatibility exception for pre-materialized events: preserve the old category/rate/option/age-band joins and legacy boolean-to-resolved-option bridge byte-for-byte until that event is materialized. In `event_owned` mode, read commercial labels, prices, enablement, eligibility metadata, inclusion keys, and category relationships only from event-owned rows; in `uninitialized` mode, return an explicit empty/unconfigured contract. Expose `eventAccommodationSetup.updatedAt` as the sole versionToken, falling back only for legacy compatibility when the setup singleton does not yet exist. Do not enforce Phase 47 eligibility rejection here, do not use `.take()` caps for authoritative config rows, and do not make global catalog rows live consumers for event-owned events.
  </action>
  <verify>npx convex codegen && npx convex dev --once && npm run typecheck</verify>
  <done>Finance and signup can import one shared loader contract without a cycle; the loader is event-scoped, complete at any supported row count, distinguishes all three setup modes, and exposes exactly one effective version token.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-event materialization, ticket-rule seeding, and atomic setup version writes</name>
  <files>convex/accommodation.ts, convex/init.ts</files>
  <action>
     In the existing authenticated accommodation mutations, replace pricing-boundary calls to `touchEventAccommodationConfigVersion` with one `touchEventAccommodationSetupVersion` helper using monotonic `Math.max(Date.now(), previous + 1)`. Lazily create the setup singleton and advance `eventAccommodationSetup.updatedAt` in the same mutation as every event-owned category, option, age-band, rate, age-pricing, resource, ticket-rule, stay-window, or extension-policy write; leave `eventAccommodationConfig.updatedAt` as non-authoritative stay metadata only. Add the admin initialization/read contract (`initializeEventAccommodationSetup` and `getEventOwnedAccommodationSetup` or the repository-equivalent symbols) that supports disabled accommodation, empty/uninitialized setup, optional base stay windows, and materializes one event from its current legacy global references on first explicit setup/save. A fresh event must remain disabled or unconfigured until an explicit admin action; do not create an automatic night-before default. Materialization must deep-copy commercial labels/keys/rates/age data into event-owned rows, preserve physical room-type/resource references, seed one `eventTicketAccommodationRules` row per event ticket from `ticketTypes.roomTypeId`, `accommodationIncluded`, and any included option keys, and never touch orders, selections, snapshots, payments, assignments, or other events. Preserve `requireIdentity`, event checks, existing stay-window derivation, and legacy seed-write validation; move locked option semantics/age-band bounds into seed data in `convex/init.ts` rather than using them as event-owned runtime write gates. Return the exact pending-order count by full indexed iteration plus only a bounded display sample, so setup.version changes expose honest pending impact without rewriting orders. Do not implement copy-from-event/template mutations, copy audit writes, eligibility enforcement, archive/delete, or UI.
  </action>
  <verify>npx convex codegen && npx convex dev --once && npm run typecheck</verify>
  <done>Initialization is per-event and atomic, setup mode becomes event_owned only for the initialized event, ticket rules are seeded from the existing ticket fields, all event-owned writes bump one setup token, and no historical or physical-inventory state is rewritten.</done>
</task>

<task type="auto">
  <name>Task 3: Add handler tests for setup modes, materialization, version ownership, and bounded parity</name>
  <files>convex/accommodation-setup.handlers.test.ts</files>
  <action>
     Create a `convex-test` suite using the established `import.meta.glob` module map, fresh schema instances, and authenticated admin identities. Cover empty/uninitialized setup initialization, accommodation-disabled state, explicit base-only setup, before-only, after-only, and both-side extension policies with boundary validation, legacy-global materialization, event-owned reads that ignore later global label/price changes, ticket-rule seeding from both included and non-included ticket types plus included option keys, event-owned write version advancement, monotonic token behavior when writes occur within one millisecond, and the fact that stay-window and extension-policy writes advance the sole setup token without creating a second pricing boundary. Add a legacy fixture that compares the shared loader's resolved categories/rates/options/age rules to the pre-refactor legacy shape, plus a large event fixture that exceeds former `.take()` caps and proves all rows are returned. Assert physical room types/resources remain referenced and untouched. Add an exact pending-order fixture that proves the full pending count is correct, the display sample is bounded, the version changes expose the updated impact, and no order rows are rewritten. Add authorization rejection coverage for setup reads/writes. Keep tests focused on schema/contract behavior; Phase 47 eligibility enforcement and copy/archive tests are explicitly not part of this suite.
  </action>
  <verify>npx vitest run --config vitest.convex.config.ts convex/accommodation-setup.handlers.test.ts</verify>
  <done>Convex handlers prove the single projection, three setup modes, per-event materialization, ticket-rule seed shape, one version boundary, legacy parity, bounded reads, typed event scoping, and admin authorization.</done>
</task>

</tasks>

<verification>
Run the new handler suite plus `npx convex codegen`, `npx convex dev --once`, and `npm run typecheck`. Confirm no new loader imports either consumer resolver and no query uses `.take()` for authoritative event-owned setup collections.
</verification>

<success_criteria>
The server has one event-owned projection and one atomic version boundary, with safe per-event materialization and handler tests proving legacy compatibility and ticket-rule seeding.
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-02-SUMMARY.md`.
</output>
