---
phase: 46-event-owned-setup-schema-generalized-pricing-shared-contract
plan: 04
type: execute
wave: 4
depends_on: [46-03]
files_modified:
  - lib/types/signup.ts
  - lib/domain/signup/submission.ts
  - lib/domain/signup/submission-token.ts
  - lib/domain/track-payment/edit-token.ts
  - convex/signupSubmission.ts
  - convex/publicTracking.ts
  - convex/signup-submission.test.ts
  - convex/track-payment-edit.handlers.test.ts
autonomous: true
must_haves:
  truths:
    - "Signup and track-payment selection writes carry only event-scoped option keys and quantities; client prices, totals, nights, eligibility, and snapshots are not accepted."
    - "Generic option selections persist as child rows and survive idempotent restore/replay without losing quantities or changing canonical price."
    - "Track-payment replacement atomically updates base selections and child option rows, and its digest/no-op path includes the complete generic selection set."
    - "Ownership, signature, rate-limit, honeypot, idempotency, and confirmedAt lock protections remain unchanged, and server authority rejects cross-event or invalid option identities."
  artifacts:
    - path: "lib/types/signup.ts"
      provides: "Shared signup selection validator/type with generic optionSelections and readable legacy booleans."
      contains: "optionSelections"
    - path: "convex/signupSubmission.ts"
      provides: "Atomic child-row persistence and restore payload support for generic signup selections."
      contains: "orderAccommodationOptionSelections"
    - path: "convex/publicTracking.ts"
      provides: "Generic, server-authoritative track-payment edit contract and child-row replacement/digest integration."
      contains: "digestAccommodationSelections"
    - path: "convex/track-payment-edit.handlers.test.ts"
      provides: "Server-only authority, idempotency, digest, and confirmation-lock regression coverage."
  key_links:
    - from: "convex/signupSubmission.ts"
      to: "convex/accommodationContext.ts"
      via: "same-transaction event option key resolution"
      pattern: "loadEventOwnedAccommodationContext"
    - from: "convex/publicTracking.ts"
      to: "convex/finance.ts"
      via: "before/after canonical server pricing"
      pattern: "loadOrderAmountDueBreakdowns"
    - from: "convex/publicTracking.ts"
      to: "orderAccommodationOptionSelections"
      via: "atomic child-row replacement and digest"
      pattern: "orderAccommodationOptionSelections"
---

<objective>
Complete the generic selection write contract across signup and the protected booking-reference editor.

Purpose: Move from fixed boolean preferences to event-scoped child selections without weakening the v5.0 server-authority and confirmation-lock protections.
Output: Shared validators/domain payloads, atomic persistence/restore/edit wiring, and handler tests for cross-event rejection, price tampering, idempotency, digest correctness, and legacy dual-read. This remains backend/data only; no component or route UI changes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-CONTEXT.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-RESEARCH.md
@.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-PATTERNS.md
@convex/_generated/ai/guidelines.md
@lib/types/signup.ts
@lib/domain/signup/submission.ts
@lib/domain/track-payment/edit-token.ts
@convex/signupSubmission.ts
@convex/publicTracking.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen signup payloads and persist generic option child rows with restore safety</name>
  <files>lib/types/signup.ts, lib/domain/signup/submission.ts, lib/domain/signup/submission-token.ts, convex/signupSubmission.ts</files>
  <action>
     Add optional `optionSelections` payload entries containing only the event-scoped option key and a normalized positive quantity, plus a bounded stay choice such as allowed extension dates; keep `upgradeSelected` and `cotSelected` readable for the transition and preserve existing required fields and token serialization rules. Update `submitSignupEnvelope` to resolve every option key and stay choice against the selected event's event-owned contract inside the same mutation, rejecting cross-event, unknown, or malformed identities with the existing typed submission errors. Do not add Phase 47 disabled/archived/ineligible rejection here; that behavior belongs to the shared eligibility resolver. Never accept or persist client price, amount, computed night count, eligibility, or snapshot authority. Insert one `orderAccommodationOptionSelections` child row per selected option per base selection atomically with the existing `orderAccommodationSelections` row, and dual-write legacy booleans only where existing consumers require round-tripping. Extend `buildRestorePayload` and its validators/token input so an idempotent retry restores child rows, quantities, and the validated stay choice exactly rather than silently falling back to booleans. Preserve server-derived stay timestamps/night count, idempotency, attendee cardinality, and submission secret checks.
  </action>
  <verify>npx vitest run --config vitest.convex.config.ts convex/signup-submission.test.ts && npm test -- --run lib/domain/signup/submission.test.ts</verify>
     <done>Generic signup selections are validated for event scope and structure, persisted as child rows, restored on retries, and legacy boolean payloads still round-trip without client money or event-scope authority; Phase 47 owns disabled/archived/ineligible rejection.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize protected track-payment edits, selection digests, and authority tests</name>
  <files>lib/domain/track-payment/edit-token.ts, convex/publicTracking.ts, convex/track-payment-edit.handlers.test.ts</files>
  <action>
     Widen edit choice/selection validators to arbitrary event option keys and quantities plus a bounded stay choice, while rejecting price, total, computed night, eligibility, room, slot, or snapshot authority arguments. Have `getTrackPaymentEditContext` return event-owned generic choices, allowed extension nights, and current child-row selections through the shared context. In `updateAccommodation`, preserve ownership/signature verification, rate limiting, honeypot, exact-attendee cardinality, confirmedAt lock, idempotency replay, server-priced before/after totals, and append-only audit behavior; replace each attendee's base row and option child rows atomically, derive stay timestamps/night count from the validated stay choice, and ensure `digestAccommodationSelections` includes the stay choice plus sorted option keys and quantities so identical selections are a no-op and changed extension nights produce a new digest. Keep legacy boolean dual-read/dual-write for existing rows. Add handler tests for a valid third option, quantity edits, optional extra-night edits when the ticket includes base accommodation, a non-included per-person-per-night upgrade charged across covered and extra nights, same-request idempotency, child-row/stay-choice digest sensitivity, price/amount tampering rejection, cross-event option rejection, unknown option rejection, and post-confirmation rejection with the confirmed snapshot still authoritative. Do not add Phase 47 disabled/archived/ineligible enforcement in this task.
  </action>
  <verify>npx vitest run --config vitest.convex.config.ts convex/track-payment-edit.handlers.test.ts && npm run typecheck</verify>
     <done>Track-payment edits update generic child selections safely and idempotently before confirmation, reject all client pricing/cross-event attempts, and remain blocked after confirmation with legacy rows intact; Phase 47 owns disabled/archived/ineligible enforcement.</done>
</task>

</tasks>

<verification>
  Run `npx convex codegen`, `npx convex dev --once`, both targeted handler suites, and the domain submission/token tests. Inspect validators to confirm no dynamic selection payload contains `priceMinor`, `amount`, `nightCount`, eligibility, or snapshot fields; inspect the edit digest to confirm child rows participate in no-op detection. Confirm disabled/archived/ineligible rejection is not implemented ahead of Phase 47.
</verification>

<success_criteria>
Signup and track-payment share an event-scoped, server-priced generic selection contract with atomic child-row persistence, legacy compatibility, and all prior security/idempotency/confirmation protections.
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-owned-setup-schema-generalized-pricing-shared-contract/46-04-SUMMARY.md`.
</output>
