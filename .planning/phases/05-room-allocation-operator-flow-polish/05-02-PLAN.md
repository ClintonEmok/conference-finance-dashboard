---
phase: 05-room-allocation-operator-flow-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/dashboard/layout.tsx
  - app/dashboard/page.tsx
  - app/dashboard/reconciliation/page.tsx
  - app/dashboard/attendees/page.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
  - app/dashboard/accommodation/page.tsx
autonomous: false
must_haves:
  truths:
    - "Admin can move from overview to outstanding balances, attendee detail, and room allocation without dead-end navigation"
    - "Dashboard labels use clear operator language for outstanding balances and room allocation"
    - "Pages present the next useful action for payment follow-up or room assignment instead of forcing operators to backtrack"
  artifacts:
    - path: "app/dashboard/layout.tsx"
      provides: "Workflow-oriented navigation labels and ordering"
      contains: "Outstanding balances"
    - path: "app/dashboard/page.tsx"
      provides: "Overview quick actions that hand operators into the main daily workflows"
      contains: "Quick actions"
    - path: "app/dashboard/reconciliation/page.tsx"
      provides: "Outstanding-balance page copy and handoffs aligned to attendee/accommodation workflows"
      contains: "Outstanding balances"
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "URL-driven focus state for selected attendee or highlighted room-assignment work"
      contains: "selected attendee"
    - path: "app/dashboard/attendees/[attendeeId]/page.tsx"
      provides: "Detail-page handoff into room allocation workflow"
      contains: "Manage room assignment"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "/dashboard/reconciliation"
      via: "overview quick action"
      pattern: "dashboard/reconciliation"
    - from: "app/dashboard/reconciliation/page.tsx"
      to: "/dashboard/attendees"
      via: "order-to-attendee follow-up handoff"
      pattern: "dashboard/attendees"
    - from: "app/dashboard/attendees/[attendeeId]/page.tsx"
      to: "/dashboard/accommodation"
      via: "manage-room-assignment CTA"
      pattern: "dashboard/accommodation"
---

<objective>
Polish the operator flow so the MVP feels coherent across finance follow-up and room assignment work.

Purpose: Satisfy FLOW-01 by tightening labels, navigation order, and cross-screen handoffs now that room assignment actually works.
Output: Clearer dashboard shell language, overview quick actions, outstanding-balance wording, and URL-driven handoff actions between attendee and accommodation screens.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-finance-visibility-reconciliation/03-02-SUMMARY.md
@.planning/phases/04-attendee-data-accommodation-foundations/04-02-SUMMARY.md
@.planning/phases/05-room-allocation-operator-flow-polish/05-01-SUMMARY.md
@app/dashboard/layout.tsx
@app/dashboard/page.tsx
@app/dashboard/reconciliation/page.tsx
@app/dashboard/attendees/page.tsx
@app/dashboard/attendees/[attendeeId]/page.tsx
@app/dashboard/accommodation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify navigation and command-center copy</name>
  <files>app/dashboard/layout.tsx, app/dashboard/page.tsx, app/dashboard/reconciliation/page.tsx</files>
  <action>Update dashboard shell labels and ordering to match the operator's actual loop: overview, outstanding balances, attendees, room allocation, orders, integrations, sync. Keep the existing route structure unless a route rename is truly necessary; prefer clearer UI copy over moving files. Rename ambiguous user-facing "reconciliation" copy toward "Outstanding balances" or equally concrete collection-follow-up wording while preserving the protected page behavior already in place. Add an overview quick-actions section that sends operators directly into the most common follow-up flows after login, rather than making them infer where to go next.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Navigation labels and the overview page guide operators cleanly into payment follow-up and room-allocation work using clear MVP language.</done>
</task>

<task type="auto">
  <name>Task 2: Add URL-driven handoffs between balance follow-up, attendee detail, and room allocation</name>
  <files>app/dashboard/reconciliation/page.tsx, app/dashboard/attendees/page.tsx, app/dashboard/attendees/[attendeeId]/page.tsx, app/dashboard/accommodation/page.tsx</files>
  <action>Add practical cross-screen links instead of generic backtracking. Support URL/query-param driven focus state where it materially speeds workflow, for example: opening the attendee ledger with a prefilled order/attendee search, opening accommodation with a selected attendee highlighted for assignment, and preserving enough context on the target page for the operator to understand why they landed there. Add explicit CTA buttons/links from outstanding-balance rows and attendee detail into the next relevant page. Do not invent a global router state store for this; use simple URL state and existing client-page patterns.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Operators can jump from outstanding-balance work into attendee records and then directly into room allocation with the relevant person or order already in focus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Finalized operator flow labels, quick actions, and cross-screen handoffs</what-built>
  <how-to-verify>
    1) Sign in and open `/dashboard`; confirm the shell labels read clearly, the nav order feels workflow-oriented, and overview quick actions link to meaningful destinations.
    2) Open `/dashboard/reconciliation`; confirm the page now uses clear outstanding-balance language and offers a direct path into attendee follow-up.
    3) Open one attendee detail page; use the room-assignment CTA to land on `/dashboard/accommodation` with the relevant attendee highlighted or otherwise in focus.
    4) Walk the full path overview -> outstanding balances -> attendee detail -> room allocation -> attendee detail and confirm there are no dead ends or confusing labels.
  </how-to-verify>
  <resume-signal>Type "approved" if the operator flow feels coherent, or describe the gaps that still feel confusing.</resume-signal>
</task>

</tasks>

<verification>
- Labels reflect the MVP reset decision to prefer "outstanding balances" / collection follow-up language over vague reconciliation terminology.
- Cross-screen links rely on stable URLs and existing page patterns, not hidden client-only state.
- Operators can move between overview, attendee, and room workflows without losing context about the person or balance they are resolving.
</verification>

<success_criteria>
- FLOW-01 satisfied: the core dashboard, attendee, and room-allocation screens now behave like one coherent operator flow.
- Phase 5 completes the MVP-critical command-center experience without reopening deferred Tikkie-automation scope.
- User-facing labels match the product direction captured in `.planning/STATE.md` and no longer lean on ambiguous "reconciliation" wording where clearer language exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-room-allocation-operator-flow-polish/05-02-SUMMARY.md`
</output>
