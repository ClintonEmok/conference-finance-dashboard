---
phase: 05-room-allocation-operator-flow-polish
plan: 01
type: execute
wave: 1
depends_on: [04-02]
files_modified:
  - lib/domain/accommodation/assignments.ts
  - app/api/dashboard/accommodation/assignments/route.ts
  - app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts
  - app/dashboard/accommodation/page.tsx
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - app/api/dashboard/attendees/route.ts
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - app/dashboard/attendees/page.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
autonomous: true
must_haves:
  truths:
    - "Admin can assign an unassigned attendee to a room and immediately see occupancy update"
    - "Admin can unassign an attendee from a room without leaving occupancy counts stale or negative"
    - "Admin can identify full rooms, empty rooms, available rooms, and unassigned attendees through filters and visible badges"
  artifacts:
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Room-allocation board query plus assign/unassign mutations with capacity safeguards"
      exports: ["getRoomAllocationBoard", "assignAttendeeToRoom", "unassignAttendeeFromRoom"]
    - path: "app/api/dashboard/accommodation/assignments/route.ts"
      provides: "Protected allocation board API and assignment mutation"
      exports: ["GET", "POST"]
    - path: "app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts"
      provides: "Protected attendee unassignment API"
      exports: ["DELETE"]
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "Room allocation manager with room filters, occupancy indicators, and assignment controls"
      contains: "Unassigned attendees"
    - path: "app/dashboard/attendees/[attendeeId]/page.tsx"
      provides: "Attendee detail showing live assigned-room state instead of placeholder-only wording"
      contains: "Room status"
  key_links:
    - from: "app/api/dashboard/accommodation/assignments/route.ts"
      to: "lib/domain/accommodation/assignments.ts"
      via: "validated protected allocation query and assign mutation"
      pattern: "getRoomAllocationBoard|assignAttendeeToRoom"
    - from: "app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts"
      to: "lib/domain/accommodation/assignments.ts"
      via: "validated protected unassign mutation"
      pattern: "unassignAttendeeFromRoom"
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/assignments"
      via: "board fetch plus assignment actions"
      pattern: "accommodation/assignments"
    - from: "lib/domain/finance/attendees.ts"
      to: "prisma.ticketTailorAttendee"
      via: "room relation include for attendee ledger rows"
      pattern: "assignedRoom"
---

<objective>
Turn accommodation inventory into a working room allocation workflow.

Purpose: Satisfy ACC-02 and ACC-03 by giving operators a single place to assign/unassign attendees, understand occupancy pressure, and trust that attendee room state stays synchronized across the dashboard.
Output: Room-allocation domain helpers, protected assignment APIs, upgraded accommodation page, and live room-status data in attendee list/detail surfaces.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attendee-data-accommodation-foundations/04-01-SUMMARY.md
@.planning/phases/04-attendee-data-accommodation-foundations/04-02-SUMMARY.md
@prisma/schema.prisma
@lib/domain/accommodation/inventory.ts
@lib/domain/finance/attendees.ts
@lib/domain/finance/attendee-detail.ts
@app/api/dashboard/accommodation/rooms/route.ts
@app/api/dashboard/attendees/route.ts
@app/api/dashboard/attendees/[attendeeId]/route.ts
@app/dashboard/accommodation/page.tsx
@app/dashboard/attendees/page.tsx
@app/dashboard/attendees/[attendeeId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add room-allocation query and protected assign/unassign APIs</name>
  <files>lib/domain/accommodation/assignments.ts, app/api/dashboard/accommodation/assignments/route.ts, app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts</files>
  <action>Create a dedicated accommodation assignment domain module instead of overloading `inventory.ts`. The board query must return rooms with hotel/room-type context, occupancy counts, occupant attendee rows, and derived availability state (`empty`, `available`, `full`), plus an unassigned-attendee collection that can be filtered by event, search, hotel, room type, and availability needs. Implement protected APIs that follow the project's existing `UNAUTHORIZED` / `BAD_REQUEST` / `INTERNAL_ERROR` contracts: `GET` for the board payload, `POST` to assign `{ attendeeId, roomId }`, and `DELETE` for attendee unassignment. Use a transaction for mutations so `TicketTailorAttendee.assignedRoomId` and `AccommodationRoom.occupiedBeds` stay in sync. Reject over-capacity assignments, missing records, and no-op room moves with clear 400 errors; do not let `occupiedBeds` drift negative.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>Protected APIs can load the allocation board, assign attendees into rooms with capacity checks, and unassign them without corrupting occupancy state.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade accommodation page into the room allocation manager</name>
  <files>app/dashboard/accommodation/page.tsx</files>
  <action>Keep the existing hotel/room-type/room inventory creation flows, but expand `/dashboard/accommodation` into the main room-allocation surface. Add filter controls for hotel, room type, availability, and attendee search; show room cards or rows with visible occupancy badges/counts; list current occupants per room; and render an unassigned-attendee section that lets operators place an attendee into a selected room. Support unassigning directly from the occupied room view. Reuse the project's explicit loading/error/refresh pattern, keep mutations non-optimistic unless you can guarantee consistency, and refresh from server truth after every assign/unassign action. Do not hide inventory creation behind a separate route; Phase 5 should leave one coherent accommodation workspace.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Operators can manage rooms and assignments from one accommodation page, filter for full/empty/available rooms, and place or remove attendees with immediate occupancy feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Surface live room state in attendee ledger and detail views</name>
  <files>lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, app/api/dashboard/attendees/route.ts, app/api/dashboard/attendees/[attendeeId]/route.ts, app/dashboard/attendees/page.tsx, app/dashboard/attendees/[attendeeId]/page.tsx</files>
  <action>Replace the static `unassigned` placeholder model with real room-state projection. Update the attendee ledger payload so each row includes either explicit unassigned state or assigned room/hotel metadata that the table can render clearly. Update attendee detail to show the live assigned room when present and keep unassigned messaging accurate when absent. Preserve current finance fields and API protections while extending the payload shape carefully so the new accommodation workflow does not regress attendee detail or list loading.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Attendee list and attendee detail both reflect real room assignment state, so accommodation changes are visible everywhere operators already work.</done>
</task>

</tasks>

<verification>
1. Assign an unassigned attendee into a room from `/dashboard/accommodation` and confirm the room occupancy count increases, the attendee leaves the unassigned list, and attendee detail now shows the assigned room.
2. Unassign that attendee and confirm the room occupancy count decreases, the attendee returns to the unassigned list, and attendee detail/list revert to unassigned state.
3. Filter the accommodation page for full rooms, empty rooms, and available rooms; confirm each filter changes the board correctly.
4. Hit one assignment API route unauthenticated and confirm the 401 `UNAUTHORIZED` JSON contract remains consistent.
</verification>

<success_criteria>
- ACC-02 satisfied: admin can assign and unassign attendees to rooms from protected dashboard workflows.
- ACC-03 substantially satisfied at the workflow level: full rooms, empty rooms, available rooms, and unassigned attendees are visible and filterable.
- Room state is no longer isolated to accommodation only; attendee surfaces stay synchronized with assignment changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-room-allocation-operator-flow-polish/05-01-SUMMARY.md`
</output>
