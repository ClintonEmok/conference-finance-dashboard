---
phase: 17-fix-critical-code-review-issues
plan: "03"
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - convex/attendees.ts
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/accommodation.ts
  - convex/schema.ts
  - lib/types/payment.ts
  - lib/types/order.ts
  - lib/types/accommodation.ts
  - lib/types/tikkie.ts
  - lib/types/attendee.ts
  - lib/types/shared.ts
  - lib/domain/accommodation/assignments.ts
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - lib/domain/finance/tikkie-event-payments.ts
  - app/api/dashboard/tikkie-event-links/route.ts
  - tests/payments/payments-route.test.ts
  - tests/payments/orders-search-route.test.ts
  - tests/attendees/attendee-detail-route.test.ts
  - tests/accommodation/allocation-filters.test.ts
  - tests/tikkie/tikkie-links.test.ts
autonomous: true
must_haves:
  truths:
    - "Audited dashboard queries no longer scan whole Convex tables just to answer routine reads."
    - "Finance and accommodation routes still return the same shapes after query hardening."
    - "Large datasets now use indexes, explicit limits, or pagination instead of unbounded `.collect()`."
  artifacts:
    - path: "convex/payments.ts"
      provides: "Indexed payment summary and unassigned payment reads"
      contains: "withIndex"
    - path: "convex/tikkie.ts"
      provides: "Bounded/indexed payment-link queries"
      contains: "take("
    - path: "convex/orders.ts"
      provides: "Count/list logic without full-table collect hot paths"
      contains: "for await"
    - path: "lib/types/payment.ts"
      provides: "Shared payment-facing interfaces moved out of implementation files"
      contains: "export type"
  key_links:
    - from: "lib/domain/finance/attendees.ts"
      to: "convex/attendees.ts"
      via: "bounded attendee query contract"
      pattern: "api\.attendees\.getAttendees"
    - from: "app/api/dashboard/tikkie-event-links/route.ts"
      to: "convex/tikkie.ts"
      via: "bounded event-link fetch path"
      pattern: "api\.tikkie\.getPaymentLinks"
---

<objective>
Remove the audited unbounded Convex read patterns so the current dashboard can scale without full-table scans.

Purpose: Phase 17 is a stabilization phase; fixing the correctness bugs is not enough if hot paths still degrade as data grows.
Output: Indexed/bounded query implementations for the audited attendees, orders, payments, Tikkie, and accommodation read surfaces while preserving existing DTOs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONTEXT.md
@.planning/phases/17-fix-critical-code-review-issues/17-RESEARCH.md
@.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md
@convex/_generated/ai/guidelines.md
@convex/attendees.ts
@convex/orders.ts
@convex/payments.ts
@convex/tikkie.ts
@convex/accommodation.ts
@convex/schema.ts
@lib/types
@lib/domain/accommodation/assignments.ts
@lib/domain/finance/attendees.ts
@lib/domain/finance/attendee-detail.ts
@lib/domain/finance/tikkie-event-payments.ts
@app/api/dashboard/tikkie-event-links/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace audited finance and attendee table scans with indexed or bounded reads</name>
  <read_first>convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/schema.ts, .planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/schema.ts</files>
  <action>Refactor the exact audited hot paths from the fix list: `attendees.getAttendees`, `orders.getOrderCount`, `payments.getUnassignedPayments`, `payments.getPaymentSummary`, `tikkie.getPaymentLinks`, and `tikkie.getPaymentLinksByOrderId`. Use existing indexes where possible (`eventId`, `orderId`, `status`, `paymentRequestToken`, `eventId`, `paymentLinkId`) and add only the smallest missing indexes needed in `convex/schema.ts`. For no-filter list paths, expose an explicit bounded result (`limit` arg with sane default or `.take(N)`), not `query(...).collect()`. For exact counting, use async iteration or a denormalized/indexed strategy instead of collecting the entire table into memory. Preserve current return field names and sort order where the UI depends on them (latest-link-first, newest-first ledgers, etc.).</action>
  <acceptance_criteria>`convex/payments.ts` uses `withIndex("orderId"` for payment summary and `withIndex("status"` for unassigned payments; `convex/tikkie.ts` no longer uses bare `.query("tikkiePaymentLinks").collect()` in `getPaymentLinks` or `getPaymentLinksByOrderId`; `convex/orders.ts` no longer uses `ctx.db.query("ticketTailorOrders").collect()` inside `getOrderCount`; `convex/attendees.ts` no longer uses a no-filter full-table collect for `getAttendees`; any newly needed indexes are declared in `convex/schema.ts`; touched queries do not introduce query `.filter((q) => ...)`; touched query handlers do not use `Date.now()`; any added indexes are reviewed against existing prefix indexes so redundant duplicates are not kept unless `_creationTime` ordering requires both; targeted route/domain tests covering touched list/count/detail paths are updated or added and pass.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "\.collect\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts`; run `rg "\.filter\(\(?q|Date\.now\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts`; run `npm test -- tests/payments/payments-route.test.ts tests/payments/orders-search-route.test.ts tests/attendees/attendee-detail-route.test.ts tests/tikkie/tikkie-links.test.ts`; read the audited functions and schema changes to confirm each now uses an index, `.take()`, pagination, or async iteration without redundant prefix indexes.</verify>
  <done>The audited finance/attendee reads are bounded or indexed and keep their existing API contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce accommodation board and downstream caller read amplification without changing payload shape</name>
  <read_first>convex/accommodation.ts, lib/domain/accommodation/assignments.ts, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, app/api/dashboard/tikkie-event-links/route.ts</read_first>
  <files>convex/accommodation.ts, lib/domain/accommodation/assignments.ts, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, app/api/dashboard/tikkie-event-links/route.ts</files>
  <action>Refactor `convex/accommodation.ts#getRoomAllocationBoard` and any directly coupled caller assumptions so the board no longer loads unrelated tables wholesale. Keep the returned room/hotel/unassigned-attendee payload shape intact, but constrain reads by the requested `eventId`, reuse assignment-truth occupancy from Plan 17-02, and replace whole-table collection with indexed reads plus targeted secondary lookups. Then update the listed domain/API callers only where the new bounded query contract requires it (for example, passing an explicit `limit`, `eventId`, or paginated args) without changing their response DTOs. Do not redesign the board payload, filter semantics, or `hasFamily` contract established in Phase 16.</action>
  <acceptance_criteria>`convex/accommodation.ts` does not perform six unrelated whole-table `.collect()` calls inside `getRoomAllocationBoard`; `lib/domain/accommodation/assignments.ts` still exports the same board DTO fields; listed downstream callers still reference the same DTO field names; touched query code avoids query `.filter((q) => ...)`; accommodation regression tests covering board/filter behavior pass; `npm run typecheck` exits 0.</acceptance_criteria>
  <verify>Run `npm run typecheck`; inspect `convex/accommodation.ts#getRoomAllocationBoard`; run `rg "getRoomAllocationBoard|api\.accommodation\.getRoomAllocationBoard" lib/domain/accommodation lib/domain/finance app/api/dashboard` to confirm callers still route through the same bounded contract; run `rg "\.filter\(\(?q" convex/accommodation.ts`; run `npm test -- tests/accommodation/allocation-filters.test.ts`.</verify>
  <done>The accommodation board and its immediate consumers avoid audited read amplification while preserving dashboard behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extract shared model interfaces into dedicated type modules while preserving current DTO shapes</name>
  <read_first>.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, lib/domain/accommodation/assignments.ts, app/dashboard/page.tsx, app/dashboard/orders/page.tsx, components/payments/payment-list.tsx, components/dashboard/event-tikkie-section.tsx</read_first>
  <files>lib/types/payment.ts, lib/types/order.ts, lib/types/accommodation.ts, lib/types/tikkie.ts, lib/types/attendee.ts, lib/types/shared.ts, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, lib/domain/accommodation/assignments.ts, app/dashboard/page.tsx, app/dashboard/orders/page.tsx, components/payments/payment-list.tsx, components/dashboard/event-tikkie-section.tsx</files>
  <action>Extract repeated or implementation-local interfaces/types for payments, orders, accommodation, Tikkie, attendees, and shared utility contracts into dedicated modules under `lib/types/`. Start from the fix-list targets and the files touched by this phase, moving declarations such as `PaymentDTO`, `OrderDTO`, room/hotel/allocation row shapes, Tikkie link/payment shapes, attendee row/detail shapes, and any shared formatter/id helper types out of implementation files when they are duplicated or hard to discover. Update imports in touched domain/UI files to use the new shared type modules. Keep the extraction behavior-preserving: do not rename DTO fields, do not change runtime serialization, and do not expand the refactor into unrelated files outside the touched phase surface.</action>
  <acceptance_criteria>`lib/types/payment.ts`, `lib/types/order.ts`, `lib/types/accommodation.ts`, `lib/types/tikkie.ts`, `lib/types/attendee.ts`, and `lib/types/shared.ts` exist unless a file is intentionally omitted because no shared type was needed and that omission is explained in the summary; touched implementation files import extracted types from `lib/types/*`; `rg "type (Payment|Order|Room|Hotel|Tikkie|Attendee)|interface (Payment|Order|Room|Hotel|Tikkie|Attendee)" lib/domain app/dashboard components` shows the shared definitions have moved out of the touched implementation files where practical; targeted tests still pass after the extraction; `npm run typecheck` exits 0.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "from \"@/lib/types/|from '@/lib/types/" lib/domain app/dashboard components`; run `npm test -- tests/payments/payments-route.test.ts tests/attendees/attendee-detail-route.test.ts tests/tikkie/tikkie-links.test.ts`; read the extracted type modules and a few updated callers to confirm DTO field names did not change.</verify>
  <done>Shared model interfaces live in dedicated type modules, reducing duplication and circular-import risk while preserving existing contracts.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `rg "\.collect\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts`
- Confirm touched callers import shared model interfaces from `lib/types/*` where extraction was planned.
- Run targeted route/domain tests for touched finance, attendee, accommodation, and Tikkie paths.
- Confirm audited read paths now use indexes, `.take()`, pagination, or async iteration.
</verification>

<success_criteria>

- All audited hot-path table scans from the fix list are removed or bounded.
- Existing finance/accommodation DTOs remain stable.
- Shared model interfaces are extracted into dedicated modules for the touched phase surface.
- Touched critical paths have regression coverage proving the refactors did not break contracts.
- Query behavior now scales better without broad table reads.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-03-SUMMARY.md`
</output>
