---
phase: 17-fix-critical-code-review-issues
plan: "03"
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - convex/attendees.ts
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/accommodation.ts
  - convex/schema.ts
  - lib/domain/accommodation/assignments.ts
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - lib/domain/finance/tikkie-event-payments.ts
  - app/api/dashboard/tikkie-event-links/route.ts
autonomous: true
must_haves:
  truths:
    - "Audited dashboard queries no longer scan whole Convex tables just to answer routine reads."
    - "Finance and accommodation routes still return the same shapes after query hardening."
    - "Large datasets now use indexes, explicit limits, or pagination instead of unbounded `.collect()`."
  artifacts:
    - path: "convex/payments.ts"
      provides: "Indexed payment summary and unassigned payment reads"
      contains: "withIndex"
    - path: "convex/tikkie.ts"
      provides: "Bounded/indexed payment-link queries"
      contains: "take("
    - path: "convex/orders.ts"
      provides: "Count/list logic without full-table collect hot paths"
      contains: "for await"
  key_links:
    - from: "lib/domain/finance/attendees.ts"
      to: "convex/attendees.ts"
      via: "bounded attendee query contract"
      pattern: "api\.attendees\.getAttendees"
    - from: "app/api/dashboard/tikkie-event-links/route.ts"
      to: "convex/tikkie.ts"
      via: "bounded event-link fetch path"
      pattern: "api\.tikkie\.getPaymentLinks"
---

<objective>
Remove the audited unbounded Convex read patterns so the current dashboard can scale without full-table scans.

Purpose: Phase 17 is a stabilization phase; fixing the correctness bugs is not enough if hot paths still degrade as data grows.
Output: Indexed/bounded query implementations for the audited attendees, orders, payments, Tikkie, and accommodation read surfaces while preserving existing DTOs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONTEXT.md
@.planning/phases/17-fix-critical-code-review-issues/17-RESEARCH.md
@.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md
@convex/_generated/ai/guidelines.md
@convex/attendees.ts
@convex/orders.ts
@convex/payments.ts
@convex/tikkie.ts
@convex/accommodation.ts
@convex/schema.ts
@lib/domain/accommodation/assignments.ts
@lib/domain/finance/attendees.ts
@lib/domain/finance/attendee-detail.ts
@lib/domain/finance/tikkie-event-payments.ts
@app/api/dashboard/tikkie-event-links/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace audited finance and attendee table scans with indexed or bounded reads</name>
  <read_first>convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/schema.ts, .planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/schema.ts</files>
  <action>Refactor the exact audited hot paths from the fix list: `attendees.getAttendees`, `orders.getOrderCount`, `payments.getUnassignedPayments`, `payments.getPaymentSummary`, `tikkie.getPaymentLinks`, and `tikkie.getPaymentLinksByOrderId`. Use existing indexes where possible (`eventId`, `orderId`, `status`, `paymentRequestToken`, `eventId`, `paymentLinkId`) and add only the smallest missing indexes needed in `convex/schema.ts`. For no-filter list paths, expose an explicit bounded result (`limit` arg with sane default or `.take(N)`), not `query(...).collect()`. For exact counting, use async iteration or a denormalized/indexed strategy instead of collecting the entire table into memory. Preserve current return field names and sort order where the UI depends on them (latest-link-first, newest-first ledgers, etc.).</action>
  <acceptance_criteria>`convex/payments.ts` uses `withIndex("orderId"` for payment summary and `withIndex("status"` for unassigned payments; `convex/tikkie.ts` no longer uses bare `.query("tikkiePaymentLinks").collect()` in `getPaymentLinks` or `getPaymentLinksByOrderId`; `convex/orders.ts` no longer uses `ctx.db.query("ticketTailorOrders").collect()` inside `getOrderCount`; `convex/attendees.ts` no longer uses a no-filter full-table collect for `getAttendees`; any newly needed indexes are declared in `convex/schema.ts`.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "\.collect\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts`; read the audited functions to confirm each now uses an index, `.take()`, pagination, or async iteration.</verify>
  <done>The audited finance/attendee reads are bounded or indexed and keep their existing API contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce accommodation board and downstream caller read amplification without changing payload shape</name>
  <read_first>convex/accommodation.ts, lib/domain/accommodation/assignments.ts, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, app/api/dashboard/tikkie-event-links/route.ts</read_first>
  <files>convex/accommodation.ts, lib/domain/accommodation/assignments.ts, lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/finance/tikkie-event-payments.ts, app/api/dashboard/tikkie-event-links/route.ts</files>
  <action>Refactor `convex/accommodation.ts#getRoomAllocationBoard` and any directly coupled caller assumptions so the board no longer loads unrelated tables wholesale. Keep the returned room/hotel/unassigned-attendee payload shape intact, but constrain reads by the requested `eventId`, reuse assignment-truth occupancy from Plan 17-02, and replace whole-table collection with indexed reads plus targeted secondary lookups. Then update the listed domain/API callers only where the new bounded query contract requires it (for example, passing an explicit `limit`, `eventId`, or paginated args) without changing their response DTOs. Do not redesign the board payload, filter semantics, or `hasFamily` contract established in Phase 16.</action>
  <acceptance_criteria>`convex/accommodation.ts` does not perform six unrelated whole-table `.collect()` calls inside `getRoomAllocationBoard`; `lib/domain/accommodation/assignments.ts` still exports the same board DTO fields; listed downstream callers still reference the same DTO field names; `npm run typecheck` exits 0.</acceptance_criteria>
  <verify>Run `npm run typecheck`; inspect `convex/accommodation.ts#getRoomAllocationBoard`; run `rg "getRoomAllocationBoard|api\.accommodation\.getRoomAllocationBoard" lib/domain/accommodation lib/domain/finance app/api/dashboard` to confirm callers still route through the same bounded contract.</verify>
  <done>The accommodation board and its immediate consumers avoid audited read amplification while preserving dashboard behavior.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `rg "\.collect\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts`
- Confirm audited read paths now use indexes, `.take()`, pagination, or async iteration.
</verification>

<success_criteria>

- All audited hot-path table scans from the fix list are removed or bounded.
- Existing finance/accommodation DTOs remain stable.
- Query behavior now scales better without broad table reads.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-03-SUMMARY.md`
</output>
