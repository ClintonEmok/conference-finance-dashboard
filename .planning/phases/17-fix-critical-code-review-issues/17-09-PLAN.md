---
phase: 17-fix-critical-code-review-issues
plan: "09"
type: execute
wave: 3
depends_on: ["17-08"]
files_modified:
  - convex/attendees.ts
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/accommodation.ts
  - convex/schema.ts
  - lib/types/payment.ts
  - lib/types/order.ts
  - lib/types/accommodation.ts
  - lib/types/tikkie.ts
  - lib/types/attendee.ts
  - lib/types/shared.ts
  - lib/domain/accommodation/assignments.ts
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - lib/domain/finance/tikkie-event-payments.ts
  - app/api/dashboard/tikkie-event-links/route.ts
autonomous: true
---

<objective>
Finish the stabilization pass by replacing audited full-table reads with indexed cursor-based pagination where the result set can grow, using only intentionally small bounded reads where pagination is not appropriate, and extracting the shared interfaces touched by this phase.
</objective>

<tasks>
- Replace the audited hot-path `.collect()` reads in attendees, orders, payments, Tikkie, and the accommodation board with indexed cursor-based pagination as the default fix for growing result sets, using Convex `paginationOptsValidator` plus `.paginate(args.paginationOpts)` per docs.
- Where a touched read is intentionally small and not user-paginated, replace `.collect()` with a clearly bounded indexed read such as `.take(N)` and document why pagination is unnecessary for that path.
- Add the smallest missing indexes needed in `convex/schema.ts`.
- Extract the shared payment/order/accommodation/Tikkie/attendee/shared types into `lib/types/*` and update touched callers without changing DTO fields.
</tasks>

<verification>
- `npm run typecheck`
- `rg "\.collect\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts`
- `rg "paginationOptsValidator|\.paginate\(" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts`
- Run targeted finance, attendee, accommodation, and Tikkie tests.
</verification>

<success_criteria>

- Audited hot-path reads use indexed pagination on growing paths, with `.take(N)` reserved for intentionally small bounded reads.
- Shared interfaces are discoverable in `lib/types/*` without contract drift.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-09-SUMMARY.md`
</output>
