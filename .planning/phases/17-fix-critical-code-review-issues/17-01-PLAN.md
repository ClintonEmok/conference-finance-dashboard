---
phase: 17-fix-critical-code-review-issues
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/auth.ts
  - convex/attendees.ts
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/accommodation.ts
  - convex/events.ts
  - convex/sync.ts
autonomous: true
---

<objective>
Add one shared Convex auth guard and apply it to the audited public write mutations without changing app-facing route contracts.
</objective>

<context>
@.planning/phases/17-fix-critical-code-review-issues/17-CONTEXT.md
@.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONVEX-FUNCTIONS.md
@convex/_generated/ai/guidelines.md

Convex Clerk note: per current Convex docs, any component calling an authenticated Convex query must be rendered as a child of `<Authenticated>` from `convex/react`; otherwise it can throw on page load while auth is unresolved.
</context>

<tasks>
- Create `convex/auth.ts` with a reusable helper that calls `await ctx.auth.getUserIdentity()` and throws `new Error("Unauthorized")` when absent.
- Apply that helper to the audited public write surfaces in `convex/attendees.ts`, `convex/orders.ts`, `convex/payments.ts`, `convex/tikkie.ts`, `convex/accommodation.ts`, `convex/events.ts`, and `convex/sync.ts` before any write.
- Keep `args:` validators and public export names intact unless a helper is truly server-only and can be converted to `internalMutation` without changing callers.
- Audit any touched client components or hooks that call authenticated Convex queries and ensure they only render beneath `<Authenticated>` from `convex/react`, adding `AuthLoading` or equivalent guarded rendering where needed so page load does not throw.
</tasks>

<verification>
- `npm run typecheck`
- `rg "ctx\.auth\.getUserIdentity\(|require.*Identity|assert.*Auth" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts convex/events.ts convex/sync.ts`
- Read touched client components/hooks and confirm authenticated Convex query call sites are only reachable beneath `<Authenticated>` (or an equivalent `useConvexAuth` gated render path).
- Read touched exports to confirm guards happen before writes.
</verification>

<success_criteria>

- Audited public Convex write surfaces reject unauthenticated callers.
- Current Next.js/domain callers keep the same contracts.
- Authenticated Convex query consumers no longer throw on page load because they are gated behind Convex auth-aware rendering.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-01-SUMMARY.md`
</output>
