---
phase: 17-fix-critical-code-review-issues
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/attendees.ts
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/accommodation.ts
  - convex/events.ts
  - convex/sync.ts
  - convex/auth.ts
  - lib/integrations/tikkie/webhook.ts
  - lib/integrations/ticket-tailor/webhook.ts
  - tests/tikkie/webhook-route.test.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
must_haves:
  truths:
    - "Unauthenticated callers cannot execute audited Convex financial or accommodation writes."
    - "Webhook deliveries are rejected when verification secrets are missing or invalid."
    - "Existing protected route contracts keep working after auth hardening."
  artifacts:
    - path: "convex/auth.ts"
      provides: "Shared Convex authenticated-identity guard"
      contains: "ctx.auth.getUserIdentity"
    - path: "lib/integrations/tikkie/webhook.ts"
      provides: "Fail-closed Tikkie webhook verification"
      contains: "return false"
    - path: "lib/integrations/ticket-tailor/webhook.ts"
      provides: "Fail-closed Ticket Tailor webhook verification"
      contains: "return false"
  key_links:
    - from: "convex/attendees.ts"
      to: "convex/auth.ts"
      via: "shared mutation guard"
      pattern: "assert.*Auth|require.*Identity"
    - from: "tests/tikkie/webhook-route.test.ts"
      to: "lib/integrations/tikkie/webhook.ts"
      via: "missing-secret regression coverage"
      pattern: "INVALID_SIGNATURE|signature verification failed"
---

<objective>
Lock down the security holes identified by the code review before any data/performance refactors land.

Purpose: Public Convex mutations are currently trusted too much, and both webhook verifiers fail open when secrets are absent. This plan makes security failure explicit instead of implicit.
Output: Shared Convex auth guard, audited public write mutations protected, and webhook verification helpers/tests switched to fail-closed behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONTEXT.md
@.planning/phases/17-fix-critical-code-review-issues/17-RESEARCH.md
@.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONVEX-FUNCTIONS.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/attendees.ts
@convex/orders.ts
@convex/payments.ts
@convex/tikkie.ts
@convex/accommodation.ts
@convex/events.ts
@convex/sync.ts
@lib/integrations/tikkie/webhook.ts
@lib/integrations/ticket-tailor/webhook.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared Convex auth guard and apply it to audited public write modules</name>
  <read_first>convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/accommodation.ts, convex/events.ts, convex/sync.ts, .planning/phases/17-fix-critical-code-review-issues/17-CONVEX-FUNCTIONS.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex/auth.ts, convex/attendees.ts, convex/orders.ts, convex/payments.ts, convex/tikkie.ts, convex/accommodation.ts, convex/events.ts, convex/sync.ts</files>
  <action>Create `convex/auth.ts` with one helper (for query/mutation/action contexts that have `ctx.auth`) that calls `await ctx.auth.getUserIdentity()` and throws `new Error("Unauthorized")` when identity is null, returning the identity otherwise. In every audited public write mutation from `17-CONVEX-FUNCTIONS.md` that remains public, call this helper before any `ctx.db.insert`, `ctx.db.patch`, or `ctx.db.delete`. Preserve current args/return shapes so existing generated refs and Next.js route/domain callers still work. If any write helper is truly server-only and has no legitimate client-hook consumer, convert that export to `internalMutation` and update same-file helper calls/ref names accordingly; do not change route URLs or Clerk `requireApiUser()` usage.</action>
  <acceptance_criteria>`convex/auth.ts` exists and contains `getUserIdentity`; `rg "ctx\.auth\.getUserIdentity\(|assert.*Auth|require.*Identity" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts convex/events.ts convex/sync.ts` returns matches in each audited write module; `rg "export const (createAttendee|createOrder|createPayment|assignPaymentToOrder|unassignPayment|createPaymentLink|updatePaymentLinkStatus|assignRoomToAttendee|unassignRoomFromAttendee) = mutation" convex` still shows the public API surface that existing callers use, unless a server-only helper was intentionally converted to `internalMutation` and all callers were updated in the same task; every touched public function still has an `args:` validator block; touched `ctx.db.get/patch/delete` calls include table names; touched Convex writes do not leave floating promises.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "ctx\.auth\.getUserIdentity\(|assert.*Auth|require.*Identity" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts convex/events.ts convex/sync.ts`; run `rg "args:" convex/attendees.ts convex/orders.ts convex/payments.ts convex/tikkie.ts convex/accommodation.ts convex/events.ts convex/sync.ts`; read the modified exports to confirm guards happen before writes and awaited db calls use explicit table names.</verify>
  <done>Unauthenticated internet callers can no longer execute the audited Convex writes, while current app-facing contracts remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Make webhook verification fail closed and lock it with regression tests</name>
  <read_first>lib/integrations/tikkie/webhook.ts, lib/integrations/ticket-tailor/webhook.ts, tests/tikkie/webhook-route.test.ts, tests/ticket-tailor/sync-route.test.ts</read_first>
  <files>lib/integrations/tikkie/webhook.ts, lib/integrations/ticket-tailor/webhook.ts, tests/tikkie/webhook-route.test.ts, tests/ticket-tailor/sync-route.test.ts</files>
  <action>Change both webhook verifier helpers so blank/missing secrets return `false` instead of `true`. Preserve current header parsing and timing-safe comparison logic when a secret exists. Add/extend tests so each webhook path proves three cases: invalid/missing signature returns 401, malformed JSON still returns 400 only when signature verification passes, and a deployment with no configured webhook secret is treated as signature failure rather than accepted traffic. Keep the existing error payload shape (`INVALID_SIGNATURE` / message strings) for route responses.</action>
  <acceptance_criteria>`lib/integrations/tikkie/webhook.ts` does not contain a missing-secret `return true`; `lib/integrations/ticket-tailor/webhook.ts` does not contain a missing-secret `return true`; `tests/tikkie/webhook-route.test.ts` contains a missing-secret rejection case; `tests/ticket-tailor/sync-route.test.ts` contains coverage for unauthorized/verification failure behavior; `npm test -- tests/tikkie/webhook-route.test.ts tests/ticket-tailor/sync-route.test.ts` exits 0; any touched Convex scheduler or `ctx.run*` call sites still target `internal.*` functions only.</acceptance_criteria>
  <verify>Run `npm test -- tests/tikkie/webhook-route.test.ts tests/ticket-tailor/sync-route.test.ts`; run `rg "return true" lib/integrations/tikkie/webhook.ts lib/integrations/ticket-tailor/webhook.ts` and confirm no fail-open branch remains for missing secrets; run `rg "ctx\.scheduler|ctx\.run(Query|Mutation|Action)|internal\." convex` and confirm touched scheduling/run targets are internal.</verify>
  <done>Webhook misconfiguration now blocks deliveries instead of silently trusting them, and the behavior is regression-tested.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/tikkie/webhook-route.test.ts tests/ticket-tailor/sync-route.test.ts`
- Confirm audited public write modules all contain an auth guard before writes.
</verification>

<success_criteria>

- Audited public Convex write surfaces reject unauthenticated access.
- Webhook helpers fail closed when secrets are missing.
- Existing route-level unauthorized/error contracts remain unchanged.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-01-SUMMARY.md`
</output>
