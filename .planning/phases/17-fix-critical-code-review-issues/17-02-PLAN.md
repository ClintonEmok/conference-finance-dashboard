---
phase: 17-fix-critical-code-review-issues
plan: "02"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - convex/accommodation.ts
  - convex/attendees.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/schema.ts
  - lib/domain/finance/payments.ts
  - lib/domain/finance/tikkie-quota.ts
  - lib/domain/finance/order-ledger.ts
  - lib/domain/finance/tikkie-links.ts
  - app/api/dashboard/tikkie-links/route.ts
  - app/api/dashboard/tikkie-event-links/route.ts
  - app/api/dashboard/tikkie-event-links/auto-match/route.ts
  - tests/tikkie/tikkie-event-links-route.test.ts
  - tests/reconciliation/reconciliation-outstanding.test.ts
  - tests/accommodation/allocation-filters.test.ts
autonomous: true
must_haves:
  truths:
    - "Room occupancy and room-capacity decisions use real attendee assignments, not a stale room counter."
    - "Payment auto-match and Tikkie quota checks cannot double-apply due to query-then-write races."
    - "CSV exports include the archive metadata operators already expect in the header."
  artifacts:
    - path: "convex/accommodation.ts"
      provides: "Assignment truth-based occupancy reads and writes"
      contains: "assignedRoomId"
    - path: "lib/domain/finance/order-ledger.ts"
      provides: "Archive-complete CSV row mapping"
      contains: "archiveReason"
    - path: "convex/tikkie.ts"
      provides: "Atomic quota-aware link creation and safer auto-match mutation"
      contains: "TikkieMonthlyQuotaExceededError"
  key_links:
    - from: "app/api/dashboard/tikkie-event-links/route.ts"
      to: "convex/tikkie.ts"
      via: "single mutation quota enforcement"
      pattern: "api\.tikkie"
    - from: "lib/domain/finance/payments.ts"
      to: "convex/payments.ts"
      via: "transactional auto-match mutation"
      pattern: "api\.payments"
---

<objective>
Repair the phase-blocking correctness bugs in accommodation occupancy, finance matching, Tikkie quota enforcement, and CSV export.

Purpose: These issues can silently corrupt counts, over-create links, or lose operator-visible data even when the app appears to work.
Output: Truth-based occupancy logic, atomic match/quota mutation flows, and archive-complete CSV export.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-fix-critical-code-review-issues/17-CONTEXT.md
@.planning/phases/17-fix-critical-code-review-issues/17-RESEARCH.md
@.planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md
@.planning/phases/16-v1-milestone-gap-closure/16-03-SUMMARY.md
@.planning/phases/16-v1-milestone-gap-closure/16-04-SUMMARY.md
@convex/accommodation.ts
@convex/attendees.ts
@convex/payments.ts
@convex/tikkie.ts
@convex/schema.ts
@lib/domain/finance/payments.ts
@lib/domain/finance/tikkie-quota.ts
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/tikkie-links.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale occupancy writes with assignment-truth occupancy logic</name>
  <read_first>convex/accommodation.ts, convex/attendees.ts, .planning/phases/17-fix-critical-code-review-issues/17-FIX-LIST.md, .planning/quick/260326-it5-allow-hotels-to-be-deleted-if-there-aree/260326-it5-SUMMARY.md, .planning/phases/16-v1-milestone-gap-closure/16-04-SUMMARY.md</read_first>
  <files>convex/accommodation.ts, convex/attendees.ts, tests/accommodation/allocation-filters.test.ts</files>
  <action>Refactor the assignment/unassignment paths so room occupancy is derived from `ticketTailorAttendees.assignedRoomId` instead of incrementing/decrementing `accommodationRooms.occupiedBeds`. Specifically: remove direct `occupiedBeds` patches from `assignRoomToAttendee`, `assignAttendeeToRoom`, `unassignRoomFromAttendee`, and `unassignAttendeeFromRoom`; keep capacity checks by counting assigned attendees through the `assignedRoomId` index inside the mutation; preserve the current returned `occupiedBeds` field in board/inventory/detail payloads, but compute it from actual occupants when building read models. If `attendees.ts` still exposes duplicate assign/unassign mutations, align them with the same truth-based semantics or remove their use in favor of the accommodation mutations without changing route contracts. Add regression coverage proving a room's reported occupancy follows attendee assignments even if the stored room document counter is stale or absent.</action>
  <acceptance_criteria>`convex/accommodation.ts` no longer patches `occupiedBeds` inside assign/unassign mutations; `convex/accommodation.ts` still exposes `occupiedBeds:` in read payload mapping; `convex/accommodation.ts` uses the `assignedRoomId` index for capacity/occupancy reads; `tests/accommodation/allocation-filters.test.ts` or a replacement accommodation regression test covers stale-room-counter scenarios; `rg "occupiedBeds:" convex/accommodation.ts` still shows derived payload fields while `rg "patch\(\"accommodationRooms\".*occupiedBeds|occupiedBeds: \(room\.occupiedBeds" convex/accommodation.ts` does not show direct mutation-counter maintenance.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "assignedRoomId|occupiedBeds" convex/accommodation.ts convex/attendees.ts`; run the targeted accommodation regression test you added/updated.</verify>
  <done>Room occupancy/capacity logic now follows actual attendee assignments and cannot drift because of duplicated counter writes.</done>
</task>

<task type="auto">
  <name>Task 2: Make CSV export, payment auto-match, and Tikkie quota enforcement transaction-safe</name>
  <read_first>lib/domain/finance/order-ledger.ts, lib/domain/finance/payments.ts, lib/domain/finance/tikkie-quota.ts, lib/domain/finance/tikkie-links.ts, app/api/dashboard/tikkie-links/route.ts, app/api/dashboard/tikkie-event-links/route.ts, app/api/dashboard/tikkie-event-links/auto-match/route.ts, convex/payments.ts, convex/tikkie.ts, convex/schema.ts, tests/tikkie/tikkie-event-links-route.test.ts, tests/reconciliation/reconciliation-outstanding.test.ts</read_first>
  <files>convex/payments.ts, convex/tikkie.ts, convex/schema.ts, lib/domain/finance/payments.ts, lib/domain/finance/tikkie-quota.ts, lib/domain/finance/order-ledger.ts, lib/domain/finance/tikkie-links.ts, app/api/dashboard/tikkie-links/route.ts, app/api/dashboard/tikkie-event-links/route.ts, app/api/dashboard/tikkie-event-links/auto-match/route.ts, tests/tikkie/tikkie-event-links-route.test.ts, tests/reconciliation/reconciliation-outstanding.test.ts</files>
  <action>Fix the remaining correctness bugs without changing operator-facing route URLs. In `lib/domain/finance/order-ledger.ts`, include `row.isArchived`, `row.archivedAt`, and `row.archiveReason` in each CSV data row in the same column order as the header. In payments/Tikkie matching, stop doing cross-request query-then-write matching in the domain layer: add or update one Convex mutation that loads unassigned candidates for an event, matches only when exactly one order shares the normalized buyer name **and** the exact `amountMinor` / `totalAmountMinor`, patches in the same transaction, and leaves ambiguous/no-amount-match records untouched. In quota enforcement, move the monthly limit check into the same Convex mutation that creates a Tikkie link; if the current schema cannot query the current month efficiently, add the smallest backward-compatible field/index necessary (for example a `createdMonthKey` string on link docs plus an index) and make the domain/routes call the new atomic mutation instead of `enforceTikkieMonthlyCreationQuota()` preflight. Preserve current quota error class names/messages and route response envelopes.</action>
  <acceptance_criteria>`lib/domain/finance/order-ledger.ts` row mapping contains `row.isArchived`, `row.archivedAt`, and `row.archiveReason`; `lib/domain/finance/payments.ts` no longer loops over unassigned payments and mutates them one-by-one from the domain layer; `lib/domain/finance/tikkie-quota.ts` is no longer the gate that decides quota before a later write; `app/api/dashboard/tikkie-links/route.ts` and `app/api/dashboard/tikkie-event-links/route.ts` call a single quota-aware create mutation path; `tests/tikkie/tikkie-event-links-route.test.ts` covers quota-exceeded behavior after the refactor; reconciliation/payment regression tests still pass with exact-amount matching semantics.</acceptance_criteria>
  <verify>Run `npm test -- tests/tikkie/tikkie-event-links-route.test.ts tests/reconciliation/reconciliation-outstanding.test.ts`; run `rg "isArchived|archivedAt|archiveReason" lib/domain/finance/order-ledger.ts`; run `rg "enforceTikkieMonthlyCreationQuota\(|autoMatchPayments\(" app/api/dashboard lib/domain/finance` and confirm the query-then-write preflight path is gone from routes/domain callers.</verify>
  <done>CSV exports are complete, payment auto-match no longer races across requests, and quota enforcement is decided atomically inside the write mutation.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Targeted accommodation, Tikkie-route, and reconciliation tests pass.
- Occupancy write paths no longer maintain `occupiedBeds` as mutable source-of-truth state.
</verification>

<success_criteria>

- Occupancy and capacity use assignment truth.
- CSV exports include archive metadata.
- Auto-match and quota logic are atomic at the Convex mutation boundary.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-02-SUMMARY.md`
</output>
