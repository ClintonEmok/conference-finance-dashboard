---
phase: 17-fix-critical-code-review-issues
plan: "02"
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - lib/integrations/tikkie/webhook.ts
  - lib/integrations/ticket-tailor/webhook.ts
  - app/api/webhooks/tikkie/route.ts
  - app/api/webhooks/ticket-tailor/route.ts
  - convex/auth.config.ts
  - tests/tikkie/webhook-route.test.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
---

<objective>
Make webhook signature verification and Convex auth configuration fail clearly instead of failing open or silently.
</objective>

<tasks>
- Change both webhook verifiers so missing or blank signing secrets (`TIKKIE_WEBHOOK_SECRET`, `TICKET_TAILOR_WEBHOOK_SECRET`) return failure, not success.
- Add regression tests for missing-secret, invalid-signature, and valid-signature malformed-payload cases.
- Keep `CLERK_JWT_ISSUER_DOMAIN` as the Convex Clerk issuer domain input documented by Convex, but replace the non-null assertion in `convex/auth.config.ts` with explicit validation or another clear misconfiguration path so missing issuer config is obvious.
</tasks>

<verification>
- `npm test -- tests/tikkie/webhook-route.test.ts tests/ticket-tailor/sync-route.test.ts`
- `rg "return true" lib/integrations/tikkie/webhook.ts lib/integrations/ticket-tailor/webhook.ts`
- `npm run typecheck`
</verification>

<success_criteria>

- Webhook misconfiguration fails closed.
- Convex auth config still uses `CLERK_JWT_ISSUER_DOMAIN`, but missing issuer config no longer hides behind `process.env.CLERK_JWT_ISSUER_DOMAIN!`.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-02-SUMMARY.md`
</output>
