---
phase: 17-fix-critical-code-review-issues
plan: "04"
type: execute
wave: 1
depends_on: ["17-03"]
files_modified:
  - convex/autoSync.ts
  - convex/crons.ts
  - lib/integrations/ticket-tailor/sync.ts
  - lib/domain/finance/tikkie-sync.ts
  - app/api/ticket-tailor/sync/route.ts
  - app/api/jobs/tikkie/full-sync/route.ts
autonomous: true
---

<objective>
Remove the circular Convex cron -> app HTTP -> Convex sync path and keep scheduled execution internal-only.
</objective>

<tasks>
- Refactor `convex/autoSync.ts` to call internal sync logic directly instead of fetching this app's own API routes.
- Keep cron and scheduler targets on `internal.*` functions only.
- Preserve operator-triggered route URLs, but make them thin wrappers around the same internal sync implementation where possible.
</tasks>

<verification>
- `npm run typecheck`
- `rg "fetch\(.*api/" convex/autoSync.ts`
- `rg "internal\." convex/autoSync.ts convex/crons.ts`
</verification>

<success_criteria>

- Auto-sync no longer depends on self-HTTP calls.
- Manual routes and cron flows share one internal execution path.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-critical-code-review-issues/17-04-SUMMARY.md`
</output>
