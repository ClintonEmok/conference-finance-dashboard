---
phase: 23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/signup/success/[bookingRef]/page.tsx
  - app/signup/success/[bookingRef]/loading.tsx
  - app/signup/success/[bookingRef]/error.tsx
  - convex/signupSubmission.ts
  - components/signup/SuccessPage/SuccessView.tsx
  - components/signup/SuccessPage/ExpandableSection.tsx
  - components/signup/SuccessPage/index.ts
  - components/signup/steps/ReviewSubmitStep.tsx
autonomous: true

must_haves:
  truths:
    - Success page is accessible at /signup/success/[bookingRef]
    - Page displays booking details with expandable sections
    - Sections include Tickets, Attendees, and Room Assignments
    - Submission data is fetched by booking reference
    - ReviewSubmitStep redirects to success page after submission
  artifacts:
    - path: "app/signup/success/[bookingRef]/page.tsx"
      provides: "Success page with data fetching"
      exports: ["default", "generateStaticParams"]
    - path: "app/signup/success/[bookingRef]/loading.tsx"
      provides: "Loading state for success page"
    - path: "convex/signupSubmission.ts"
      provides: "Query to fetch submission by booking reference"
      exports: ["getByBookingRef"]
    - path: "components/signup/SuccessPage/SuccessView.tsx"
      provides: "Main success view component"
      exports: ["SuccessView"]
    - path: "components/signup/SuccessPage/ExpandableSection.tsx"
      provides: "Reusable expandable section component"
      exports: ["ExpandableSection"]
  key_links:
    - from: "components/signup/steps/ReviewSubmitStep.tsx"
      to: "app/signup/success/[bookingRef]/page.tsx"
      via: "redirect() after successful submission"
      pattern: "redirect(`/signup/success/${bookingRef}`)"
    - from: "app/signup/success/[bookingRef]/page.tsx"
      to: "convex/signupSubmission.ts:getByBookingRef"
      via: "fetchQuery()"
      pattern: "api.signupSubmission.getByBookingRef"
---

<objective>
Create permanent success page with expandable sections and booking reference-based data fetching.

Purpose: Provide users with a shareable, permanent confirmation page showing their booking details.
Output: Success page route, data query, expandable UI sections.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-CONTEXT.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md

@convex/signupSubmission.ts
@convex/schema.ts
@app/api/signup/submit/route.ts
@components/signup/steps/ReviewSubmitStep.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Convex query to fetch submission by booking reference</name>
  <files>convex/signupSubmission.ts</files>
  <read_first>
    - convex/signupSubmission.ts (see existing queries and patterns)
    - convex/schema.ts (see submissions and submissionAttendees table structure)
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 3 for query pattern)
  </read_first>
  <action>
    Add getByBookingRef query to convex/signupSubmission.ts:
    
    1. Args: { bookingRef: v.string() }
    2. Returns: v.union(v.null(), v.object({...})) with fields:
       - submissionId: v.id("submissions")
       - bookingRef: v.string()
       - bookerName: v.string()
       - bookerEmail: v.string()
       - bookerPhone: v.optional(v.string())
       - eventId: v.id("events")
       - eventSlug: v.string()
       - submittedAt: v.number()
       - attendees: v.array(v.object({
           name: v.string(),
           email: v.optional(v.string()),
           ticketType: v.string(),
           assignedRoom: v.optional(v.string()),
         }))
       - roomAssignments: v.array(v.object({
           roomType: v.string(),
           hotelName: v.string(),
           bedCount: v.number(),
         }))
       - totalAmountMinor: v.optional(v.number())
       - ticketSelections: v.array(v.object({
           ticketTypeId: v.string(),
           ticketTypeName: v.string(),
           quantity: v.number(),
           pricePerTicketMinor: v.number(),
         }))
    3. Handler:
       - Query submissions table with index "by_bookingRef"
       - If not found, return null
       - Fetch submissionAttendees by submissionId
       - Fetch ticket selections from submissionTickets table
       - Build roomAssignments from attendee data
       - Return structured response
    
    Ensure the query has proper returns validator (required by Convex guidelines).
  </action>
  <verify>
    - Verify: getByBookingRef is exported as a query
    - Verify: Returns validator matches the actual return structure
    - Verify: Uses proper index lookup (by_bookingRef)
    - Run: `npx convex dev` to validate
  </verify>
  <done>
    Convex query getByBookingRef created with full submission data
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExpandableSection component</name>
  <files>components/signup/SuccessPage/ExpandableSection.tsx, components/signup/SuccessPage/index.ts</files>
  <read_first>
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 6 for component pattern)
    - components/ui (check for existing accordion/collapsible components)
  </read_first>
  <action>
    Create components/signup/SuccessPage/ExpandableSection.tsx:
    
    1. "use client" directive at top
    2. Import useState from "react"
    3. Import ChevronDown, ChevronUp from lucide-react (or use existing icon pattern)
    4. Interface ExpandableSectionProps:
       - title: string
       - icon: React.ReactNode
       - children: React.ReactNode
       - defaultExpanded?: boolean
       - badge?: string (optional count badge like "3 attendees")
    5. Component:
       - State: isExpanded controlled by prop or default
       - Clickable header button with flex layout
       - Icon on left, title center-left, optional badge, chevron on right
       - Animated expand/collapse of content area
       - Border and rounded corners for card appearance
    
    Style guidelines:
    - Use Tailwind classes: rounded-lg border, p-4, hover:bg-gray-50
    - Chevron icon rotates or swaps based on state
    - Smooth transition for expand/collapse
    - Badge uses subtle background (bg-blue-100 text-blue-800)
    
    Create components/signup/SuccessPage/index.ts that exports ExpandableSection and any future components.
  </action>
  <verify>
    - Verify: Component accepts all props from interface
    - Verify: Clicking header toggles expansion
    - Verify: defaultExpanded prop works correctly
    - Verify: Badge renders when provided
    - Run: TypeScript check passes
  </verify>
  <done>
    ExpandableSection component created with toggle functionality
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SuccessView component with sections</name>
  <files>components/signup/SuccessPage/SuccessView.tsx, components/signup/SuccessPage/index.ts</files>
  <read_first>
    - components/signup/steps/ReviewSubmitStep.tsx (see current success display for styling patterns)
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-CONTEXT.md (see D-13 through D-16 for success page requirements)
  </read_first>
  <action>
    Create components/signup/SuccessPage/SuccessView.tsx:
    
    1. Define interface SuccessViewProps:
       - submission: { bookingRef, bookerName, bookerEmail, bookerPhone?, submittedAt, attendees, roomAssignments, ticketSelections, totalAmountMinor? }
       - event: { name, startsAt, location, description? }
    
    2. Component structure:
       - Header: "Booking Confirmed!" with checkmark icon
       - Booking reference card: Large display with copy button
       - Event info section: Name, date (formatted), location
       - Expandable sections:
         a) Tickets section: List ticket types with quantities and prices
         b) Attendees section: List all attendee names
         c) Room Assignments section: Show room allocations by hotel/room type
       - Footer: Contact info and "Manage your booking" link (placeholder)
    
    3. Use ExpandableSection for collapsible content
    4. Import icons: Ticket, Users, Bed, Calendar, MapPin, CheckCircle
    5. Format date using existing format utilities (check lib/format.ts)
    6. Format money using formatMoney from lib/format.ts
    
    Style:
    - Container max-width with centered layout
    - Green success indicators
    - Card-based layout with shadows
    - Responsive for mobile
    
    Export from index.ts.
  </action>
  <verify>
    - Verify: Component accepts submission and event props
    - Verify: All three expandable sections render
    - Verify: Booking reference is prominently displayed
    - Verify: Date formatting uses project utilities
    - Verify: Money formatting uses formatMoney
    - Run: TypeScript check passes
  </verify>
  <done>
    SuccessView component created with all expandable sections
  </done>
</task>

<task type="auto">
  <name>Task 4: Create success page route with loading and error states</name>
  <files>app/signup/success/[bookingRef]/page.tsx, app/signup/success/[bookingRef]/loading.tsx, app/signup/success/[bookingRef]/error.tsx</files>
  <read_first>
    - app/events/[slug]/page.tsx (see server component pattern with fetchQuery)
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 3 for page pattern)
  </read_first>
  <action>
    Create app/signup/success/[bookingRef]/page.tsx:
    
    1. Server component (async function)
    2. Import: notFound from "next/navigation"
    3. Import: fetchQuery from "convex/nextjs"
    4. Import: api from "@/convex/_generated/api"
    5. Import: SuccessView from "@/components/signup/SuccessPage"
    6. Props: { params: Promise<{ bookingRef: string }> }
    7. Handler:
       - Await params to get bookingRef
       - Try/catch block for data fetching
       - Fetch submission: fetchQuery(api.signupSubmission.getByBookingRef, { bookingRef })
       - If null, call notFound()
       - Fetch event: fetchQuery(api.signupCatalog.getPublicSignupCatalog, { slug: submission.eventSlug })
       - Render SuccessView with submission and event
    8. Export generateStaticParams returning empty array (dynamic only)
    
    Create app/signup/success/[bookingRef]/loading.tsx:
    - Simple loading spinner or skeleton
    - Centered on page
    - Text: "Loading your booking..."
    
    Create app/signup/success/[bookingRef]/error.tsx:
    - "use client" directive
    - Error boundary component
    - Props: { error: Error, reset: () => void }
    - Display error message
    - "Try again" button that calls reset()
    - Link back to home or events page
  </action>
  <verify>
    - Verify: Page fetches data using fetchQuery
    - Verify: notFound() called when submission doesn't exist
    - Verify: Loading component exports default
    - Verify: Error component is client component with proper props
    - Run: TypeScript check passes
    - Run: `npx convex dev` to ensure queries compile
  </verify>
  <done>
    Success page route created with loading and error states
  </done>
</task>

<task type="auto">
  <name>Task 5: Update ReviewSubmitStep to redirect to success page</name>
  <files>components/signup/steps/ReviewSubmitStep.tsx</files>
  <read_first>
    - components/signup/steps/ReviewSubmitStep.tsx (see current submission handling and success display)
    - app/api/signup/submit/route.ts (see submission response structure)
  </read_first>
  <action>
    Modify components/signup/steps/ReviewSubmitStep.tsx:
    
    1. Import redirect from "next/navigation"
    2. Find the submission success handler (where setSubmissionResult or similar is called)
    3. After successful submission, instead of showing inline success UI:
       - Extract bookingRef from response
       - Call redirect(`/signup/success/${bookingRef}`)
    4. Remove or simplify the inline success state rendering
    5. Keep the loading state during submission
    
    The flow should be:
    - User clicks Submit
    - Loading spinner shows
    - API call completes with { bookingRef, submissionId, submittedAt }
    - Redirect to /signup/success/[bookingRef]
    
    Remove any success state that's no longer needed (the success page handles display).
  </action>
  <verify>
    - Verify: redirect imported from next/navigation
    - Verify: Success response triggers redirect with bookingRef
    - Verify: URL format is /signup/success/{bookingRef}
    - Verify: Loading state still shows during submission
    - Run: TypeScript check passes
  </verify>
  <done>
    ReviewSubmitStep updated to redirect to success page
  </done>
</task>

</tasks>

<verification>
- [ ] Convex query getByBookingRef exists with proper returns validator
- [ ] ExpandableSection component created and exported
- [ ] SuccessView component displays all three sections
- [ ] Success page route created with [bookingRef] param
- [ ] Loading.tsx shows loading state
- [ ] Error.tsx handles errors with reset
- [ ] ReviewSubmitStep redirects to success page
- [ ] All TypeScript compiles without errors
</verification>

<success_criteria>

1. `/signup/success/BK-TEST-123` route exists and renders
2. getByBookingRef query returns full submission data
3. SuccessView shows expandable sections for Tickets, Attendees, Rooms
4. ReviewSubmitStep redirects after successful submission
5. Loading and error states work correctly
   </success_criteria>

<output>
After completion, create `.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-02-SUMMARY.md`
</output>
