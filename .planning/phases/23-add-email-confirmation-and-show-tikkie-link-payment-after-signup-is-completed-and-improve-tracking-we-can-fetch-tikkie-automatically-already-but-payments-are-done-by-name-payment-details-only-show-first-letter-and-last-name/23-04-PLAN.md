---
phase: 23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/utils/privacy.ts
  - lib/domain/finance/tikkie-event-payments.ts
  - components/finance/PaymentRow.tsx
  - components/dashboard/reconciliation/ReconciliationTable.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
  - lib/utils/__tests__/privacy.test.ts
  - lib/domain/finance/__tests__/tikkie-matching.test.ts
autonomous: true

must_haves:
  truths:
    - Payment payer names display as "J. Smith" format (first initial + last name)
    - Auto-matching checks both booker and attendee names
    - Name masking utility is reusable across all views
    - Privacy masking applied to reconciliation, attendee details, and payment displays
  artifacts:
    - path: "lib/utils/privacy.ts"
      provides: "Name masking utilities"
      exports: ["maskName", "maskPaymentPayer"]
    - path: "lib/domain/finance/tikkie-event-payments.ts"
      provides: "Updated payment matching with attendee names"
      exports: ["findMatchingOrder"]
    - path: "lib/utils/__tests__/privacy.test.ts"
      provides: "Unit tests for name masking"
    - path: "lib/domain/finance/__tests__/tikkie-matching.test.ts"
      provides: "Integration tests for payment matching"
  key_links:
    - from: "components/finance/PaymentRow.tsx"
      to: "lib/utils/privacy.ts:maskPaymentPayer"
      via: "Import and call in render"
    - from: "lib/domain/finance/tikkie-event-payments.ts"
      to: "convex/tikkieAttendees"
      via: "Query attendee names for matching"
---

<objective>
Implement privacy-aware name masking and extend payment matching to include attendee names.

Purpose: Protect privacy by masking payment details while improving payment tracking accuracy.
Output: Privacy utilities, updated matching logic, masked UI displays.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-CONTEXT.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md

@lib/domain/finance/tikkie-event-payments.ts
@convex/tikkie.ts
@components/finance/PaymentRow.tsx
@components/dashboard/reconciliation/ReconciliationTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy utilities with name masking</name>
  <files>lib/utils/privacy.ts, lib/utils/index.ts</files>
  <read_first>
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 4 for masking pattern)
    - lib/utils/index.ts (see existing utility exports)
  </read_first>
  <action>
    Create lib/utils/privacy.ts:
    
    1. Export function maskName(fullName: string): string
       - Trim input
       - If empty, return empty string
       - Split by whitespace: const parts = trimmed.split(/\s+/)
       - If single part: return as-is (or apply different rule)
       - Get first initial: parts[0][0]?.toUpperCase() + "."
       - Get last name: parts[parts.length - 1]
       - Return: `${firstInitial}. ${lastName}`
    
    2. Export function maskPaymentPayer(name: string): string
       - Simply calls maskName(name)
       - Separate function for semantic clarity
    
    3. Add JSDoc comments explaining:
       - Purpose: privacy protection
       - Format: "J. Smith" from "John Smith"
       - Edge cases: single names, multiple spaces
    
    4. Examples in comments:
       - "John Smith" -> "J. Smith"
       - "Mary Jane Watson" -> "M. Watson"
       - "O'Connor" -> "O'Connor"
       - "" -> ""
    
    Update lib/utils/index.ts to export * from "./privacy" (if index.ts exists and uses this pattern).
  </action>
  <verify>
    - Verify: maskName function handles edge cases
    - Verify: First initial is capitalized with period
    - Verify: Last name preserves original case
    - Verify: JSDoc comments explain the function
    - Run: TypeScript check passes
  </verify>
  <done>
    Privacy utilities created with name masking functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for privacy utilities</name>
  <files>lib/utils/__tests__/privacy.test.ts</files>
  <read_first>
    - Existing test files in project (check for vitest/jest patterns)
    - vitest.config.ts (if exists)
  </read_first>
  <action>
    Create lib/utils/__tests__/privacy.test.ts:
    
    1. Import: { describe, it, expect } from "vitest"
    2. Import: { maskName, maskPaymentPayer } from "../privacy"
    
    3. Test cases for maskName:
       - Standard name: "John Smith" -> "J. Smith"
       - Multi-part name: "Mary Jane Watson" -> "M. Watson"
       - Single name: "O'Connor" -> "O'Connor"
       - Extra whitespace: "  John   Smith  " -> "J. Smith"
       - Empty string: "" -> ""
       - Lowercase input: "john smith" -> "J. Smith"
       - Mixed case: "jOhN sMiTh" -> "J. sMiTh"
       - Hyphenated last name: "John Smith-Jones" -> "J. Smith-Jones"
       - Special characters in name: "Jean-Luc Picard" -> "J. Picard"
    
    4. Test cases for maskPaymentPayer:
       - Verify it calls maskName (or test same cases)
       - "Booker Name" -> "B. Name"
    
    5. Follow project test conventions (check existing test files for patterns).
  </action>
  <verify>
    - Run: `npm test -- privacy.test.ts` (or equivalent command)
    - Verify: All test cases pass
    - Verify: Tests cover edge cases (empty, whitespace, single name)
    - Verify: Test file is discovered by test runner
  </verify>
  <done>
    Unit tests created for privacy utilities with 100% pass rate
  </done>
</task>

<task type="auto">
  <name>Task 3: Update payment matching to include attendee names</name>
  <files>lib/domain/finance/tikkie-event-payments.ts</files>
  <read_first>
    - lib/domain/finance/tikkie-event-payments.ts (see existing findMatchingOrder or similar function)
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 4 for matching pattern)
    - STATE.md line 115: "Payment auto-match requires both normalized buyer name AND exact amount"
  </read_first>
  <action>
    Update lib/domain/finance/tikkie-event-payments.ts:
    
    1. Add interface PaymentMatchingOptions:
       - includeAttendeeNames: boolean (default: true)
    
    2. Update findMatchingOrder function signature:
       - Add options parameter: options?: PaymentMatchingOptions
       - Default: { includeAttendeeNames: true }
    
    3. Update matching logic:
       - Keep existing booker name matching
       - After booker check fails, if includeAttendeeNames and order.attendees exists:
         * Iterate through order.attendees
         * For each attendee, check: normalizedNamesMatch(normalizeName(attendee.name), normalizedPayerName)
         * AND amountsMatch(order.totalAmountMinor, payment.amountMinor)
         * If match found, return the order
    
    4. Ensure amountsMatch uses exact matching (per decision D-11):
       - return expected === actual
    
    5. Keep normalizeName and normalizedNamesMatch helper functions:
       - normalizeName: lowercase and remove special chars
       - normalizedNamesMatch: includes or partial match
    
    The function should check attendee names ONLY if booker name doesn't match.
  </action>
  <verify>
    - Verify: findMatchingOrder accepts options parameter
    - Verify: Booker name matching still works
    - Verify: Attendee names checked when booker doesn't match
    - Verify: Exact amount matching preserved
    - Verify: All TypeScript types are correct
    - Run: TypeScript check passes
  </verify>
  <done>
    Payment matching updated to include attendee names
  </done>
</task>

<task type="auto">
  <name>Task 4: Create integration tests for payment matching</name>
  <files>lib/domain/finance/__tests__/tikkie-matching.test.ts</files>
  <read_first>
    - lib/domain/finance/tikkie-event-payments.ts (updated in previous task)
    - Existing test patterns in project
  </read_first>
  <action>
    Create lib/domain/finance/__tests__/tikkie-matching.test.ts:
    
    1. Import test utilities from vitest
    2. Import: { findMatchingOrder } from "../tikkie-event-payments"
    
    3. Test data:
       - Create mock orders with bookerName and attendees array
       - Create mock payments with payerName and amountMinor
    
    4. Test cases:
       - Booker name matches exactly: should return order
       - Booker name partial match: should return order
       - Attendee name matches (booker doesn't): should return order
       - No name matches: should return null
       - Name matches but amount differs: should return null (exact amount required)
       - includeAttendeeNames: false: should NOT check attendees
       - Multiple attendees, one matches: should return order
       - Case insensitive matching: "john" matches "John Smith"
    
    5. Example test structure:
       ```typescript
       const order = {
         buyerName: "Jane Doe",
         totalAmountMinor: 10000,
         attendees: [
           { name: "John Smith" },
           { name: "Bob Wilson" }
         ]
       };
       
       const payment = {
         payerName: "John Smith",
         amountMinor: 10000
       };
       
       expect(findMatchingOrder(payment, [order])).toBe(order);
       ```
  </action>
  <verify>
    - Run: `npm test -- tikkie-matching.test.ts`
    - Verify: All test cases pass
    - Verify: Tests cover both booker and attendee matching
    - Verify: Exact amount requirement is tested
    - Verify: includeAttendeeNames option is tested
  </verify>
  <done>
    Integration tests created for payment matching logic
  </done>
</task>

<task type="auto">
  <name>Task 5: Apply privacy masking to UI components</name>
  <files>components/finance/PaymentRow.tsx, components/dashboard/reconciliation/ReconciliationTable.tsx, app/dashboard/attendees/[attendeeId]/page.tsx</files>
  <read_first>
    - components/finance/PaymentRow.tsx (see current payment display)
    - components/dashboard/reconciliation/ReconciliationTable.tsx (see reconciliation display)
    - app/dashboard/attendees/[attendeeId]/page.tsx (see attendee payment display)
  </read_first>
  <action>
    Apply maskPaymentPayer to all payment displays:
    
    1. components/finance/PaymentRow.tsx:
       - Import: { maskPaymentPayer } from "@/lib/utils/privacy"
       - In render, replace payment.payerName with maskPaymentPayer(payment.payerName)
       - Ensure the masked name is displayed in the table cell
    
    2. components/dashboard/reconciliation/ReconciliationTable.tsx:
       - Find where payment payer names are displayed
       - Import maskPaymentPayer
       - Apply masking to all payer name displays
    
    3. app/dashboard/attendees/[attendeeId]/page.tsx:
       - Find payment/matching sections
       - Import maskPaymentPayer
       - Apply masking to displayed payer names
    
    4. Search for any other components displaying payer names:
       - grep -r "payerName" components/ --include="*.tsx"
       - Apply masking to any matches
    
    Note: Keep the full name in data/tooltips if needed for operator reference, but display the masked version as primary.
  </action>
  <verify>
    - Verify: PaymentRow displays masked names
    - Verify: ReconciliationTable displays masked names
    - Verify: Attendee detail page displays masked names
    - Verify: Format is consistently "J. Smith"
    - Run: TypeScript check passes
    - Run: `npm run build` to ensure no runtime errors
  </verify>
  <done>
    Privacy masking applied to all payment display components
  </done>
</task>

</tasks>

<verification>
- [ ] lib/utils/privacy.ts exports maskName and maskPaymentPayer
- [ ] Unit tests for privacy utilities pass
- [ ] Payment matching includes attendee names
- [ ] Integration tests for matching pass
- [ ] PaymentRow component uses maskPaymentPayer
- [ ] ReconciliationTable uses maskPaymentPayer
- [ ] Attendee detail page uses maskPaymentPayer
- [ ] All TypeScript compiles without errors
</verification>

<success_criteria>

1. `maskName("John Smith")` returns "J. Smith"
2. Payment matching finds orders by attendee name when booker doesn't match
3. All payment displays show "J. Smith" format instead of full names
4. Unit tests pass for privacy utilities
5. Integration tests pass for payment matching
   </success_criteria>

<output>
After completion, create `.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-04-SUMMARY.md`
</output>
