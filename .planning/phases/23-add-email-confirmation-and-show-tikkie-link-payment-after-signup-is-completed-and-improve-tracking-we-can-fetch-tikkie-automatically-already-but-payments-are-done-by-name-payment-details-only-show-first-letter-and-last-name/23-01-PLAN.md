---
phase: 23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/email.ts
  - lib/email/templates/signup-confirmation.tsx
  - lib/email/templates/index.ts
  - .env.example
  - convex/_generated/api.d.ts
autonomous: true
user_setup:
  - service: resend
    why: "Transactional email sending"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: RESEND_FROM_EMAIL
        source: "Resend Dashboard -> Domains -> Verify domain -> Use noreply@yourdomain.com"
      - name: RESEND_FROM_NAME
        value: "Conference Finance"
      - name: RESEND_WEBHOOK_SECRET
        source: "Resend Dashboard -> Webhooks -> Create webhook -> copy signing secret"
        optional: true
        note: "Required only for delivery status tracking"

resend_component_setup:
  package: "@convex-dev/resend"
  install_command: "npm install @convex-dev/resend"
  version: "0.2.3"
  documentation:
    - https://www.convex.dev/components/resend/resend.md
    - https://www.convex.dev/components/resend/llms.txt

  setup_checklist:
    - step: 1
      action: "Install package"
      command: "npm install @convex-dev/resend"
      verify: "Check package.json for @convex-dev/resend dependency"

    - step: 2
      action: "Configure component in convex/convex.config.ts"
      code: |
        import { defineApp } from "convex/server";
        import resend from "@convex-dev/resend/convex.config.js";

        const app = defineApp();
        app.use(resend);

        export default app;
      verify: "File exists and contains resend component registration"

    - step: 3
      action: "Set environment variables"
      required_vars:
        - name: RESEND_API_KEY
          get_from: "https://resend.com/api-keys"
          format: "re_xxxxxxxx"
        - name: RESEND_FROM_EMAIL
          get_from: "https://resend.com/domains"
          note: "Must be a verified domain"
        - name: RESEND_FROM_NAME
          value: "Conference Finance"
      optional_vars:
        - name: RESEND_WEBHOOK_SECRET
          get_from: "Resend Dashboard -> Webhooks"
          note: "For delivery tracking (email.delivered, email.bounced events)"

    - step: 4
      action: "Create webhook endpoint (optional, for delivery tracking)"
      file: "convex/http.ts"
      code: |
        import { httpRouter } from "convex/server";
        import { httpAction } from "./_generated/server";
        import { resend } from "./email";

        const http = httpRouter();

        http.route({
          path: "/resend-webhook",
          method: "POST",
          handler: httpAction(async (ctx, req) => {
            return await resend.handleResendEventWebhook(ctx, req);
          }),
        });

        export default http;
      verify: "convex/http.ts exports http router with /resend-webhook endpoint"
      note: "Webhook URL will be: https://<project>.convex.site/resend-webhook"

    - step: 5
      action: "Configure webhook in Resend Dashboard"
      instructions:
        - "Go to https://resend.com/webhooks"
        - "Create new webhook"
        - "URL: https://<your-project>.convex.site/resend-webhook"
        - "Enable events: email.delivered, email.bounced, email.opened, email.clicked, email.complained"
        - "Copy webhook secret and set as RESEND_WEBHOOK_SECRET"

    - step: 6
      action: "Verify setup"
      test_command: "npx convex dev"
      verify:
        - "No compilation errors"
        - "Component tables visible in Convex Dashboard under 'resend' component"

    - step: 7
      action: "Test email sending"
      note: "Component defaults to testMode: true - only allows test addresses like delivered@resend.dev"
      test_addresses:
        - "delivered@resend.dev"
        - "bounced@resend.dev"
        - "complained@resend.dev"
      go_production: "Set testMode: false in Resend constructor options"

  key_features:
    - "Automatic queuing and batching via Convex workpools"
    - "Durable execution with retry logic"
    - "Idempotency key management (prevents duplicate sends)"
    - "Rate limiting compliant with Resend API"
    - "Webhook support for delivery tracking"
    - "React Email integration support"
    - "Test mode safeguards (default: true)"

  important_notes:
    - "testMode defaults to true - must explicitly set to false for production"
    - "Emails table stores all emails with status tracking"
    - "Delivery events table stores webhook events"
    - "Manual cleanup recommended via cron job for old emails"
    - "React Email requires Node action runtime (use 'use node' directive)"

must_haves:
  truths:
    - Signup confirmation emails are sent asynchronously after submission
    - Emails include booking reference, event details, and Tikkie payment link
    - Failed email sends are logged for manual review
    - Email templates render correctly with booking data
  artifacts:
    - path: "convex/email.ts"
      provides: "Email sending action with Resend integration"
      exports: ["sendSignupConfirmation"]
    - path: "lib/email/templates/signup-confirmation.tsx"
      provides: "React email template for signup confirmations"
      exports: ["default"]
    - path: "convex/schema.ts"
      provides: "Failed and sent email tables for audit logging"
      contains: ["failedEmails", "sentEmails"]
  key_links:
    - from: "app/api/signup/submit/route.ts"
      to: "convex/email.ts:sendSignupConfirmation"
      via: "convex.action() call after successful submission"
      pattern: "api.email.sendSignupConfirmation"
---

<objective>
Set up Resend email infrastructure and create async email sending for signup confirmations.

Purpose: Ensure users receive confirmation emails with booking details and payment links after signup submission.
Output: Working email action, React template, schema tables for audit logging.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-CONTEXT.md
@.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md

@convex/\_generated/ai/guidelines.md
@convex/schema.ts
@app/api/signup/submit/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure @convex-dev/resend component</name>
  <files>package.json, .env.example, convex/convex.config.ts</files>
  <read_first>
    - package.json (check existing dependencies)
    - .env.example (see current env vars pattern)
    - convex/convex.config.ts (see current component setup)
  </read_first>
  <action>
    1. Install Convex Resend component: `npm install @convex-dev/resend`
    2. Configure component in convex/convex.config.ts:
       ```typescript
       import { defineApp } from "convex/server";
       import resend from "@convex-dev/resend/convex.config.js";
       
       const app = defineApp();
       app.use(resend);
       
       export default app;
       ```
    3. Add to .env.example:
       ```
       # Resend Email (via @convex-dev/resend component)
       RESEND_API_KEY=
       RESEND_FROM_EMAIL=noreply@example.com
       RESEND_FROM_NAME="Conference Finance"
       RESEND_WEBHOOK_SECRET=  # Optional: for delivery tracking
       ```
    4. Install React Email dependencies (for templates): `npm install @react-email/components @react-email/render`
  </action>
  <verify>
    - Run: `npm list @convex-dev/resend @react-email/components @react-email/render`
    - Verify: Package versions shown in output
    - Verify: convex/convex.config.ts contains resend component registration
    - Verify: .env.example contains all required env vars
  </verify>
  <done>
    @convex-dev/resend component installed, configured in convex.config.ts, and environment variables documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create email schema tables for audit logging</name>
  <files>convex/schema.ts</files>
  <read_first>
    - convex/schema.ts (see existing table patterns, use v from convex/values)
  </read_first>
  <action>
    Add two new tables to the schema:
    
    1. failedEmails table:
       - recipient: v.string() - email address
       - bookingRef: v.string() - booking reference
       - error: v.string() - error message
       - emailType: v.string() - type of email (e.g., "signup_confirmation")
       - retryCount: v.number() - number of retry attempts
       - status: v.union(v.literal("failed"), v.literal("pending_retry"))
       - createdAt: v.number() - timestamp
       - Index: by_bookingRef on ["bookingRef"]
       - Index: by_status on ["status"]
    
    2. sentEmails table:
       - recipient: v.string() - email address
       - bookingRef: v.string() - booking reference
       - emailId: v.optional(v.string()) - Resend email ID
       - emailType: v.string() - type of email
       - sentAt: v.number() - timestamp
       - Index: by_bookingRef on ["bookingRef"]
    
    Follow the exact same pattern as existing tables in schema.ts. Use defineTable from convex/server and v from convex/values.
  </action>
  <verify>
    - Verify: convex/schema.ts contains "failedEmails" table definition
    - Verify: convex/schema.ts contains "sentEmails" table definition
    - Verify: Both tables have proper index definitions using .index()
    - Run: `npx convex dev` to validate schema (or check for TypeScript errors)
  </verify>
  <done>
    Schema updated with failedEmails and sentEmails tables with proper indexes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create React email template for signup confirmation</name>
  <files>lib/email/templates/signup-confirmation.tsx, lib/email/templates/index.ts</files>
  <read_first>
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 3 for template pattern)
  </read_first>
  <action>
    Create lib/email/templates/signup-confirmation.tsx:
    
    1. Import from @react-email/components: Body, Container, Head, Heading, Html, Preview, Text, Button, Section
    2. Define interface SignupConfirmationEmailProps with:
       - bookerName: string
       - bookingRef: string
       - eventName: string
       - eventDate: string
       - eventLocation: string
       - tikkieUrl: string | null
       - attendeeCount: number
       - roomAssignments: Array<{ roomType: string; hotelName: string; bedCount: number }>
       - successPageUrl: string
    3. Export default function SignupConfirmationEmail that renders:
       - Html wrapper with Head
       - Preview text: "Your booking for {eventName} is confirmed"
       - Body with white background container
       - Heading: "Booking Confirmed!"
       - Personalized greeting: "Hi {bookerName},"
       - Booking reference card with gray background
       - Conditional payment section if tikkieUrl exists (QR code placeholder + link button)
       - Link to full success page
       - Footer with event details
    
    Create lib/email/templates/index.ts that re-exports the template.
    
    Use inline styles following React Email patterns. Keep styles simple for email client compatibility.
  </action>
  <verify>
    - Verify: lib/email/templates/signup-confirmation.tsx exists and exports default component
    - Verify: Component accepts all required props from SignupConfirmationEmailProps
    - Verify: Preview text includes event name
    - Verify: Conditional Tikkie section renders only when tikkieUrl provided
    - Run: TypeScript compilation check - `npx tsc --noEmit lib/email/templates/signup-confirmation.tsx`
  </verify>
  <done>
    Signup confirmation email template created with all required sections
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Convex action for sending emails using @convex-dev/resend</name>
  <files>convex/email.ts</files>
  <read_first>
    - convex/_generated/ai/guidelines.md (section on Actions)
    - .planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-RESEARCH.md (section 2 for action pattern)
    - https://www.convex.dev/components/resend/resend.md (React Email section)
  </read_first>
  <action>
    Create convex/email.ts using @convex-dev/resend component:
    
    1. Add `"use node";` at the top (required for React Email rendering)
    2. Import: action, internalAction, internalMutation from ./_generated/server
    3. Import: v from convex/values
    4. Import: Resend from "@convex-dev/resend"
    5. Import: components from ./_generated/api
    6. Import: render from "@react-email/render"
    7. Import: SignupConfirmationEmail from "../lib/email/templates/signup-confirmation"
    8. Import: internal from ./_generated/api
    
    9. Initialize Resend component instance:
       ```typescript
       export const resend = new Resend(components.resend, {
         testMode: false,  // Set to true for development
         // Optional: onEmailEvent: internal.email.handleEmailEvent,
       });
       ```
    
    10. Export sendSignupConfirmation as internalAction with args:
        - to: v.string()
        - bookerName: v.string()
        - bookingRef: v.string()
        - eventName: v.string()
        - eventDate: v.string()
        - eventLocation: v.string()
        - tikkieUrl: v.optional(v.string())
        - attendeeCount: v.number()
        - roomAssignments: v.array(v.object({ roomType: v.string(), hotelName: v.string(), bedCount: v.number() }))
        - successPageUrl: v.string()
    
    11. Handler implementation:
        - Render email template to HTML using render() from @react-email/render
        - Call resend.sendEmail(ctx, {
            from: `${process.env.RESEND_FROM_NAME} <${process.env.RESEND_FROM_EMAIL}>`,
            to: args.to,
            subject: `Booking Confirmed: ${args.eventName}`,
            html: renderedHtml,
            text: `Your booking ${args.bookingRef} for ${args.eventName} is confirmed.`,
          })
        - Note: sendEmail returns EmailId immediately (queues internally)
        - Log to sentEmails via ctx.runMutation(internal.email.logSentEmail)
        - Return { success: true, emailId: string }
    
    12. Export logSentEmail as internalMutation:
        - Args: recipient, bookingRef, emailId (v.string()), emailType (v.string())
        - Handler: insert into "sentEmails" with sentAt: Date.now()
    
    13. Export getEmailStatus as query:
        - Args: bookingRef: v.string()
        - Handler: fetch from sentEmails by bookingRef
        - Optionally check resend.status(ctx, emailId) for delivery status
    
    14. Optional: Create webhook handler in convex/http.ts:
        ```typescript
        import { httpRouter } from "convex/server";
        import { httpAction } from "./_generated/server";
        import { resend } from "./email";
        
        const http = httpRouter();
        http.route({
          path: "/resend-webhook",
          method: "POST",
          handler: httpAction(async (ctx, req) => {
            return await resend.handleResendEventWebhook(ctx, req);
          }),
        });
        export default http;
        ```
  </action>
  <verify>
    - Verify: convex/email.ts starts with `"use node";`
    - Verify: Uses `new Resend(components.resend, {...})` pattern
    - Verify: sendSignupConfirmation is exported as internalAction with all required args
    - Verify: logSentEmail internalMutation exists
    - Verify: Uses resend.sendEmail() not direct SDK
    - Run: `npx convex dev` to compile and validate
    - Note: Component automatically handles failed emails internally (no need for failedEmails table)
  </verify>
  <done>
    Convex email action created using @convex-dev/resend component with automatic queuing and retry
  </done>
</task>

</tasks>

<verification>
- [ ] @convex-dev/resend component installed and configured in convex/convex.config.ts
- [ ] Environment variables documented in .env.example (RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_FROM_NAME, RESEND_WEBHOOK_SECRET)
- [ ] Schema updated with sentEmails table (failedEmails handled by component)
- [ ] Signup confirmation email template created
- [ ] Convex email action created using @convex-dev/resend component
- [ ] Log sent email mutation created
- [ ] All TypeScript compiles without errors
- [ ] User setup documented for Resend API key and domain verification
- [ ] Optional: Webhook endpoint configured for delivery tracking
</verification>

<success_criteria>

1. `npm list @convex-dev/resend` shows installed package
2. `convex/convex.config.ts` includes resend component registration
3. `convex/email.ts` exports `sendSignupConfirmation` internalAction using @convex-dev/resend
4. `convex/schema.ts` contains `sentEmails` table (component handles email storage internally)
5. `lib/email/templates/signup-confirmation.tsx` exports valid React component
6. All Convex functions compile without errors
7. Optional: `convex/http.ts` exports webhook handler for delivery tracking
   </success_criteria>

<output>
After completion, create `.planning/phases/23-add-email-confirmation-and-show-tikkie-link-payment-after-signup-is-completed-and-improve-tracking-we-can-fetch-tikkie-automatically-already-but-payments-are-done-by-name-payment-details-only-show-first-letter-and-last-name/23-01-SUMMARY.md`
</output>
