---
phase: 04-attendee-data-accommodation-foundations
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_accommodation_inventory_foundation/migration.sql
  - lib/domain/finance/attendee-detail.ts
  - lib/domain/accommodation/inventory.ts
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - app/api/dashboard/accommodation/hotels/route.ts
  - app/api/dashboard/accommodation/room-types/route.ts
  - app/api/dashboard/accommodation/rooms/route.ts
  - app/dashboard/attendees/[attendeeId]/page.tsx
  - app/dashboard/accommodation/page.tsx
  - app/dashboard/layout.tsx
autonomous: false
must_haves:
  truths:
    - "Admin can open an attendee detail view showing finance context and room-status placeholder"
    - "Admin can define hotels, room types, and room capacity in the dashboard"
    - "Accommodation inventory data persists and reappears on refresh"
  artifacts:
    - path: "lib/domain/finance/attendee-detail.ts"
      provides: "Server-side attendee detail query with payment summary, payment-history timeline, outstanding balance, and room-status projection"
      exports: ["getAttendeeDetail"]
    - path: "lib/domain/accommodation/inventory.ts"
      provides: "Hotel, room-type, and room inventory CRUD/query helpers"
      exports: ["listAccommodationInventory", "createHotel", "createRoomType", "createRoom"]
    - path: "app/api/dashboard/attendees/[attendeeId]/route.ts"
      provides: "Protected attendee detail API"
      exports: ["GET"]
    - path: "app/dashboard/attendees/[attendeeId]/page.tsx"
      provides: "Operator attendee detail screen"
      contains: "Outstanding balance"
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "Inventory management screen for hotels, room types, and rooms"
      contains: "Hotels"
  key_links:
    - from: "app/dashboard/attendees/[attendeeId]/page.tsx"
      to: "/api/dashboard/attendees/[attendeeId]"
      via: "detail fetch on page load"
      pattern: "fetch.*api/dashboard/attendees"
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/hotels"
      via: "inventory create/list actions"
      pattern: "accommodation/hotels"
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/room-types"
      via: "inventory create/list actions"
      pattern: "accommodation/room-types"
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/rooms"
      via: "inventory create/list actions"
      pattern: "accommodation/rooms"
---

<objective>
Deliver the attendee detail screen and the first accommodation inventory management surface.

Purpose: Satisfy DASH-04 and ACC-01 by giving operators a concrete attendee record to inspect plus the hotel/room structure needed before assignment workflows begin.
Output: Accommodation inventory models + APIs, attendee detail domain/API/UI, accommodation dashboard page, and updated dashboard navigation.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-finance-visibility-reconciliation/03-02-SUMMARY.md
@.planning/phases/04-attendee-data-accommodation-foundations/04-01-SUMMARY.md
@prisma/schema.prisma
@lib/domain/finance/tikkie-links.ts
@app/api/dashboard/tikkie-links/route.ts
@app/dashboard/attendees/page.tsx
@app/dashboard/reconciliation/page.tsx
@app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accommodation inventory foundation models and protected CRUD APIs</name>
  <files>prisma/schema.prisma, prisma/migrations/*_accommodation_inventory_foundation/migration.sql, lib/domain/accommodation/inventory.ts, app/api/dashboard/accommodation/hotels/route.ts, app/api/dashboard/accommodation/room-types/route.ts, app/api/dashboard/accommodation/rooms/route.ts</files>
  <action>Extend the Prisma schema with accommodation inventory models that support hotel definitions, reusable room types, and room records with capacity/occupancy metadata needed for later assignment work. Keep this phase focused on inventory definition, not assignment mutations. Implement authenticated create/list behavior for hotels, room types, and rooms through protected dashboard APIs and centralize validation in a domain helper so names, capacities, and parent relationships fail fast with 400 `BAD_REQUEST` contracts. Persist enough metadata for operators to distinguish hotels, room-type labels, room identifiers, and bed/capacity counts.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>Admin-facing APIs can persist and return hotels, room types, and rooms with stable validation and durable schema support.</done>
</task>

<task type="auto">
  <name>Task 2: Build attendee detail query, protected API, and detail screen</name>
  <files>lib/domain/finance/attendee-detail.ts, app/api/dashboard/attendees/[attendeeId]/route.ts, app/dashboard/attendees/[attendeeId]/page.tsx</files>
  <action>Create `getAttendeeDetail(attendeeId)` that joins the attendee imported in 04-01 with its order, event, and any existing Tikkie payment-link history. Return a detail payload with attendee identity, ticket/order references, total amount, outstanding balance, payment history timeline, installment-progress summary, and room assignment status (`assigned` if future relation exists, otherwise explicit `unassigned` placeholder). Expose the data via authenticated `GET /api/dashboard/attendees/[attendeeId]` with 401/404/400 behavior matching established patterns. Build the page to show finance summary cards, payment history list, and a clearly labeled room-status section that signals inventory exists now while assignment actions arrive in Phase 5.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Operators can open an attendee detail page and understand payment/outstanding context plus current room-status state from one screen.</done>
</task>

<task type="auto">
  <name>Task 3: Add accommodation inventory dashboard page and wire navigation</name>
  <files>app/dashboard/accommodation/page.tsx, app/dashboard/layout.tsx</files>
  <action>Build `/dashboard/accommodation` as an operator page with three sections: hotels, room types, and rooms. Use the same card/table/form language already established in dashboard pages. Each section should support creating new records, show existing inventory after refresh, and display validation or API errors inline. Include room capacity visibility directly in the room table so the next phase can layer assignment indicators onto the same surface. Update dashboard navigation to include accommodation access while preserving attendee, orders, reconciliation, integrations, sync, and logout flows.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin can define accommodation inventory from the dashboard and re-open the page to see persisted hotels, room types, and rooms.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Attendee detail page plus accommodation inventory management surfaces</what-built>
  <how-to-verify>
    1) Sign in and open `/dashboard/attendees`; click into one attendee row and confirm the detail page shows payment summary, payment history/installment progress, outstanding balance, and room-status placeholder text.
    2) Open `/dashboard/accommodation`; create at least one hotel, one room type, and one room with capacity, then refresh the page and confirm they persist.
    3) Confirm dashboard navigation now includes both attendee and accommodation destinations and that existing routes still work.
    4) In a private browser, hit one new accommodation API route and confirm the 401 `UNAUTHORIZED` JSON contract remains intact.
  </how-to-verify>
  <resume-signal>Type "approved" if the detail and inventory flows behave correctly, or report the gaps to fix.</resume-signal>
</task>

</tasks>

<verification>
- Attendee detail reuses the attendee identity created in 04-01 rather than reconstructing data ad hoc from raw orders.
- Accommodation inventory persists across refresh and is represented with explicit hotel -> room type -> room structure.
- All new dashboard APIs remain protected and follow the project's established validation/error contracts.
</verification>

<success_criteria>
- DASH-04 satisfied: admin can open attendee detail with payment summary, installment progress, outstanding balance, and room-status state.
- ACC-01 satisfied: admin can define hotels, room types, and room capacity from the dashboard.
- Phase 5 can focus on room assignment behavior instead of first inventing attendee/inventory foundations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attendee-data-accommodation-foundations/04-02-SUMMARY.md`
</output>
