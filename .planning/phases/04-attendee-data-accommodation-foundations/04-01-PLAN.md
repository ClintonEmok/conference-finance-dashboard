---
phase: 04-attendee-data-accommodation-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_attendee_sync_foundation/migration.sql
  - lib/integrations/ticket-tailor/client.ts
  - lib/integrations/ticket-tailor/sync.ts
  - app/api/ticket-tailor/sync/route.ts
  - lib/domain/finance/attendees.ts
  - app/api/dashboard/attendees/route.ts
  - app/dashboard/attendees/page.tsx
  - app/dashboard/layout.tsx
  - tests/ticket-tailor/client.test.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
must_haves:
  truths:
    - "Ticket Tailor sync stores attendee-level records linked to the right order and event"
    - "Admin can view attendee-centric rows instead of only buyer/order rows"
    - "Attendee rows expose outstanding balance and room-status placeholder data for follow-on operations"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Durable attendee-level Ticket Tailor model linked to orders and events"
      contains: "model TicketTailorAttendee"
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Issued-ticket or attendee sync upserts plus attendee counts/diagnostics"
      exports: ["runTicketTailorSync"]
    - path: "lib/domain/finance/attendees.ts"
      provides: "Server-side attendee list query with finance summary fields"
      exports: ["getAttendeeLedger"]
    - path: "app/api/dashboard/attendees/route.ts"
      provides: "Protected attendee list API"
      exports: ["GET"]
    - path: "app/dashboard/attendees/page.tsx"
      provides: "Dashboard attendee list with filters and detail links"
      contains: "Attendees"
  key_links:
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "prisma.ticketTailorAttendee"
      via: "idempotent attendee upsert during sync"
      pattern: "prisma\\.ticketTailorAttendee"
    - from: "app/api/dashboard/attendees/route.ts"
      to: "lib/domain/finance/attendees.ts"
      via: "validated protected query"
      pattern: "getAttendeeLedger"
    - from: "app/dashboard/attendees/page.tsx"
      to: "/api/dashboard/attendees"
      via: "filter-driven client fetch"
      pattern: "fetch.*api/dashboard/attendees"
---

<objective>
Persist attendee-level Ticket Tailor data and expose the first attendee-centric finance surface.

Purpose: Satisfy TT-04 and lay the foundation for DASH-04 by making the real people behind each order queryable, visible, and navigable in the dashboard.
Output: Prisma attendee model + migration, extended Ticket Tailor sync pipeline, attendee list domain/API/UI, and dashboard navigation entry.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ticket-data-reliability/02-01-SUMMARY.md
@.planning/phases/03-finance-visibility-reconciliation/03-02-SUMMARY.md
@prisma/schema.prisma
@lib/integrations/ticket-tailor/client.ts
@lib/integrations/ticket-tailor/sync.ts
@app/api/ticket-tailor/sync/route.ts
@app/dashboard/orders/page.tsx
@app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable attendee-level Ticket Tailor storage</name>
  <files>prisma/schema.prisma, prisma/migrations/*_attendee_sync_foundation/migration.sql</files>
  <action>Extend the Prisma schema with an attendee model sourced from issued tickets and linked to `TicketTailorOrder` and `TicketTailorEvent`. Store provider-issued-ticket identifier(s), provider order/event identifiers, attendee display fields (`name`, `email`), ticket label/type, ticket status/check-in signals when available, and raw provider payload JSON for traceability. Keep the model idempotent with unique constraints on provider attendee/ticket identifiers and indexes supporting order/event lookup. Do not add room-assignment mutation flows here; only include nullable room-status placeholder fields if they materially reduce rework for Phase 5.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>Database can persist real attendees per order/event with durable identifiers and migration-backed schema changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Ticket Tailor sync to import issued tickets or attendees</name>
  <files>lib/integrations/ticket-tailor/client.ts, lib/integrations/ticket-tailor/sync.ts, app/api/ticket-tailor/sync/route.ts, tests/ticket-tailor/client.test.ts, tests/ticket-tailor/sync-route.test.ts</files>
  <action>Expand the Ticket Tailor client and sync pipeline so each sync run fetches attendee-level data for scoped orders/events, normalizes the provider payload shape, and upserts attendee rows alongside orders. Reuse the established fetch -> normalize -> upsert -> run-summary pattern. Preserve existing order import behavior and fallback handling; add attendee counters/diagnostics to the sync summary and API response so operators can verify how many attendee records were fetched/upserted or skipped. Update tests to cover the new client extraction/fallback behavior and the protected sync-route response contract.</action>
  <verify>npm run test && npm run typecheck</verify>
  <done>Running Ticket Tailor sync imports attendee-level records idempotently and reports attendee-specific counts/diagnostics without regressing existing order sync behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add attendee ledger query, protected API, and dashboard list page</name>
  <files>lib/domain/finance/attendees.ts, app/api/dashboard/attendees/route.ts, app/dashboard/attendees/page.tsx, app/dashboard/layout.tsx</files>
  <action>Create an attendee-centric finance module that returns paginated attendee rows with event/order references, attendee name/email, ticket label, canonical order status, total amount, outstanding balance, and a room-status placeholder (`unassigned` until Phase 5). Expose it through authenticated `GET /api/dashboard/attendees` with strict query validation for `eventId`, `from`, `to`, `search`, and pagination, following existing 401/400 contract patterns. Build `/dashboard/attendees` as a client page using the established filter-apply table pattern and include a clear link/button per row to `/dashboard/attendees/[attendeeId]` for the next plan's detail view. Update dashboard navigation to surface the new attendee area without removing Overview, Orders, Reconciliation, Integrations, or Run sync.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin can open an attendee list in the dashboard, filter/search real attendee rows, and navigate toward an attendee detail route using protected API data.</done>
</task>

</tasks>

<verification>
1. Run a scoped Ticket Tailor sync and confirm the response now includes attendee-related counts alongside existing order counts.
2. Confirm attendee records exist in the database and are linked to the expected order/event rows.
3. Sign in and open `/dashboard/attendees`; confirm real attendee rows render, filters work, and each row links to a detail route.
4. Hit `/api/dashboard/attendees` unauthenticated and confirm the 401 `UNAUTHORIZED` JSON contract remains consistent.
</verification>

<success_criteria>
- TT-04 satisfied: sync stores attendee-level records for each Ticket Tailor order.
- Dashboard now has an attendee-centric list surface instead of forcing operators to reason only from buyers/orders.
- Phase 5 room-allocation work can build on durable attendee identities rather than parsing provider payloads again.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attendee-data-accommodation-foundations/04-01-SUMMARY.md`
</output>
