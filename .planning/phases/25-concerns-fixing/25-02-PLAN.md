---
phase: 25-concerns-fixing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/sync.ts
  - convex/payments.ts
  - app/signup/success/[bookingRef]/page.tsx
  - prisma/ (entire directory deleted)
  - tests/tikkie/tikkie-links.test.ts (deleted)
autonomous: true

must_haves:
  truths:
    - "No prisma/ directory exists in the repo"
    - "No Prisma imports exist in any source file"
    - "Stale Prisma-based tests are removed"
    - "Debug console.log is removed from sync.ts"
    - "Duplicate cleanup mutations are consolidated into a single helper"
    - "Signup success page has no dead code or unreachable branches"
  artifacts:
    - path: "convex/sync.ts"
      provides: "Clean sync mutations without debug logging"
      not_contains: "console.log"
    - path: "convex/payments.ts"
      provides: "Consolidated cleanup mutations"
      contains: "function cleanupLegacyTikkiePayments"
    - path: "app/signup/success/[bookingRef]/page.tsx"
      provides: "Clean success page with no dead code"
      max_lines: 80
  key_links:
    - from: "convex/payments.ts"
      to: "convex/payments.ts"
      via: "shared cleanup helper called by both public and internal mutations"
      pattern: "cleanupLegacyTikkiePayments\\("
---

<objective>
Clean up dead code, Prisma remnants, debug logging, and duplicate mutations.

Purpose: Eliminate confusion from dead code, remove repo bloat from Prisma artifacts, and reduce maintenance burden from duplicated logic.

Output: Deleted prisma/ directory, deleted stale test file, cleaned sync.ts, consolidated payments.ts, cleaned signup success page.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-concerns-fixing/25-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONCERNS-UX-DEVEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Prisma remnants and stale tests, remove debug logging</name>
  <read_first>
    - prisma/ (verify contents before deletion: schema.prisma, migrations/, dev.db, test-db.sqlite)
    - tests/tikkie/tikkie-links.test.ts (verify it mocks Prisma interfaces)
    - convex/sync.ts line 44 (the debug console.log)
  </read_first>
  <files>prisma/ (delete entire directory), tests/tikkie/tikkie-links.test.ts (delete), convex/sync.ts</files>
  <action>
    1. **Delete the entire prisma/ directory:**
       - Run `rm -rf prisma/` from project root
       - This removes: schema.prisma, migrations/, dev.db, test-db.sqlite, prisma/test-db.sqlite
       - Verify deletion: `ls prisma/` should return "No such file or directory"
       - Confirm no remaining Prisma imports exist: `grep -rn "from.*prisma\|import.*prisma" --include="*.ts" --include="*.tsx" .` should return nothing (already verified — zero results)

    2. **Delete stale Prisma-based test file:**
       - Run `rm tests/tikkie/tikkie-links.test.ts`
       - This file (868 lines) mocks `prisma.tikkiePaymentLink.findMany` and other Prisma interfaces that no longer exist
       - Verify deletion: `ls tests/tikkie/tikkie-links.test.ts` should return "No such file or directory"

    3. **Remove debug console.log from convex/sync.ts:**
       - Remove line 44: `console.log("server identity", await ctx.auth.getUserIdentity())`
       - This line exposes authentication identity in logs and is redundant since `requireIdentity(ctx)` is called on line 45
       - The mutation handler on line 40-57 should start with `await requireIdentity(ctx)` directly, then proceed to `ctx.db.insert(...)`

  </action>
  <verify>
    - `test -d prisma && echo "FAIL: prisma dir exists" || echo "OK: prisma dir deleted"`
    - `test -f tests/tikkie/tikkie-links.test.ts && echo "FAIL: stale test exists" || echo "OK: stale test deleted"`
    - `grep -c "console.log" convex/sync.ts` should be 0 (no console.log statements remain)
    - `grep -rn "from.*prisma\|import.*prisma" --include="*.ts" --include="*.tsx" . | grep -v node_modules | grep -v ".next"` should return nothing
  </verify>
  <done>
    - prisma/ directory no longer exists
    - tests/tikkie/tikkie-links.test.ts no longer exists
    - convex/sync.ts has no console.log statements
    - No Prisma imports remain anywhere in the codebase
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate duplicate cleanup mutations in payments.ts</name>
  <read_first>
    - convex/payments.ts lines 153-250 (cleanupLegacyTikkiePayments public mutation)
    - convex/payments.ts lines 417-505 (internalCleanupLegacyTikkiePayments internal mutation)
    - Compare the two — they contain identical ~95 lines of logic
  </read_first>
  <files>convex/payments.ts</files>
  <action>
    1. **Extract shared cleanup logic into a helper function:**
       - Read both `cleanupLegacyTikkiePayments` (public, line ~153) and `internalCleanupLegacyTikkiePayments` (internal, line ~417)
       - The handler bodies are identical — both perform the same cleanup operations on legacy Tikkie payment records
       - Create a helper function `async function cleanupLegacyTikkiePaymentsHelper(ctx: MutationCtx): Promise<{ cleaned: number }>` containing the shared logic
       - Place the helper near the top of the file, after imports and before the first export

    2. **Simplify both mutations to call the helper:**
       - `cleanupLegacyTikkiePayments` mutation: handler becomes `await requireIdentity(ctx); return await cleanupLegacyTikkiePaymentsHelper(ctx)`
       - `internalCleanupLegacyTikkiePayments` mutation: handler becomes `return await cleanupLegacyTikkiePaymentsHelper(ctx)` (no auth guard — it's internal)

    3. **Preserve the public/internal distinction:**
       - The public mutation keeps its `mutation` decorator and `requireIdentity` guard
       - The internal mutation keeps its `internalMutation` decorator (no auth guard needed — called only from within Convex)
       - Both keep their existing args and return types unchanged

    4. **Verify no callers break:**
       - Search for references: `grep -rn "cleanupLegacyTikkiePayments\|internalCleanupLegacyTikkiePayments" --include="*.ts" --include="*.tsx" .`
       - Ensure all call sites still reference the correct function names

  </action>
  <verify>
    - Run `npx tsc --noEmit` — no TypeScript errors
    - `grep -c "cleanupLegacyTikkiePaymentsHelper" convex/payments.ts` should be 3 (1 definition + 2 calls)
    - Both mutations still exist: `grep -c "export const cleanupLegacyTikkiePayments\|export const internalCleanupLegacyTikkiePayments" convex/payments.ts` should be 2
    - The helper function is not exported: `grep "export.*cleanupLegacyTikkiePaymentsHelper" convex/payments.ts` should return nothing
  </verify>
  <done>
    - Shared cleanup logic exists in one helper function `cleanupLegacyTikkiePaymentsHelper`
    - Public mutation calls helper after `requireIdentity(ctx)`
    - Internal mutation calls helper directly (no auth guard)
    - Both mutations preserve their existing args, return types, and export names
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `test -d prisma` returns false (prisma directory deleted)
- `test -f tests/tikkie/tikkie-links.test.ts` returns false (stale test deleted)
- `grep -c "console.log" convex/sync.ts` returns 0 (debug logging removed)
- `grep -c "cleanupLegacyTikkiePaymentsHelper" convex/payments.ts` returns 3 (helper defined + 2 calls)
- `npx tsc --noEmit` passes with zero errors
- `npm test` passes (remaining tests still work after stale test deletion)
</verification>

<success_criteria>

- prisma/ directory completely removed from repo
- Stale Prisma-based test file deleted
- Debug console.log removed from sync.ts
- Duplicate cleanup mutations consolidated into single helper
- All remaining tests pass
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/25-concerns-fixing/25-02-SUMMARY.md`
</output>
