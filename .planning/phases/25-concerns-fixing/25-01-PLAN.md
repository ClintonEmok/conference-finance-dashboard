---
phase: 25-concerns-fixing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/orders.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/attendees.ts
  - convex/events.ts
  - convex/accommodation.ts
  - convex/sync.ts
autonomous: true

must_haves:
  truths:
    - "Unauthenticated clients cannot query financial data (orders, payments, tikkie links)"
    - "Unauthenticated clients cannot query attendee PII (names, emails, phones)"
    - "Unauthenticated clients cannot query accommodation assignments"
    - "Public signup flow still works (event listing, signup submission unaffected)"
    - "Internal/cron operations still work (internalQuery/internalMutation unchanged)"
  artifacts:
    - path: "convex/orders.ts"
      provides: "Auth-guarded order queries"
      contains: "requireIdentity"
    - path: "convex/payments.ts"
      provides: "Auth-guarded payment queries"
      contains: "requireIdentity"
    - path: "convex/tikkie.ts"
      provides: "Auth-guarded tikkie queries"
      contains: "requireIdentity"
    - path: "convex/attendees.ts"
      provides: "Auth-guarded attendee queries"
      contains: "requireIdentity"
    - path: "convex/events.ts"
      provides: "Auth-guarded event queries (public queries remain safe)"
      contains: "requireIdentity"
    - path: "convex/accommodation.ts"
      provides: "Auth-guarded accommodation queries"
      contains: "requireIdentity"
    - path: "convex/sync.ts"
      provides: "Auth-guarded sync/webhook queries"
      contains: "requireIdentity"
  key_links:
    - from: "convex/orders.ts"
      to: "convex/auth.ts"
      via: "import { requireIdentity } from './auth'"
      pattern: "requireIdentity\\(ctx\\)"
    - from: "convex/payments.ts"
      to: "convex/auth.ts"
      via: "import { requireIdentity } from './auth'"
      pattern: "requireIdentity\\(ctx\\)"
    - from: "convex/tikkie.ts"
      to: "convex/auth.ts"
      via: "import { requireIdentity } from './auth'"
      pattern: "requireIdentity\\(ctx\\)"
---

<objective>
Add `requireIdentity(ctx)` auth guards to all unprotected Convex queries that return sensitive data.

Purpose: Prevent any client with the Convex deployment URL from reading financial data, attendee PII, and accommodation assignments directly — bypassing the Next.js API layer.

Output: 7 Convex query files with auth guards added to sensitive queries.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-concerns-fixing/25-CONTEXT.md
@.planning/codebase/CONCERNS.md
@convex/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth guards to orders.ts, payments.ts, tikkie.ts queries</name>
  <read_first>
    - convex/orders.ts (see current query structure, which already import requireIdentity but don't use it in queries)
    - convex/payments.ts (see current query structure)
    - convex/tikkie.ts (see current query structure)
    - convex/auth.ts (the requireIdentity helper to call)
  </read_first>
  <files>convex/orders.ts, convex/payments.ts, convex/tikkie.ts</files>
  <action>
    Add `await requireIdentity(ctx)` as the first line in the handler of each query that returns sensitive data. The import `import { requireIdentity } from "./auth"` already exists in all three files — do NOT add duplicate imports.

    **convex/orders.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getOrders` (line ~74) — returns order financial data
    - `getOrderById` (line ~61) — returns single order details
    - `getOrdersWithFilters` (line ~350) — returns filtered orders
    - `getOrderPaymentStatus` (line ~617) — returns payment status
    - `getOrderLedger` (line ~103) — returns order + attendee data
    - `searchOrders` — returns order search results

    Do NOT add auth guards to any `internalQuery` exports — those are for cron/system use.

    **convex/payments.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getPayments` (line ~12) — returns all payment records
    - `getPaymentSummary` (line ~344) — returns payment summary data
    - `getUnassignedPayments` (line ~71) — returns unassigned payments

    Do NOT add auth guards to `internalMutation` exports or `getPaymentById` (single-ID lookup, low risk).

    **convex/tikkie.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getPaymentLinks` (line ~50) — returns all Tikkie payment links with amounts
    - `getPaymentLinkByToken` (line ~72) — returns payment link by token (keep public — needed for payment page, but verify it only returns the link URL, not financial details)
    - `getPaymentTemplates` — returns payment templates

    For `getPaymentLinkByToken`: This query is called from the public signup success page to display a payment link. It returns a single payment link by token. Keep this one public since it's a lookup-by-token (the token itself is the access control). Add a comment explaining why it remains unguarded: `// Public: token-based access control, no sensitive data exposed`.

    After adding each guard, verify the file still compiles by checking that `requireIdentity` is imported and the handler is an `async` function (it already is in all cases).

  </action>
  <verify>
    - Run `npx tsc --noEmit` — no TypeScript errors
    - Grep for `requireIdentity(ctx)` in all three files: `grep -c "requireIdentity(ctx)" convex/orders.ts convex/payments.ts convex/tikkie.ts` — each should show 3+ occurrences
    - Verify no `internalQuery` handlers have requireIdentity added (those must remain unguarded for cron)
  </verify>
  <done>
    - All sensitive queries in orders.ts, payments.ts, tikkie.ts call `await requireIdentity(ctx)` as first handler statement
    - `getPaymentLinkByToken` remains public with an explanatory comment
    - No internalQuery/internalMutation handlers modified
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add auth guards to attendees.ts, events.ts, accommodation.ts, sync.ts queries</name>
  <read_first>
    - convex/attendees.ts (see current query structure)
    - convex/events.ts (see current query structure, getEvents returns non-sensitive event list)
    - convex/accommodation.ts (see current query structure)
    - convex/sync.ts (see current query structure, note the debug console.log on line 44)
    - convex/auth.ts (the requireIdentity helper)
  </read_first>
  <files>convex/attendees.ts, convex/events.ts, convex/accommodation.ts, convex/sync.ts</files>
  <action>
    Add `await requireIdentity(ctx)` as the first line in the handler of each query that returns sensitive data.

    **convex/attendees.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getAttendees` (line ~42) — returns attendee PII (names, emails, phones)
    - `getAttendeeByEmail` (line ~107) — returns attendee by email
    - `getAttendeeById` — returns single attendee details

    The import `import { requireIdentity } from "./auth"` already exists — do NOT add duplicate imports.

    **convex/events.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getEventsForLedger` (line ~62) — returns event data for financial ledger

    For `getEvents` (line ~14): This returns basic event metadata (slug, title, dates, currency) — no financial or PII data. It's used by the public signup flow to display event details. Keep this public. Add a comment: `// Public: returns non-sensitive event metadata only, used by signup flow`.

    For `getEventById` and `getEventBySlug`: These return event metadata. `getEventBySlug` is used by the signup success page. Keep both public with a comment: `// Public: returns non-sensitive event metadata`.

    For `getEventsWithAccommodation`: Returns event + accommodation metadata. Keep public with comment: `// Public: returns non-sensitive accommodation metadata`.

    **convex/accommodation.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getRoomAllocationBoard` (line ~220) — returns room assignments + attendee data
    - `getRoomsWithDetails` (line ~696) — returns room details + occupancy
    - `listAccommodationInventory` (line ~734) — returns hotel/room inventory

    The import `import { requireIdentity } from "./auth"` already exists — do NOT add duplicate imports.

    **convex/sync.ts** — Add `await requireIdentity(ctx)` to the handler of:
    - `getWebhookEvents` (line ~117) — returns webhook event data
    - `getPendingWebhookEvents` (line ~517) — returns pending webhook events

    The import `import { requireIdentity } from "./auth"` already exists — do NOT add duplicate imports.

    Do NOT add auth guards to any `internalQuery` or `internalMutation` exports.

    Also remove the debug `console.log("server identity", await ctx.auth.getUserIdentity())` on line 44 of sync.ts — it exposes auth identity in logs and is unnecessary since `requireIdentity(ctx)` is called immediately after on line 45.

  </action>
  <verify>
    - Run `npx tsc --noEmit` — no TypeScript errors
    - Grep for `requireIdentity(ctx)` in all four files: `grep -c "requireIdentity(ctx)" convex/attendees.ts convex/events.ts convex/accommodation.ts convex/sync.ts`
    - Verify `console.log("server identity"` is removed from sync.ts: `grep -c "server identity" convex/sync.ts` should return 0
    - Verify public event queries have explanatory comments: `grep -A1 "// Public:" convex/events.ts`
    - Verify no `internalQuery`/`internalMutation` handlers have requireIdentity added
  </verify>
  <done>
    - All sensitive queries in attendees.ts, accommodation.ts, sync.ts call `await requireIdentity(ctx)` as first handler statement
    - getEvents, getEventById, getEventBySlug, getEventsWithAccommodation remain public with explanatory comments
    - Debug console.log removed from sync.ts
    - No internalQuery/internalMutation handlers modified
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- Run `npx tsc --noEmit` — zero TypeScript errors across all 7 files
- `grep -rn "requireIdentity(ctx)" convex/orders.ts convex/payments.ts convex/tikkie.ts convex/attendees.ts convex/accommodation.ts convex/sync.ts` — shows auth guards in all sensitive queries
- `grep -rn "console.log.*server identity" convex/sync.ts` — returns empty (debug log removed)
- Public event queries in events.ts have `// Public:` comments explaining why they remain unguarded
- No internalQuery/internalMutation handlers were modified
</verification>

<success_criteria>

- All Convex queries returning financial data, attendee PII, or accommodation assignments require authentication
- Public queries (event listing, event by slug) remain accessible and have explanatory comments
- Internal/cron operations remain unaffected
- Debug logging removed from sync.ts
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/25-concerns-fixing/25-01-SUMMARY.md`
</output>
