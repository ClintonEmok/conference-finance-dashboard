---
phase: 25-concerns-fixing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/autoSync.ts
autonomous: true

must_haves:
  truths:
    - "autoSync.ts no longer defines its own HTTP client functions"
    - "autoSync.ts imports and uses shared client from lib/integrations/ticket-tailor/client.ts"
    - "autoSync cron action still syncs Ticket Tailor events and orders correctly"
    - "Family linking logic still works via ctx.runQuery/runMutation"
  artifacts:
    - path: "convex/autoSync.ts"
      provides: "Cron-triggered sync action using shared HTTP client"
      contains: "import.*from.*lib/integrations/ticket-tailor/client"
      not_contains: "function ttFetch"
    - path: "lib/integrations/ticket-tailor/client.ts"
      provides: "Shared Ticket Tailor HTTP client (unchanged)"
      contains: "export.*ticketTailorFetch"
  key_links:
    - from: "convex/autoSync.ts"
      to: "lib/integrations/ticket-tailor/client.ts"
      via: "import of shared HTTP client functions"
      pattern: "import.*ticketTailor.*from.*client"
---

<objective>
Consolidate the duplicate Ticket Tailor HTTP client in autoSync.ts with the shared client in lib/integrations/ticket-tailor/client.ts.

Purpose: Eliminate ~180 lines of duplicated HTTP client code. Bug fixes or API changes only need to be applied in one place.

Output: autoSync.ts refactored to use shared client, reducing file size and eliminating duplication.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-concerns-fixing/25-CONTEXT.md
@.planning/codebase/CONCERNS.md
@lib/integrations/ticket-tailor/client.ts
@convex/autoSync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map shared client API to autoSync usage patterns</name>
  <read_first>
    - lib/integrations/ticket-tailor/client.ts (full file — understand all exported functions and their signatures)
    - convex/autoSync.ts (full file — understand all HTTP client usage patterns)
    - lib/integrations/ticket-tailor/config.ts (understand config structure)
  </read_first>
  <files>convex/autoSync.ts</files>
  <action>
    1. **Read and map the shared client exports:**
       - Read all of `lib/integrations/ticket-tailor/client.ts` to understand what functions are exported
       - Key exports to look for: `ticketTailorFetch`, `ticketTailorFetchPaginated`, `ticketTailorFetchAllEvents`, `ticketTailorFetchAllOrders`, `ticketTailorFetchAttendeesForOrder`, etc.
       - Read `lib/integrations/ticket-tailor/config.ts` to understand how config (base URL, API key) is resolved

    2. **Map autoSync HTTP client calls to shared client equivalents:**
       - `ttFetch<T>(path, query?)` in autoSync (line ~80) → maps to `ticketTailorFetch<T>(path, options?)` in shared client
       - `ttFetchPaginated(path, pageSize, maxPages)` in autoSync (line ~170) → maps to `ticketTailorFetchPaginated<T>(path, options?)` in shared client
       - `extractItems(payload)` in autoSync (line ~141) → check if shared client already handles item extraction internally
       - `extractAttendeeItems(payload)` in autoSync (line ~157) → check if shared client already handles attendee extraction

    3. **Identify differences to reconcile:**
       - autoSync uses `btoa()` for base64 encoding (line 68), shared client uses `Buffer.from()` (line 47) — both produce same result, but `btoa` is more portable for Convex edge runtime
       - autoSync defines `TT_BASE_URL` with `/v1` suffix normalization — shared client gets this from `getTicketTailorConfig()` — verify they produce the same base URL
       - autoSync's `ttFetch` and shared client's `ticketTailorFetch` both implement retry with exponential backoff — verify the retry logic is equivalent

    4. **Document the mapping** — create a mental map of which autoSync lines map to which shared client functions. This informs the refactoring in Task 2.

  </action>
  <verify>
    - Can list all exported functions from shared client: `grep "^export " lib/integrations/ticket-tailor/client.ts`
    - Can identify all ttFetch/ttFetchPaginated call sites in autoSync: `grep -n "ttFetch\|ttFetchPaginated\|extractItems\|extractAttendeeItems" convex/autoSync.ts`
    - Understand the config difference: `cat lib/integrations/ticket-tailor/config.ts`
  </verify>
  <done>
    - Clear mapping exists between autoSync HTTP client calls and shared client functions
    - Understand any behavioral differences that need reconciliation
    - Know exactly which lines in autoSync to replace
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor autoSync.ts to use shared HTTP client</name>
  <read_first>
    - convex/autoSync.ts (full file — all 866 lines)
    - lib/integrations/ticket-tailor/client.ts (all exported functions)
    - lib/integrations/ticket-tailor/config.ts (config structure)
  </read_first>
  <files>convex/autoSync.ts</files>
  <action>
    1. **Add import for shared client:**
       - At the top of autoSync.ts, add: `import { ticketTailorFetch, ticketTailorFetchPaginated } from "../lib/integrations/ticket-tailor/client"`
       - Adjust the import path as needed based on actual exported function names from the shared client

    2. **Remove duplicate HTTP client code (lines ~54-233):**
       - Delete the following definitions entirely:
         - `TT_BASE_URL` constant (line ~56-62)
         - `TT_API_KEY` constant (line ~63)
         - `TT_TIMEOUT` constant (line ~64)
         - `TT_MAX_RETRIES` constant (line ~65)
         - `ttHeaders()` function (line ~67-74)
         - `sleep()` function (line ~76-78) — only if shared client provides its own
         - `ttFetch<T>()` function (line ~80-139)
         - `extractItems()` function (line ~141-155)
         - `extractAttendeeItems()` function (line ~157-168)
         - `ttFetchPaginated()` function (line ~170-233)
       - Keep the utility functions that are NOT HTTP-client-related:
         - `pickString()` (line ~12)
         - `parseDate()` (line ~18)
         - `parseInteger()` (line ~28)
         - `toMinorAmount()` (line ~38)
         - `asRecord()` (line ~48)

    3. **Update all call sites:**
       - Replace every `await ttFetch<T>(path, query)` with `await ticketTailorFetch<T>(path, { query })` (adjust argument shape to match shared client signature)
       - Replace every `await ttFetchPaginated(path, pageSize, maxPages)` with `await ticketTailorFetchPaginated<T>(path, { pageSize, maxPages })` (adjust argument shape)
       - Replace every `extractItems(payload)` with the shared client equivalent, or inline the extraction if the shared client already returns extracted items
       - Replace every `extractAttendeeItems(payload)` similarly

    4. **Handle the `btoa` vs `Buffer.from` difference:**
       - The shared client uses `Buffer.from(apiKey).toString("base64")` (line 47 of client.ts)
       - autoSync used `btoa(TT_API_KEY)` (line 68)
       - A quick fix was already applied in Phase 260331-2jp (`replace Buffer.from with cross-platform btoa`) — verify the shared client now uses `btoa` too. If not, this is fine — Convex actions run in Node.js where `Buffer` is available.

    5. **Verify the action entry point still works:**
       - The `syncTicketTailorOrders` internalAction (or whatever the main action is named) should still:
         - Fetch events from Ticket Tailor API
         - For each event, fetch orders
         - For each order, fetch attendees
         - Call internal mutations to upsert data
         - Link families
       - None of this business logic changes — only the HTTP transport layer changes

    6. **Run TypeScript check:**
       - `npx tsc --noEmit` — verify no type errors from the refactored imports and function calls

  </action>
  <verify>
    - `grep -c "function ttFetch\|function ttFetchPaginated\|function extractItems\|function extractAttendeeItems" convex/autoSync.ts` should return 0 (all duplicate functions removed)
    - `grep -c "import.*ticketTailor" convex/autoSync.ts` should return 1 (import added)
    - `grep -c "ttFetch\|ttFetchPaginated" convex/autoSync.ts` should return 0 (no old function calls remain)
    - `npx tsc --noEmit` — no TypeScript errors
    - `wc -l convex/autoSync.ts` — file should be significantly smaller (was 866 lines, expect ~650-700 after removing ~180 lines of duplicate code)
  </verify>
  <done>
    - autoSync.ts imports and uses shared HTTP client from lib/integrations/ticket-tailor/client.ts
    - All duplicate HTTP client functions (ttFetch, ttFetchPaginated, extractItems, extractAttendeeItems) removed
    - All call sites updated to use shared client function names and argument shapes
    - Utility functions (pickString, parseDate, parseInteger, toMinorAmount, asRecord) preserved
    - TypeScript compiles without errors
    - File size reduced by ~180 lines
  </done>
</task>

</tasks>

<verification>
- `grep -c "function ttFetch\|function ttFetchPaginated\|function extractItems\|function extractAttendeeItems" convex/autoSync.ts` returns 0
- `grep -c "import.*ticketTailor" convex/autoSync.ts` returns 1
- `grep -c "ttFetch\|ttFetchPaginated" convex/autoSync.ts` returns 0 (old calls removed, only shared client calls remain)
- `npx tsc --noEmit` passes with zero errors
- autoSync.ts file size reduced from 866 lines to ~650-700 lines
- Utility functions (pickString, parseDate, parseInteger, toMinorAmount, asRecord) still present
</verification>

<success_criteria>

- Duplicate HTTP client code removed from autoSync.ts (~180 lines)
- autoSync.ts uses shared client from lib/integrations/ticket-tailor/client.ts
- All call sites updated to match shared client API
- TypeScript compiles cleanly
- No behavioral changes to sync logic — only transport layer changed
  </success_criteria>

<output>
After completion, create `.planning/phases/25-concerns-fixing/25-03-SUMMARY.md`
</output>
