---
phase: 01-foundation-secure-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth.ts
  - app/api/auth/[...all]/route.ts
  - middleware.ts
  - app/login/page.tsx
  - app/dashboard/layout.tsx
  - app/dashboard/page.tsx
  - app/api/protected/ping/route.ts
autonomous: true
must_haves:
  truths:
    - "Finance admin can request magic-link sign-in and complete login"
    - "Unauthenticated users are redirected to login for dashboard pages"
    - "Protected API requests without session return 401 JSON"
    - "User can log out from dashboard header and lose access to protected routes"
  artifacts:
    - path: "lib/auth.ts"
      provides: "Better Auth server config with magic-link plugin and 7-day rolling session"
      contains: "betterAuth"
    - path: "app/api/auth/[...all]/route.ts"
      provides: "Auth API handlers for Better Auth"
      exports: ["GET", "POST"]
    - path: "middleware.ts"
      provides: "Route-level redirect guard for /dashboard and /login"
      contains: "callbackUrl"
    - path: "app/login/page.tsx"
      provides: "Magic-link request form that forwards callbackUrl"
      contains: "email"
    - path: "app/dashboard/layout.tsx"
      provides: "Server-side session gate and visible logout control"
      contains: "auth.api.getSession"
    - path: "app/api/protected/ping/route.ts"
      provides: "Reference protected API route returning explicit 401 JSON"
      exports: ["GET"]
  key_links:
    - from: "app/login/page.tsx"
      to: "app/api/auth/[...all]/route.ts"
      via: "magic-link sign-in request"
      pattern: "signIn\.magicLink|/api/auth"
    - from: "middleware.ts"
      to: "app/login/page.tsx"
      via: "redirect with callbackUrl query"
      pattern: "callbackUrl"
    - from: "app/dashboard/layout.tsx"
      to: "lib/auth.ts"
      via: "server session retrieval"
      pattern: "auth\.api\.getSession"
---

<objective>
Implement secure dashboard access with magic-link authentication so finance users can sign in, reach protected routes, and be blocked when unauthenticated.

Purpose: Establish the security baseline required for every later finance feature.
Output: Working auth/session guardrails across page routes, protected API behavior, and logout UX.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-secure-access/01-CONTEXT.md
@.planning/phases/01-foundation-secure-access/01-DISCOVERY.md
@package.json
@app/layout.tsx
@components/theme-provider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Better Auth magic-link backend for App Router</name>
  <files>lib/auth.ts, app/api/auth/[...all]/route.ts</files>
  <action>Create Better Auth server configuration with magic-link plugin, a 7-day rolling session policy, and exported auth client/server helpers for app usage. Implement sendMagicLink callback scaffold using env-backed sender placeholders (do not hardcode secrets). Add App Router auth catch-all route using toNextJsHandler(auth) exporting GET/POST so all Better Auth endpoints resolve under /api/auth.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Auth handlers compile, magic-link endpoint is registered, and session policy is defined as 7-day rolling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement login flow + route protection with callbackUrl redirecting</name>
  <files>middleware.ts, app/login/page.tsx, app/dashboard/layout.tsx, app/dashboard/page.tsx</files>
  <action>Add middleware that redirects unauthenticated /dashboard requests to /login?callbackUrl=..., and redirects authenticated /login requests to /dashboard. Build login page with email input and submit action that invokes magic-link sign-in including callbackUrl forwarding to Better Auth. Add protected dashboard layout that performs authoritative server-side session check before render; if missing, redirect to /login with callbackUrl. Include a visible logout button in dashboard header that ends session and returns user to /login.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Unauthenticated users cannot render dashboard pages, authenticated users can, callback destination is preserved through login, and logout removes dashboard access.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce protected API unauth behavior for server actions/routes</name>
  <files>app/api/protected/ping/route.ts</files>
  <action>Create a reference protected route handler that checks session from headers and returns explicit JSON `{ error: { code: "UNAUTHORIZED", message: "Authentication required" } }` with 401 when session is absent. Return 200 JSON only when authenticated. Keep tone neutral/operational per context decisions.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Protected API contract is present and demonstrates required 401 JSON behavior for unauthenticated access.</done>
</task>

</tasks>

<verification>
1. Start app and request `/dashboard` while signed out; confirm redirect to `/login?callbackUrl=...`.
2. Submit login email; complete magic-link flow; confirm return to attempted protected route.
3. Call `/api/protected/ping` without session and verify 401 + `UNAUTHORIZED` code.
4. Click logout from dashboard header and confirm protected route access is removed.
</verification>

<success_criteria>
- FOUND-01 satisfied: finance user can sign in and reach protected dashboard in active session.
- SEC-01 baseline satisfied for unauthenticated access paths: dashboard pages redirect and protected API returns 401 JSON.
- Logout is available and functional from the dashboard shell.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-secure-access/01-01-SUMMARY.md`
</output>
