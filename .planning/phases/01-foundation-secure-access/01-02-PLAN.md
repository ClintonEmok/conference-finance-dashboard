---
phase: 01-foundation-secure-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/integrations/ticket-tailor/config.ts
  - lib/integrations/tikkie/config.ts
  - lib/integrations/status.ts
  - app/dashboard/integrations/page.tsx
  - app/api/integrations/status/route.ts
autonomous: true
must_haves:
  truths:
    - "Admin can see whether Ticket Tailor is configured"
    - "Admin can see whether Tikkie is configured"
    - "Missing or invalid integration settings do not crash the app"
    - "Runtime status includes clear validation/connectivity result for each provider"
  artifacts:
    - path: "lib/integrations/ticket-tailor/config.ts"
      provides: "Ticket Tailor env parsing and format validation"
      contains: "process.env"
    - path: "lib/integrations/tikkie/config.ts"
      provides: "Tikkie env parsing and format validation"
      contains: "process.env"
    - path: "lib/integrations/status.ts"
      provides: "Provider-agnostic status builder with connectivity ping result"
      exports: ["getIntegrationStatus"]
    - path: "app/api/integrations/status/route.ts"
      provides: "Runtime status API for integration panel"
      exports: ["GET"]
    - path: "app/dashboard/integrations/page.tsx"
      provides: "Internal status panel at /dashboard/integrations"
      contains: "Ticket Tailor"
  key_links:
    - from: "app/dashboard/integrations/page.tsx"
      to: "app/api/integrations/status/route.ts"
      via: "server fetch/render of status payload"
      pattern: "/api/integrations/status"
    - from: "app/api/integrations/status/route.ts"
      to: "lib/integrations/status.ts"
      via: "status aggregation call"
      pattern: "getIntegrationStatus"
    - from: "lib/integrations/status.ts"
      to: "lib/integrations/ticket-tailor/config.ts"
      via: "provider config validation"
      pattern: "ticket-tailor"
---

<objective>
Implement environment-based integration configuration validation and an internal status panel so finance admins can safely verify Ticket Tailor and Tikkie readiness.

Purpose: Ensure external integration prerequisites are observable and safe before data sync/payment workflows begin.
Output: Runtime integration status API + dashboard view with soft-fail behavior for missing/invalid credentials.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-secure-access/01-CONTEXT.md
@package.json
@app/layout.tsx
@components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build provider config validators with safe parse results</name>
  <files>lib/integrations/ticket-tailor/config.ts, lib/integrations/tikkie/config.ts</files>
  <action>Create dedicated config modules for Ticket Tailor and Tikkie that read required env vars, run presence checks plus basic format checks (e.g., URL/token/key shape), and return typed safe results (`configured` boolean, normalized errors list, sanitized metadata). Do not throw on missing vars; return non-fatal status so app stays usable.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Both providers expose reusable validation functions that never crash app startup and clearly report missing/invalid settings.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime integration status aggregator and API endpoint</name>
  <files>lib/integrations/status.ts, app/api/integrations/status/route.ts</files>
  <action>Implement unified status builder combining both provider validators and lightweight connectivity pings (HEAD/GET against provider health/test endpoint or equivalent low-risk request). Classify each provider state as `configured`, `misconfigured`, or `unreachable` with neutral operational messages. Expose GET `/api/integrations/status` returning timestamped JSON payload for panel consumption.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Status API returns structured records for Ticket Tailor and Tikkie including validation + connectivity outcomes without throwing on failures.</done>
</task>

<task type="auto">
  <name>Task 3: Create `/dashboard/integrations` status panel</name>
  <files>app/dashboard/integrations/page.tsx</files>
  <action>Build internal integration status page under dashboard that renders both providers with badges/state text, key non-sensitive diagnostics, and recommended next actions (e.g., set env var, verify API key, check network reachability). Ensure no secrets/tokens are rendered. Keep styling consistent with existing shadcn foundation.</action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>Admin can open `/dashboard/integrations` and see clear readiness status for Ticket Tailor and Tikkie while app remains functional even when unconfigured.</done>
</task>

</tasks>

<verification>
1. With no integration env vars set, app still loads and `/dashboard/integrations` shows both providers as not configured.
2. With intentionally malformed values, panel shows misconfigured status and actionable neutral diagnostics.
3. With valid values, status endpoint returns configured states and latest connectivity result.
4. Response/panel never displays raw secrets.
</verification>

<success_criteria>
- FOUND-02 satisfied: integration credentials are configurable via environment settings only.
- Phase 1 decision honored: missing/invalid config soft-fails while app remains usable.
- `/dashboard/integrations` provides runtime validation + connectivity visibility for both providers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-secure-access/01-02-SUMMARY.md`
</output>
