---
phase: 04-tikkie-collection-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_tikkie_payment_link_models/migration.sql
  - lib/integrations/tikkie/client.ts
  - lib/domain/finance/tikkie-links.ts
  - app/api/dashboard/tikkie-links/route.ts
  - app/dashboard/reconciliation/page.tsx
  - .env.example
autonomous: true
user_setup:
  - service: tikkie
    why: "Link creation requires valid API credentials and app-token permissions"
    env_vars:
      - name: TIKKIE_API_KEY
        source: "ABN AMRO Developer Portal -> My Apps -> API Key"
      - name: TIKKIE_APP_TOKEN
        source: "Tikkie Business Portal (prod) or POST /sandboxapps response (sandbox)"
must_haves:
  truths:
    - "Admin can generate a Tikkie payment link from reconciliation context"
    - "Generated payment links are persisted and visible in-app with created status"
    - "Admin can copy/share the generated link URL from the dashboard"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Persistent Tikkie payment-link storage linked to Ticket Tailor order identifiers"
      contains: "model TikkiePaymentLink"
    - path: "lib/integrations/tikkie/client.ts"
      provides: "Typed Tikkie create/get API adapter using API-Key and X-App-Token headers"
      exports: ["createPaymentRequest", "getPaymentRequest", "getPaymentRequestPayments"]
    - path: "lib/domain/finance/tikkie-links.ts"
      provides: "Domain service to create + persist payment links with deterministic metadata"
      exports: ["createTikkiePaymentLink", "listTikkiePaymentLinksByOrder"]
    - path: "app/api/dashboard/tikkie-links/route.ts"
      provides: "Protected endpoint to generate and list payment links"
      exports: ["GET", "POST"]
    - path: "app/dashboard/reconciliation/page.tsx"
      provides: "Reconciliation action to generate/copy links and show latest link status"
      contains: "Generate Tikkie link"
  key_links:
    - from: "app/dashboard/reconciliation/page.tsx"
      to: "/api/dashboard/tikkie-links"
      via: "button action + fetch"
      pattern: "fetch.*api/dashboard/tikkie-links"
    - from: "app/api/dashboard/tikkie-links/route.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "validated request -> domain command"
      pattern: "createTikkiePaymentLink|listTikkiePaymentLinksByOrder"
    - from: "lib/domain/finance/tikkie-links.ts"
      to: "lib/integrations/tikkie/client.ts"
      via: "create payment request and map provider payload"
      pattern: "createPaymentRequest"
---

<objective>
Implement the Tikkie link-generation path end-to-end from reconciliation rows to persisted links.

Purpose: Satisfy TK-01 and the creation portion of TK-02/TK-03 so admins can immediately act on outstanding balances.
Output: Durable payment-link model, protected generation/list API, and reconciliation UI actions for generate + copy/share.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-finance-visibility-reconciliation/03-02-SUMMARY.md
@.planning/phases/04-tikkie-collection-workflow/04-DISCOVERY.md
@TikkieAPI_v2.yaml
@prisma/schema.prisma
@lib/integrations/tikkie/config.ts
@lib/domain/finance/reconciliation.ts
@app/api/dashboard/reconciliation/route.ts
@app/dashboard/reconciliation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent Tikkie payment-link data model and domain contracts</name>
  <files>prisma/schema.prisma, prisma/migrations/*_tikkie_payment_link_models/migration.sql, lib/domain/finance/tikkie-links.ts, .env.example</files>
  <action>Add Prisma models/enums for Tikkie link tracking: `TikkiePaymentLinkStatus` (`created|paid|expired`) and `TikkiePaymentLink` linked by `providerOrderId` + `providerEventId`, storing provider token/url/status, amount minor units, description, expiry date, provider raw payload, created/updated/status timestamps, and unique index on provider paymentRequestToken. Add supporting domain module interfaces and DTOs for create/list operations (no provider HTTP yet). Update `.env.example` with `TIKKIE_WEBHOOK_SECRET` placeholder for upcoming webhook verification in Plan 04-02. Keep naming aligned with existing TicketTailor* models and avoid nullable ambiguity for required provider identifiers.</action>
  <verify>npx prisma generate && npm run typecheck</verify>
  <done>Database schema and typed domain contracts exist for storing and reading Tikkie link records tied to reconciliation orders.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tikkie client and protected link-generation/list API</name>
  <files>lib/integrations/tikkie/client.ts, lib/domain/finance/tikkie-links.ts, app/api/dashboard/tikkie-links/route.ts</files>
  <action>Create a typed Tikkie adapter using configured base URL + required headers (`API-Key`, `X-App-Token`) with explicit error mapping for 400/401/403/5xx. Implement `createPaymentRequest` against `POST /paymentrequests` and read helpers for later sync (`GET /paymentrequests/{token}`, `GET /paymentrequests/{token}/payments?pageNumber&pageSize`). In domain service, implement `createTikkiePaymentLink({ providerOrderId, providerEventId, amountMinor, description, expiryDate, referenceId })` that calls adapter, maps provider status `OPEN -> created`, persists full metadata, and upserts deterministically by paymentRequestToken. Add protected `POST /api/dashboard/tikkie-links` (session required, strict validation, 400 BAD_REQUEST contract) and `GET /api/dashboard/tikkie-links?providerOrderId=...` for reconciliation UI retrieval.</action>
  <verify>npm run typecheck && npm run lint && npm run test</verify>
  <done>Authenticated dashboard clients can create and fetch persisted Tikkie links through stable API contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add reconciliation link-generation and copy/share actions</name>
  <files>app/dashboard/reconciliation/page.tsx</files>
  <action>Extend reconciliation rows with a Tikkie action column: amount-aware `Generate Tikkie link` button for eligible rows (outstanding > 0), loading/error feedback per row, and `Copy link` action once a URL exists. On generate, call `POST /api/dashboard/tikkie-links` with row context and refresh row-level link state from `GET /api/dashboard/tikkie-links`. Display latest link badge/state (`created|paid|expired`) and created timestamp so operator can confirm immediate persistence. Preserve existing filter/apply behavior and do not regress current reconciliation reasons table.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Admin can create a payment link from reconciliation, then copy/share it from the same row with visible persisted status.</done>
</task>

</tasks>

<verification>
1. Authenticate and open `/dashboard/reconciliation`; generate a link on a row with outstanding amount.
2. Confirm row now shows a persisted link status badge (`created`) and a copy action.
3. Refresh the page; confirm generated link data remains visible (database-backed).
4. Call `/api/dashboard/tikkie-links` unauthenticated and confirm 401 `UNAUTHORIZED` JSON contract.
</verification>

<success_criteria>
- TK-01 satisfied: admin can generate Tikkie links from reconciliation context.
- Creation-side of TK-02 satisfied: link metadata is persisted with deterministic provider linkage.
- TK-03 baseline satisfied: admin can copy/share generated link URL from dashboard UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tikkie-collection-workflow/04-01-SUMMARY.md`
</output>
