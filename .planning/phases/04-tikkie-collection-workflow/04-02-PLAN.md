---
phase: 04-tikkie-collection-workflow
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_tikkie_payment_link_status_events/migration.sql
  - lib/domain/finance/tikkie-links.ts
  - lib/integrations/tikkie/webhook.ts
  - app/api/webhooks/tikkie/route.ts
  - app/api/jobs/tikkie/status-sync/route.ts
  - app/api/dashboard/tikkie-links/route.ts
  - app/dashboard/reconciliation/page.tsx
  - .env.example
autonomous: false
user_setup:
  - service: tikkie
    why: "Webhook-driven status updates require dashboard callback registration in Tikkie"
    dashboard_config:
      - task: "Create payment request notification subscription pointing to /api/webhooks/tikkie"
        location: "Tikkie API (POST /paymentrequestssubscription) or Business Portal webhook settings"
must_haves:
  truths:
    - "Generated links transition from created to paid/expired and those updates are visible in-app"
    - "If webhook delivery is missed, fallback sync updates stale created links"
    - "Admin can trust the link status shown on reconciliation as current enough for follow-up decisions"
  artifacts:
    - path: "lib/integrations/tikkie/webhook.ts"
      provides: "Idempotent Tikkie webhook ingest + status transition handling"
      exports: ["verifyTikkieWebhook", "processTikkieWebhookNotification"]
    - path: "app/api/webhooks/tikkie/route.ts"
      provides: "Public webhook receiver that validates and processes payment notifications"
      exports: ["POST"]
    - path: "app/api/jobs/tikkie/status-sync/route.ts"
      provides: "Protected fallback status polling job for created links"
      exports: ["POST"]
    - path: "lib/domain/finance/tikkie-links.ts"
      provides: "Shared status refresh logic from webhook or provider polling"
      exports: ["refreshTikkiePaymentLinkStatus", "syncPendingTikkiePaymentLinks"]
    - path: "app/dashboard/reconciliation/page.tsx"
      provides: "UI status refresh trigger and updated status timestamp visibility"
      contains: "Refresh status"
  key_links:
    - from: "app/api/webhooks/tikkie/route.ts"
      to: "lib/integrations/tikkie/webhook.ts"
      via: "signature validation + notification dispatch"
      pattern: "verifyTikkieWebhook|processTikkieWebhookNotification"
    - from: "lib/integrations/tikkie/webhook.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "provider token -> persisted link status transition"
      pattern: "refreshTikkiePaymentLinkStatus"
    - from: "app/api/jobs/tikkie/status-sync/route.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "polling batch for created links"
      pattern: "syncPendingTikkiePaymentLinks"
    - from: "app/dashboard/reconciliation/page.tsx"
      to: "/api/dashboard/tikkie-links"
      via: "status refresh/list query"
      pattern: "fetch.*api/dashboard/tikkie-links"
---

<objective>
Add reliable payment-status synchronization so Tikkie links stay current via webhook with polling fallback.

Purpose: Complete TK-02/TK-03 by making status transitions operationally trustworthy even with webhook delivery gaps.
Output: Webhook receiver, fallback status-sync job, shared transition logic, and reconciliation UI refresh/status visibility.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tikkie-collection-workflow/04-DISCOVERY.md
@.planning/phases/04-tikkie-collection-workflow/04-01-SUMMARY.md
@TikkieAPI_v2.yaml
@lib/integrations/ticket-tailor/webhook.ts
@app/api/webhooks/ticket-tailor/route.ts
@app/api/jobs/ticket-tailor/retry/route.ts
@app/dashboard/reconciliation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement idempotent Tikkie status transition domain + webhook processing</name>
  <files>prisma/schema.prisma, prisma/migrations/*_tikkie_payment_link_status_events/migration.sql, lib/domain/finance/tikkie-links.ts, lib/integrations/tikkie/webhook.ts, app/api/webhooks/tikkie/route.ts, .env.example</files>
  <action>Add status transition persistence support (e.g., transition log table or lastTransition fields) so updates are auditable and idempotent. Implement Tikkie webhook module that validates optional `TIKKIE_WEBHOOK_SECRET` (if configured), parses payment/refund notifications, resolves `paymentRequestToken`, fetches authoritative provider state if needed, and applies monotonic app-state transitions (`created -> paid|expired`; never revert paid to created). Create public `POST /api/webhooks/tikkie` route with explicit 400/401 contracts for invalid payload/signature and 200 acknowledgement for accepted notifications. Keep processing logic resilient to duplicate webhook deliveries.</action>
  <verify>npm run typecheck && npm run test</verify>
  <done>Webhook notifications can safely update persisted link states without duplicate or regressive transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add protected fallback polling sync and dashboard refresh wiring</name>
  <files>lib/domain/finance/tikkie-links.ts, app/api/jobs/tikkie/status-sync/route.ts, app/api/dashboard/tikkie-links/route.ts, app/dashboard/reconciliation/page.tsx</files>
  <action>Implement `syncPendingTikkiePaymentLinks({ limit })` that polls provider state for links still `created`, maps statuses/payments to app states, and updates timestamps/diagnostics. Add protected `POST /api/jobs/tikkie/status-sync?limit=` endpoint (auth required) for manual/cron execution with summary payload `{ scanned, updated, unchanged, failed }`. Extend `GET /api/dashboard/tikkie-links` to return latest status timestamps and source (`webhook|poll`) metadata. Update reconciliation UI with per-row `Refresh status` action (or table-level refresh) invoking polling/list flow and rendering `lastStatusCheckedAt` so operators can confirm recency.</action>
  <verify>npm run typecheck && npm run lint && npm run test && npm run build</verify>
  <done>Missed webhook events are recoverable via polling, and reconciliation UI can show/refetch current payment status on demand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tikkie status synchronization pipeline (webhook + fallback polling) wired to reconciliation visibility</what-built>
  <how-to-verify>
    1) Generate a Tikkie link from `/dashboard/reconciliation` (from Plan 04-01 baseline).
    2) Trigger a known status change in sandbox (pay or expire simulation), then run `POST /api/jobs/tikkie/status-sync` while authenticated.
    3) Refresh `/dashboard/reconciliation` and confirm link status changes from `created` to `paid` or `expired` with updated timestamp.
    4) (If webhook configured) replay/send webhook payload to `/api/webhooks/tikkie` and confirm duplicate deliveries do not create duplicate transitions.
    5) Call `/api/jobs/tikkie/status-sync` unauthenticated and confirm 401 `UNAUTHORIZED` contract.
  </how-to-verify>
  <resume-signal>Type "approved" if statuses update correctly, or report failing step numbers.</resume-signal>
</task>

</tasks>

<verification>
- Webhook and polling both converge stored statuses to provider truth without regressions.
- Reconciliation surface exposes status recency metadata for operator trust.
- Private operational sync endpoint remains authenticated and contract-stable.
</verification>

<success_criteria>
- TK-02 satisfied: metadata is stored and statuses update through webhook or polling.
- TK-03 satisfied: admin sees current payment status for generated links in dashboard context.
- Phase 4 goal met: payment-link generation + tracking works end-to-end for finance follow-up workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tikkie-collection-workflow/04-02-SUMMARY.md`
</output>
