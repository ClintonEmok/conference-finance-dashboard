---
phase: 28-single-sidebar-shell
plan: "01"
type: execute
wave: 1
depends_on:
  - 27-04
files_modified:
  - app/dashboard/dashboard-surface.tsx
  - app/dashboard/dashboard-shell.tsx
  - app/dashboard/events/[slug]/layout.tsx
  - app/dashboard/events/[slug]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Selected-event routes show one sidebar only."
    - "The sidebar starts with EventSwitcher and a short flat list of short section labels."
    - "Event title, muted slug subtitle, Public page, and Back to picker appear in a compact two-line header above content."
    - "Sidebar footer facts show startsAt, timezone, and currency as key/value rows."
    - "The fullscreen picker at /dashboard and the create flow at /dashboard/events/new remain fullscreen and unchanged."
    - "Invalid slugs fall back to the picker with a safe back-to-picker path."
    - "Slug-scoped event routes still resolve from the URL after refresh and on deep links."
  artifacts:
    - path: app/dashboard/dashboard-surface.tsx
      provides: "Route split between fullscreen chooser and slug-scoped workspace"
      contains: "fullscreen route handling"
    - path: app/dashboard/dashboard-shell.tsx
      provides: "Global shell chrome that no longer duplicates event-scoped sidebar navigation"
      contains: "EventSwitcher"
    - path: app/dashboard/events/[slug]/layout.tsx
      provides: "Single-sidebar event-local shell with header strip and facts footer"
      contains: "Back to picker"
    - path: app/dashboard/events/[slug]/page.tsx
      provides: "Event home surface under the new shell"
      contains: "event home"
  key_links:
    - from: app/dashboard/dashboard-surface.tsx
      to: app/dashboard/page.tsx
      via: "fullscreen picker route stays outside the shell"
      pattern: "/dashboard"
    - from: app/dashboard/dashboard-surface.tsx
      to: app/dashboard/events/new/page.tsx
      via: "create flow remains fullscreen"
      pattern: "/dashboard/events/new"
    - from: app/dashboard/dashboard-shell.tsx
      to: app/dashboard/events/[slug]/layout.tsx
      via: "slug-scoped routes no longer render duplicate sidebar chrome"
      pattern: "currentSlug"
    - from: app/dashboard/events/[slug]/layout.tsx
      to: components/dashboard/event-switcher.tsx
      via: "single sidebar begins with shared event switching"
      pattern: "EventSwitcher"
    - from: app/dashboard/events/[slug]/layout.tsx
      to: app/dashboard/events/[slug]/page.tsx
      via: "shared header strip sits above every event-scoped child page"
      pattern: "slug"
---

<objective>
Make event-scoped dashboard pages feel like one workspace by removing duplicate sidebar chrome, moving event context into a compact header strip, and keeping slug navigation and the fullscreen picker intact.

Purpose: Phase 27 finished the event-first entry flow; Phase 28 removes the leftover double-shell feeling so selected-event pages read as a single focused workspace.
Output: One sidebar for event-scoped pages, compact event context above content, and unchanged fullscreen picker / create-entry routes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-event-scoped-dashboard/27-CONTEXT.md
@.planning/phases/28-single-sidebar-shell/28-CONTEXT.md
@.planning/phases/28-single-sidebar-shell/28-ARCHITECTURE-ANALYSIS.md
@.planning/phases/28-single-sidebar-shell/28-UX-IA-REVIEW.md
@.planning/phases/28-single-sidebar-shell/28-FLOW-CHECK.md
@app/dashboard/dashboard-surface.tsx
@app/dashboard/dashboard-shell.tsx
@app/dashboard/events/[slug]/layout.tsx
@app/dashboard/events/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split shell ownership by route class</name>
  <files>app/dashboard/dashboard-surface.tsx, app/dashboard/dashboard-shell.tsx</files>
  <action>
    Route slug-scoped event pages so they no longer render the duplicate dashboard-shell sidebar chrome. Keep `/dashboard`, `/dashboard/events`, and `/dashboard/events/new` on the fullscreen picker/create path, preserve pathname-derived `currentSlug`, and leave only minimal global utility chrome on selected-event routes. Do not move slug state into client-only state, and do not change the fullscreen picker surface.
  </action>
  <verify>Run `npm run typecheck`, then inspect the route split to confirm slug routes are no longer served by the duplicate sidebar shell and fullscreen routes still bypass it.</verify>
  <verify>Confirm invalid event slugs still show the safe fallback back to the picker, and nested event routes inherit the same single-sidebar shell.</verify>
  <done>Selected-event routes no longer show the dashboard shell sidebar as competing chrome, while fullscreen chooser/create routes remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the event-local layout into the single-sidebar shell</name>
  <files>app/dashboard/events/[slug]/layout.tsx, app/dashboard/events/[slug]/page.tsx</files>
  <action>
    Collapse the event-local layout into one sidebar only: start the sidebar with `EventSwitcher`, follow with a short flat list of short section labels, and pin it on desktop with internal scroll handling overflow. Move event title, muted slug subtitle, `Public page`, and `Back to picker` into the compact two-line header block above the content, and put `startsAt`, timezone, and currency into a sidebar footer card as key/value rows. Remove obsolete copy/comments that imply the sidebar carries the event context, and keep slug resolution path-derived so refresh/deep-link behavior survives intact.
  </action>
  <verify>Run `npm run typecheck`, then inspect the event-scoped layout and home page to confirm only one sidebar remains, the header strip is present, and the facts footer uses rows for `startsAt`/timezone/currency.</verify>
  <done>Every event-scoped route shares the new single-sidebar shell and the duplicate sidebar chrome is gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Single-sidebar event shell with compact header context and preserved fullscreen picker.</what-built>
  <how-to-verify>
    1. Open `/dashboard` and confirm the fullscreen event picker still looks unchanged.
    2. Open `/dashboard/events/[slug]` and confirm there is exactly one sidebar starting with `EventSwitcher`.
    3. Confirm the compact header shows event title, muted slug subtitle, `Public page`, and `Back to picker` inline text buttons.
    4. Refresh or deep-link to a nested event route and confirm the active slug remains correct.
    5. Open an invalid `/dashboard/events/[slug]` path and confirm it falls back safely to the picker.
    6. Open `/dashboard/events/new` and confirm it still renders fullscreen, not inside the event shell.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm `/dashboard` and `/dashboard/events/new` stay fullscreen.
- Confirm `/dashboard/events/[slug]` renders one sidebar only and keeps slug-scoped navigation stable on refresh.
- Confirm invalid slugs fall back to the picker and nested routes share the same shell.
</verification>

<success_criteria>
- Exactly one sidebar is visible on selected-event routes.
- Event context lives in the compact header/footer regions instead of duplicate sidebar chrome.
- Fullscreen picker and create-flow routes remain untouched.
- Deep links and refreshes keep the selected event scoped from the URL.
</success_criteria>

<output>
After completion, create `.planning/phases/28-single-sidebar-shell/28-01-SUMMARY.md`
</output>
