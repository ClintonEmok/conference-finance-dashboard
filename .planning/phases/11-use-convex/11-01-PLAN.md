---
phase: 11-use-convex
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - convex.json
  - tsconfig.json
autonomous: false
must_haves:
  truths:
    - "Better Convex packages installed"
    - "Folder structure configured"
    - "Dev server can start"
  artifacts:
    - path: "package.json"
      contains: "convex"
    - path: "convex.json"
      provides: "Better Convex configuration"
key_links: []
---

<objective>
Setup: Install Better Convex packages, configure folder structure, initialize dev server.
</objective>

<tasks>

<task type="auto">
  <name>Install Better Convex dependencies</name>
  <files>package.json</files>
  <action>
    Install Better Convex packages using bun:
    - bun add convex better-convex zod @tanstack/react-query
  </action>
  <verify>bun pm shows convex and better-convex installed</verify>
  <done>Dependencies installed</done>
</task>

<task type="auto">
  <name>Configure Better Convex folder structure</name>
  <files>convex.json</files>
  <action>
    Create convex.json:
    ```json
    {
      "functions": "convex/functions",
      "codegen": { "staticApi": true, "staticDataModel": true }
    }
    ```
    
    Create directories:
    - convex/functions
    - convex/lib
    - src/lib/convex
    
    Update tsconfig.json paths for @convex/*
  </action>
  <verify>convex.json exists with config</verify>
  <done>Folder structure ready</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what>Initialize Better Convex dev server</what>
  <how>
    1. Run `bunx convex login`
    2. Run `bunx better-convex dev`
    3. Verify types generated in convex/functions/_generated/
  </how>
  <resume-signal>Type "done" when dev server is running</resume-signal>
</task>

</tasks>

<success_criteria>

- Better Convex installed
- Dev server runs without errors
  </success_criteria>
