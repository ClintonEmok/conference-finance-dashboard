---
phase: 11-use-convex
plan: "04"
type: execute
wave: 2
depends_on:
  - "11-03"
files_modified:
  - lib/domain/**/*.ts
  - lib/integrations/**/*.ts
autonomous: true
must_haves:
  truths:
    - "Domain layer uses Convex instead of Prisma"
    - "Integrations call Convex mutations"
  artifacts:
    - path: "lib/domain/finance/*.ts"
      provides: "Updated to use Convex"
key_links: []
---

<objective>
Update domain layer to use Convex functions instead of Prisma.
</objective>

<tasks>

<task type="auto">
  <name>Update finance domain</name>
  <files>lib/domain/finance/*.ts</files>
  <action>
    Update files in lib/domain/finance/:
    - Replace prisma imports with Convex function calls
    - order-ledger.ts
    - attendee-detail.ts
    - payments.ts
    - tikkie-links.ts
    - tikkie-templates.ts
    - reporting.ts
    
    Pattern:
    // Before
    const orders = await prisma.ticketTailorOrder.findMany(...)
    
    // After  
    const orders = await convex.query(functions.orders.getOrders, {...})
  </action>
  <verify>No prisma imports in lib/domain/finance/</verify>
  <done>Finance domain updated</done>
</task>

<task type="auto">
  <name>Update accommodation domain</name>
  <files>lib/domain/accommodation/*.ts</files>
  <action>
    Update files in lib/domain/accommodation/:
    - inventory.ts
    - assignments.ts
  </action>
  <verify>No prisma imports in lib/domain/accommodation/</verify>
  <done>Accommodation domain updated</done>
</task>

<task type="auto">
  <name>Update integrations</name>
  <files>lib/integrations/**/*.ts</files>
  <action>
    Update integrations to call Convex instead of Prisma:
    - lib/integrations/ticket-tailor/sync.ts
    - lib/integrations/tikkie/webhook.ts
    
    These adapters stay the same API-wise - just persist to Convex instead.
  </action>
  <verify>Integrations use Convex</verify>
  <done>Integrations updated</done>
</task>

</tasks>

<success_criteria>

- All domain files use Convex functions
- Integrations persist to Convex
- No Prisma dependencies in domain layer
  </success_criteria>
