---
phase: 11-use-convex
plan: "03"
type: execute
wave: 1
depends_on:
  - "11-02"
files_modified:
  - convex/functions/*.ts
autonomous: true
must_haves:
  truths:
    - "Convex functions replace Prisma queries"
    - "All CRUD operations available"
  artifacts:
    - path: "convex/functions/orders.ts"
      provides: "Order queries and mutations"
    - path: "convex/functions/attendees.ts"
      provides: "Attendee queries and mutations"
key_links: []
---

<objective>
Create Convex functions for data operations.
</objective>

<tasks>

<task type="auto">
  <name>Create orders functions</name>
  <files>convex/functions/orders.ts</files>
  <action>
    Create functions:
    - getOrders - list orders with filters
    - getOrderById - single order
    - getOrderLedger - orders with attendees
  </action>
  <verify>convex/functions/orders.ts exists</verify>
  <done>Orders functions ready</done>
</task>

<task type="auto">
  <name>Create attendees functions</name>
  <files>convex/functions/attendees.ts</files>
  <action>
    Create functions:
    - getAttendees - list with filters
    - getAttendeeById - single attendee
    - updateAttendee - update fields
  </action>
  <verify>convex/functions/attendees.ts exists</verify>
  <done>Attendees functions ready</done>
</task>

<task type="auto">
  <name>Create events functions</name>
  <files>convex/functions/events.ts</files>
  <action>
    Create functions:
    - getEvents
    - getEventById
  </action>
  <verify>convex/functions/events.ts exists</verify>
  <done>Events functions ready</done>
</task>

<task type="auto">
  <name>Create accommodation functions</name>
  <files>convex/functions/accommodation.ts</files>
  <action>
    Create functions:
    - getHotels
    - getRooms
    - assignRoom
    - unassignRoom
  </action>
  <verify>convex/functions/accommodation.ts exists</verify>
  <done>Accommodation functions ready</done>
</task>

<task type="auto">
  <name>Create payments functions</name>
  <files>convex/functions/payments.ts</files>
  <action>
    Create functions:
    - getPayments - list with filters
    - createPayment
    - assignPaymentToOrder
    - syncTikkiePayments
    - autoMatchPayments
  </action>
  <verify>convex/functions/payments.ts exists</verify>
  <done>Payments functions ready</done>
</task>

<task type="auto">
  <name>Create tikkie functions</name>
  <files>convex/functions/tikkie.ts</files>
  <action>
    Create functions:
    - createPaymentLink
    - getPaymentLinks
    - refreshPaymentStatus
  </action>
  <verify>convex/functions/tikkie.ts exists</verify>
  <done>Tikkie functions ready</done>
</task>

<task type="auto">
  <name>Create sync functions</name>
  <files>convex/functions/sync.ts</files>
  <action>
    Create functions:
    - runTicketTailorSync
    - getSyncStatus
  </action>
  <verify>convex/functions/sync.ts exists</verify>
  <done>Sync functions ready</done>
</task>

</tasks>

<success_criteria>

- All domain operations available via Convex functions
- CRUD for orders, attendees, events, accommodation, payments, tikkie, sync
  </success_criteria>
