---
phase: 11-use-convex
plan: "02"
type: execute
wave: 1
depends_on:
  - "11-01"
files_modified:
  - convex/schema.ts
autonomous: true
must_haves:
  truths:
    - "All Prisma models converted to Convex schema"
    - "Proper indexes defined"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Database schema replacing Prisma"
key_links: []
---

<objective>
Convert Prisma schema to Better Convex schema.
</objective>

<tasks>

<task type="auto">
  <name>Create Convex schema</name>
  <files>convex/schema.ts</files>
  <action>
    Create convex/schema.ts with all tables:
    
    Import from better-convex/orm: defineSchema, defineRelations, convexTable, text, boolean, id, index, etc.
    
    Convert models from prisma/schema.prisma:
    - ticketTailorWebhookEvents
    - ticketTailorEvents
    - ticketTailorOrders
    - ticketTailorAttendees
    - accommodationHotels
    - accommodationEventHotels
    - tikkiePaymentTemplates
    - accommodationRoomTypes
    - accommodationRooms
    - tikkiePaymentLinks
    - tikkiePaymentLinkTransitions
    - ticketTailorSyncRuns
    - attendeeFamilyGroups
    - attendeeFamilyMembers
    - payments
    - roomAllocations
    
    Add indexes for common queries (by_eventId, by_status, by_email, etc.)
  </action>
  <verify>convex/schema.ts defines all tables</verify>
  <done>Schema created</done>
</task>

<task type="auto">
  <name>Create cRPC builder</name>
  <files>convex/lib/crpc.ts</files>
  <action>
    Create convex/lib/crpc.ts:
    ```typescript
    import { initCRPC } from '../functions/generated/server';
    const c = initCRPC.create();
    export const publicQuery = c.query;
    export const publicMutation = c.mutation;
    ```
  </action>
  <verify>convex/lib/crpc.ts exists</verify>
  <done>cRPC builder ready</done>
</task>

</tasks>

<success_criteria>

- All tables defined in Convex schema
- Indexes for common queries
- cRPC builder configured
  </success_criteria>
