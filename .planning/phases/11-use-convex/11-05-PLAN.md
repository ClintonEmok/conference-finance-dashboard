---
phase: 11-use-convex
plan: "05"
type: execute
wave: 2
depends_on:
  - "11-04"
files_modified:
  - app/api/**/*.ts
  - .env
autonomous: false
must_haves:
  truths:
    - "API routes use Convex functions"
    - "App builds without errors"
    - "Auth works"
  artifacts:
    - path: "app/api/**/*.ts"
      provides: "Routes using Convex"
key_links: []
---

<objective>
Update API routes, verify end-to-end, and deploy.
</objective>

<tasks>

<task type="auto">
  <name>Update API routes</name>
  <files>app/api/**/*.ts</files>
  <action>
    Update API routes to call Convex functions:
    - Remove direct Prisma imports
    - Import and call Convex functions
    - Keep auth checks (use Convex Auth)
  </action>
  <verify>API routes return data from Convex</verify>
  <done>API routes updated</done>
</task>

<task type="auto">
  <name>Add env vars</name>
  <files>.env, .env.example</files>
  <action>
    Add to .env:
    - NEXT_PUBLIC_CONVEX_URL
    - NEXT_PUBLIC_CONVEX_SITE_URL
    
    Update .env.example
  </action>
  <verify>Env vars configured</verify>
  <done>Environment ready</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what>Verify end-to-end</what>
  <how>
    1. Run `npm run build` - verify no errors
    2. Start dev server
    3. Test: login, view orders, view attendees
    4. Test: Tikkie integration
  </how>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Deploy to production</name>
  <files>convex/</files>
  <action>
    Deploy:
    1. Run `npx convex deploy`
    2. Update env vars for production
    3. Deploy Next.js app
  </action>
  <verify>Production deployed</verify>
  <done>Deployed</done>
</task>

</tasks>

<success_criteria>

- App builds without errors
- All features work with Convex backend
- Production deployment successful
  </success_criteria>
