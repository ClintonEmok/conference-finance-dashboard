---
phase: 22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/signup/state.ts
  - components/signup/SignupFlowShell.tsx
  - components/signup/steps/AttendeeDetailsStep.tsx
autonomous: true

must_haves:
  truths:
    - Signup flow order is tickets → attendees → rooms → review (not tickets → rooms → attendees → review)
    - Attendee details step includes location (city/region) field
    - Step validation and navigation work correctly with new order
    - Ticket quantity changes preserve existing attendee data intelligently
  artifacts:
    - path: components/signup/state.ts
      provides: SIGNUP_STEP_ORDER array reordered
      contains: "attendees" before "rooms" in step order
    - path: components/signup/state.ts
      provides: AttendeeDraft with location field
      contains: location: string
    - path: components/signup/SignupFlowShell.tsx
      provides: Updated step navigation and validation logic
      min_lines: 540
    - path: components/signup/steps/AttendeeDetailsStep.tsx
      provides: Location input field in attendee form
      contains: location input or city input
  key_links:
    - from: SignupFlowShell.tsx
      to: state.ts
      via: imports SIGNUP_STEP_ORDER
      pattern: import.*SIGNUP_STEP_ORDER
    - from: SignupFlowShell.tsx
      to: AttendeeDetailsStep.tsx
      via: renders step component
      pattern: step === \"attendees\"
    - from: state.ts
      to: deriveAttendeeDraftsFromTicketSelections
      via: preserves attendee data on ticket changes
      pattern: previousByKey.get\(attendeeKey\)
---

<objective>
Reorder the signup flow so attendees enter details before room allocation, and add the new "location" field to attendee details.

Purpose: This is the foundational change for Phase 22. By collecting attendee information (names, gender, location) before room allocation, families can make informed grouping decisions. The location field enables operator location-based allocation strategies.
Output: Updated state management with new step order and extended attendee draft structure.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui/22-CONTEXT.md
@components/signup/state.ts
@components/signup/SignupFlowShell.tsx
@components/signup/steps/AttendeeDetailsStep.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder SIGNUP_STEP_ORDER constant</name>
  <files>components/signup/state.ts</files>
  <read_first>
    - components/signup/state.ts
  </read_first>
  <action>
    Change SIGNUP_STEP_ORDER from ["tickets", "rooms", "attendees", "review"] to ["tickets", "attendees", "rooms", "review"].

    Also update the SignupStep type which is derived from this array.

    Update the SIGNUP_STEP_ORDER constant declaration to:
    ```typescript
    export const SIGNUP_STEP_ORDER = [
      "tickets",
      "attendees",
      "rooms",
      "review",
    ] as const
    ```

  </action>
  <verify>
    grep -q 'export const SIGNUP_STEP_ORDER = \[' components/signup/state.ts && \
    grep -A5 'SIGNUP_STEP_ORDER' components/signup/state.ts | grep -q '"attendees"' && \
    grep -A5 'SIGNUP_STEP_ORDER' components/signup/state.ts | grep -q '"rooms"' && \
    echo "Step order verified"
  </verify>
  <done>
    SIGNUP_STEP_ORDER contains "attendees" at index 1 and "rooms" at index 2 (was reversed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add location field to AttendeeDraft type</name>
  <files>components/signup/state.ts</files>
  <read_first>
    - components/signup/state.ts
  </read_first>
  <action>
    Add "location" field to the AttendeeDraft type definition. The field should be a string (city/region) and should be included in the attendee draft structure.

    Update the AttendeeDraft type to include:
    ```typescript
    export type AttendeeDraft = {
      attendeeKey: string
      ticketTypeId: string
      ticketLabel: string
      name: string
      email: string
      phone: string
      gender: SignupGender | ""
      location: string
      dietaryRestrictions: string
      roommatePreference: string
      roommateAvoid: string
    }
    ```

    Also update deriveAttendeeDraftsFromTicketSelections to include empty location string for new attendees:
    ```typescript
    next.push(
      previous ?? {
        attendeeKey,
        ticketTypeId: ticket.ticketTypeId,
        ticketLabel: ticket.label,
        name: "",
        email: "",
        phone: "",
        gender: "",
        location: "",
        dietaryRestrictions: "",
        roommatePreference: "",
        roommateAvoid: "",
      }
    )
    ```

  </action>
  <verify>
    grep -q 'location: string' components/signup/state.ts && \
    grep 'deriveAttendeeDraftsFromTicketSelections' -A20 components/signup/state.ts | grep -q 'location: ""' && \
    echo "Location field added to AttendeeDraft"
  </verify>
  <done>
    AttendeeDraft type includes location: string field, and new attendees are initialized with empty location string
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SignupFlowShell for new step order and validation</name>
  <files>components/signup/SignupFlowShell.tsx</files>
  <read_first>
    - components/signup/SignupFlowShell.tsx
    - components/signup/state.ts
  </read_first>
  <action>
    Update SignupFlowShell.tsx to handle the reordered steps correctly:

    1. Update the completedByStep logic in useMemo to reflect new step order:
       - "attendees" should now be validated before "rooms"
       - The rooms completion check should still check acknowledgeRandomFill and unfilledBeds
       - Make sure completedByStep.rooms checks event.accommodation.eligible

    2. Update the stepTitle mapping for the display:
       - "attendees" should show "Attendee details"
       - "rooms" should show "Rooms"

    3. Update moveToStep and moveNext functions:
       - When moving FROM "attendees" step TO next, validate attendee details
       - When moving TO "rooms" step, ensure attendees are valid
       - Keep the attendee validation check before proceeding to rooms

    4. Update the step rendering section:
       - The order in the JSX should match SIGNUP_STEP_ORDER
       - Keep the conditional rendering: activeDraft.step === "attendees" etc.

    5. Update step navigation warnings:
       - When navigating back FROM "rooms" TO "attendees", no warning needed (context D-14)
       - When changing ticket quantities and attendees exist, preserve attendee data (context D-15)

    Key changes:
    - Ensure attendee validation happens when leaving attendees step
    - Ensure room assignment step can only be accessed after valid attendees
    - Ensure review step requires both valid attendees AND room completion

  </action>
  <verify>
    grep -q 'attendees.*attendeeValidationSnapshot.isValid' components/signup/SignupFlowShell.tsx && \
    grep -q 'rooms.*roomSummary.unfilledBeds' components/signup/SignupFlowShell.tsx && \
    grep -q 'step === "attendees"' components/signup/SignupFlowShell.tsx && \
    grep -q 'step === "rooms"' components/signup/SignupFlowShell.tsx && \
    echo "Step flow validation logic updated"
  </verify>
  <done>
    SignupFlowShell correctly validates attendee details before allowing rooms step, room assignments before review step, and all step navigation respects new order
  </done>
</task>

<task type="auto">
  <name>Task 4: Add location field to AttendeeDetailsStep form</name>
  <files>components/signup/steps/AttendeeDetailsStep.tsx</files>
  <read_first>
    - components/signup/steps/AttendeeDetailsStep.tsx
    - components/signup/state.ts
  </read_first>
  <action>
    Add a location (city/region) input field to the AttendeeDetailsStep form.

    The field should:
    - Use a standard text input
    - Have label "Location (city/region)"
    - Be included in the validation logic
    - Store value in attendee.location
    - Follow the same pattern as other fields (name, email, phone, etc.)

    If the AttendeeDetailsStep doesn't already have validation for required fields, add validation for the location field as well.

    Update the validation summary to include location field checks.

    Example structure (adapt to existing code):
    ```tsx
    <div>
      <Label htmlFor={`${attendee.attendeeKey}-location`}>
        Location (city/region)
      </Label>
      <Input
        id={`${attendee.attendeeKey}-location`}
        value={attendee.location}
        onChange={(e) => onAttendeeChange(attendee.attendeeKey, "location", e.target.value)}
        placeholder="e.g., Amsterdam"
      />
    </div>
    ```

  </action>
  <verify>
    grep -q 'location' components/signup/steps/AttendeeDetailsStep.tsx && \
    grep -q 'Location' components/signup/steps/AttendeeDetailsStep.tsx && \
    grep -q 'city/region\|city' components/signup/steps/AttendeeDetailsStep.tsx && \
    echo "Location field added to AttendeeDetailsStep"
  </verify>
  <done>
    AttendeeDetailsStep includes location input field with proper label, state binding, and validation
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Signup flow renders without errors
3. Step navigation works: can move tickets → attendees → rooms → review
4. Location field appears and accepts input
5. Attendee validation includes location field check
6. Ticket quantity changes preserve existing attendee data
</verification>

<success_criteria>

- [ ] SIGNUP_STEP_ORDER is ["tickets", "attendees", "rooms", "review"]
- [ ] AttendeeDraft type includes location: string field
- [ ] New attendees are initialized with empty location string
- [ ] AttendeeDetailsStep renders location input field
- [ ] SignupFlowShell validates attendees before allowing rooms step
- [ ] Step navigation respects the new order
- [ ] TypeScript compiles without errors
      </success_criteria>

<output>
After completion, create `.planning/phases/22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui/22-01-SUMMARY.md`
</output>
