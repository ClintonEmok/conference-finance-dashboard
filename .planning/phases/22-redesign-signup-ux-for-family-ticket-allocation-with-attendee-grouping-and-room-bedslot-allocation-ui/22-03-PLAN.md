---
phase: 22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui
plan: 03
type: execute
wave: 2
depends_on: [22-01, 22-02]
files_modified:
  - components/signup/steps/ReviewSubmitStep.tsx
  - components/signup/assignment.ts
  - components/signup/ReviewSection.tsx
autonomous: true

must_haves:
  truths:
    - Review step uses expandable sections for tickets, attendees, and allocations
    - Allocation summary shows who is in which room, grouped by room
    - Users can expand/collapse each section to review details
    - Submission validation ensures all required fields are complete
    - Final review clearly shows all selections before submit
  artifacts:
    - path: components/signup/steps/ReviewSubmitStep.tsx
      provides: Restructured review step with expandable sections
      min_lines: 250
    - path: components/signup/assignment.ts
      provides: buildAllocationSummary function
      exports: [buildAllocationSummary]
    - path: components/signup/ReviewSection.tsx
      provides: Reusable expandable section component
  key_links:
    - from: ReviewSubmitStep.tsx
      to: assignment.ts
      via: imports buildAllocationSummary
      pattern: import.*buildAllocationSummary
    - from: ReviewSubmitStep.tsx
      to: draft state
      via: displays ticketSelections, attendees, assignments
      pattern: draft\.(ticketSelections|attendees|assignments)
---

<objective>
Restructure the review and submit step with expandable sections showing tickets, attendee details, and room allocation summary grouped by room.

Purpose: The final review step needs to give users a clear overview of their entire booking before submission. Expandable sections prevent information overload while allowing detailed review. The allocation summary (per context D-12) shows who is in which room.
Output: Redesigned ReviewSubmitStep with collapsible sections and room-based allocation summary.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui/22-CONTEXT.md
@components/signup/steps/ReviewSubmitStep.tsx
@components/signup/assignment.ts
@components/signup/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add allocation summary builder to assignment.ts</name>
  <files>components/signup/assignment.ts</files>
  <read_first>
    - components/signup/assignment.ts
    - components/signup/state.ts
  </read_first>
  <action>
    Add a buildAllocationSummary function to components/signup/assignment.ts that creates a room-based summary of allocations. The function should:
    - Take AssignmentBoard, AttendeeDraft array, and slots array as input
    - Group assignments by room label
    - Calculate filled/unfilled beds per room
    - Track unassigned attendees
    - Return a structured AllocationSummary type

    Also add the AllocationSummary type with:
    - totalAttendees, totalRooms counts
    - Array of rooms with roomLabel, roomTypeLabel, occupants array, unfilledBeds
    - Array of unassignedAttendees with key, name, ticketLabel

    The occupants in each room should include attendeeKey, name, ticketLabel, gender, and location.

  </action>
  <verify>
    grep -q 'buildAllocationSummary' components/signup/assignment.ts && \
    grep -q 'AllocationSummary' components/signup/assignment.ts && \
    grep -q 'unassignedAttendees' components/signup/assignment.ts && \
    echo "Allocation summary builder added"
  </verify>
  <done>
    assignment.ts exports buildAllocationSummary and AllocationSummary type with room-based grouping and unassigned attendee tracking
  </done>
</task>

<task type="auto">
  <name>Task 2: Create expandable section components</name>
  <files>components/signup/ReviewSection.tsx</files>
  <read_first>
    - components/signup/steps/ReviewSubmitStep.tsx
  </read_first>
  <action>
    Create a reusable ReviewSection component for the expandable review sections.

    The component should:
    - Accept title, optional subtitle, optional badge, defaultExpanded boolean, and children
    - Use Card, CardHeader, CardContent from shadcn/ui
    - Have a clickable header that toggles expansion
    - Show ChevronUp/ChevronDown icon based on expanded state
    - Render children in CardContent only when expanded
    - Badge should show as a rounded pill with primary color styling

    Use lucide-react for ChevronDown and ChevronUp icons.

  </action>
  <verify>
    test -f components/signup/ReviewSection.tsx && \
    grep -q 'ReviewSection' components/signup/ReviewSection.tsx && \
    grep -q 'isExpanded' components/signup/ReviewSection.tsx && \
    grep -q 'ChevronDown\|ChevronUp' components/signup/ReviewSection.tsx && \
    echo "ReviewSection component created"
  </verify>
  <done>
    ReviewSection.tsx exists with expandable card UI, title, optional badge, and subtitle
  </done>
</task>

<task type="auto">
  <name>Task 3: Redesign ReviewSubmitStep with expandable sections</name>
  <files>components/signup/steps/ReviewSubmitStep.tsx</files>
  <read_first>
    - components/signup/steps/ReviewSubmitStep.tsx
    - components/signup/assignment.ts
    - components/signup/ReviewSection.tsx
    - components/signup/state.ts
  </read_first>
  <action>
    Completely redesign components/signup/steps/ReviewSubmitStep.tsx with expandable sections:

    1. Import new dependencies: ReviewSection, buildAllocationSummary, AllocationSummary

    2. Add event prop to receive accommodation slots data

    3. Create three expandable sections using ReviewSection:
       - Tickets: shows selected tickets with quantities and prices, badge shows ticket count
       - Attendee Details: shows all attendee info (name, email, phone, gender, location, dietary, roommate pref), badge shows attendee count
       - Room Allocations: shows allocation summary grouped by room with occupants and unfilled beds warning, badge shows room count

    4. Use useMemo to compute allocationSummary from buildAllocationSummary

    5. Tickets section should calculate total price from priceMinor times quantity

    6. Room Allocations section should:
       - Show each room with label and type
       - List occupants with bullet points
       - Show unfilled beds count with amber warning styling
       - Show unassigned attendees in amber warning box if any

    7. Preserve existing submit functionality, error handling, and restore choice UI

    8. Default Tickets and Room Allocations to expanded, Attendee Details to collapsed

  </action>
  <verify>
    grep -q 'ReviewSection' components/signup/steps/ReviewSubmitStep.tsx && \
    grep -q 'buildAllocationSummary' components/signup/steps/ReviewSubmitStep.tsx && \
    grep -q 'Tickets' components/signup/steps/ReviewSubmitStep.tsx && \
    grep -q 'Attendee Details' components/signup/steps/ReviewSubmitStep.tsx && \
    grep -q 'Room Allocations' components/signup/steps/ReviewSubmitStep.tsx && \
    grep -q 'unfilledBeds' components/signup/steps/ReviewSubmitStep.tsx && \
    echo "ReviewSubmitStep redesigned with expandable sections"
  </verify>
  <done>
    ReviewSubmitStep has three expandable sections (Tickets, Attendee Details, Room Allocations), shows allocation summary grouped by room with unfilled beds warning, and preserves submit functionality
  </done>
</task>

<task type="auto">
  <name>Task 4: Update SignupFlowShell to pass event to ReviewSubmitStep</name>
  <files>components/signup/SignupFlowShell.tsx</files>
  <read_first>
    - components/signup/SignupFlowShell.tsx
    - components/signup/steps/ReviewSubmitStep.tsx
  </read_first>
  <action>
    Update the ReviewSubmitStep rendering in SignupFlowShell to pass the event data needed for allocation summary:

    Add event={activeEvent} prop to the ReviewSubmitStep component call.

    This enables ReviewSubmitStep to access event.accommodation.slots for building the allocation summary.

  </action>
  <verify>
    grep -A12 'step === "review"' components/signup/SignupFlowShell.tsx | grep -q 'event={activeEvent}' && \
    echo "SignupFlowShell passes event to ReviewSubmitStep"
  </verify>
  <done>
    SignupFlowShell passes activeEvent to ReviewSubmitStep for accommodation slot data access
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: npx tsc --noEmit
2. Review step renders with three expandable sections:
   - Tickets section shows selected tickets and total
   - Attendee Details section shows all attendee info
   - Room Allocations section shows who is in which room
3. Sections can be expanded and collapsed
4. Room allocation summary correctly groups attendees by room
5. Unfilled beds are displayed with warning text
6. Unassigned attendees are highlighted if any
7. Submit functionality still works
</verification>

<success_criteria>

- [ ] assignment.ts exports buildAllocationSummary and AllocationSummary type
- [ ] ReviewSection.tsx component created with expandable card UI
- [ ] ReviewSubmitStep has Tickets, Attendee Details, Room Allocations sections
- [ ] Sections are expandable/collapsible
- [ ] Room allocations grouped by room with occupant names
- [ ] Unfilled beds shown with warning in allocation summary
- [ ] Unassigned attendees highlighted if applicable
- [ ] SignupFlowShell passes event data to ReviewSubmitStep
- [ ] TypeScript compiles without errors
      </success_criteria>

<output>
After completion, create .planning/phases/22-redesign-signup-ux-for-family-ticket-allocation-with-attendee-grouping-and-room-bedslot-allocation-ui/22-03-SUMMARY.md
</output>
