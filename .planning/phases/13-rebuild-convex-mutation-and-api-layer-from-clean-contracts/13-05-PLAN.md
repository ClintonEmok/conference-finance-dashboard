---
phase: 13-rebuild-convex-mutation-and-api-layer-from-clean-contracts
plan: "05"
type: execute
wave: 3
depends_on: ["13-02", "13-03", "13-04"]
files_modified:
  - convex/sync.ts
  - lib/integrations/ticket-tailor/client.ts
  - lib/integrations/ticket-tailor/sync.ts
  - lib/integrations/ticket-tailor/webhook.ts
  - app/api/ticket-tailor/sync/route.ts
  - app/api/ticket-tailor/webhook-events/route.ts
  - app/api/jobs/ticket-tailor/retry/route.ts
  - app/api/webhooks/ticket-tailor/route.ts
  - lib/convex/server.ts
  - tests/ticket-tailor/client.test.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
must_haves:
  truths:
    - "Ticket Tailor sync/webhook flows use clean typed Convex contracts instead of migration-era string helpers."
    - "No legacy nested-generated imports or raw Convex string dispatch remain in app/lib/convex code."
    - "The full app still passes build, typecheck, and targeted regression tests after the cleanup."
  artifacts:
    - path: "convex/sync.ts"
      provides: "Clean public/internal sync contract surface"
      contains: "internalMutation"
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Typed Ticket Tailor sync boundary"
      contains: "api.sync"
    - path: "lib/convex/server.ts"
      provides: "Final server bridge without legacy raw-string dispatch"
      contains: "runConvex"
  key_links:
    - from: "lib/integrations/ticket-tailor/webhook.ts"
      to: "convex/sync.ts"
      via: "typed sync refs"
      pattern: "api\.sync"
    - from: "app/api/webhooks/ticket-tailor/route.ts"
      to: "lib/integrations/ticket-tailor/webhook.ts"
      via: "public webhook route"
      pattern: "handle"
---

<objective>
Finish the Convex contract rebuild by cleaning up sync/webhook flows, removing legacy string-dispatch support, and running full regression checks.

Purpose: The phase is not complete until the remaining Ticket Tailor sync boundary is rebuilt and the temporary migration shims from Plan 01 are removed.
Output: Refactored `convex/sync.ts`, typed integration/webhook callers, no legacy string dispatch left in app/lib/convex code, and full regression verification.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md
@.planning/phases/04-attendee-data-accommodation-foundations/04-01-SUMMARY.md
@.planning/phases/11-use-convex/11-04-SUMMARY.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/sync.ts
@lib/integrations/ticket-tailor/client.ts
@lib/integrations/ticket-tailor/sync.ts
@lib/integrations/ticket-tailor/webhook.ts
@lib/convex/server.ts
@tests/ticket-tailor/client.test.ts
@tests/ticket-tailor/sync-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Ticket Tailor sync and webhook boundaries on typed Convex contracts</name>
  <read_first>convex/sync.ts, lib/integrations/ticket-tailor/client.ts, lib/integrations/ticket-tailor/sync.ts, lib/integrations/ticket-tailor/webhook.ts, app/api/ticket-tailor/sync/route.ts, app/api/ticket-tailor/webhook-events/route.ts, app/api/jobs/ticket-tailor/retry/route.ts, app/api/webhooks/ticket-tailor/route.ts, tests/ticket-tailor/client.test.ts, tests/ticket-tailor/sync-route.test.ts, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md</read_first>
  <files>convex/sync.ts, lib/integrations/ticket-tailor/client.ts, lib/integrations/ticket-tailor/sync.ts, lib/integrations/ticket-tailor/webhook.ts, app/api/ticket-tailor/sync/route.ts, app/api/ticket-tailor/webhook-events/route.ts, app/api/jobs/ticket-tailor/retry/route.ts, app/api/webhooks/ticket-tailor/route.ts, tests/ticket-tailor/client.test.ts, tests/ticket-tailor/sync-route.test.ts</files>
  <action>Refactor `convex/sync.ts` into a clean contract surface: keep only true app-facing/public sync entrypoints public, and move low-level order/event/attendee upserts, retry selection, and webhook persistence helpers behind internal functions or plain helpers. Update `lib/integrations/ticket-tailor/sync.ts` and `lib/integrations/ticket-tailor/webhook.ts` to call typed `api.sync.*` refs through the shared server bridge instead of raw string dispatch. Preserve the current sync summary fields (`eventsScanned`, `ordersFetched`, `ordersUpserted`, `attendeesFetched`, `attendeesUpserted`, `normalizedFallbackCount`, `failedItems`) and preserve the current protected/public route boundaries for manual sync, retry jobs, webhook-event inspection, and public webhook ingestion. Update the existing Ticket Tailor tests to reflect the cleaned-up contract surface without weakening coverage.</action>
  <acceptance_criteria>`convex/sync.ts` contains `internalMutation` or `internalQuery`; `lib/integrations/ticket-tailor/sync.ts` contains `api.sync`; `lib/integrations/ticket-tailor/webhook.ts` contains `api.sync`; `rg '"sync:' lib/integrations/ticket-tailor app/api/ticket-tailor app/api/jobs/ticket-tailor app/api/webhooks/ticket-tailor` returns no matches; `npm test -- tests/ticket-tailor/client.test.ts tests/ticket-tailor/sync-route.test.ts` exits 0.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `npm test -- tests/ticket-tailor/client.test.ts tests/ticket-tailor/sync-route.test.ts`; run the raw-string grep from acceptance criteria.</verify>
  <done>The Ticket Tailor integration boundary uses typed sync contracts end-to-end and keeps its existing operator/webhook payload shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy string-dispatch support and run full regression</name>
  <read_first>lib/convex/server.ts, lib/convex/api.ts, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-VALIDATION.md, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md</read_first>
  <files>lib/convex/server.ts, lib/convex/api.ts</files>
  <action>After Plans 02-04 have migrated all callers, remove the remaining migration-only legacy support from `lib/convex/server.ts`. The final shared bridge must no longer accept raw string paths such as `"orders:searchOrders"` or slash-path variants; it should only accept generated query/mutation refs. Delete any leftover compatibility mapping, remove dead helper code such as path-normalization logic, and make the final grep-cleanup part of the task. Do not reintroduce `any` casts or nested generated imports during cleanup.</action>
  <acceptance_criteria>`lib/convex/server.ts` does not contain `toConvexFunctionPath`; `rg 'convexQuery\(\"|convexMutation\(\"|@/convex/functions/_generated|as any' lib app convex` returns no matches; `npx convex codegen` exits 0; `npm run build` exits 0; `npm run typecheck` exits 0; `npm test` exits 0.</acceptance_criteria>
  <verify>Run `npx convex codegen`; run `npm run build`; run `npm run typecheck`; run `npm test`; run `rg 'convexQuery\(\"|convexMutation\(\"|@/convex/functions/_generated|as any' lib app convex`.</verify>
  <done>The repo no longer depends on migration-era raw Convex path strings or nested generated imports, and the full app regression suite passes.</done>
</task>

</tasks>

<verification>
- Run `npx convex codegen`.
- Run `npm run build`.
- Run `npm run typecheck`.
- Run `npm test`.
- Confirm repo-wide grep finds no remaining raw Convex string dispatch or nested generated imports.
</verification>

<success_criteria>

- Sync/webhook flows use typed Convex refs.
- `lib/convex/server.ts` no longer supports raw string-path dispatch.
- Full build, typecheck, and test suite pass after the cleanup.
  </success_criteria>

<output>
After completion, create `.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-05-SUMMARY.md`
</output>
