---
phase: 13-rebuild-convex-mutation-and-api-layer-from-clean-contracts
plan: "03"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - convex/attendees.ts
  - convex/accommodation.ts
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - lib/domain/accommodation/inventory.ts
  - lib/domain/accommodation/assignments.ts
  - app/api/dashboard/attendees/route.ts
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - app/api/dashboard/accommodation/inventory/route.ts
  - app/api/dashboard/accommodation/hotels/route.ts
  - app/api/dashboard/accommodation/room-types/route.ts
  - app/api/dashboard/accommodation/rooms/route.ts
  - app/api/dashboard/accommodation/rooms/[roomId]/route.ts
  - app/api/dashboard/accommodation/event-hotels/route.ts
  - app/api/dashboard/accommodation/assignments/route.ts
  - app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts
  - app/api/dashboard/accommodation/auto-allocate/route.ts
autonomous: true
must_haves:
  truths:
    - "Attendee and accommodation routes use typed Convex contracts instead of migration-era string helpers."
    - "Assignment and inventory write helpers are not all exposed as broad public mutations."
    - "Attendee detail and accommodation board payloads keep the current operator-facing fields and enums."
  artifacts:
    - path: "convex/attendees.ts"
      provides: "Focused attendee contracts with proper Convex ids"
      contains: "v.id(\"ticketTailorAttendees\")"
    - path: "convex/accommodation.ts"
      provides: "Clean accommodation public/internal contract split"
      contains: "internalMutation"
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Typed app-facing accommodation assignment boundary"
      contains: "api.accommodation"
  key_links:
    - from: "lib/domain/finance/attendee-detail.ts"
      to: "convex/attendees.ts"
      via: "typed query refs"
      pattern: "api\.attendees"
    - from: "app/api/dashboard/accommodation/assignments/route.ts"
      to: "lib/domain/accommodation/assignments.ts"
      via: "protected route handler"
      pattern: "requireApiUser"
---

<objective>
Rebuild attendee and accommodation contracts so the operator-facing room/allocation flows rest on focused typed Convex APIs.

Purpose: The attendee and accommodation modules currently mix filtering, board projection, and mutation plumbing in oversized public files. This plan turns them into clean contracts without changing the dashboard workflow.
Output: Refactored `convex/attendees.ts` and `convex/accommodation.ts` plus updated attendee/accommodation domain modules and protected routes using typed refs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-CONTEXT.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md
@.planning/phases/05-room-allocation-operator-flow-polish/05-01-SUMMARY.md
@.planning/phases/11-use-convex/11-04-SUMMARY.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/attendees.ts
@convex/accommodation.ts
@lib/domain/finance/attendee-detail.ts
@lib/domain/accommodation/inventory.ts
@lib/domain/accommodation/assignments.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor attendee and accommodation Convex modules into focused public/internal contracts</name>
  <read_first>convex/attendees.ts, convex/accommodation.ts, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md, .planning/phases/05-room-allocation-operator-flow-polish/05-01-SUMMARY.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex/attendees.ts, convex/accommodation.ts</files>
  <action>Refactor `convex/attendees.ts` and `convex/accommodation.ts` so the public surface matches the operator flows that actually exist: attendee ledger/detail reads, accommodation inventory/board reads, assign/unassign/update entrypoints, and any auto-allocation trigger that is already part of the dashboard. Move low-level occupancy recalculation, shared lookup helpers, and fan-out write plumbing behind plain helpers or `internalMutation` / `internalQuery` exports. Replace `as any` id usage with proper `Id<...>` / `v.id(...)` validators wherever the table is known. Remove hot-path `.collect()` + `.filter()` patterns for attendee lookup, room lookup, event-hotel lookup, and occupancy checks where indexes can express the read. Preserve the current availability enum values (`empty`, `available`, `full`), attendee room-state fields, and over-capacity protections. Do not use this cleanup to rename or simplify the route-facing board/detail payloads.</action>
  <acceptance_criteria>`convex/attendees.ts` contains `v.id("ticketTailorAttendees")`; `convex/accommodation.ts` contains `internalMutation` or `internalQuery`; `convex/accommodation.ts` contains `withIndex`; `rg "as any" convex/attendees.ts convex/accommodation.ts` returns no matches; the assign/unassign path still includes explicit capacity or occupancy guard logic.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "internalMutation|internalQuery|withIndex|v.id\(" convex/attendees.ts convex/accommodation.ts`; run `rg "as any" convex/attendees.ts convex/accommodation.ts`.</verify>
  <done>The attendee/accommodation Convex layer exposes focused public contracts, hides low-level write helpers internally, and uses proper typed ids.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate attendee and accommodation domain/routes to typed Convex refs</name>
  <read_first>lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/accommodation/inventory.ts, lib/domain/accommodation/assignments.ts, app/api/dashboard/attendees/route.ts, app/api/dashboard/attendees/[attendeeId]/route.ts, app/api/dashboard/accommodation/inventory/route.ts, app/api/dashboard/accommodation/assignments/route.ts, app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts, lib/convex/server.ts</read_first>
  <files>lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, lib/domain/accommodation/inventory.ts, lib/domain/accommodation/assignments.ts, app/api/dashboard/attendees/route.ts, app/api/dashboard/attendees/[attendeeId]/route.ts, app/api/dashboard/accommodation/inventory/route.ts, app/api/dashboard/accommodation/hotels/route.ts, app/api/dashboard/accommodation/room-types/route.ts, app/api/dashboard/accommodation/rooms/route.ts, app/api/dashboard/accommodation/rooms/[roomId]/route.ts, app/api/dashboard/accommodation/event-hotels/route.ts, app/api/dashboard/accommodation/assignments/route.ts, app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts, app/api/dashboard/accommodation/auto-allocate/route.ts</files>
  <action>Update the attendee and accommodation app-facing boundaries to call typed generated refs through the shared server bridge. Replace string dispatch such as `"accommodation:createRooms"`, `"accommodation:listAccommodationInventory"`, and attendee string paths with `api.accommodation.*` / `api.attendees.*` refs. Keep the existing `requireApiUser()` guard, the established `BAD_REQUEST` / `UNAUTHORIZED` / `INTERNAL_ERROR` envelopes, and the current operator-facing response shapes for attendee detail, attendee ledger, accommodation inventory, and assignment flows exactly where they already exist. Do not change route URLs or the semantics of full-room rejection, unassign behavior, or auto-allocation output fields.</action>
  <acceptance_criteria>`lib/domain/finance/attendee-detail.ts` contains `api.attendees`; `lib/domain/accommodation/inventory.ts` contains `api.accommodation`; `lib/domain/accommodation/assignments.ts` contains `api.accommodation`; `rg '"attendees:|"accommodation:' lib/domain/finance lib/domain/accommodation app/api/dashboard/attendees app/api/dashboard/accommodation` returns no matches; all listed route files still contain `requireApiUser`.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg '"attendees:|"accommodation:' lib/domain/finance lib/domain/accommodation app/api/dashboard/attendees app/api/dashboard/accommodation`; read the route files to confirm auth/error contracts remain intact.</verify>
  <done>The attendee/accommodation route layer now calls typed Convex refs end-to-end without changing the current dashboard workflow contracts.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm attendee/accommodation callers no longer use raw string dispatch.
- Confirm `convex/attendees.ts` and `convex/accommodation.ts` no longer use `as any` ids.
</verification>

<success_criteria>

- Attendee/accommodation Convex files use focused typed contracts and internal helpers.
- App-facing attendee/accommodation domain files use generated refs.
- Protected routes keep the existing operator JSON contracts.
  </success_criteria>

<output>
After completion, create `.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-03-SUMMARY.md`
</output>
