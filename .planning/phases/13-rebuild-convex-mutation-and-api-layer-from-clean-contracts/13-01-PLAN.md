---
phase: 13-rebuild-convex-mutation-and-api-layer-from-clean-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex.json
  - convex/schema.ts
  - convex/orders.ts
  - convex/attendees.ts
  - convex/events.ts
  - convex/accommodation.ts
  - convex/payments.ts
  - convex/tikkie.ts
  - convex/sync.ts
  - lib/convex/api.ts
  - lib/convex/server.ts
autonomous: true
must_haves:
  truths:
    - "The repo has one canonical Convex app tree and one generated API location."
    - "Server-side Convex calls can use generated refs instead of raw string paths."
    - "Clerk-authenticated server requests reach Convex through one typed bridge."
  artifacts:
    - path: "convex/orders.ts"
      provides: "Canonical top-level Convex module layout"
      contains: 'from "./_generated/server"'
    - path: "lib/convex/api.ts"
      provides: "Typed generated API export"
      contains: "@/convex/_generated/api"
    - path: "lib/convex/server.ts"
      provides: "Typed Clerk-aware server bridge"
      contains: 'getToken({ template: "convex" })'
  key_links:
    - from: "lib/convex/api.ts"
      to: "convex/_generated/api"
      via: "generated function references"
      pattern: "convex/_generated/api"
    - from: "lib/convex/server.ts"
      to: "Clerk"
      via: "convex JWT template"
      pattern: 'template: "convex"'
---

<objective>
Make the Convex project topology and shared server bridge clean before any domain refactor starts.

Purpose: Phase 11 left the repo with duplicated Convex roots, nested generated imports, and string-path server calls. This plan establishes one canonical Convex app tree and one typed server boundary the rest of the phase can build on.
Output: Canonical top-level `convex/*.ts` modules, standardized generated imports, and a Clerk-aware typed server bridge in `lib/convex/server.ts`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-CONTEXT.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md
@.planning/phases/11-use-convex/11-01-SUMMARY.md
@.planning/phases/11-use-convex/11-05-SUMMARY.md
@.planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-01-SUMMARY.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex.json
@convex/schema.ts
@lib/convex/api.ts
@lib/convex/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the Convex app tree and generated import paths</name>
  <read_first>convex.json, convex/schema.ts, convex/functions/schema.ts, convex/functions/orders.ts, convex/functions/attendees.ts, convex/functions/events.ts, convex/functions/accommodation.ts, convex/functions/payments.ts, convex/functions/tikkie.ts, convex/functions/sync.ts, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex.json, convex/schema.ts, convex/orders.ts, convex/attendees.ts, convex/events.ts, convex/accommodation.ts, convex/payments.ts, convex/tikkie.ts, convex/sync.ts, convex/functions/schema.ts, convex/functions/orders.ts, convex/functions/attendees.ts, convex/functions/events.ts, convex/functions/accommodation.ts, convex/functions/payments.ts, convex/functions/tikkie.ts, convex/functions/sync.ts</files>
  <action>Make top-level `convex/` the only canonical Convex functions tree. Move the domain modules from `convex/functions/*.ts` to `convex/*.ts` with the same basenames (`orders.ts`, `attendees.ts`, `events.ts`, `accommodation.ts`, `payments.ts`, `tikkie.ts`, `sync.ts`) so generated refs remain `api.orders.*`, `api.attendees.*`, and so on. Keep `convex/schema.ts` as the sole schema file and remove the duplicate `convex/functions/schema.ts`. Update `convex.json` to target the top-level `convex` directory for codegen, regenerate `convex/_generated/*`, and update each moved file to import from `./_generated/server` / `./_generated/dataModel`. Remove runtime imports that point at `convex/functions/_generated/*`; do not leave both generated locations active. This is topology cleanup only: do not change any existing Next.js route JSON contracts or auth behavior in this plan.</action>
  <acceptance_criteria>`convex.json` contains `"functions": "convex"`; `convex/orders.ts` exists; `convex/attendees.ts` exists; `convex/events.ts` exists; `convex/accommodation.ts` exists; `convex/payments.ts` exists; `convex/tikkie.ts` exists; `convex/sync.ts` exists; `convex/functions/schema.ts` does not exist; `rg "convex/functions/_generated|from \"\./functions/_generated" convex lib app` returns no matches.</acceptance_criteria>
  <verify>Run `npx convex codegen`; run `npm run typecheck`; run `rg "convex/functions/_generated|from \"\./functions/_generated" convex lib app`.</verify>
  <done>The repository has one canonical Convex module tree under `convex/`, and generated refs/types come only from `convex/_generated/*`.</done>
</task>

<task type="auto">
  <name>Task 2: Build a typed Clerk-aware server bridge and remove `any` API access</name>
  <read_first>lib/convex/api.ts, lib/convex/server.ts, lib/convex/client.tsx, .planning/phases/12-use-clerk-as-only-auth-remove-stale-better-auth/12-01-SUMMARY.md, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md</read_first>
  <files>lib/convex/api.ts, lib/convex/server.ts</files>
  <action>Refactor `lib/convex/api.ts` to re-export the generated `api` from `@/convex/_generated/api` without casting it to `any`. Rewrite `lib/convex/server.ts` into the shared server bridge for all Next.js server callers: fetch the Clerk auth token with `auth().getToken({ template: "convex" })`, accept generated query/mutation references as the primary interface, and centralize Convex error handling in one place. Keep migration-safe compatibility by leaving `convexQuery` / `convexMutation` available only if they can delegate through the new typed bridge without raw `/api/query` string-dispatch logic; all new call sites in later plans must use typed function refs, and this file must be ready for final legacy-wrapper removal in Plan 05. Keep auth handling and error propagation compatible with the current route boundary so later plans can preserve existing 400/404/500 behavior exactly.</action>
  <acceptance_criteria>`lib/convex/api.ts` contains `@/convex/_generated/api`; `lib/convex/api.ts` does not contain `as any`; `lib/convex/server.ts` contains `template: "convex"`; `lib/convex/server.ts` contains a typed helper for queries; `lib/convex/server.ts` contains a typed helper for mutations.</acceptance_criteria>
  <verify>Run `npm run typecheck`; read `lib/convex/api.ts` and `lib/convex/server.ts`; confirm no `as any` remains in `lib/convex/api.ts`.</verify>
  <done>Server-side Convex access is centered on a typed Clerk-aware bridge, and generated API refs are no longer erased to `any`.</done>
</task>

</tasks>

<verification>
- Run `npx convex codegen`.
- Run `npm run typecheck`.
- Confirm no imports remain from `convex/functions/_generated/*`.
- Confirm `lib/convex/api.ts` no longer casts the generated API to `any`.
</verification>

<success_criteria>

- `convex/` is the canonical functions tree and `convex/schema.ts` is the only schema source.
- Generated imports resolve from `convex/_generated/*` only.
- `lib/convex/server.ts` is ready to power typed server-side Convex calls with Clerk auth.
  </success_criteria>

<output>
After completion, create `.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-01-SUMMARY.md`
</output>
