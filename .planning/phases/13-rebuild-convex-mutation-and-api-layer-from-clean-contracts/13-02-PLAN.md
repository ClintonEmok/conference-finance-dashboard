---
phase: 13-rebuild-convex-mutation-and-api-layer-from-clean-contracts
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - convex/orders.ts
  - convex/events.ts
  - lib/domain/finance/reporting.ts
  - lib/domain/finance/order-ledger.ts
  - lib/domain/finance/reconciliation.ts
  - app/api/dashboard/revenue/route.ts
  - app/api/dashboard/orders/route.ts
  - app/api/dashboard/orders/export/route.ts
  - app/api/dashboard/orders/[orderId]/route.ts
  - app/api/orders/search/route.ts
  - app/api/reconciliation/route.ts
autonomous: true
must_haves:
  truths:
    - "Order/reporting routes read from bounded typed Convex contracts instead of string-path helpers."
    - "Revenue, reconciliation, and order detail payloads keep their current operator-facing shape."
    - "Hot-path order/reporting queries no longer rely on table-wide scans where indexes or bounded reads fit."
  artifacts:
    - path: "convex/orders.ts"
      provides: "Focused public and internal order/reporting contracts"
      contains: "returns:"
    - path: "lib/domain/finance/order-ledger.ts"
      provides: "App-facing order ledger boundary using typed Convex refs"
      contains: "api.orders"
    - path: "app/api/dashboard/revenue/route.ts"
      provides: "Protected revenue route still serving the established JSON contract"
      contains: "requireApiUser"
  key_links:
    - from: "lib/domain/finance/reporting.ts"
      to: "convex/orders.ts"
      via: "typed query refs"
      pattern: "api\.orders"
    - from: "app/api/orders/search/route.ts"
      to: "lib/convex/server.ts"
      via: "typed server bridge"
      pattern: "runConvex|convexQuery"
---

<objective>
Rebuild the orders/reporting/reconciliation read layer on top of bounded typed Convex contracts.

Purpose: Order search, revenue, and reconciliation are core operator reads. They need clean query contracts before the remaining write-side cleanup can trust the backend boundary.
Output: Refactored `convex/orders.ts` and `convex/events.ts` plus updated finance domain modules and protected routes using typed generated refs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-CONTEXT.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md
@.planning/phases/11-use-convex/11-04-SUMMARY.md
@.planning/phases/11-use-convex/11-05-SUMMARY.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/orders.ts
@convex/events.ts
@lib/domain/finance/reporting.ts
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/reconciliation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refocus order/reporting queries into bounded public contracts and internal helpers</name>
  <read_first>convex/orders.ts, convex/events.ts, .planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-RESEARCH.md, convex/_generated/ai/guidelines.md</read_first>
  <files>convex/orders.ts, convex/events.ts</files>
  <action>Refactor `convex/orders.ts` and `convex/events.ts` so the public surface matches the current app needs instead of exposing broad migration-era helpers. Keep or introduce only the operator-facing public queries the app actually uses for this slice: order search, dashboard order ledger, revenue/reconciliation input projections, and order detail lookup by provider identifiers. Move lower-level aggregation and shared lookups into plain helper functions or `internalQuery` exports where the Next.js layer does not call them directly. Replace table-wide `.collect()` plus in-memory `.filter()` on hot paths with indexed reads and bounded result sets. Where the UI needs pagination or capped lists, enforce explicit bounds (`limit`/`pageSize` or `paginationOptsValidator`) and keep the returned fields aligned with today’s JSON payloads (`providerOrderId`, `eventName`, ISO timestamps, totals, status counts, and reconciliation summary fields). This plan is the highest-priority finance read surface: preserve route-level response shapes exactly. Keep only the diagnostics and extra counters that existing routes or tests already require.</action>
  <acceptance_criteria>`convex/orders.ts` contains `returns:`; `convex/orders.ts` contains `withIndex`; `convex/orders.ts` contains either `paginationOptsValidator` or explicit bounded `limit` logic; `convex/orders.ts` does not contain unbounded `.collect()` on the order search or dashboard ledger path; `convex/events.ts` exposes only event projection helpers still required by reporting/order consumers.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg "withIndex|paginationOptsValidator|returns:" convex/orders.ts convex/events.ts`; read the updated query exports to confirm operator payload fields are preserved.</verify>
  <done>The orders/reporting Convex layer exposes focused, bounded contracts that match the current operator read surface.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate order/reporting domain modules and protected routes to typed refs</name>
  <read_first>lib/domain/finance/reporting.ts, lib/domain/finance/order-ledger.ts, lib/domain/finance/reconciliation.ts, app/api/dashboard/revenue/route.ts, app/api/dashboard/orders/route.ts, app/api/dashboard/orders/export/route.ts, app/api/dashboard/orders/[orderId]/route.ts, app/api/orders/search/route.ts, app/api/reconciliation/route.ts, lib/convex/server.ts</read_first>
  <files>lib/domain/finance/reporting.ts, lib/domain/finance/order-ledger.ts, lib/domain/finance/reconciliation.ts, app/api/dashboard/revenue/route.ts, app/api/dashboard/orders/route.ts, app/api/dashboard/orders/export/route.ts, app/api/dashboard/orders/[orderId]/route.ts, app/api/orders/search/route.ts, app/api/reconciliation/route.ts</files>
  <action>Update the order/reporting domain modules and protected route handlers to call the new typed server bridge with generated refs from `@/lib/convex/api`. Replace raw string calls like `"orders:searchOrders"` and slash-path strings like `"orders/getOrdersForReconciliation"` with generated refs such as `api.orders.searchOrders` and the new focused reporting/order functions created in Task 1. Preserve the existing auth gate (`requireApiUser()`), 400/404/500 JSON envelopes, CSV export behavior, and the operator-facing response shapes already consumed by the dashboard pages exactly, including field names, nesting, and current message wording wherever the route already defines it.</action>
  <acceptance_criteria>`lib/domain/finance/reporting.ts` contains `api.orders` or `api.events`; `lib/domain/finance/order-ledger.ts` contains `api.orders`; `lib/domain/finance/reconciliation.ts` contains `api.orders`; `app/api/orders/search/route.ts` does not contain `"orders:`; `app/api/dashboard/orders/[orderId]/route.ts` does not contain `"orders:`; each listed route still contains `requireApiUser`.</acceptance_criteria>
  <verify>Run `npm run typecheck`; run `rg '"orders:|"orders/' lib/domain/finance app/api/dashboard app/api/orders app/api/reconciliation`; read the route files to confirm auth and error contracts are intact.</verify>
  <done>The order/reporting route layer uses typed Convex refs end-to-end without changing the current protected JSON contracts.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm order/reporting callers no longer use raw `"orders:*"` or `"orders/*"` strings.
- Confirm protected routes still gate through `requireApiUser()`.
</verification>

<success_criteria>

- Orders/reporting Convex queries are bounded and typed.
- Finance order/reporting domain files use generated refs instead of string dispatch.
- Protected order/reporting routes keep their current response contracts.
  </success_criteria>

<output>
After completion, create `.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-02-SUMMARY.md`
</output>
