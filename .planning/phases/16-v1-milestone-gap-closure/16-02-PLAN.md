---
phase: 16-v1-milestone-gap-closure
plan: 16-02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/accommodation/assignments.ts
  - convex/accommodation.ts
  - tests/accommodation/allocation-filters.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Accommodation board family/location filters actually change returned attendee sets"
    - "Board payload exposes real family indicators instead of placeholder values"
  artifacts:
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Filter plumbing from API-domain boundary into Convex board query"
    - path: "convex/accommodation.ts"
      provides: "Server-side filtering and mapped attendee family metadata"
    - path: "tests/accommodation/allocation-filters.test.ts"
      provides: "Regression tests for location/family filtering and hasFamily mapping"
  key_links:
    - from: "lib/domain/accommodation/assignments.ts"
      to: "convex/accommodation.ts:getRoomAllocationBoard"
      via: "convexQuery args include familyGroupId + location"
      pattern: "familyGroupId|location"
    - from: "convex/accommodation.ts"
      to: "Room allocation board response"
      via: "unassignedAttendees mapping"
      pattern: "hasFamily"
---

<objective>
Close milestone gaps for ACC-04 and ACC-05 by wiring family/location accommodation signals end-to-end.

Purpose: The UI already exposes signal filters, but they are currently dropped before query execution, producing false operator confidence.
Output: Fully wired filter path and truthful family metadata in allocation board responses.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@app/api/dashboard/accommodation/assignments/route.ts
@lib/domain/accommodation/assignments.ts
@convex/accommodation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb family/location filters through domain and Convex query</name>
  <files>lib/domain/accommodation/assignments.ts, convex/accommodation.ts</files>
  <action>Update `getRoomAllocationBoard` flow so `familyGroupId` and `location` are normalized and forwarded through `convexQuery(api.accommodation.getRoomAllocationBoard, ...)`. Extend Convex query args validators and filtering logic to apply these filters to unassigned attendees (without regressing existing `eventId`, `genderType`, `allocationPriority`, and `hasPriority` behavior). Ensure returned `filters` echoes applied values rather than null placeholders.</action>
  <verify>Run `npm run typecheck` and confirm board responses differ when `familyGroupId`/`location` query params are toggled against seeded data.</verify>
  <done>Family/location controls are operational end-to-end: changing either filter changes result sets and reflected filter metadata in payload.</done>
</task>

<task type="auto">
  <name>Task 2: Return real family indicators and lock behavior with tests</name>
  <files>convex/accommodation.ts, tests/accommodation/allocation-filters.test.ts</files>
  <action>Replace hardcoded `hasFamily: false` with computed family truth derived from attendee grouping data (prefer explicit `familyGroupId` when present; otherwise use same-order grouping fallback). Add targeted tests for (a) location filtering, (b) family-group filtering, and (c) `hasFamily` truthy/falsey mapping on realistic attendee fixtures.</action>
  <verify>Run `npm test -- tests/accommodation/allocation-filters.test.ts`.</verify>
  <done>Board payload family flags are data-driven, and regression tests fail if filter plumbing or family mapping is removed.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/accommodation/allocation-filters.test.ts`
</verification>

<success_criteria>

- Location and family filters are no longer no-op controls.
- Allocation board response includes trustworthy family metadata for each unassigned attendee.
- ACC-04/ACC-05 blocking findings from milestone audit are cleared in code.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-v1-milestone-gap-closure/16-02-SUMMARY.md`
</output>
