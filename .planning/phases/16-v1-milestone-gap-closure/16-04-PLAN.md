---
phase: 16-v1-milestone-gap-closure
plan: 16-04
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - app/dashboard/accommodation/page.tsx
  - app/dashboard/accommodation/filter-state.ts
  - tests/accommodation/accommodation-filter-state.test.ts
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Applying accommodation signal filters updates URL state and returned board data consistently"
    - "Signal-only filter changes trigger workspace reloads without requiring unrelated filter edits"
    - "Family indicator badges in the accommodation queue reflect the payload contract (`hasFamily`)"
  artifacts:
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "Signal-filter apply/reload wiring and hasFamily-driven attendee rendering"
    - path: "app/dashboard/accommodation/filter-state.ts"
      provides: "Canonical signal filter serialization/parsing used by URL and load triggers"
    - path: "tests/accommodation/accommodation-filter-state.test.ts"
      provides: "Regression coverage for signal filter URL/state wiring and contract-safe family signal access"
  key_links:
    - from: "app/dashboard/accommodation/page.tsx:applyFilters"
      to: "syncUrlState"
      via: "serialized filter payload"
      pattern: "genderType|allocationPriority|hasPriority|location"
    - from: "app/dashboard/accommodation/page.tsx:loadWorkspace"
      to: "/api/dashboard/accommodation/assignments"
      via: "query string generation"
      pattern: "familyGroupId|location|genderType|allocationPriority|hasPriority"
    - from: "unassignedAttendee.hasFamily"
      to: "family badge rendering"
      via: "UI conditional"
      pattern: "hasFamily"
---

<objective>
Close the remaining v1.0 ACC-05 milestone blocker by fixing accommodation signal filter apply/reload wiring and the family indicator contract mismatch.

Purpose: Operators need reliable signal filtering behavior; stale apply flows or mismatched family badges break trust in accommodation decisions.
Output: Accommodation page wiring that keeps URL state, workspace fetches, and family visual signals aligned with the backend contract.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/16-v1-milestone-gap-closure/16-02-SUMMARY.md

@app/dashboard/accommodation/page.tsx
@lib/domain/accommodation/assignments.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire signal filters through apply flow, URL state, and workspace reload triggers</name>
  <files>app/dashboard/accommodation/page.tsx, app/dashboard/accommodation/filter-state.ts</files>
  <action>Implement a canonical signal-filter state helper used by `applyFilters()` and `loadWorkspace()` so `genderType`, `allocationPriority`, `hasPriority`, and `location` are never dropped when syncing URL/query state. Ensure signal-only changes trigger board reloads by adding the applied signal state to the reload trigger path (dependency list or explicit applied-filter snapshot), while preserving existing event/hotel/room filter behavior.</action>
  <verify>Run `npm run typecheck` and confirm generated assignment fetch URLs include signal params after clicking Apply with only signal filter changes.</verify>
  <done>Signal filter selections are reflected in URL state and board API fetches on every apply action, including signal-only updates.</done>
</task>

<task type="auto">
  <name>Task 2: Align family indicator rendering with `hasFamily` contract and add regression tests</name>
  <files>app/dashboard/accommodation/page.tsx, tests/accommodation/accommodation-filter-state.test.ts</files>
  <action>Update accommodation queue/family badge rendering to rely on `hasFamily` from the board payload (not `familyGroupId` checks). Add targeted regression tests for filter-state serialization/parsing and request-query construction to ensure signal params are retained and family-signal UI gating reads `hasFamily` consistently.</action>
  <verify>Run `npm test -- tests/accommodation/accommodation-filter-state.test.ts`.</verify>
  <done>Family badges render from the canonical `hasFamily` signal and tests fail if signal filter wiring or family-contract usage regresses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Accommodation signal filter apply/reload flow + family badge contract alignment</what-built>
  <how-to-verify>
    1) Open `/dashboard/accommodation` with seeded attendees spanning different location/gender/priority/family states.
    2) Change only signal filters (`gender`, `allocation priority`, `has priority`, `location`) and click Apply.
    3) Confirm URL query params update and unassigned attendee list changes without editing non-signal filters.
    4) Confirm family badges appear/disappear based on backend-provided `hasFamily` truth, not only explicit family group ids.
  </how-to-verify>
  <resume-signal>Type "approved" if behavior matches, or share failing step(s) and observed output.</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/accommodation/accommodation-filter-state.test.ts`
- Human verification: signal-only apply/reload behavior and family badge rendering on `/dashboard/accommodation`
</verification>

<success_criteria>

- ACC-05 gap truth passes: accommodation signal filters are applied end-to-end and remain reactive on apply/reload.
- Accommodation family indicators follow the `hasFamily` payload contract without UI/domain mismatch.
- v1.0 milestone audit has no remaining code-level blocker for accommodation signal filtering.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-v1-milestone-gap-closure/16-04-SUMMARY.md`
</output>
