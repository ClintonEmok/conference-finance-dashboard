---
phase: 16-v1-milestone-gap-closure
plan: 16-03
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - lib/domain/accommodation/assignments.ts
  - tests/accommodation/allocation-proposal.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Auto-allocation suggestions apply compatibility guardrails beyond free-bed checks"
    - "Allocation summary reports a real familyGroupsKeptTogether metric"
  artifacts:
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Compatibility-aware proposal generation with family/gender/priority logic"
    - path: "tests/accommodation/allocation-proposal.test.ts"
      provides: "Behavioral coverage for guardrails and family cohesion metric"
  key_links:
    - from: "app/api/dashboard/accommodation/auto-allocate/route.ts"
      to: "lib/domain/accommodation/assignments.ts:generateAllocationProposal"
      via: "domain call"
      pattern: "generateAllocationProposal"
    - from: "generateAllocationProposal"
      to: "summary.familyGroupsKeptTogether"
      via: "computed from accepted family placements"
      pattern: "familyGroupsKeptTogether"
---

<objective>
Close the ACC-06 gap by upgrading smart allocation from first-fit to compatibility-aware proposal logic.

Purpose: Operators need trustworthy suggestions that preserve family cohesion and avoid obvious roommate incompatibilities.
Output: Deterministic, test-backed allocation proposals with meaningful placement reasons and family cohesion metrics.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@lib/domain/accommodation/assignments.ts
@app/api/dashboard/accommodation/auto-allocate/route.ts
@.planning/phases/16-v1-milestone-gap-closure/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compatibility-aware allocation proposal strategy</name>
  <files>lib/domain/accommodation/assignments.ts</files>
  <action>Refactor `generateAllocationProposal` so room selection scores candidates by compatibility instead of taking first available room. Include at minimum: (1) family cohesion preference (co-locate same familyGroup/order-group where capacity allows), (2) gender compatibility guardrails (avoid placing clearly incompatible mixed occupants unless room/attendee permits mixed/unknown), and (3) priority-aware ordering (`CRITICAL`/`HIGH` first with deterministic tie-breaks). Keep output contract unchanged but improve `reason` strings to reflect applied rule(s).</action>
  <verify>Run `npm run typecheck` and inspect a proposal payload to confirm reasons mention compatibility rationale rather than generic bed availability text.</verify>
  <done>Proposal engine no longer behaves as naive first-fit; suggested rooms reflect family/gender/priority rules and produce deterministic ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Add behavioral tests for guardrails and family cohesion metric</name>
  <files>tests/accommodation/allocation-proposal.test.ts</files>
  <action>Create focused tests that exercise proposal behavior over representative board fixtures: family members placed together when feasible, incompatible rooms rejected, high-priority attendees considered first, and `summary.familyGroupsKeptTogether` computed from actual grouped placements (not hardcoded). Keep fixtures minimal and deterministic so failures pinpoint rule regressions.</action>
  <verify>Run `npm test -- tests/accommodation/allocation-proposal.test.ts`.</verify>
  <done>Tests fail if compatibility checks are removed or family cohesion metric regresses to constant/placeholder values.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/accommodation/allocation-proposal.test.ts`
</verification>

<success_criteria>

- Auto-allocation suggestions honor compatibility guardrails, not just free beds.
- `familyGroupsKeptTogether` is computed from actual placements.
- ACC-06 blocker from milestone audit is addressed with executable tests.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-v1-milestone-gap-closure/16-03-SUMMARY.md`
</output>
