---
phase: 16-v1-milestone-gap-closure
plan: 16-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/ticket-tailor/sync/route.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Manual Ticket Tailor sync endpoint rejects unauthenticated callers with the standard unauthorized payload"
    - "Authenticated operators can still run scoped sync without changing response shape"
  artifacts:
    - path: "app/api/ticket-tailor/sync/route.ts"
      provides: "Protected POST sync route using shared Clerk auth guard"
      exports: ["POST"]
    - path: "tests/ticket-tailor/sync-route.test.ts"
      provides: "Route-level regression coverage for unauthorized and authorized sync calls"
  key_links:
    - from: "app/api/ticket-tailor/sync/route.ts"
      to: "lib/auth/server.ts"
      via: "requireApiUser()"
      pattern: "requireApiUser"
    - from: "tests/ticket-tailor/sync-route.test.ts"
      to: "app/api/ticket-tailor/sync/route.ts"
      via: "POST handler invocation with mocked auth states"
      pattern: "UNAUTHORIZED|Authentication required"
---

<objective>
Close the v1 milestone security gap by protecting the manual Ticket Tailor sync endpoint.

Purpose: This endpoint performs operator-only data ingestion and must enforce the same auth boundary as other protected dashboard routes.
Output: Auth-guarded sync route plus route tests proving unauthorized rejection and authorized continuity.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@app/api/ticket-tailor/sync/route.ts
@lib/auth/server.ts
@tests/ticket-tailor/sync-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared auth guard to manual sync route</name>
  <files>app/api/ticket-tailor/sync/route.ts</files>
  <action>Import and call `requireApiUser()` at the start of `POST`. If it returns `NextResponse`, return it directly so the route preserves the established unauthorized contract from shared auth helpers. Keep existing scope parsing, success payload (`ok`, `runId`, `status`, `scope`, `counts`, `diagnostics`), and 400/500 error handling unchanged.</action>
  <verify>Run `npm run typecheck` and confirm `POST` now contains `requireApiUser()` guard before `parseManualSyncScope`.</verify>
  <done>Unauthenticated POST `/api/ticket-tailor/sync` returns the standard unauthorized JSON/401; authenticated calls still execute sync with unchanged success payload shape.</done>
</task>

<task type="auto">
  <name>Task 2: Add route regression tests for auth boundary</name>
  <files>tests/ticket-tailor/sync-route.test.ts</files>
  <action>Update sync route tests to mock `@/lib/auth/server` and cover both auth states: (1) `requireApiUser` returns unauthorized response -> route returns 401 with `UNAUTHORIZED` contract; (2) `requireApiUser` returns user object -> route still processes valid scoped payload and keeps prior behavior. Preserve existing bad-request coverage and avoid brittle assertions on volatile diagnostics text.</action>
  <verify>Run `npm test -- tests/ticket-tailor/sync-route.test.ts`.</verify>
  <done>Tests fail if auth guard is removed, and pass when unauthorized/authorized behaviors match protected API conventions.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/ticket-tailor/sync-route.test.ts`
</verification>

<success_criteria>

- Manual sync route is no longer callable without authentication.
- Unauthorized response contract matches other protected API routes.
- Existing operator sync flow remains intact for authenticated users.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-v1-milestone-gap-closure/16-01-SUMMARY.md`
</output>
