---
phase: 15-event-level-tikkie-ui-attendee-tikkie-cleanup
plan: 15-01
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - components/dashboard/event-tikkie-section.tsx
  - app/dashboard/financial/page.tsx
autonomous: true

must_haves:
  truths:
    - "Operator can see event-level Tikkie payment link status and payment list on the financial page"
    - "Operator can create an event-level Tikkie link from the financial page"
    - "Operator can manually assign unmatched Tikkie payments to orders from the financial page"
    - "Operator can trigger auto-match for event Tikkie payments from the financial page"
    - "Operator can select which event to view Tikkie data for"
  artifacts:
    - path: "components/dashboard/event-tikkie-section.tsx"
      provides: "Collapsible event Tikkie section with event picker, payment list, summary, link creation, manual assignment, auto-match"
      min_lines: 120
    - path: "app/dashboard/financial/page.tsx"
      provides: "Financial page with EventTikkieSection integrated"
  key_links:
    - from: "components/dashboard/event-tikkie-section.tsx"
      to: "/api/dashboard/tikkie-event-links"
      via: "fetch in useEffect with selected eventId"
      pattern: "fetch.*tikkie-event-links"
    - from: "components/dashboard/event-tikkie-section.tsx"
      to: "/api/dashboard/tikkie-event-links"
      via: "POST to create link, PATCH to manually match"
      pattern: "fetch.*tikkie-event-links.*method"
    - from: "app/dashboard/financial/page.tsx"
      to: "components/dashboard/event-tikkie-section.tsx"
      via: "import and render EventTikkieSection"
      pattern: "EventTikkieSection"
---

# Plan 15-01: Financial page event Tikkie section

## Objective

Add an event-level Tikkie payment tracking section to the financial page (`/dashboard/financial`). This section shows a collapsible card with an event picker, payment link status, payment list with manual assignment, summary stats, and a button to create an event Tikkie link.

**Purpose:** Centralize Tikkie payment management at the event level on the financial workspace page, replacing scattered per-order Tikkie creation (being removed in 15-02).

**Output:** New `EventTikkieSection` component + modified financial page that renders it.

## Execution Context

@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-event-level-tikkie-ui-attendee-tikkie-cleanup/15-CONTEXT.md

### Key source files (read before implementing)

- `app/dashboard/financial/page.tsx` — Financial page to modify (298 lines, client component with useEffect + fetch, shadcn/ui cards)
- `app/api/dashboard/tikkie-event-links/route.ts` — API routes: GET (link+payments+stats by eventId), POST (create), PATCH (manual match)
  - `lib/convex/hooks/tikkie.ts` — Available Convex hooks (NOTE: event-level hooks reference functions that Phase 14 adds to generated types — use REST API fetch as primary interface until Phase 14 is executed)
  - `components/payments/assign-dialog.tsx` — Pattern reference for manual payment-to-order assignment UI
- `components/payments/payment-list.tsx` — Pattern reference for payment list table display
- `convex/events.ts` — `getEvents` query for event list
- `components/ui/` — shadcn/ui Card, Button, Badge, Table, Skeleton, Input components available

### Locked decisions (from CONTEXT.md)

- **D-01:** Tikkie section lives on `/dashboard/financial` as a collapsible section
- **D-02:** Financial page gains an event picker; section shows data for selected event
- **D-03:** Tikkie link creation (generate link button) lives on financial page only
- **D-04:** Payment list shows: payer name, amount, date, matched/unmatched status
- **D-05:** Manual assignment — button on unmatched payments to pick an order
- **D-06:** Summary header shows: total payments, matched count, unmatched count, total amount
- **D-07:** Auto-match triggered manually (button) and on payment sync

### Agent's discretion

- Exact layout of the collapsible section
- How event picker integrates with existing financial page data
- Loading/error states for the Tikkie section

## Tasks

<task type="auto">
  <name>Task 1: Create EventTikkieSection component</name>
  <files>components/dashboard/event-tikkie-section.tsx</files>
  <read_first>
    - `app/api/dashboard/tikkie-event-links/route.ts` — API contract (GET/POST/PATCH)
    - `lib/convex/hooks/tikkie.ts` — Available Convex hooks
    - `components/payments/assign-dialog.tsx` — Manual assignment dialog pattern
    - `components/payments/payment-list.tsx` — Payment list table pattern
    - `components/ui/` — Available shadcn components (Card, Button, Badge, Table, Skeleton)
  </read_first>
  <action>
    Create a new `"use client"` component at `components/dashboard/event-tikkie-section.tsx`.

    **Props:** `{ events: Array<{ providerEventId: string; name: string | null }> }`

    **State:**
    - `selectedEventId` — from event picker (default to first event)
    - `eventData` — `{ link, payments, stats }` from GET `/api/dashboard/tikkie-event-links?eventId=...`
    - `isLoading`, `errorMessage`
    - `isCollapsed` — toggle for collapsible section

    **Data loading:**
    - `useEffect` watching `selectedEventId` → fetch `GET /api/dashboard/tikkie-event-links?eventId=${selectedEventId}`
    - Parse response as `{ link: object | null, payments: array, stats: { totalPayments, matchedPayments, unmatchedPayments, totalAmountMinor } }`

    **UI structure (shadcn Card-based, collapsible):**
    1. **Header row:** Event picker (`<select>` over events prop), collapse toggle, "Create Tikkie link" button
    2. **Summary bar (when link exists):** Total payments count, matched count (green), unmatched count (yellow/orange), total amount (formatMoney)
    3. **Payment list (when expanded + link exists):** Table with columns: Payer Name, Amount, Date, Status (matched/unmatched badge), Action (Assign button on unmatched rows)
    4. **Assign button handler:** Opens a modal/dropdown similar to `AssignDialog` pattern — list orders to match against, PATCH to `/api/dashboard/tikkie-event-links` with `{ paymentId, orderId }`
    5.     **Auto-match button:** Calls PATCH or POST to `/api/dashboard/tikkie-event-links` to trigger auto-match, then refetches. Use fetch() to the REST API (not Convex hooks) since generated types may not include Phase 14 functions yet.
    6. **Empty state:** When no link exists, show "No Tikkie link for this event" + create button
    7. **Loading state:** Skeleton cards matching the layout

    **Create link handler:**
    - POST to `/api/dashboard/tikkie-event-links` with `{ eventId: selectedEventId, providerEventId: selectedEventId }`
    - On success, refetch event data

    **formatMoney helper:** Reuse same pattern as financial page — `Intl.NumberFormat("en-US", { style: "currency", currency: "EUR" }).format(minor / 100)`

    **Match status badge:**
    - `matched` / `auto_matched` / `manual_assignment` → green Badge
    - `unmatched` → yellow/orange Badge

  </action>
  <verify>
    - File exists at `components/dashboard/event-tikkie-section.tsx`
    - File starts with `"use client"` directive
    - Contains `EventTikkieSection` named export or default export
    - Imports from `@/components/ui/card`, `@/components/ui/button`, `@/components/ui/badge`
    - References `/api/dashboard/tikkie-event-links` in fetch calls
    - Has event picker (`<select>` or similar) rendering events from props
    - Has collapsible toggle (`isCollapsed` state or similar)
    - `npx tsc --noEmit` passes for this file
  </verify>
  <done>
    Component renders event picker, collapsible section with payment list, summary stats, create link button, manual assign on unmatched payments, and auto-match button. All wired to `/api/dashboard/tikkie-event-links` API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire EventTikkieSection into financial page</name>
  <files>app/dashboard/financial/page.tsx</files>
  <read_first>
    - `app/dashboard/financial/page.tsx` — Current page structure (298 lines)
    - `components/dashboard/event-tikkie-section.tsx` — Component created in Task 1
    - `convex/events.ts` — `getEvents` query for event list
  </read_first>
  <action>
    Modify `app/dashboard/financial/page.tsx` to integrate the EventTikkieSection.

    **Changes:**
    1. Import `EventTikkieSection` from `@/components/dashboard/event-tikkie-section`
    2. Add `events` state: `useState<Array<{ providerEventId: string; name: string | null }>>([])`
    3. In the existing `useEffect` load function, also fetch events list. Use either:
       - The existing reconciliation endpoint response (which includes `availableEvents`)
       - A direct Convex query via the hook if available, or
       - Add the events to the existing Promise.all fetch (fetch `/api/dashboard/reconciliation` already returns `availableEvents` — extract from there)
    4. Add `<EventTikkieSection events={events} />` below the existing cards section, before the error message block
    5. The section should appear between the summary cards grid and the "Daily finance loop" section

  </action>
  <verify>
    - `app/dashboard/financial/page.tsx` imports `EventTikkieSection`
    - Events state is populated from API response
    - `<EventTikkieSection events={events} />` appears in JSX
    - `npx tsc --noEmit` passes
    - No existing functionality removed (revenue cards, action links, outstanding rows all still render)
  </verify>
  <done>
    Financial page shows event Tikkie section with event picker, collapsible UI, payment list, link creation, manual assignment, and auto-match. Existing financial page content unchanged.
  </done>
</task>

## Verification

- `npx tsc --noEmit` passes with no new errors
- Financial page renders at `/dashboard/financial` with Tikkie section visible
- Event picker populates from existing event data
- Selecting an event loads its Tikkie link + payments from the API
- Create link button POSTs to `/api/dashboard/tikkie-event-links`
- Manual assign PATCHes to `/api/dashboard/tikkie-event-links`
- Section collapses/expands on toggle

## Success Criteria

- [ ] `components/dashboard/event-tikkie-section.tsx` exists with event picker, collapsible section, payment list, summary, create/assign/auto-match
- [ ] `app/dashboard/financial/page.tsx` imports and renders `EventTikkieSection`
- [ ] All interactions wired to `/api/dashboard/tikkie-event-links` (GET/POST/PATCH)
- [ ] TypeScript compiles clean

## Output

After completion, create `.planning/phases/15-event-level-tikkie-ui-attendee-tikkie-cleanup/15-01-SUMMARY.md`
