---
phase: 10-payment-reconciliation-tikkie-bank-cash
plan: "05"
type: execute
wave: 1
depends_on:
  - "10-04"
files_modified:
  - app/api/reconciliation/route.ts
  - app/dashboard/reconciliation/page.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Reconciliation dashboard shows unassigned/partial/paid/overpaid states based on order payment status"
  artifacts:
    - path: app/api/reconciliation/route.ts
      provides: "Summary with order-level payment status (partial/paid/overpaid)"
    - path: app/dashboard/reconciliation/page.tsx
      provides: "Dashboard displaying correct payment states"
  key_links:
    - from: app/api/reconciliation/route.ts
      to: prisma.ticketTailorOrder
      via: "Calculate order total vs payment sum"
---

<objective>
Close Gap 4: Update reconciliation to show order-level payment status (partial/paid/overpaid) instead of just payment-level statuses.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-CONTEXT.md
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-VERIFICATION.md
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-03-SUMMARY.md
@app/api/reconciliation/route.ts
@app/dashboard/reconciliation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Update reconciliation API with order-level payment status</name>
  <files>app/api/reconciliation/route.ts</files>
  <action>
Update GET /api/reconciliation to return order-level payment status:

The current implementation returns payment-level statuses (unassigned/ambiguous/manual/auto_matched).
The requirement is to show orders by their payment status (partial/paid/overpaid):

1. Calculate for each order:
   - orderTotal: sum of all attendee fees (from order)
   - paidAmount: sum of all payments linked to the order
   - status:
     - "unassigned" = no payments linked to order
     - "partial" = paidAmount > 0 AND paidAmount < orderTotal
     - "paid" = paidAmount >= orderTotal
     - "overpaid" = paidAmount > orderTotal (extra amount could be donation or error)

2. Update the summary to return:

```typescript
{
  summary: {
    unassigned: number,   // orders with no payments
    partial: number,      // orders with partial payment
    paid: number,         // orders fully paid
    overpaid: number,     // orders with excess payment
  },
  bySource: { ... },      // keep existing breakdown by payment source
}
```

3. Also update the orders endpoint (or create new one) to return orders with their payment status so the dashboard can display them properly.
   </action>
   <verify>
   grep -q "partial" app/api/reconciliation/route.ts && grep -q "overpaid" app/api/reconciliation/route.ts && echo "Reconciliation API updated"
   </verify>
   <done>
   GET /api/reconciliation returns order-level payment status (unassigned/partial/paid/overpaid)
   </done>
   </task>

<task type="auto">
  <name>Update reconciliation dashboard to display correct statuses</name>
  <files>app/dashboard/reconciliation/page.tsx</files>
  <action>
Update the reconciliation dashboard page to display order-level payment status:

1. Update summary cards to show:
   - Unassigned (yellow): orders with no payments linked
   - Partial (orange): orders with partial payment
   - Paid (green): orders fully paid
   - Overpaid (purple): orders with excess payment

2. Update the payment/order list to show the correct statuses

3. Remove or deprecate the old payment-level status display (unassigned/ambiguous/manual/auto_matched)

Note: The CONTEXT.md specified:

- D-10: States: Unassigned, Partial, Fully Paid, Overpaid
- D-11: Show summary dashboard with counts per state
- D-13: Order states by importance/action needed (unassigned first)
  </action>
  <verify>
  grep -q "partial" app/dashboard/reconciliation/page.tsx && grep -q "overpaid" app/dashboard/reconciliation/page.tsx && echo "Dashboard updated"
  </verify>
  <done>
  Reconciliation dashboard displays unassigned/partial/paid/overpaid states correctly
  </done>
  </task>

</tasks>

<verification>
- Fetch /api/reconciliation - verify response contains partial and overpaid counts
- Visit /dashboard/reconciliation - verify cards show correct payment states
</verification>

<success_criteria>

1. Dashboard shows unassigned/partial/paid/overpaid states (Gap 4 closed)
2. Summary reflects order-level payment status, not payment-level status
   </success_criteria>

<output>
After completion, create `.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-05-SUMMARY.md`
</output>
