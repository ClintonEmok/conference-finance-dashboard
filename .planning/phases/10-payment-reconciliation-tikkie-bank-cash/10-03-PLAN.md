---
phase: 10-payment-reconciliation-tikkie-bank-cash
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - app/api/reconciliation/route.ts
  - app/api/payments/route.ts
  - app/dashboard/reconciliation/page.tsx
  - components/payments/payment-list.tsx
  - components/payments/assign-dialog.tsx
autonomous: true
must_haves:
  truths:
    - "Dashboard shows reconciliation summary with counts per status"
    - "Payments are listed with filtering by status and source"
    - "Admin can view payment details and assign unassigned payments"
  artifacts:
    - path: app/api/reconciliation/route.ts
      provides: "Summary counts by payment status"
    - path: app/api/payments/route.ts
      provides: "List payments with filters"
    - path: app/dashboard/reconciliation/page.tsx
      provides: "Main reconciliation dashboard UI"
  key_links:
    - from: app/dashboard/reconciliation/page.tsx
      to: app/api/reconciliation/route.ts
      via: "fetch summary counts on load"
    - from: app/dashboard/reconciliation/page.tsx
      to: app/api/payments/route.ts
      via: "fetch filtered payment list"
---

<objective>
Build reconciliation dashboard with summary view and payment management.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-CONTEXT.md
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-RESEARCH.md
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-01-SUMMARY.md
@.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-02-SUMMARY.md
@lib/domain/finance/payments.ts
</context>

<tasks>

<task type="auto">
  <name>Create reconciliation summary API</name>
  <files>app/api/reconciliation/route.ts</files>
<action>
Create GET /api/reconciliation endpoint:

1. Validate auth (require session)
2. Query Payment table grouped by status:
   - unassigned: count where status = 'unassigned'
   - ambiguous: count where status = 'ambiguous'
   - manual_assignment: count where status = 'manual_assignment'
   - auto_matched: count where status = 'auto_matched'
3. Also return:
   - total: total payment count
   - totalAmount: sum of all payment amounts
   - bySource: breakdown by PaymentSource
4. Return:
   ```typescript
   {
     summary: { unassigned, ambiguous, manual_assignment, auto_matched, total },
     totalAmountMinor: number,
     bySource: { tikkie: number, bank_transfer: number, cash: number }
   }
   ```
   </action>
   <verify>
   ls app/api/reconciliation/route.ts && grep -q "GET" app/api/reconciliation/route.ts
   </verify>
   <done>
   GET /api/reconciliation returns status counts and totals
   </done>
   </task>

<task type="auto">
  <name>Create payments list API</name>
  <files>app/api/payments/route.ts</files>
<action>
Create GET /api/payments endpoint with query params:

1. Validate auth (require session)
2. Accept query params:
   - status?: 'unassigned' | 'ambiguous' | 'manual_assignment' | 'auto_matched'
   - source?: 'tikkie' | 'bank_transfer' | 'cash'
   - orderId?: string
   - page?: number (default 1)
   - limit?: number (default 20)
3. Build Prisma where clause from filters
4. Include order relation for display
5. Return: { payments: Payment[], total: number, page, limit }

Also add POST for creating payments (used by bank/cash entry from Plan 02).
</action>
<verify>
ls app/api/payments/route.ts && grep -q "GET" app/api/payments/route.ts && grep -q "POST" app/api/payments/route.ts
</verify>
<done>
GET /api/payments returns paginated list with filters
</done>
</task>

<task type="auto">
  <name>Create payment list component</name>
  <files>components/payments/payment-list.tsx</files>
<action>
Create components/payments/payment-list.tsx:

1. Props: { filters?: { status?, source?, orderId? } }
2. Fetch payments on mount using /api/payments
3. Display as table with columns:
   - Date (paidAt)
   - Source (icon: Tikkie/bank/cash)
   - Payer name
   - Amount (format as EUR)
   - Status (badge: unassigned=yellow, ambiguous=orange, assigned=green)
   - Order (buyer name or "-")
   - Actions (View, Assign if unassigned/ambiguous)
4. Add filter controls:
   - Status dropdown
   - Source dropdown
5. Pagination controls
6. Click row or action to open detail/assignment dialog

Use shadcn/ui: Table, Badge, DropdownMenu, Pagination.
Follow patterns from existing payment/reconciliation tables.
</action>
<verify>
ls components/payments/payment-list.tsx && grep -q "status" components/payments/payment-list.tsx
</verify>
<done>
Payment list displays with filters and status badges
</done>
</task>

<task type="auto">
  <name>Create assign dialog component</name>
  <files>components/payments/assign-dialog.tsx</files>
<action>
Create components/payments/assign-dialog.tsx:

1. Props: { payment: Payment, open: boolean, onOpenChange, onAssigned }
2. Show payment details (payer name, amount, date)
3. Order search/select:
   - Input to search orders by buyer name
   - Show matching orders in dropdown
   - Display order details (buyer, total, outstanding)
4. Confirm button to call PATCH /api/payments/:id/assign
5. On success: close dialog, show toast, trigger onAssigned callback

Use shadcn/ui: Dialog, Input, Select, Button.
Follow existing dialog patterns in codebase.
</action>
<verify>
ls components/payments/assign-dialog.tsx && grep -q "assign" components/payments/assign-dialog.tsx
</verify>
<done>
Assign dialog allows searching and selecting order for payment
</done>
</task>

<task type="auto">
  <name>Create reconciliation dashboard page</name>
  <files>app/dashboard/reconciliation/page.tsx</files>
<action>
Create app/dashboard/reconciliation/page.tsx:

1. Fetch summary from /api/reconciliation on mount
2. Display summary cards (4 columns on desktop):
   - Unassigned (yellow): count + "Needs attention"
   - Ambiguous (orange): count + "Review needed"
   - Manual (blue): count + "Manually assigned"
   - Auto-matched (green): count + "Automatically matched"
3. Below cards:
   - "Add Payment" button → opens manual entry form (from Plan 02)
   - "Sync Tikkie" button → triggers /api/payments/tikkie/sync
4. Payment list below (using PaymentList component)
5. Default filter: show unassigned first

Use existing dashboard layout pattern, shadcn/ui Card components.
Add to navigation: /dashboard/reconciliation accessible from finance section.
</action>
<verify>
ls app/dashboard/reconciliation/page.tsx && grep -q "summary" app/dashboard/reconciliation/page.tsx
</verify>
<done>
Reconciliation dashboard shows summary cards and payment list
</done>
</task>

</tasks>

<verification>
- Summary API returns accurate counts per status
- Payments list loads with filters working
- Dashboard displays summary cards with correct data
- Manual assignment flow works end-to-end
</verification>

<success_criteria>

1. Dashboard shows unassigned, ambiguous, manual, auto counts
2. Payment list shows all payments with filtering
3. Admin can assign unassigned payments to orders
4. Manual payment entry button accessible from dashboard
5. Tikkie sync button triggers payment sync
   </success_criteria>

<output>
After completion, create `.planning/phases/10-payment-reconciliation-tikkie-bank-cash/10-03-SUMMARY.md`
</output>
