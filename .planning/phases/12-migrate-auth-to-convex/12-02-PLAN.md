---
phase: 12-migrate-auth-to-convex
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
requirements_addressed: []
files_modified:
  - tests/auth/convex-auth-contract.test.ts
  - tests/auth/middleware-session.test.ts
  - package.json
  - lib/prisma.ts
  - prisma/schema.prisma
  - prisma/dev.db
  - prisma/test-db.sqlite
  - prisma/prisma/test-db.sqlite
  - prisma/migrations/20260318175357_init_auth/migration.sql
  - prisma/migrations/20260318192657_add_ticket_tailor_webhook_events/migration.sql
  - prisma/migrations/20260318192809_add_webhook_canonical_payload/migration.sql
  - prisma/migrations/20260318224156_ticket_tailor_sync_models/migration.sql
  - prisma/migrations/20260319101050_tikkie_payment_link_models/migration.sql
  - prisma/migrations/20260319105355_attendee_sync_foundation/migration.sql
  - prisma/migrations/20260319111221_accommodation_inventory_foundation/migration.sql
  - prisma/migrations/20260319112310_tikkie_payment_link_status_events/migration.sql
  - prisma/migrations/20260319133000_event_hotel_scope/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: false
must_haves:
  truths:
    - "Admins can still sign up, sign in, refresh, and sign out with cookie sessions after the Convex cutover"
    - "Protected dashboard access still redirects signed-out visitors to login"
    - "The repository no longer ships Prisma as a runtime dependency"
  artifacts:
    - path: "tests/auth/convex-auth-contract.test.ts"
      provides: "Regression coverage for Convex-backed auth route/session contract"
      contains: "describe"
    - path: "tests/auth/middleware-session.test.ts"
      provides: "Redirect coverage for signed-out and signed-in dashboard navigation"
      contains: "middleware"
    - path: "package.json"
      provides: "Dependency list without Prisma runtime packages"
      contains: "better-auth"
  key_links:
    - from: "tests/auth/convex-auth-contract.test.ts"
      to: "lib/auth.ts"
      via: "session lookup and route contract assertions"
      pattern: "getSession|auth"
    - from: "middleware.ts"
      to: "app/dashboard/layout.tsx"
      via: "shared cookie/session redirect expectations"
      pattern: "callbackUrl"
---

<objective>
Prove the Convex auth cutover works, then remove Prisma completely.

Purpose: Prevent a half-migration where auth technically compiles but runtime session flows or protected-route behavior regress, and ensure the repo no longer carries dead Prisma auth infrastructure.
Output: Auth regression tests, deleted Prisma artifacts, green build/test/typecheck, and a final human verification checklist for login/logout/session behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-auth-to-convex/12-CONTEXT.md
@.planning/phases/12-migrate-auth-to-convex/12-RESEARCH.md
@.planning/phases/12-migrate-auth-to-convex/12-VALIDATION.md
@.planning/phases/12-migrate-auth-to-convex/12-01-PLAN.md
@middleware.ts
@app/dashboard/layout.tsx
@app/login/login-form.tsx
@app/dashboard/logout-button.tsx
@app/api/protected/ping/route.ts
@package.json
@lib/prisma.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused auth regression coverage for the Convex cutover</name>
  <read_first>
    - tests/ticket-tailor/sync-route.test.ts
    - middleware.ts
    - app/dashboard/layout.tsx
    - app/login/login-form.tsx
    - app/dashboard/logout-button.tsx
    - app/api/protected/ping/route.ts
    - lib/auth.ts
  </read_first>
  <files>
    tests/auth/convex-auth-contract.test.ts
    tests/auth/middleware-session.test.ts
  </files>
  <action>Create focused Vitest coverage for the auth contract that must survive the migration. Add one test file for the server-side auth/session contract and one for middleware redirects. Cover these exact cases: (1) unauthenticated protected request returns/redirects as expected, (2) authenticated protected request succeeds when `auth.api.getSession` resolves a session, (3) dashboard redirect keeps `callbackUrl=%2Fdashboard` for signed-out users, (4) login/signup/logout client flow still targets `/api/auth/[...all]` through the existing Better Auth client contract. Reuse the repo's existing mocking style from `tests/ticket-tailor/sync-route.test.ts` so this phase verifies compatibility with the current `auth.api.getSession` call pattern instead of inventing a new abstraction.</action>
  <acceptance_criteria>
    - `tests/auth/convex-auth-contract.test.ts` exists
    - `tests/auth/convex-auth-contract.test.ts` contains `auth.api.getSession`
    - `tests/auth/middleware-session.test.ts` exists
    - `tests/auth/middleware-session.test.ts` contains `callbackUrl=%2Fdashboard`
    - `npm test -- tests/auth/*.test.ts tests/ticket-tailor/sync-route.test.ts` exits 0
  </acceptance_criteria>
  <verify>npm test -- tests/auth/*.test.ts tests/ticket-tailor/sync-route.test.ts</verify>
  <done>The cutover has automated coverage for protected-route/session behavior and preserves the existing auth mocking contract used elsewhere in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Prisma packages and files after Convex auth passes</name>
  <read_first>
    - package.json
    - lib/prisma.ts
    - prisma/schema.prisma
    - .planning/phases/12-migrate-auth-to-convex/12-RESEARCH.md
  </read_first>
  <files>
    package.json
    lib/prisma.ts
    prisma/schema.prisma
    prisma/dev.db
    prisma/test-db.sqlite
    prisma/prisma/test-db.sqlite
    prisma/migrations/20260318175357_init_auth/migration.sql
    prisma/migrations/20260318192657_add_ticket_tailor_webhook_events/migration.sql
    prisma/migrations/20260318192809_add_webhook_canonical_payload/migration.sql
    prisma/migrations/20260318224156_ticket_tailor_sync_models/migration.sql
    prisma/migrations/20260319101050_tikkie_payment_link_models/migration.sql
    prisma/migrations/20260319105355_attendee_sync_foundation/migration.sql
    prisma/migrations/20260319111221_accommodation_inventory_foundation/migration.sql
    prisma/migrations/20260319112310_tikkie_payment_link_status_events/migration.sql
    prisma/migrations/20260319133000_event_hotel_scope/migration.sql
    prisma/migrations/migration_lock.toml
  </files>
  <action>Delete the remaining Prisma runtime and project artifacts once Task 1 is green. Remove `@better-auth/prisma-adapter`, `@prisma/client`, and `prisma` from `package.json`. Delete `lib/prisma.ts`, the Prisma schema, the SQLite database files, and the checked-in migration SQL files so the repository reflects the clean-break decision from phase context. After removal, verify there are no remaining Prisma imports in app/runtime code and run the full regression/build pass (`npm test && npm run typecheck && npm run build`). Do not touch historical `.planning` references; only remove live runtime/project artifacts.</action>
  <acceptance_criteria>
    - `package.json` does not contain `@better-auth/prisma-adapter`
    - `package.json` does not contain `@prisma/client`
    - `package.json` does not contain `"prisma":`
    - `lib/prisma.ts` is deleted
    - `prisma/schema.prisma` is deleted
    - `npm test && npm run typecheck && npm run build` exits 0
  </acceptance_criteria>
  <verify>npm test && npm run typecheck && npm run build</verify>
  <done>The repo has no live Prisma dependency or runtime files, and the app still passes automated checks after the auth cutover.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the real login/logout/session flow in the browser</name>
  <read_first>
    - .planning/phases/12-migrate-auth-to-convex/12-RESEARCH.md
    - app/login/login-form.tsx
    - app/dashboard/logout-button.tsx
    - middleware.ts
  </read_first>
  <what-built>Convex-backed auth runtime with Prisma removed and automated auth regression coverage in place.</what-built>
  <how-to-verify>
    1. Start the app with the normal local dev command.
    2. Visit `/dashboard` while signed out and confirm you are redirected to `/login?callbackUrl=%2Fdashboard`.
    3. From `/login`, create an account or sign in with a local test account.
    4. Confirm you land on `/dashboard` and a refresh keeps you signed in.
    5. Click the existing logout button and confirm you return to `/login`.
    6. Try `/dashboard` again and confirm the signed-out redirect still happens.
  </how-to-verify>
  <acceptance_criteria>
    - Browser navigation reaches `/login?callbackUrl=%2Fdashboard` when signed out
    - Successful sign-in lands on `/dashboard`
    - Refresh on `/dashboard` keeps the session active
    - Logout returns the browser to `/login`
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the auth issue you observed.</resume-signal>
</task>

</tasks>

<verification>
- Auth regression tests pass before Prisma deletion and still pass after deletion.
- Full test/typecheck/build pass succeeds with Prisma removed.
- Human confirms signup/signin/dashboard refresh/logout/redirect behavior works end-to-end.
</verification>

<success_criteria>

- Browser auth behavior matches the pre-migration flow.
- Protected routes still reject signed-out access.
- Prisma packages and files are gone from the live repo.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-auth-to-convex/12-02-SUMMARY.md`
</output>
