---
phase: 12-migrate-auth-to-convex
plan: "01"
type: execute
wave: 1
depends_on: []
requirements_addressed: []
files_modified:
  - convex/schema.ts
  - convex/functions/schema.ts
  - convex/functions/auth.ts
  - convex/functions/generated/auth.ts
  - lib/auth.ts
  - app/api/auth/[...all]/route.ts
autonomous: true
must_haves:
  truths:
    - "Auth requests hit a Convex-backed Better Auth runtime instead of Prisma"
    - "The /api/auth/[...all] endpoint keeps serving the same Better Auth contract"
    - "Server code can still resolve sessions through lib/auth.ts"
  artifacts:
    - path: "convex/functions/auth.ts"
      provides: "Better Convex auth definition with preserved session and email/password settings"
      contains: "defineAuth"
    - path: "convex/schema.ts"
      provides: "Adapter-compatible auth tables"
      contains: "user: defineTable"
    - path: "lib/auth.ts"
      provides: "App-facing auth compatibility entrypoint without Prisma imports"
      contains: "export"
  key_links:
    - from: "convex/functions/auth.ts"
      to: "convex/functions/generated/auth.ts"
      via: "better-convex codegen activation"
      pattern: "authEnabled"
    - from: "lib/auth.ts"
      to: "app/api/auth/[...all]/route.ts"
      via: "toNextJsHandler(auth)"
      pattern: "toNextJsHandler"
---

<objective>
Activate Better Auth on Convex and preserve the app's existing auth entrypoints.

Purpose: Make Convex the only auth datastore without forcing downstream API routes, server components, or the login UI to learn a new contract.
Output: Adapter-compatible auth schema, live Better Convex auth runtime, and a Prisma-free `lib/auth.ts` used by the existing Next.js auth route.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-auth-to-convex/12-CONTEXT.md
@.planning/phases/12-migrate-auth-to-convex/12-RESEARCH.md
@.planning/phases/12-migrate-auth-to-convex/12-VALIDATION.md
@.planning/phases/11-use-convex/11-01-SUMMARY.md
@.planning/phases/11-use-convex/11-05-SUMMARY.md
@convex/schema.ts
@convex/functions/schema.ts
@convex/functions/generated/auth.ts
@lib/auth.ts
@app/api/auth/[...all]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Convex auth schema and generated runtime adapter-compatible</name>
  <read_first>
    - convex/schema.ts
    - convex/functions/schema.ts
    - convex/functions/generated/auth.ts
    - node_modules/better-convex/dist/auth/index.d.ts
    - .planning/phases/12-migrate-auth-to-convex/12-RESEARCH.md
  </read_first>
  <files>
    convex/schema.ts
    convex/functions/schema.ts
    convex/functions/auth.ts
    convex/functions/generated/auth.ts
  </files>
  <action>Create `convex/functions/auth.ts` so Better Convex stops exporting a disabled auth runtime. In both schema files, replace the current Prisma-shaped plural auth tables with Better Auth adapter-compatible auth models named `user`, `session`, `account`, and `verification` (not `users`, `sessions`, `accounts`, `verifications`). Preserve the rest of the domain schema unchanged. Include the fields Better Auth needs for email/password auth and session storage, including stable ids, `createdAt`, `updatedAt`, email/session-token lookup support, and the existing app-facing data (`name`, `email`, `emailVerified`, optional `image`, session token, session expiry, optional IP/user agent, provider/account fields, verification identifier/value/expiry). After adding the auth definition file and schema changes, run the Better Convex/Convex codegen flow so `convex/functions/generated/auth.ts` becomes enabled instead of `createDisabledAuthRuntime(...missing_auth_file...)`.</action>
  <acceptance_criteria>
    - `convex/functions/auth.ts` exists and contains `defineAuth`
    - `convex/schema.ts` contains `user: defineTable(`
    - `convex/schema.ts` does not contain `users: defineTable(`
    - `convex/functions/schema.ts` contains `session: defineTable(`
    - `convex/functions/generated/auth.ts` no longer contains `missing_auth_file`
    - `convex/functions/generated/auth.ts` contains `authEnabled`
  </acceptance_criteria>
  <verify>npm run typecheck</verify>
  <done>Convex contains a real Better Auth definition and generated auth runtime wired to adapter-compatible auth tables.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the app auth entrypoint to Convex while preserving the route contract</name>
  <read_first>
    - lib/auth.ts
    - app/api/auth/[...all]/route.ts
    - app/dashboard/layout.tsx
    - app/api/protected/ping/route.ts
    - .planning/phases/12-migrate-auth-to-convex/12-CONTEXT.md
  </read_first>
  <files>
    lib/auth.ts
    app/api/auth/[...all]/route.ts
  </files>
  <action>Rewrite `lib/auth.ts` so it no longer imports `prismaAdapter` or `@/lib/prisma`. Export the Convex-backed Better Auth runtime through the same app-facing `auth` symbol used today, preserving `emailAndPassword.enabled = true`, `requireEmailVerification = false`, `session.expiresIn = 60 * 60 * 24 * 7`, and `session.updateAge = 60 * 60 * 24`. Preserve the existing local-development environment fallback warnings for `BETTER_AUTH_URL` and `BETTER_AUTH_SECRET`, but source the runtime from the Better Convex auth definition created in Task 1. Then update `app/api/auth/[...all]/route.ts` so `/api/auth/[...all]` continues exporting `GET` and `POST` handlers against that Convex-backed runtime without changing the endpoint path or response contract.</action>
  <acceptance_criteria>
    - `lib/auth.ts` does not contain `prismaAdapter`
    - `lib/auth.ts` does not contain `@/lib/prisma`
    - `lib/auth.ts` contains `expiresIn: 60 * 60 * 24 * 7`
    - `lib/auth.ts` contains `updateAge: 60 * 60 * 24`
    - `app/api/auth/[...all]/route.ts` contains `export const { GET, POST }`
    - `app/api/auth/[...all]/route.ts` contains `toNextJsHandler(auth)` or imports a Better Convex-generated handler for the same route
  </acceptance_criteria>
  <verify>npm run build</verify>
  <done>The app exposes the same auth route contract through a Convex-backed Better Auth runtime and `lib/auth.ts` remains the stable import for session lookups.</done>
</task>

</tasks>

<verification>
- Better Convex generated auth runtime is enabled
- `lib/auth.ts` is Prisma-free and still exports the app-facing auth entrypoint
- `/api/auth/[...all]` still exposes `GET` and `POST`
</verification>

<success_criteria>

- Auth data is configured to persist in Convex, not Prisma.
- Existing server code can keep using `auth.api.getSession({ headers })` through `lib/auth.ts`.
- The auth route remains stable for the current login/signup/logout UI.
  </success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-auth-to-convex/12-01-SUMMARY.md`
</output>
