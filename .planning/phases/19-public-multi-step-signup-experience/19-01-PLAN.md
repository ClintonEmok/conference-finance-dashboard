---
phase: 19-public-multi-step-signup-experience
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [USF-04]
files_modified:
  - app/events/[slug]/page.tsx
  - app/signup/[slug]/page.tsx
  - components/signup/SignupFlowShell.tsx
  - components/signup/steps/TicketStep.tsx
  - components/signup/state.ts
autonomous: true
must_haves:
  truths:
    - "Public users can open /events/[slug] and see signup-critical event details plus Start signup CTA."
    - "Public users can enter a 4-step signup shell with linear progression and back/edit support."
    - "Ticket quantities determine attendee-row count and downstream flow state."
    - "In-progress signup draft restores after refresh/back navigation for the same event slug."
  artifacts:
    - path: "app/events/[slug]/page.tsx"
      provides: "Public event entry route with status/CTA and flow preview"
      exports: ["default"]
    - path: "app/signup/[slug]/page.tsx"
      provides: "Public signup route mounting the flow shell"
      exports: ["default"]
    - path: "components/signup/SignupFlowShell.tsx"
      provides: "Step orchestration, linear guards, and draft persistence"
      contains: "stepper + next/back/edit transitions"
    - path: "components/signup/steps/TicketStep.tsx"
      provides: "Ticket quantity controls with disabled reason rendering"
      contains: "availability status + plus/minus quantity"
    - path: "components/signup/state.ts"
      provides: "Typed signup draft state, defaults, and invalidation helpers"
      contains: "deriveAttendeeDraftsFromTicketSelections"
  key_links:
    - from: "app/events/[slug]/page.tsx"
      to: "components/signup/SignupFlowShell.tsx"
      via: "Start signup CTA to /signup/[slug]"
      pattern: "href=\"/signup/"
    - from: "components/signup/SignupFlowShell.tsx"
      to: "lib/convex/hooks/signup.ts"
      via: "usePublicSignupCatalog for event/ticket source"
      pattern: "usePublicSignupCatalog\(\)"
    - from: "components/signup/steps/TicketStep.tsx"
      to: "components/signup/state.ts"
      via: "ticket quantity updates recalculate attendee rows"
      pattern: "deriveAttendeeDraftsFromTicketSelections"
---

<objective>
Create the public entry route and 4-step signup shell foundation so non-admin users can start the guided flow and select tickets with resilient local draft recovery.

Purpose: Establishes the user-facing skeleton that all later rooming/validation/submit work depends on.
Output: Public event page + signup route + typed flow shell + ticket-selection step wired to canonical catalog data.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-CONTEXT.md
@.planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md
@.planning/phases/19-public-multi-step-signup-experience/19-RESEARCH.md
@.planning/phases/18-dual-source-event-signup-platform/18-01-SUMMARY.md
@lib/convex/hooks/signup.ts
@lib/types/signup.ts
@proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build public event entry page and route into signup flow</name>
  <files>app/events/[slug]/page.tsx, app/signup/[slug]/page.tsx</files>
  <read_first>app/page.tsx, app/dashboard/page.tsx, lib/convex/hooks/signup.ts, .planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md</read_first>
  <action>
    Create `app/events/[slug]/page.tsx` as a public client-rendered entry surface that:
    - Resolves the event by slug from `usePublicSignupCatalog()` output.
    - Renders hero basics (title, startsAt/endsAt, location placeholder, short summary fallback), signup status/reason, and a primary `Start signup` CTA.
    - Includes ticket overview cards (name, price, selectable/disabled status reason) without exposing exact remaining counts.
    - Includes accommodation context block and fixed warning copy: `Unassigned beds may be random-filled by another attendee.` when accommodation is in scope.
    - Includes explicit 4-step preview text in order: `Tickets -> Rooms -> Attendee details -> Review & submit`.
    Also create `app/signup/[slug]/page.tsx` mounting `SignupFlowShell` with slug param and guarding unknown slug with a clear not-found state (no redirect loops).
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `app/events/[slug]/page.tsx` contains `Start signup` CTA and `Tickets -> Rooms -> Attendee details -> Review & submit` preview text.
    - Ticket overview rendering includes disabled reason/state copy and does not render exact remaining capacity counts.
    - `app/signup/[slug]/page.tsx` imports and renders `SignupFlowShell` with slug-derived event context.
    - Public routes are introduced under `app/events/[slug]` and `app/signup/[slug]` only (no edits to `proxy.ts` protection matcher).
  </acceptance_criteria>
  <done>Users can start signup from a public per-event page and enter the flow route for a valid slug.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed signup state machine with draft persistence and linear gating</name>
  <files>components/signup/state.ts, components/signup/SignupFlowShell.tsx</files>
  <read_first>app/dashboard/accommodation/inventory/page.tsx, lib/types/signup.ts, .planning/MILESTONE-CONTEXT.md, .planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md</read_first>
  <action>
    Create `components/signup/state.ts` with concrete exported state helpers and types:
    - Step enum/order fixed to `tickets | rooms | attendees | review`.
    - `createInitialSignupDraft(eventId: string, source: "integration" | "internal")`.
    - `deriveAttendeeDraftsFromTicketSelections(ticketSelections)` that deterministically creates attendee rows from quantities.
    - `invalidateDownstreamForTicketChange(...)` and `invalidateDownstreamForRoomChange(...)` to clear dependent confirmations when upstream data mutates.
    Create `components/signup/SignupFlowShell.tsx` that:
    - Shows labeled 4-step progress UI with active/completed states.
    - Enforces linear next-step gating (cannot jump to unfinished step; back/edit allowed).
    - Persists and restores draft in `localStorage` key format `signup-draft:${eventId}`.
    - Rehydrates safely on mount, validates slug/event mismatch before applying stored draft.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `components/signup/state.ts` exports `createInitialSignupDraft`, `deriveAttendeeDraftsFromTicketSelections`, `invalidateDownstreamForTicketChange`, and `invalidateDownstreamForRoomChange`.
    - `components/signup/SignupFlowShell.tsx` contains visible labels for all four steps and tracks active/completed state.
    - Flow shell prevents forward jumps to incomplete steps while preserving back/edit navigation.
    - Draft persistence uses `localStorage` with `signup-draft:${eventId}` key and restores after refresh for matching event.
  </acceptance_criteria>
  <done>Flow shell has deterministic step-state behavior with resilient local draft restore.</done>
</task>

<task type="auto">
  <name>Task 3: Add ticket-selection step with quantity controls and attendee-seed derivation</name>
  <files>components/signup/steps/TicketStep.tsx, components/signup/SignupFlowShell.tsx</files>
  <read_first>lib/types/signup.ts, components/ui/button.tsx, components/ui/card.tsx, components/signup/state.ts</read_first>
  <action>
    Create `components/signup/steps/TicketStep.tsx` and integrate it into `SignupFlowShell` as step 1:
    - Render each ticket type with `label`, EUR price display, status badge, and disabled reason copy when not selectable.
    - Provide explicit `-` and `+` quantity controls with lower bound `0` and upper bound `10` per ticket type.
    - On quantity changes, regenerate attendee row seeds via `deriveAttendeeDraftsFromTicketSelections` and immediately invalidate downstream room/review confirmation state.
    - Block `Next` when total selected quantity is `0`, showing actionable copy: `Select at least one ticket to continue.`
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `TicketStep.tsx` renders ticket status + disabled reason for non-selectable ticket rows.
    - Quantity controls enforce `0..10` bounds and update total attendee count used by shell state.
    - `SignupFlowShell.tsx` uses `deriveAttendeeDraftsFromTicketSelections` after ticket edits.
    - Step-advance guard displays `Select at least one ticket to continue.` when no tickets are selected.
  </acceptance_criteria>
  <done>Ticket step is fully wired and drives downstream attendee/step state deterministically.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- signup`
- Manual dev check: `/events/[slug]` -> `Start signup` -> ticket quantities -> refresh and confirm draft restore.
</verification>

<success_criteria>

- Public event entry page exists and routes into the signup flow for a valid slug.
- Flow shell enforces linear step progression with explicit 4-step progress indicators.
- Ticket selection derives attendee count and invalidates dependent downstream state on edits.
- Draft state survives refresh/back navigation for the same event.
  </success_criteria>

<output>
After completion, create `.planning/phases/19-public-multi-step-signup-experience/19-01-SUMMARY.md`
</output>
