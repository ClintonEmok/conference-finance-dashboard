---
phase: 19-public-multi-step-signup-experience
plan: 02
type: execute
wave: 2
depends_on: [19-01]
requirements: [USF-04, USF-05]
files_modified:
  - components/signup/steps/RoomAssignmentStep.tsx
  - components/signup/assignment.ts
  - components/signup/assignment.test.ts
  - components/signup/SignupFlowShell.tsx
autonomous: true
must_haves:
  truths:
    - "Booker can assign attendee names to bed slots in one continuous flow."
    - "Assignment UI only allows valid assignable slots as drop targets."
    - "Unfilled beds remain visibly warned throughout assignment and before leaving the step."
    - "Users can continue with open beds only after explicit random-fill acknowledgement."
  artifacts:
    - path: "components/signup/steps/RoomAssignmentStep.tsx"
      provides: "Room-step drag/drop bed mapping interaction"
      contains: "draggable attendees + assignable bed slot targets"
    - path: "components/signup/assignment.ts"
      provides: "Pure assignment helpers for valid target filtering and warning metrics"
      contains: "getAssignableSlotTargets + summarizeUnfilledBeds"
    - path: "components/signup/assignment.test.ts"
      provides: "Regression tests for assignment helper behavior"
      contains: "valid target and unfilled-bed warning assertions"
    - path: "components/signup/SignupFlowShell.tsx"
      provides: "Room-step integration and gating/acknowledgement enforcement"
      contains: "cannot continue with unfilled beds unless acknowledged"
  key_links:
    - from: "components/signup/steps/RoomAssignmentStep.tsx"
      to: "components/signup/assignment.ts"
      via: "valid slot target computation"
      pattern: "getAssignableSlotTargets"
    - from: "components/signup/SignupFlowShell.tsx"
      to: "components/signup/steps/RoomAssignmentStep.tsx"
      via: "rooms step render + onAssignmentChange"
      pattern: "RoomAssignmentStep"
    - from: "components/signup/SignupFlowShell.tsx"
      to: "step transition guard"
      via: "requires random-fill acknowledgment for open beds"
      pattern: "acknowledgeRandomFill"
---

<objective>
Implement the accommodation assignment step so family/group bed mapping works in-flow with explicit random-fill risk handling.

Purpose: Delivers the core booker-managed room assignment behavior required for Phase 19.
Output: Drag/drop room assignment UI + validity guards + unfilled-bed warning/acknowledgement flow.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-CONTEXT.md
@.planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md
@.planning/phases/19-public-multi-step-signup-experience/19-RESEARCH.md
@.planning/phases/19-public-multi-step-signup-experience/19-01-SUMMARY.md
@.planning/phases/18-dual-source-event-signup-platform/18-01-SUMMARY.md
@lib/types/signup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic room-assignment helpers and tests</name>
  <files>components/signup/assignment.ts, components/signup/assignment.test.ts</files>
  <read_first>lib/types/signup.ts, components/signup/state.ts, .planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md</read_first>
  <action>
    Create pure helper module `components/signup/assignment.ts` with explicit exports:
    - `getAssignableSlotTargets(catalogEvent)` returning only slots where `isAssignable === true`.
    - `buildAssignmentBoard(attendees, slots, assignments)` producing stable row objects for rendering.
    - `summarizeUnfilledBeds(board)` returning `{ totalBeds, filledBeds, unfilledBeds }`.
    - `canDropAttendeeIntoSlot(attendeeId, slotId, board)` enforcing one-attendee-per-slot and one-slot-per-attendee.
    Add `components/signup/assignment.test.ts` covering:
    - Non-assignable slots excluded from target list.
    - Duplicate attendee or duplicate slot mapping rejected.
    - Unfilled bed summary metrics remain correct after assign/unassign operations.
  </action>
  <verify>npm run test -- signup-flow</verify>
  <acceptance_criteria>
    - `components/signup/assignment.ts` exports all four helpers named in this task.
    - Tests include assertions for invalid drop scenarios (duplicate attendee and duplicate slot).
    - Test suite validates unfilled-bed summary output using concrete expected counts.
  </acceptance_criteria>
  <done>Assignment logic is deterministic, test-backed, and reusable by the room-step UI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement drag/drop room assignment step with persistent warning and acknowledgment gate</name>
  <files>components/signup/steps/RoomAssignmentStep.tsx, components/signup/SignupFlowShell.tsx</files>
  <read_first>components/signup/SignupFlowShell.tsx, components/ui/card.tsx, components/ui/badge.tsx, components/signup/assignment.ts</read_first>
  <action>
    Create `components/signup/steps/RoomAssignmentStep.tsx` and wire as step 2 in `SignupFlowShell`:
    - Render attendee chips/cards as draggable items and bed slots as drop targets (HTML5 drag events are acceptable; do not add new dependencies).
    - Only render assignable targets from `getAssignableSlotTargets`; show non-assignable slots as non-interactive informational rows.
    - Keep a persistent warning banner in the step body whenever `unfilledBeds > 0` with exact copy: `Open beds may be random-filled by another attendee.`
    - Add explicit acknowledgment checkbox state `acknowledgeRandomFill` required when `unfilledBeds > 0`.
    - Prevent transition to next step until either all beds are assigned OR acknowledgment is checked.
    - When ticket quantities change upstream, clear assignments and reset acknowledgment via downstream invalidation helper.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `RoomAssignmentStep.tsx` implements draggable attendees and drop targets with assignment updates.
    - Persistent warning copy `Open beds may be random-filled by another attendee.` is present when unfilled beds exist.
    - `SignupFlowShell.tsx` blocks step transition when `unfilledBeds > 0 && !acknowledgeRandomFill`.
    - Upstream ticket edits clear existing assignments and reset room-step acknowledgment state.
  </acceptance_criteria>
  <done>Booker-managed room assignment works in-flow with explicit random-fill risk confirmation.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- signup-flow`
- Manual dev check: assign/unassign attendees, verify warning persistence and acknowledgment gate behavior.
</verification>

<success_criteria>

- USF-04 satisfied: users can map attendees to beds in one submission flow.
- USF-05 satisfied: unassigned/open beds are always visible and require explicit acknowledgment before continuing.
- Invalid slot targets are not actionable in the primary assignment interaction.
  </success_criteria>

<output>
After completion, create `.planning/phases/19-public-multi-step-signup-experience/19-02-SUMMARY.md`
</output>
