---
phase: 19-public-multi-step-signup-experience
plan: 03
type: execute
wave: 3
depends_on: [19-02]
requirements: [RMD-01, RMD-02, RMD-03]
files_modified:
  - components/signup/steps/AttendeeDetailsStep.tsx
  - components/signup/steps/ReviewSubmitStep.tsx
  - components/signup/submission-client.ts
  - components/signup/submission-client.test.ts
  - components/signup/SignupFlowShell.tsx
autonomous: true
must_haves:
  truths:
    - "Each attendee captures required rooming fields before submission can proceed."
    - "Validation errors are visible both inline and in step-level summary form."
    - "Review step submits through /api/signup/submit and handles known conflict/error contracts."
    - "When restore payload is returned, user must choose continue prior submission vs edit/update path."
    - "Successful submit shows booking reference, submission recap, and next-step messaging."
  artifacts:
    - path: "components/signup/steps/AttendeeDetailsStep.tsx"
      provides: "Required attendee rooming form fields + validation UI"
      contains: "gender/location/dietary/roommate/phone inputs"
    - path: "components/signup/steps/ReviewSubmitStep.tsx"
      provides: "Review, submit, restore-choice, and success state UX"
      contains: "bookingRef + submittedAt confirmation"
    - path: "components/signup/submission-client.ts"
      provides: "Typed submit helper mapping route responses and error contracts"
      contains: "POST /api/signup/submit with x-idempotency-key"
    - path: "components/signup/submission-client.test.ts"
      provides: "Submit helper regression tests for success, restore payload, and known errors"
      contains: "CAPACITY_EXCEEDED/TICKET_UNAVAILABLE/ASSIGNMENT_UNAVAILABLE/SUBMISSION_CONFLICT mapping"
    - path: "components/signup/SignupFlowShell.tsx"
      provides: "Integration of attendee details and review/submit steps with transition guards"
      contains: "final payload build from draft state"
  key_links:
    - from: "components/signup/submission-client.ts"
      to: "app/api/signup/submit/route.ts"
      via: "fetch POST"
      pattern: "fetch\(\"/api/signup/submit\""
    - from: "components/signup/steps/AttendeeDetailsStep.tsx"
      to: "components/signup/SignupFlowShell.tsx"
      via: "field-level + summary validation before review"
      pattern: "validationSummary"
    - from: "components/signup/steps/ReviewSubmitStep.tsx"
      to: "restore payload branch"
      via: "continue prior vs edit current decision UI"
      pattern: "restorePayload"
---

<objective>
Deliver attendee details, validation, review, and submission UX so the full public 4-step flow can complete with clear recovery and confirmation states.

Purpose: Completes Phase 19 rooming-data correctness and submit experience requirements.
Output: Attendee details step + review/submit step + typed submit client + restore-choice and success confirmation UX.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-public-multi-step-signup-experience/19-CONTEXT.md
@.planning/phases/19-public-multi-step-signup-experience/19-RESEARCH.md
@.planning/phases/19-public-multi-step-signup-experience/19-02-SUMMARY.md
@.planning/phases/18-dual-source-event-signup-platform/18-02-SUMMARY.md
@.planning/phases/18-dual-source-event-signup-platform/18-03-SUMMARY.md
@lib/types/signup.ts
@app/api/signup/submit/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement attendee details step with dual-surface validation</name>
  <files>components/signup/steps/AttendeeDetailsStep.tsx, components/signup/SignupFlowShell.tsx</files>
  <read_first>components/signup/SignupFlowShell.tsx, components/ui/input.tsx, components/ui/label.tsx, .planning/MILESTONE-CONTEXT.md</read_first>
  <action>
    Create `components/signup/steps/AttendeeDetailsStep.tsx` and integrate as step 3:
    - Render per-attendee fields: `name`, `phone`, `gender`, `location`, `dietaryRestrictions`, `roommatePreference`, `roommateAvoid`, optional `email`.
    - Enforce required validations for `phone`, `gender`, `location`, `dietaryRestrictions`, `roommatePreference`, and `roommateAvoid` before step transition.
    - Show inline field errors and a top summary block listing attendee index + missing fields.
    - Keep roommate request support bi-directional: positive preference (`roommatePreference`) and soft exclusion (`roommateAvoid`).
    - Run the same validation function on transition into review and again immediately before final submit.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `AttendeeDetailsStep.tsx` includes all required rooming inputs and persists values per attendee row.
    - Validation summary block is rendered when required fields are missing and identifies attendee rows with missing data.
    - Inline error rendering exists for each required field.
    - `SignupFlowShell.tsx` prevents move to review when attendee validation fails.
  </acceptance_criteria>
  <done>Attendee rooming data is fully captured and validation blocks incomplete submissions.</done>
</task>

<task type="auto">
  <name>Task 2: Add review/submit step with restore-choice handling and success confirmation</name>
  <files>components/signup/steps/ReviewSubmitStep.tsx, components/signup/submission-client.ts, components/signup/SignupFlowShell.tsx</files>
  <read_first>app/api/signup/submit/route.ts, lib/types/signup.ts, components/signup/state.ts, components/ui/dialog.tsx</read_first>
  <action>
    Create `components/signup/submission-client.ts` with typed submit helper:
    - Builds payload matching `SignupSubmissionEnvelope` contract from current draft.
    - Sends `POST /api/signup/submit` with `Content-Type: application/json` and `x-idempotency-key` header.
    - Maps known response errors (`INVALID_SUBMISSION`, `CAPACITY_EXCEEDED`, `TICKET_UNAVAILABLE`, `ASSIGNMENT_UNAVAILABLE`, `SUBMISSION_CONFLICT`, `RATE_LIMITED`, `HONEYPOT_TRIGGERED`) into typed UI-safe objects.
    Create `components/signup/steps/ReviewSubmitStep.tsx` and integrate as step 4:
    - Render final review sections (booker, tickets, assignments, attendee details, notes).
    - On restore payload response, present explicit user decision controls: `Continue previous submission` and `Edit current details`.
    - On success, render confirmation state with `bookingRef`, `submittedAt`, attendee count, and next-step copy.
    - Keep submit button idempotent in UI (`isSubmitting` lock + no double-fire).
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `submission-client.ts` posts to `/api/signup/submit` and sets `x-idempotency-key` header.
    - Review step displays both restore-choice actions when `restorePayload` exists.
    - Success view includes `bookingRef` and `submittedAt` values from API response.
    - Duplicate submit click while pending is prevented by disabled/loading submit control.
  </acceptance_criteria>
  <done>Flow can submit end-to-end with clear replay recovery and success confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: Add submit-client regression tests for route contract handling</name>
  <files>components/signup/submission-client.test.ts</files>
  <read_first>app/api/signup/submit/route.test.ts, lib/types/signup.ts, components/signup/submission-client.ts</read_first>
  <action>
    Add `components/signup/submission-client.test.ts` Vitest coverage for:
    - Successful submit returns `submissionId`, `bookingRef`, `submittedAt`.
    - Restore-payload response exposes restore branch metadata to UI.
    - Conflict/error code mapping for `CAPACITY_EXCEEDED`, `TICKET_UNAVAILABLE`, `ASSIGNMENT_UNAVAILABLE`, `SUBMISSION_CONFLICT`.
    - Validation and abuse error mapping for `INVALID_SUBMISSION`, `RATE_LIMITED`, `HONEYPOT_TRIGGERED`.
    - Unknown error fallback maps to `SUBMISSION_FAILED` with safe message.
  </action>
  <verify>npm run test -- signup-flow</verify>
  <acceptance_criteria>
    - Test file asserts all named known codes map to deterministic client error objects.
    - Success and restore-payload cases are both covered.
    - Unknown/unexpected payloads fall back to `SUBMISSION_FAILED` behavior in tests.
  </acceptance_criteria>
  <done>Submission client behavior is test-locked against route contract regressions.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- signup-flow`
- Manual dev check: complete all 4 steps, submit, verify booking confirmation and restore-choice UX.
</verification>

<success_criteria>

- RMD-01 and RMD-02 satisfied: required attendee rooming data and roommate preference/avoid notes are captured per attendee.
- RMD-03 satisfied: submission is blocked until required attendee fields are complete.
- Review/submit UX handles success, conflicts, and restore-payload decisions explicitly.
- Public flow now runs end-to-end from ticket selection through submission confirmation.
  </success_criteria>

<output>
After completion, create `.planning/phases/19-public-multi-step-signup-experience/19-03-SUMMARY.md`
</output>
