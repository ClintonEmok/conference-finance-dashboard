---
phase: 27-event-scoped-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/page.tsx
  - app/dashboard/events/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /dashboard after login lands admins on the event chooser instead of the broad KPI dashboard."
    - "Admins can open an existing event from the chooser and reach /dashboard/events/[slug]."
    - "Creating a new event stays one click away from the chooser."
    - "The chooser makes it obvious which events are published versus draft."
  artifacts:
    - path: app/dashboard/page.tsx
      provides: "Dashboard landing redirect to the event chooser"
      contains: 'redirect("/dashboard/events")'
    - path: app/dashboard/events/page.tsx
      provides: "Primary event-picker surface"
      contains: "Choose an event"
  key_links:
    - from: app/dashboard/page.tsx
      to: /dashboard/events
      via: "server redirect from the dashboard root"
      pattern: 'redirect\("/dashboard/events"\)'
    - from: app/dashboard/events/page.tsx
      to: /dashboard/events/[slug]
      via: "row/card navigation when an event is selected"
      pattern: '/dashboard/events/\${event\.slug}'
    - from: app/dashboard/events/page.tsx
      to: /dashboard/events/new
      via: "first-class new-event CTA"
      pattern: '/dashboard/events/new'
---

<objective>
Make /dashboard land on the event chooser so signed-in admins pick an event before entering any scoped dashboard work.

Purpose: The current dashboard root still behaves like a broad finance command center. Phase 27 needs the first screen after login to be event-first, with a clear path into an existing event or the new-event flow.
Output: A dashboard root that redirects into the chooser and a chooser page that acts as the primary event entry point.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS-UX-DEVEX.md
@app/dashboard/page.tsx
@app/dashboard/events/page.tsx
@app/dashboard/events/new/page.tsx
@app/dashboard/events/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the dashboard root into an event-first entry point</name>
  <files>app/dashboard/page.tsx</files>
  <read_first>
    - app/dashboard/page.tsx
    - app/dashboard/layout.tsx
    - app/dashboard/events/page.tsx
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <action>
    Replace the current client dashboard analytics surface with a server-only landing gate that immediately sends authenticated users to /dashboard/events using Next.js redirect(). Remove the current KPI fetches, filters, and quick-action content from this route so /dashboard is no longer the default broad operations view. Keep the auth-protected dashboard layout intact; only the landing behavior changes.
  </action>
  <acceptance_criteria>`app/dashboard/page.tsx` contains `redirect("/dashboard/events")`; `app/dashboard/page.tsx` does not contain `use client`; `app/dashboard/page.tsx` does not contain `fetch(\`/api/dashboard/revenue`</acceptance_criteria>
  <verify>Read `app/dashboard/page.tsx` and confirm the file is a redirect-only landing route; run `npm run typecheck`.</verify>
  <done>Admins no longer see the broad finance command center at /dashboard; they land in the event chooser instead.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the event list into the primary chooser</name>
  <files>app/dashboard/events/page.tsx</files>
  <read_first>
    - app/dashboard/events/page.tsx
    - app/dashboard/events/new/page.tsx
    - app/dashboard/events/[slug]/page.tsx
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
    Rework the chooser page copy and actions so it reads like an explicit selection surface: title it `Choose an event`, keep the internal-events filter and search/status filtering, and make the two primary actions `Open event` and `New event`. Each event row/card should route to `/dashboard/events/${event.slug}`, published events should still expose the public URLs, and the empty state should keep pointing people to the new-event flow instead of feeling like a dead end.
  </action>
  <acceptance_criteria>`app/dashboard/events/page.tsx` contains `Choose an event`; `app/dashboard/events/page.tsx` contains `Open event`; `app/dashboard/events/page.tsx` contains `/dashboard/events/new`; `app/dashboard/events/page.tsx` contains `/dashboard/events/${event.slug}`</acceptance_criteria>
  <verify>Read `app/dashboard/events/page.tsx` and confirm the chooser language and event-scoped links are present; run `npm run typecheck`.</verify>
  <done>The chooser page clearly supports opening an existing event or starting a new one without sending users back to the broad dashboard.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm `/dashboard` is a redirect-only route.
- Confirm the chooser page contains `Choose an event`, `Open event`, and `/dashboard/events/new`.
</verification>

<success_criteria>
- `/dashboard` no longer renders the broad command-center dashboard.
- Signed-in admins land on the event chooser by default.
- Existing events and new-event creation are the two obvious chooser actions.
  </success_criteria>

<output>
After completion, create `.planning/phases/27-event-scoped-dashboard/27-01-SUMMARY.md`
</output>
