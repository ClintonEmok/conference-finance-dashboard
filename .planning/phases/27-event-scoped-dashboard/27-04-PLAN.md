---
phase: 27-event-scoped-dashboard
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/dashboard-shell.tsx
  - app/dashboard/events/[slug]/layout.tsx
gap_closure: true
autonomous: false

must_haves:
  truths:
    - "The global dashboard sidebar no longer reads like a broad command center."
    - "The event-scoped sidebar contains only the shared event switcher."
    - "Event context and section navigation remain visible somewhere other than the sidebar."
    - "Switching events still preserves slug-based URL scope."
  artifacts:
    - path: app/dashboard/dashboard-shell.tsx
      provides: "Slim global dashboard shell"
      contains: "EventSwitcher"
    - path: app/dashboard/events/[slug]/layout.tsx
      provides: "Event-scoped layout with switcher-only sidebar"
      contains: "EventSwitcher"
  key_links:
    - from: app/dashboard/dashboard-shell.tsx
      to: app/dashboard/events/page.tsx
      via: "chooser shortcut remains lightweight and secondary"
      pattern: "/dashboard/events"
    - from: app/dashboard/events/[slug]/layout.tsx
      to: app/dashboard/events/[slug]
      via: "route slug continues to drive active event context"
      pattern: "/dashboard/events/${slug}"
    - from: app/dashboard/events/[slug]/layout.tsx
      to: components/dashboard/event-switcher.tsx
      via: "shared switcher remains the only sidebar control"
      pattern: "EventSwitcher"
---

<objective>
Reduce the dashboard chrome so the sidebar only does one job: switching events.

Purpose: The event-scoped dashboard still feels like a multi-section workspace because the sidebar carries active-event details and section navigation. This plan moves that extra chrome out of the sidebar so the event switcher is the only sidebar control left.
Output: A slim global shell and a switcher-only event-scoped sidebar with the remaining context relocated into the main content area.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-event-scoped-dashboard/27-UAT.md
@.planning/phases/27-event-scoped-dashboard/27-CONTEXT.md
@.planning/phases/27-event-scoped-dashboard/27-PLAN-CHECK.md
@.planning/phases/27-event-scoped-dashboard/27-01-SUMMARY.md
@.planning/phases/27-event-scoped-dashboard/27-02-SUMMARY.md
@app/dashboard/dashboard-shell.tsx
@app/dashboard/events/[slug]/layout.tsx
@components/dashboard/event-switcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim the global dashboard shell sidebar</name>
  <files>app/dashboard/dashboard-shell.tsx</files>
  <read_first>
    - app/dashboard/dashboard-shell.tsx
    - components/dashboard/event-switcher.tsx
    - app/dashboard/layout.tsx
    - .planning/phases/27-event-scoped-dashboard/27-PLAN-CHECK.md
  </read_first>
  <action>
    Remove the broad command-center feel from the global sidebar. Keep the shared event switcher prominent, preserve breadcrumbs and logout, and strip out the old Overview/Finance/Operations-style groups and footer chrome that compete with event switching. Any remaining global links should be low-emphasis utilities, not primary navigation.
  </action>
  <acceptance_criteria>`app/dashboard/dashboard-shell.tsx` contains `EventSwitcher`; `app/dashboard/dashboard-shell.tsx` no longer presents the old broad primary nav groups; `app/dashboard/dashboard-shell.tsx` still contains `LogoutButton`</acceptance_criteria>
  <verify>Run `npm run typecheck`, then inspect the shell to confirm the sidebar reads as lightweight utility chrome.</verify>
  <done>The global shell no longer frames the app as a broad finance console.</done>
</task>

<task type="auto">
  <name>Task 2: Move event details and navigation out of the event sidebar</name>
  <files>app/dashboard/events/[slug]/layout.tsx</files>
  <read_first>
    - app/dashboard/events/[slug]/layout.tsx
    - components/dashboard/event-switcher.tsx
    - app/dashboard/events/[slug]/page.tsx
    - app/dashboard/events/[slug]/overview/page.tsx
  </read_first>
  <action>
    Replace the existing sidebar card/nav stack with just the shared `EventSwitcher`. Move the active-event summary, public-page link, back-to-chooser link, and section navigation into a top-of-page header or inline chrome above the content column so the sidebar itself contains only switcher UI. Keep the selected event explicit and preserve slug-based navigation.
  </action>
  <acceptance_criteria>`app/dashboard/events/[slug]/layout.tsx` contains `EventSwitcher`; `app/dashboard/events/[slug]/layout.tsx` no longer contains the section nav stack in the sidebar; `app/dashboard/events/[slug]/layout.tsx` still contains `View public page` somewhere in the layout</acceptance_criteria>
  <verify>Run `npm run typecheck`, then inspect the scoped layout to confirm the sidebar is switcher-only and the rest of the event context moved elsewhere.</verify>
  <done>The event-scoped sidebar contains only the switcher while the remaining event context stays visible outside the sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Switcher-only sidebars in global and event-scoped dashboard chrome.</what-built>
  <how-to-verify>
    1. Sign in and open any `/dashboard/events/[slug]` route.
    2. Confirm the sidebar contains only the event switcher, not active-event details or section navigation.
    3. Confirm the moved event context/public-page link still appears above the main content.
    4. Open a non-event dashboard route and confirm the shell still feels lightweight.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm the event-scoped sidebar contains only the switcher.
- Confirm the global shell no longer reads like a broad command center.
</verification>

<success_criteria>
- The event-scoped sidebar is switcher-only.
- Remaining event details/navigation are still visible outside the sidebar.
- The global dashboard chrome stays lightweight and event-first.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-scoped-dashboard/27-04-SUMMARY.md`
</output>
