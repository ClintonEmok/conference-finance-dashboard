---
phase: 27-event-scoped-dashboard
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/layout.tsx
  - app/dashboard/dashboard-surface.tsx
  - app/dashboard/loading.tsx
gap_closure: true
autonomous: false

must_haves:
  truths:
    - "After login, chooser routes render fullscreen without the persistent dashboard shell chrome."
    - "Switching events shows a viewport-filling loading state instead of a shell-shaped skeleton."
    - "Chooser-first routing stays intact: /dashboard still bridges into /dashboard/events."
    - "Slug-scoped event pages still use the dashboard shell."
  artifacts:
    - path: app/dashboard/layout.tsx
      provides: "Auth gate plus route-aware dashboard surface boundary"
      contains: "DashboardSurface"
    - path: app/dashboard/dashboard-surface.tsx
      provides: "Client route gate that decides whether to show DashboardShell"
      contains: "usePathname"
    - path: app/dashboard/loading.tsx
      provides: "Full-viewport dashboard transition/loading screen"
      contains: "min-h-screen"
  key_links:
    - from: app/dashboard/layout.tsx
      to: app/dashboard/dashboard-surface.tsx
      via: "server layout delegates chrome decisions to a client route gate"
      pattern: "DashboardSurface"
    - from: app/dashboard/dashboard-surface.tsx
      to: app/dashboard/dashboard-shell.tsx
      via: "pathname-aware conditional shell wrapper"
      pattern: "DashboardShell"
    - from: app/dashboard/loading.tsx
      to: dashboard route transitions
      via: "viewport-filling loading UI during event switches"
      pattern: "min-h-screen"
---

<objective>
Make the chooser feel like a fullscreen entry screen after login and make event switching feel like a fullscreen route transition, without breaking the chooser-first URL contract.

Purpose: The current chooser is technically correct but still feels nested inside dashboard chrome, and event switching only exposes a shell-shaped loading state. This plan removes that feeling mismatch while keeping the existing `/dashboard -> /dashboard/events` handoff.
Output: A route-aware dashboard surface and a fullscreen loading state for chooser/event transitions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-event-scoped-dashboard/27-UAT.md
@.planning/phases/27-event-scoped-dashboard/27-CONTEXT.md
@.planning/phases/27-event-scoped-dashboard/27-PLAN-CHECK.md
@.planning/phases/27-event-scoped-dashboard/27-01-SUMMARY.md
@.planning/phases/27-event-scoped-dashboard/27-02-SUMMARY.md
@app/dashboard/layout.tsx
@app/dashboard/page.tsx
@app/dashboard/loading.tsx
@app/dashboard/events/page.tsx
@app/dashboard/events/[slug]/layout.tsx
@app/dashboard/dashboard-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a route-aware dashboard surface</name>
  <files>app/dashboard/layout.tsx, app/dashboard/dashboard-surface.tsx</files>
  <read_first>
    - app/dashboard/layout.tsx
    - app/dashboard/dashboard-shell.tsx
    - app/dashboard/events/page.tsx
    - app/dashboard/events/[slug]/layout.tsx
    - .planning/phases/27-event-scoped-dashboard/27-UAT.md
  </read_first>
  <action>
    Keep the server layout as the auth gate, then introduce a client dashboard surface that uses `usePathname()` to decide when the persistent `DashboardShell` should render. Treat `/dashboard`, `/dashboard/events`, and `/dashboard/events/new` as fullscreen chooser/entry routes, and render the shell only for slug-scoped event pages and their descendants. Preserve the chooser-first routing contract; do not move the chooser off `/dashboard/events`.
  </action>
  <acceptance_criteria>`app/dashboard/layout.tsx` contains `DashboardSurface`; `app/dashboard/dashboard-surface.tsx` contains `usePathname`; `app/dashboard/dashboard-surface.tsx` contains `DashboardShell`</acceptance_criteria>
  <verify>Run `npm run typecheck`, then inspect the two files to confirm chooser routes bypass the shell while slug-scoped event routes still use it.</verify>
  <done>Chooser routes can render fullscreen after login, but event-scoped pages still retain the dashboard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the shell-shaped loading state</name>
  <files>app/dashboard/loading.tsx</files>
  <read_first>
    - app/dashboard/loading.tsx
    - app/dashboard/events/page.tsx
    - app/dashboard/events/[slug]/layout.tsx
  </read_first>
  <action>
    Rewrite the dashboard loading UI so it fills the viewport and reads like a route transition screen, not a card/table skeleton inside dashboard chrome. Keep the visual language subtle, but make the loading state strong enough to cover chooser entry and event-to-event switching without exposing the shell shape underneath.
  </action>
  <acceptance_criteria>`app/dashboard/loading.tsx` contains `min-h-screen`; `app/dashboard/loading.tsx` no longer resembles the old shell/table skeleton</acceptance_criteria>
  <verify>Run `npm run typecheck`, then navigate through `/dashboard/events` and an event switch to confirm the loading state fills the screen.</verify>
  <done>Event switching now shows a fullscreen loading experience instead of a shell-shaped placeholder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fullscreen chooser entry and fullscreen dashboard loading transitions.</what-built>
  <how-to-verify>
    1. Sign in and visit `/dashboard`.
    2. Confirm you land on `/dashboard/events` with no persistent dashboard shell around the chooser.
    3. Switch to another event and confirm the loading state covers the viewport during navigation.
    4. Confirm `/dashboard/events/[slug]` still renders inside the event-scoped shell.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm chooser routes render without the persistent dashboard shell.
- Confirm event switching shows a viewport-filling loading screen.
</verification>

<success_criteria>
- `/dashboard` still bridges into the chooser, but the chooser itself feels fullscreen.
- Switching between event pages shows a fullscreen loading experience.
- The chooser-first routing contract remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-scoped-dashboard/27-03-SUMMARY.md`
</output>
