---
phase: 27-event-scoped-dashboard
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/event-switcher.tsx
  - app/dashboard/dashboard-shell.tsx
  - app/dashboard/events/[slug]/layout.tsx
autonomous: true

must_haves:
  truths:
    - "The global shell makes event switching obvious without looking like a day-to-day event workspace."
    - "Scoped event pages show the active event and let admins switch events without losing URL scope."
    - "Event changes use the event slug in the URL so refreshes stay scoped."
  artifacts:
    - path: components/dashboard/event-switcher.tsx
      provides: "Shared active-event picker for dashboard chrome"
      contains: "useEvents"
    - path: app/dashboard/dashboard-shell.tsx
      provides: "Slimmed global dashboard shell with top-level nav and switcher"
      contains: "EventSwitcher"
    - path: app/dashboard/events/[slug]/layout.tsx
      provides: "Scoped event layout that reuses the shared switcher"
      contains: "EventSwitcher"
  key_links:
    - from: components/dashboard/event-switcher.tsx
      to: /dashboard/events/[slug]
      via: "router navigation when an event is selected"
      pattern: 'router\.push\(`/dashboard/events/\$\{slug\}`\)'
    - from: components/dashboard/event-switcher.tsx
      to: /dashboard/events
      via: "chooser navigation when no event is selected"
      pattern: '/dashboard/events'
    - from: app/dashboard/events/[slug]/layout.tsx
      to: components/dashboard/event-switcher.tsx
      via: "shared chrome component"
      pattern: 'EventSwitcher'
---

<objective>
Make the dashboard chrome event-first: a shared switcher should be visible from both the global shell and scoped event pages, and switching events should preserve the slug-based URL contract.

Purpose: Once the chooser sends admins into a specific event, the surrounding shell should reinforce that scope instead of pushing them back into a broad, day-to-day dashboard. A shared switcher keeps the event context consistent across the app.
Output: A reusable event switcher plus shell/layout wiring that keeps the active event obvious.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS-UX-DEVEX.md
@components/dashboard/nav-breadcrumbs.tsx
@lib/convex/hooks/events.ts
@app/dashboard/dashboard-shell.tsx
@app/dashboard/events/[slug]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a shared dashboard event switcher</name>
  <files>components/dashboard/event-switcher.tsx</files>
  <read_first>
    - components/dashboard/nav-breadcrumbs.tsx
    - lib/convex/hooks/events.ts
    - app/dashboard/events/[slug]/layout.tsx
    - app/dashboard/dashboard-shell.tsx
  </read_first>
  <action>
    Create a client-side `EventSwitcher` component that loads the available events with `useEvents()`, accepts a `currentSlug` prop so the active event is explicit, and navigates with `router.push()` to either `/dashboard/events/${slug}` for a selection or `/dashboard/events` for the chooser. The global shell should derive the current slug from `usePathname()` and pass it in; the event-scoped layout should pass its route slug directly. Keep the control compact enough to fit inside dashboard chrome; this is navigation, not a data-heavy event workspace.
  </action>
  <acceptance_criteria>`components/dashboard/event-switcher.tsx` contains `useEvents`; `components/dashboard/event-switcher.tsx` contains `router.push`; `components/dashboard/event-switcher.tsx` contains `/dashboard/events/${`; `components/dashboard/event-switcher.tsx` contains `/dashboard/events`</acceptance_criteria>
  <verify>Read the new component and confirm it only handles switching/navigation; run `npm run typecheck`.</verify>
  <done>A reusable event switcher exists and can route between the chooser and any event-scoped dashboard page.</done>
</task>

<task type="auto">
  <name>Task 2: Put the shared switcher into the global dashboard shell</name>
  <files>app/dashboard/dashboard-shell.tsx</files>
  <read_first>
    - app/dashboard/dashboard-shell.tsx
    - components/dashboard/event-switcher.tsx
    - components/dashboard/nav-breadcrumbs.tsx
    - app/dashboard/layout.tsx
  </read_first>
  <action>
    Refactor the global dashboard shell so the new event switcher is inserted at the top of the sidebar, above any other navigation. Remove the old Overview group entirely, keep only a low-emphasis Events/chooser shortcut plus utility sections, and hide Finance and Operations from the primary nav so the shell no longer reads like a broad command center. Keep the existing breadcrumb and logout affordances, but make the shell feel like a light wrapper around event switching and utilities rather than day-to-day event work.
  </action>
  <acceptance_criteria>`app/dashboard/dashboard-shell.tsx` contains `EventSwitcher`; `app/dashboard/dashboard-shell.tsx` contains `NavBreadcrumbs`; `app/dashboard/dashboard-shell.tsx` still contains `LogoutButton`</acceptance_criteria>
  <verify>Read `app/dashboard/dashboard-shell.tsx` and confirm the switcher is wired into the chrome; run `npm run typecheck`.</verify>
  <done>The global shell surfaces event switching first and no longer frames the dashboard as a broad command center.</done>
</task>

<task type="auto">
  <name>Task 3: Reuse the switcher in event-scoped layouts</name>
  <files>app/dashboard/events/[slug]/layout.tsx</files>
  <read_first>
    - app/dashboard/events/[slug]/layout.tsx
    - components/dashboard/event-switcher.tsx
    - app/dashboard/events/[slug]/page.tsx
    - app/dashboard/events/[slug]/overview/page.tsx
  </read_first>
  <action>
    Replace the fixed active-event card in the event-scoped layout with the shared event switcher and a compact status block. Pass the route `slug` directly into the switcher so the active event is obvious without extra guessing. Keep the public-page link and the event-specific sub-navigation, but make switching to another event or back to the chooser feel like a first-class action inside scoped pages rather than a detour through the global dashboard.
  </action>
  <acceptance_criteria>`app/dashboard/events/[slug]/layout.tsx` contains `EventSwitcher`; `app/dashboard/events/[slug]/layout.tsx` contains `href="/dashboard/events"`; `app/dashboard/events/[slug]/layout.tsx` contains `View public page`</acceptance_criteria>
  <verify>Read `app/dashboard/events/[slug]/layout.tsx` and confirm the shared switcher is used in scoped chrome; run `npm run typecheck`.</verify>
  <done>Scoped event pages keep the active event visible and let admins change scope without leaving the event workspace.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Confirm the shared event switcher exists and is used in both the global shell and the event-scoped layout.
- Confirm the switcher routes to `/dashboard/events` and `/dashboard/events/[slug]`.
</verification>

<success_criteria>
- A shared event switcher exists for both global and scoped dashboard chrome.
- Event-scoped pages keep the active event visible and switchable.
- The shell reads as event-first navigation, not as a broad finance command center.
  </success_criteria>

<output>
After completion, create `.planning/phases/27-event-scoped-dashboard/27-02-SUMMARY.md`
</output>
