---
phase: 29-deterministic-money-model
plan: "03"
type: execute
wave: 2
depends_on: ["29-01"]
requirements: [FIN-03]
files_modified:
  - app/dashboard/events/[slug]/reconciliation/page.tsx
  - app/dashboard/reconciliation/page.tsx
autonomous: true
must_haves:
  truths:
    - "The reconciliation workspace is available at the slug-scoped event route."
    - "Unmatched payments and outstanding orders are visible side by side on the same screen."
    - "An operator can select one payment and one order, confirm Match payment, and create the assignment record."
    - "Successful matching removes the matched rows from the active lists and preserves filters/event context."
  artifacts:
    - path: app/dashboard/events/[slug]/reconciliation/page.tsx
      provides: slug-scoped side-by-side reconciliation workspace
    - path: app/dashboard/reconciliation/page.tsx
      provides: legacy bridge redirect into the canonical reconciliation route
  key_links:
    - from: app/dashboard/events/[slug]/reconciliation/page.tsx
      to: app/api/payments/route.ts
      via: load unmatched payment rows
      pattern: /api/payments
    - from: app/dashboard/events/[slug]/reconciliation/page.tsx
      to: app/api/dashboard/reconciliation/route.ts
      via: load outstanding order rows
      pattern: /api/dashboard/reconciliation
    - from: app/dashboard/events/[slug]/reconciliation/page.tsx
      to: app/api/payments/[id]/assign/route.ts
      via: PATCH match action
      pattern: /api/payments/.*/assign
---

<objective>
Deliver the slug-scoped reconciliation workspace with explicit payment-to-order matching, while keeping the old global route as a bridge.

Purpose: FIN-03 needs a usable operator surface before Phase 30 expands the full allocation model.
Output: a split-screen reconciliation page with manual matching and compatibility routing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-deterministic-money-model/29-CONTEXT.md
@.planning/phases/29-deterministic-money-model/29-UI-SPEC.md
@app/dashboard/reconciliation/page.tsx
@app/api/dashboard/reconciliation/route.ts
@app/api/payments/route.ts
@app/api/payments/[id]/assign/route.ts
@lib/domain/finance/reconciliation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the slug-scoped split-screen reconciliation workspace</name>
  <files>app/dashboard/events/[slug]/reconciliation/page.tsx</files>
  <read_first>
    @app/dashboard/reconciliation/page.tsx
    @app/api/dashboard/reconciliation/route.ts
    @app/api/payments/route.ts
    @lib/domain/finance/reconciliation.ts
    @app/dashboard/events/[slug]/layout.tsx
  </read_first>
  <action>Replace the global reconciliation UX with a slug-scoped workspace that loads the current event by slug, then renders unmatched payments on the left and outstanding orders on the right. Keep the current filter pattern where it helps, but switch the layout to a two-column table workspace with single-select state per panel, a sticky match bar, and no card-based list fallback. Use the existing reconciliation and payments APIs instead of inventing a new backend contract.</action>
  <acceptance_criteria>
    <criterion>The page contains left/right payment and order panels on the same screen.</criterion>
    <criterion>The page loads the event by slug before fetching scoped rows.</criterion>
    <criterion>Selection is single-select for both panels and exposes a `Match payment` action.</criterion>
    <criterion>UI copy matches the UI contract: `Match payment`, `No records match these filters`, and `Could not load canonical money data. Check your network and retry.`</criterion>
  </acceptance_criteria>
  <verify>`grep -n "Match payment\|No records match these filters\|Could not load canonical money data" app/dashboard/events/[slug]/reconciliation/page.tsx`</verify>
  <done>The canonical reconciliation workspace reads like one matching surface instead of two unrelated lists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the match action and bridge the old reconciliation route</name>
  <files>app/dashboard/reconciliation/page.tsx</files>
  <read_first>
    @app/dashboard/reconciliation/page.tsx
    @app/api/payments/[id]/assign/route.ts
    @app/dashboard/events/[slug]/reconciliation/page.tsx
  </read_first>
  <action>Wire the sticky match flow to `PATCH /api/payments/[id]/assign` with the selected payment id and order id, then keep the result state stable: on success, remove the matched rows from the live lists and preserve the active filters and event context; on failure, keep the selections and surface a retryable error banner. Convert the old global reconciliation page into a compatibility bridge that routes users into the slug-scoped surface instead of maintaining a second divergent UX.</action>
  <acceptance_criteria>
    <criterion>The match action submits to `/api/payments/[id]/assign`.</criterion>
    <criterion>Successful matching removes both rows from the active lists.</criterion>
    <criterion>Failures keep filters/selection intact and show an error banner.</criterion>
    <criterion>The old `/dashboard/reconciliation` page no longer owns the primary UX.</criterion>
  </acceptance_criteria>
  <verify>`npm run typecheck`</verify>
  <done>Manual payment-to-order matching works from the event-scoped page and the old route is only a bridge.</done>
</task>

</tasks>

<verification>
1. The event-scoped reconciliation page renders both datasets side by side.
2. The match action calls the existing assign API and updates the list state.
3. The compatibility route no longer competes with the canonical page.
</verification>

<success_criteria>
The reconciliation workflow is usable from the event-scoped shell, and payment matching is explicit, auditable, and routed through the existing assignment API.
</success_criteria>

<output>
After completion, create `.planning/phases/29-deterministic-money-model/29-03-SUMMARY.md`
</output>
