---
phase: 29-deterministic-money-model
plan: "04"
type: execute
wave: 3
depends_on: ["29-02", "29-03"]
requirements: [FIN-01, FIN-02, FIN-03]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "The orders route renders the slug-scoped ledger with attendees visible under each order."
    - "The reconciliation route shows side-by-side payments and outstanding orders and can execute a match."
    - "Legacy order and reconciliation entrypoints still resolve to the canonical event-scoped surfaces."
  artifacts:
    - path: app/dashboard/events/[slug]/orders/page.tsx
      provides: canonical event-scoped orders ledger
    - path: app/dashboard/events/[slug]/reconciliation/page.tsx
      provides: canonical event-scoped reconciliation workspace
    - path: app/dashboard/manage-orders/page.tsx
      provides: legacy orders bridge
    - path: app/dashboard/reconciliation/page.tsx
      provides: legacy reconciliation bridge
  key_links:
    - from: app/dashboard/events/[slug]/layout.tsx
      to: app/dashboard/events/[slug]/orders/page.tsx
      via: sidebar navigation
      pattern: /orders
    - from: app/dashboard/events/[slug]/layout.tsx
      to: app/dashboard/events/[slug]/reconciliation/page.tsx
      via: sidebar navigation
      pattern: /reconciliation
---

<objective>
Human-verify the canonical orders and reconciliation surfaces before the phase is marked done.

Purpose: the UI contract and bridge redirects need one real operator pass before the phase can ship.
Output: a verified yes/no on the new event-scoped finance flows.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-deterministic-money-model/29-CONTEXT.md
@.planning/phases/29-deterministic-money-model/29-UI-SPEC.md
@app/dashboard/events/[slug]/orders/page.tsx
@app/dashboard/events/[slug]/reconciliation/page.tsx
@app/dashboard/manage-orders/page.tsx
@app/dashboard/reconciliation/page.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Slug-scoped orders and reconciliation pages plus the legacy bridge routes.</what-built>
  <files>app/dashboard/events/[slug]/orders/page.tsx, app/dashboard/events/[slug]/reconciliation/page.tsx, app/dashboard/manage-orders/page.tsx, app/dashboard/reconciliation/page.tsx</files>
  <read_first>
    @.planning/phases/29-deterministic-money-model/29-UI-SPEC.md
    @app/dashboard/events/[slug]/orders/page.tsx
    @app/dashboard/events/[slug]/reconciliation/page.tsx
    @app/dashboard/manage-orders/page.tsx
    @app/dashboard/reconciliation/page.tsx
  </read_first>
  <how-to-verify>
    1. Open `/dashboard/events/{slug}/orders` and confirm the page has no event selector, shows attendees under each order, and still supports filters/export.
    2. Open `/dashboard/events/{slug}/reconciliation` and confirm the workspace is split into payments on the left and outstanding orders on the right.
    3. Select one payment and one order, click `Match payment`, confirm the dialog, and verify both rows disappear from the active lists.
    4. Visit `/dashboard/manage-orders` and `/dashboard/reconciliation` and confirm they bridge into the canonical event-scoped surfaces.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>The orders page visually matches the event-scoped finance UI contract.</criterion>
    <criterion>The reconciliation page visually matches the split-screen matching UI contract.</criterion>
    <criterion>The legacy bridge routes still end on the canonical event-scoped surfaces.</criterion>
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the mismatch.</resume-signal>
</task>

</tasks>

<verification>
Human verification must confirm the UI contract and bridge behavior end to end.
</verification>

<success_criteria>
The phase is complete only after the canonical event-scoped finance pages are visually and functionally verified in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/29-deterministic-money-model/29-04-SUMMARY.md`
</output>
