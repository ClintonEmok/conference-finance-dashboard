---
phase: 29-deterministic-money-model
plan: "01"
type: tdd
wave: 1
depends_on: []
requirements: [FIN-01, FIN-02, FIN-03]
files_modified:
  - lib/domain/finance/amounts.ts
  - convex/finance.ts
  - lib/domain/finance/order-ledger.ts
  - lib/domain/finance/reconciliation.ts
  - tests/finance/money-model.test.ts
autonomous: true
must_haves:
  truths:
    - "Order totals resolve to one canonical minor-unit value across ledger, detail, reconciliation, and export contexts."
    - "Attendee payables come from internal order facts and do not drift to equal-split heuristics."
    - "Payment matching and outstanding balances use the same canonical amount math everywhere."
    - "Zero, missing, or empty inputs collapse to 0 instead of NaN, negative values, or inconsistent splits."
  artifacts:
    - path: lib/domain/finance/amounts.ts
      provides: deterministic amount breakdown and allocation helpers
    - path: convex/finance.ts
      provides: canonical order amount breakdown loader
    - path: lib/domain/finance/order-ledger.ts
      provides: ledger rows that surface canonical amountDueMinor consistently
    - path: lib/domain/finance/reconciliation.ts
      provides: outstanding calculation driven by canonical totals and matched payments
    - path: tests/finance/money-model.test.ts
      provides: regression coverage for FIN-01, FIN-02, and FIN-03
  key_links:
    - from: convex/finance.ts
      to: lib/domain/finance/amounts.ts
      via: deriveOrderAmountBreakdown()
      pattern: deriveOrderAmountBreakdown\(
    - from: lib/domain/finance/order-ledger.ts
      to: convex.orders.getOrdersWithFilters
      via: canonical amountDueMinor normalization
      pattern: amountDueMinor
    - from: lib/domain/finance/reconciliation.ts
      to: lib/domain/finance/matched-payments.ts
      via: matched totals keyed by canonical order id
      pattern: buildMatchedTotalsByOrderId
---

<objective>
Lock in deterministic minor-unit math so the canonical money model has one source of truth before the event-scoped pages depend on it.

Purpose: FIN-01, FIN-02, and FIN-03 must all resolve from the same calculation layer or later UI work will keep surfacing inconsistent amounts.
Output: tested canonical amount helpers and the domain readers that consume them.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-deterministic-money-model/29-CONTEXT.md
@.planning/phases/29-deterministic-money-model/29-UI-SPEC.md
@lib/domain/finance/amounts.ts
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/reconciliation.ts
@convex/finance.ts
@tests/finance/internal-orders-canonicalization.test.ts
@tests/finance/order-ledger.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing money-model regressions</name>
  <files>tests/finance/money-model.test.ts</files>
  <read_first>
    @tests/finance/internal-orders-canonicalization.test.ts
    @tests/finance/order-ledger.test.ts
    @lib/domain/finance/amounts.ts
    @lib/domain/finance/order-ledger.ts
    @lib/domain/finance/reconciliation.ts
  </read_first>
  <action>Create a focused Vitest file that fails first on the canonical money contract: one set of cases for `deriveOrderAmountBreakdown()` and `deriveBalanceAmounts()`, one set for ledger rows surfacing a stable `amountDueMinor`, and one set for reconciliation rows using the same canonical total/minor-unit math. Keep the scenarios concrete: zero selections, partial payment, split payment, and overpayment. Do not change implementation code yet; the red run should prove the missing contract.</action>
  <acceptance_criteria>
    <criterion>`tests/finance/money-model.test.ts` contains explicit expectations for canonical totals, attendee payables, and allocation math.</criterion>
    <criterion>`npm test -- tests/finance/money-model.test.ts` fails before implementation.</criterion>
    <criterion>No source file is modified in this task.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/finance/money-model.test.ts`</verify>
  <done>The new regression file clearly documents the FIN-01/FIN-02/FIN-03 failure mode and is red for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: Make the money model deterministic and green</name>
  <files>lib/domain/finance/amounts.ts</files>
  <read_first>
    @lib/domain/finance/amounts.ts
    @convex/finance.ts
    @lib/domain/finance/order-ledger.ts
    @lib/domain/finance/reconciliation.ts
    @tests/finance/money-model.test.ts
  </read_first>
  <action>Implement the smallest deterministic-calculation changes needed to satisfy the red tests: keep minor-unit arithmetic bounded to non-negative integers, ensure order amount breakdowns derive from internal selections only, and make ledger/reconciliation readers consume the same canonical helpers instead of diverging fallback math. Preserve existing query/filter contracts and do not introduce new schema or API shapes in this phase.</action>
  <acceptance_criteria>
    <criterion>`npm test -- tests/finance/money-model.test.ts tests/finance/order-ledger.test.ts tests/finance/internal-orders-canonicalization.test.ts` passes.</criterion>
    <criterion>`npm run typecheck` passes.</criterion>
    <criterion>`lib/domain/finance/amounts.ts` exports deterministic helpers with no NaN-producing branches.</criterion>
  </acceptance_criteria>
  <verify>`npm test -- tests/finance/money-model.test.ts tests/finance/order-ledger.test.ts tests/finance/internal-orders-canonicalization.test.ts && npm run typecheck`</verify>
  <done>FIN-01, FIN-02, and FIN-03 now resolve through one canonical minor-unit calculation path.</done>
</task>

</tasks>

<verification>
1. New canonical money regressions pass.
2. Existing finance regression coverage still passes.
3. TypeScript compiles cleanly with the updated money model.
</verification>

<success_criteria>
The canonical money model returns one deterministic answer for order totals, attendee payables, and allocation math, and the updated tests prove that behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/29-deterministic-money-model/29-01-SUMMARY.md`
</output>
