---
phase: 29-deterministic-money-model
plan: "02"
type: execute
wave: 2
depends_on: ["29-01"]
requirements: [FIN-01, FIN-02]
files_modified:
  - app/dashboard/events/[slug]/orders/page.tsx
  - app/dashboard/events/[slug]/orders/[orderId]/page.tsx
  - app/dashboard/manage-orders/page.tsx
  - app/dashboard/manage-orders/[orderId]/page.tsx
  - app/dashboard/events/[slug]/layout.tsx
autonomous: true
must_haves:
  truths:
    - "The orders ledger is reachable at the slug-scoped event route and no longer needs an event selector dropdown."
    - "Each order shows attendees clearly under the order row or in the order detail view."
    - "Filters, pagination, CSV export, and back navigation still work under the new canonical route."
    - "Legacy manage-orders URLs still land on the canonical event-scoped orders surface."
  artifacts:
    - path: app/dashboard/events/[slug]/orders/page.tsx
      provides: slug-scoped orders ledger page
    - path: app/dashboard/events/[slug]/orders/[orderId]/page.tsx
      provides: slug-scoped order detail page with inline attendees
    - path: app/dashboard/manage-orders/page.tsx
      provides: legacy bridge redirect for list entrypoints
    - path: app/dashboard/manage-orders/[orderId]/page.tsx
      provides: legacy bridge redirect for detail entrypoints
    - path: app/dashboard/events/[slug]/layout.tsx
      provides: event-shell navigation targets for Orders and Reconciliation
  key_links:
    - from: app/dashboard/events/[slug]/orders/page.tsx
      to: convex.events.getEventBySlug
      via: resolve slug to eventId before fetching orders
      pattern: getEventBySlug
    - from: app/dashboard/events/[slug]/orders/page.tsx
      to: api.orders.getOrdersWithFilters
      via: event-scoped ledger fetch
      pattern: /api/dashboard/orders
    - from: app/dashboard/manage-orders/page.tsx
      to: app/dashboard/events/[slug]/orders/page.tsx
      via: redirect bridge
      pattern: /dashboard/events/.*/orders
---

<objective>
Move the orders ledger/detail experience behind slug-scoped event URLs while keeping the legacy manage-orders entrypoints as compatibility bridges.

Purpose: event-first navigation only works if the canonical orders surface is already scoped by event slug.
Output: new slug-scoped orders pages and old URLs that still resolve cleanly.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-deterministic-money-model/29-CONTEXT.md
@.planning/phases/29-deterministic-money-model/29-UI-SPEC.md
@app/dashboard/manage-orders/page.tsx
@app/dashboard/manage-orders/[orderId]/page.tsx
@app/dashboard/events/[slug]/layout.tsx
@app/dashboard/events/[slug]/page.tsx
@convex/events.ts
@lib/domain/finance/order-ledger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the slug-scoped orders ledger and detail pages</name>
  <files>app/dashboard/events/[slug]/orders/page.tsx, app/dashboard/events/[slug]/orders/[orderId]/page.tsx</files>
  <read_first>
    @app/dashboard/manage-orders/page.tsx
    @app/dashboard/manage-orders/[orderId]/page.tsx
    @app/dashboard/events/[slug]/layout.tsx
    @convex/events.ts
    @lib/domain/finance/order-ledger.ts
  </read_first>
  <action>Copy the current manage-orders experience into the slug-scoped route tree, then remove the event selector entirely. Resolve `slug` to the event record with `api.events.getEventBySlug`, pass the resulting event id into the existing orders fetch, keep filters/search/date-range/export behavior, and render attendees inline under each order row or inside the order detail view. Preserve the current canonical money formatting and back-link behavior, but make the event slug the source of truth for the page URL.</action>
  <acceptance_criteria>
    <criterion>Both new files exist at the exact slug-scoped paths.</criterion>
    <criterion>The new ledger page uses `getEventBySlug` before fetching orders.</criterion>
    <criterion>The new ledger page no longer renders an event selector dropdown.</criterion>
    <criterion>Attendee rows are visibly nested under each order in the new flow.</criterion>
  </acceptance_criteria>
  <verify>`grep -n "getEventBySlug\|event selector\|/dashboard/events/.*/orders" app/dashboard/events/[slug]/orders/page.tsx app/dashboard/events/[slug]/orders/[orderId]/page.tsx`</verify>
  <done>The canonical orders experience is event-slug scoped and still shows the same ledger/detail data, just without global entrypoint assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge old order URLs to the canonical route and update the shell nav</name>
  <files>app/dashboard/manage-orders/page.tsx, app/dashboard/manage-orders/[orderId]/page.tsx, app/dashboard/events/[slug]/layout.tsx</files>
  <read_first>
    @app/dashboard/manage-orders/page.tsx
    @app/dashboard/manage-orders/[orderId]/page.tsx
    @app/dashboard/events/[slug]/layout.tsx
  </read_first>
  <action>Turn the legacy manage-orders entrypoints into compatibility bridges that resolve the current event slug and redirect into `/dashboard/events/[slug]/orders` or `/dashboard/events/[slug]/orders/[orderId]`. In the event shell, add explicit Orders and Reconciliation navigation targets so the canonical event-scoped routes are discoverable from the sidebar without relying on the old global pages.</action>
  <acceptance_criteria>
    <criterion>`/dashboard/manage-orders` redirects into `/dashboard/events/[slug]/orders`.</criterion>
    <criterion>`/dashboard/manage-orders/[orderId]` redirects into `/dashboard/events/[slug]/orders/[orderId]`.</criterion>
    <criterion>`app/dashboard/events/[slug]/layout.tsx` contains both `Orders` and `Reconciliation` nav items.</criterion>
    <criterion>`npm run typecheck` passes after the route move.</criterion>
  </acceptance_criteria>
  <verify>`npm run typecheck`</verify>
  <done>Users can enter the orders surface through the event shell or old URLs and always end up on the slug-scoped canonical page.</done>
</task>

</tasks>

<verification>
1. The slug-scoped orders pages compile and use the event slug as the route contract.
2. Legacy orders URLs still resolve through redirects.
3. The event shell exposes the new canonical navigation targets.
</verification>

<success_criteria>
The event-scoped orders experience is the canonical entrypoint, and the old manage-orders URLs remain safe bridges instead of separate experiences.
</success_criteria>

<output>
After completion, create `.planning/phases/29-deterministic-money-model/29-02-SUMMARY.md`
</output>
