---
phase: 07-complete-tikkie-integration
plan: 01
title: Make Tikkie refresh provider-authoritative via GET payment request
type: execute
wave: 1
depends_on: [06-03]
requirements: []
files_modified:
  - lib/integrations/tikkie/client.ts
  - lib/domain/finance/tikkie-links.ts
  - lib/integrations/tikkie/webhook.ts
  - app/api/jobs/tikkie/status-sync/route.ts
  - app/api/webhooks/tikkie/route.ts
  - tests/tikkie/tikkie-links.test.ts
  - tests/tikkie/webhook-route.test.ts
autonomous: true
must_haves:
  truths:
    - "Webhook and polling updates reconcile link status from GET /paymentrequests/{paymentRequestToken} as provider truth"
    - "Paid links never regress to created/expired even when later refreshes disagree"
    - "Manual status sync still gives operators trustworthy scanned/updated/unchanged/failed counts"
  artifacts:
    - path: "lib/domain/finance/tikkie-links.ts"
      provides: "Provider-authoritative refresh logic with guarded transition rules"
      exports: ["refreshTikkiePaymentLinkStatus", "syncPendingTikkiePaymentLinks"]
    - path: "lib/integrations/tikkie/client.ts"
      provides: "Typed GET payment request contract and normalized upstream error surface"
      exports: ["getPaymentRequest"]
    - path: "app/api/jobs/tikkie/status-sync/route.ts"
      provides: "Protected fallback status-sync job endpoint with bounded limit handling"
      exports: ["POST"]
    - path: "app/api/webhooks/tikkie/route.ts"
      provides: "Webhook trigger path that delegates final status reconciliation to domain refresh"
      exports: ["POST"]
  key_links:
    - from: "lib/integrations/tikkie/webhook.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "refreshTikkiePaymentLinkStatus({ source: 'webhook' })"
      pattern: "refreshTikkiePaymentLinkStatus"
    - from: "app/api/jobs/tikkie/status-sync/route.ts"
      to: "lib/domain/finance/tikkie-links.ts"
      via: "syncPendingTikkiePaymentLinks"
      pattern: "syncPendingTikkiePaymentLinks"
    - from: "lib/domain/finance/tikkie-links.ts"
      to: "GET /paymentrequests/{paymentRequestToken}"
      via: "client.getPaymentRequest"
      pattern: "getPaymentRequest"
---

<objective>
Complete the status-trust gap by making provider GET retrieval the canonical status source.

Purpose: Align the existing webhook/job pipeline with Tikkie guidance that notifications are best effort and GET monitoring is required.
Output: Domain and route behavior where webhook/poll are triggers, GET payment request is truth, and transitions stay monotonic.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-complete-tikkie-integration/07-RESEARCH.md
@.planning/phases/06-tikkie-integration/06-01-SUMMARY.md
@.planning/phases/06-tikkie-integration/06-03-SUMMARY.md
@TikkieAPI_v2.yaml
@lib/integrations/tikkie/client.ts
@lib/domain/finance/tikkie-links.ts
@lib/integrations/tikkie/webhook.ts
@app/api/jobs/tikkie/status-sync/route.ts
@app/api/webhooks/tikkie/route.ts
@tests/tikkie/tikkie-links.test.ts
@tests/tikkie/webhook-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework refresh resolution to use GET payment request as canonical provider source</name>
  <read_first>@TikkieAPI_v2.yaml, @lib/domain/finance/tikkie-links.ts, @lib/integrations/tikkie/client.ts</read_first>
  <files>lib/domain/finance/tikkie-links.ts, lib/integrations/tikkie/client.ts</files>
  <action>Refactor refresh logic so `refreshTikkiePaymentLinkStatus` derives final app status from `GET /paymentrequests/{paymentRequestToken}` fields first (`status`, `numberOfPayments`, `totalAmountPaidInCents`) and treats this as authoritative provider truth. Keep monotonic transition guarantees (`created -> paid|expired`, no `paid -> created`) and keep existing API surface contracts stable for callers. If compatibility fallback logic is retained, guard it so it only executes when payment totals are missing/ambiguous, not as the default path.</action>
  <verify>npm run test -- tests/tikkie/tikkie-links.test.ts</verify>
  <acceptance_criteria>Refresh no longer depends on list-payments as primary truth, and paid detection from payment-request fields is covered by tests for OPEN, CLOSED with paid totals, and CLOSED/EXPIRED without paid totals.</acceptance_criteria>
  <done>Domain refresh is provider-authoritative from payment-request retrieval and preserves non-regressing transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Keep webhook and status-sync routes as trigger paths with trustworthy operator outcomes</name>
  <read_first>@lib/integrations/tikkie/webhook.ts, @app/api/jobs/tikkie/status-sync/route.ts, @app/api/webhooks/tikkie/route.ts</read_first>
  <files>lib/integrations/tikkie/webhook.ts, app/api/jobs/tikkie/status-sync/route.ts, app/api/webhooks/tikkie/route.ts, lib/domain/finance/tikkie-links.ts</files>
  <action>Ensure webhook and poll flows only trigger reconciliation and do not independently set terminal state. Preserve duplicate notification short-circuiting via `providerNotificationKey`, explicit missing-link behavior, and current auth contracts on job endpoints. Keep bounded polling semantics (`limit` cap) and confirm response counters remain meaningful for operator trust.</action>
  <verify>npm run test -- tests/tikkie/webhook-route.test.ts tests/tikkie/tikkie-links.test.ts</verify>
  <acceptance_criteria>Webhook duplicate events return accepted duplicate without extra provider fetch, missing links are non-fatal accepted missing, and status-sync output still reports scanned/updated/unchanged/failed with correct values.</acceptance_criteria>
  <done>Webhook and job routes are trigger-only paths backed by provider-authoritative refresh and stable operator-facing contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Expand tests for provider-authoritative and monotonic refresh behavior</name>
  <read_first>@tests/tikkie/tikkie-links.test.ts, @tests/tikkie/webhook-route.test.ts</read_first>
  <files>tests/tikkie/tikkie-links.test.ts, tests/tikkie/webhook-route.test.ts</files>
  <action>Add or update tests to assert: (1) paid is inferred from payment-request aggregate fields, (2) created links transition to expired when provider indicates non-open unpaid state, (3) paid links do not regress on later refreshes, and (4) route-level contracts remain unchanged (`401 UNAUTHORIZED`, `400 BAD_PAYLOAD`, `200 accepted`). Keep tests deterministic with existing mocking style and avoid introducing network-bound integration tests.</action>
  <verify>npm run test -- tests/tikkie/tikkie-links.test.ts tests/tikkie/webhook-route.test.ts && npm run typecheck</verify>
  <acceptance_criteria>Tests explicitly prove provider-authoritative mapping and transition monotonicity, and all targeted tests pass without changing established API error codes.</acceptance_criteria>
  <done>Regression coverage protects the new status-truth contract and existing webhook/job route behavior.</done>
</task>

</tasks>

<verification>
1. Trigger refresh for a known created link and verify status derivation comes from payment-request payload fields, not webhook payload alone.
2. Replay duplicate webhook notification and verify no duplicate transition or status churn occurs.
3. Run manual `POST /api/jobs/tikkie/status-sync?limit=...` and confirm operator counters remain coherent.
4. Confirm route error contracts stay stable for unauthenticated and malformed requests.
</verification>

<success_criteria>
- Phase 7 status-trust objective is met: provider GET retrieval is canonical for payment-link state.
- Webhook and poll mechanisms are resilient delivery triggers, not competing truth sources.
- Existing operator contracts stay intact while reliability increases.
</success_criteria>

<output>
After completion, create `.planning/phases/07-complete-tikkie-integration/07-01-SUMMARY.md`
</output>
