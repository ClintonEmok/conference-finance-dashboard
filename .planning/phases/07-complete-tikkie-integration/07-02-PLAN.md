---
phase: 07-complete-tikkie-integration
plan: 02
title: Add guarded subscription setup path for payment request notifications
type: execute
wave: 2
depends_on: [07-01]
requirements: []
files_modified:
  - lib/integrations/tikkie/client.ts
  - lib/integrations/tikkie/config.ts
  - app/api/admin/tikkie/subscription/route.ts
  - .env.example
  - tests/tikkie/subscription-route.test.ts
autonomous: true
user_setup:
  - service: tikkie
    why: "Readiness requires a real callback URL and explicit subscription registration in sandbox/ops flow"
    env_vars:
      - name: TIKKIE_SUBSCRIPTION_SETUP_ENABLED
        source: "App environment variables (keep false in production until rollout decision)"
      - name: TIKKIE_WEBHOOK_CALLBACK_URL
        source: "Public URL for this deployment's /api/webhooks/tikkie endpoint"
    dashboard_config:
      - task: "Confirm the app token has payment request permission before invoking subscription setup"
        location: "Tikkie Business Portal -> API settings -> App token permissions"
must_haves:
  truths:
    - "Operators can invoke one protected setup endpoint to provision payment-request subscription when explicitly enabled"
    - "Production flow is not auto-activated; setup route is disabled by default until toggle is set"
    - "Subscription setup is auditable from response data (including subscriptionId) and safe to rerun intentionally"
  artifacts:
    - path: "app/api/admin/tikkie/subscription/route.ts"
      provides: "Authenticated admin route that calls POST /paymentrequestssubscription behind explicit guard"
      exports: ["POST"]
    - path: "lib/integrations/tikkie/client.ts"
      provides: "Typed subscribePaymentRequestNotifications client function"
      exports: ["subscribePaymentRequestNotifications"]
    - path: "lib/integrations/tikkie/config.ts"
      provides: "Validated subscription setup toggles and callback URL handling"
      exports: ["getTikkieConfig"]
    - path: "tests/tikkie/subscription-route.test.ts"
      provides: "Route contract coverage for disabled, unauthorized, bad config, and success paths"
      contains: "POST /api/admin/tikkie/subscription"
  key_links:
    - from: "app/api/admin/tikkie/subscription/route.ts"
      to: "lib/integrations/tikkie/client.ts"
      via: "subscribePaymentRequestNotifications"
      pattern: "subscribePaymentRequestNotifications"
    - from: "app/api/admin/tikkie/subscription/route.ts"
      to: "lib/integrations/tikkie/config.ts"
      via: "toggle and callback URL guard"
      pattern: "TIKKIE_SUBSCRIPTION_SETUP_ENABLED|TIKKIE_WEBHOOK_CALLBACK_URL"
---

<objective>
Create a safe, non-production-default readiness path for Tikkie subscription provisioning.

Purpose: Prepare `/paymentrequestssubscription` operational setup without coupling it to normal payment-link generation or enabling it by accident.
Output: Guarded admin endpoint, typed provider client call, config/env documentation, and tests proving disabled-by-default behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-complete-tikkie-integration/07-RESEARCH.md
@.planning/phases/07-complete-tikkie-integration/07-01-SUMMARY.md
@TikkieAPI_v2.yaml
@lib/integrations/tikkie/client.ts
@lib/integrations/tikkie/config.ts
@app/api/jobs/tikkie/status-sync/route.ts
@app/api/webhooks/tikkie/route.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tikkie client/config for subscription provisioning with explicit guards</name>
  <read_first>@lib/integrations/tikkie/client.ts, @lib/integrations/tikkie/config.ts, @TikkieAPI_v2.yaml</read_first>
  <files>lib/integrations/tikkie/client.ts, lib/integrations/tikkie/config.ts, .env.example</files>
  <action>Add typed client support for `POST /paymentrequestssubscription` (request body `url`, response `subscriptionId`) and keep current error-kind mapping conventions. Extend config handling to parse and validate a strict feature flag (`TIKKIE_SUBSCRIPTION_SETUP_ENABLED`) and callback URL (`TIKKIE_WEBHOOK_CALLBACK_URL`) so the setup route can fail fast on bad configuration. Document these env vars in `.env.example` with safe defaults that keep setup disabled.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>Client exposes one typed subscription method, config reports invalid/missing callback URL clearly, and `.env.example` includes both new variables with setup disabled by default.</acceptance_criteria>
  <done>Subscription setup prerequisites are encoded as reusable client/config contracts, not ad-hoc route logic.</done>
</task>

<task type="auto">
  <name>Task 2: Build protected admin route for on-demand subscription setup</name>
  <read_first>@app/api/jobs/tikkie/status-sync/route.ts, @app/api/webhooks/tikkie/route.ts, @lib/integrations/tikkie/client.ts</read_first>
  <files>app/api/admin/tikkie/subscription/route.ts, lib/integrations/tikkie/client.ts, lib/integrations/tikkie/config.ts</files>
  <action>Create `POST /api/admin/tikkie/subscription` following established auth guard patterns (`401 UNAUTHORIZED` when no session). Route behavior must: reject when setup feature flag is disabled, require a valid callback URL from env/config, call `subscribePaymentRequestNotifications`, and return structured result including `subscriptionId` and target callback URL. Do not invoke this route from webhook processing, status-sync jobs, or payment-link creation flows.</action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>Endpoint is callable only by authenticated users, returns explicit disabled/configuration errors when gated, and returns provider subscription confirmation on success without affecting existing runtime flows.</acceptance_criteria>
  <done>Subscription setup path exists as an isolated admin action, ready for staged rollout but not active in normal production operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-level tests for disabled-by-default and safe success semantics</name>
  <read_first>@tests/tikkie/webhook-route.test.ts, @tests/tikkie/tikkie-links.test.ts</read_first>
  <files>tests/tikkie/subscription-route.test.ts</files>
  <action>Create focused tests for `POST /api/admin/tikkie/subscription`: unauthorized request returns 401 contract, disabled flag returns deterministic rejection, invalid callback URL returns bad-request style diagnostics, and enabled+valid config calls provider client once and returns subscription metadata. Use existing Vitest mocking patterns and keep tests isolated from live network calls.</action>
  <verify>npm run test -- tests/tikkie/subscription-route.test.ts && npm run typecheck</verify>
  <acceptance_criteria>All subscription route branches are exercised by tests and prove disabled-by-default posture plus explicit successful provisioning response.</acceptance_criteria>
  <done>Readiness path is regression-protected and can be safely validated in sandbox before any production activation decision.</done>
</task>

</tasks>

<verification>
1. Call `POST /api/admin/tikkie/subscription` with no session and confirm `401 UNAUTHORIZED`.
2. Call with session while `TIKKIE_SUBSCRIPTION_SETUP_ENABLED=false` and confirm explicit disabled response.
3. Call with enabled toggle and valid callback URL in sandbox and confirm response includes provider `subscriptionId`.
4. Confirm existing webhook and status-sync routes remain unchanged and do not auto-provision subscriptions.
</verification>

<success_criteria>
- Phase 7 readiness objective is met: a guarded subscription setup path exists without turning on production behavior by default.
- Operational setup can be executed intentionally and verified with explicit API responses.
- Existing Tikkie generation/refresh flows remain decoupled from subscription provisioning.
</success_criteria>

<output>
After completion, create `.planning/phases/07-complete-tikkie-integration/07-02-SUMMARY.md`
</output>
