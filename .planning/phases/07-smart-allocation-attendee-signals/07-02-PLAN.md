---
phase: 07-smart-allocation-attendee-signals
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - lib/domain/accommodation/assignments.ts
  - app/api/dashboard/accommodation/assignments/route.ts
  - app/api/dashboard/accommodation/assignments/[attendeeId]/route.ts
  - app/api/dashboard/accommodation/auto-allocate/route.ts
  - app/dashboard/accommodation/page.tsx
  - app/dashboard/attendees/page.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
  - tests/**/*.test.ts
autonomous: true
must_haves:
  truths:
    - "Operators can filter accommodation work by gender, family grouping, and priority accommodation signals"
    - "Auto-allocation proposals avoid incompatible roommate matching and prioritize high-need attendees first"
    - "Family groups are kept together when feasible and surfaced clearly when not"
  artifacts:
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Signal-aware allocation board query and smart proposal logic"
      exports: ["getRoomAllocationBoard"]
    - path: "app/api/dashboard/accommodation/assignments/route.ts"
      provides: "Protected accommodation board API with signal-aware filters"
      exports: ["GET", "POST"]
    - path: "app/api/dashboard/accommodation/auto-allocate/route.ts"
      provides: "Protected smart-allocation proposal endpoint"
      exports: ["POST"]
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "Room allocation workspace with signal filters, badges, and proposal actions"
      contains: "Unassigned attendees"
  key_links:
    - from: "app/api/dashboard/accommodation/assignments/route.ts"
      to: "lib/domain/accommodation/assignments.ts"
      via: "validated signal-aware board query"
      pattern: "getRoomAllocationBoard"
    - from: "app/api/dashboard/accommodation/auto-allocate/route.ts"
      to: "lib/domain/accommodation/assignments.ts"
      via: "protected smart proposal generation"
      pattern: "allocate|proposal|suggest"
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/assignments"
      via: "signal-filtered room allocation board fetch"
      pattern: "accommodation/assignments"
---

<objective>
Use normalized attendee signals to drive room-allocation filtering and smart proposals.

Purpose: Satisfy ACC-05 and ACC-06 by making the accommodation workflow aware of gender, family grouping, location, remarks, and priority accommodation cases.
Output: Signal-aware board filters, protected APIs, and proposal logic that helps operators place attendees with fewer manual decisions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-smart-allocation-attendee-signals/07-CONTEXT.md
@.planning/phases/07-smart-allocation-attendee-signals/07-01-SUMMARY.md
@lib/domain/accommodation/assignments.ts
@app/dashboard/accommodation/page.tsx
@app/api/dashboard/accommodation/assignments/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend accommodation board queries with signal-aware filters</name>
  <files>lib/domain/accommodation/assignments.ts, app/api/dashboard/accommodation/assignments/route.ts, app/dashboard/accommodation/page.tsx</files>
  <action>Expand the allocation board query and protected API contract to support filters for gender, family grouping, location, and priority accommodation signals. Make these filters visible in the accommodation workspace without breaking current assignment and occupancy behavior.</action>
  <verify>npm run test && npm run typecheck</verify>
  <done>Operators can filter rooms and unassigned attendees using normalized attendee accommodation signals.</done>
</task>

<task type="auto">
  <name>Task 2: Add smart allocation proposal logic with safety guardrails</name>
  <files>lib/domain/accommodation/assignments.ts, app/api/dashboard/accommodation/auto-allocate/route.ts, tests/**/*.test.ts</files>
  <action>Implement rule-based proposal logic that places priority accommodation cases first, keeps same-family attendees together when possible, and avoids incompatible roommate matching. Treat male/female roommate constraints as hard guardrails and location/remarks as secondary signals for better placement.</action>
  <verify>npm run test && npm run typecheck && npm run build</verify>
  <done>Operators can trigger smart room proposals that respect gender compatibility, family grouping, and priority accommodation ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Surface proposal outcomes and signal badges in dashboard workflows</name>
  <files>app/dashboard/accommodation/page.tsx, app/dashboard/attendees/page.tsx, app/dashboard/attendees/[attendeeId]/page.tsx</files>
  <action>Show the normalized attendee signals and proposal outcomes where operators make accommodation decisions. Include clear badges or labels for gender, family grouping, priority status, and unplaced constraints so the workflow remains explainable and override-friendly.</action>
  <verify>npm run typecheck && npm run lint && npm run build</verify>
  <done>Operators can understand why the system suggested a placement and can still override it confidently.</done>
</task>

</tasks>

<verification>
1. Load the accommodation board and confirm signal filters meaningfully narrow rooms and unassigned attendees.
2. Trigger smart allocation proposals and confirm high-priority attendees are considered first.
3. Confirm proposed roommate placements do not mix incompatible gender constraints.
4. Confirm same-order family groups are kept together when sufficient room capacity exists, or clearly flagged when they cannot be.
</verification>

<success_criteria>
- ACC-05 satisfied: operators can filter accommodation work by signal fields that matter for rooming decisions.
- ACC-06 satisfied: smart allocation proposals use family, gender, and priority rules instead of simple first-fit assignment.
- The allocation workflow becomes materially faster without becoming opaque or hard to override.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-allocation-attendee-signals/07-02-SUMMARY.md`
</output>
