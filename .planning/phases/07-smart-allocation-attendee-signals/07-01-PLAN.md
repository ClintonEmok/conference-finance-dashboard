---
phase: 07-smart-allocation-attendee-signals
plan: 01
type: execute
wave: 1
depends_on: [05-02]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_attendee_signal_foundation/migration.sql
  - lib/domain/ticket-tailor/custom-answers.ts
  - lib/integrations/ticket-tailor/sync.ts
  - lib/domain/finance/attendees.ts
  - lib/domain/finance/attendee-detail.ts
  - app/api/dashboard/attendees/route.ts
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - app/dashboard/attendees/page.tsx
  - app/dashboard/attendees/[attendeeId]/page.tsx
  - tests/ticket-tailor/*.test.ts
autonomous: true
must_haves:
  truths:
    - "Ticket Tailor attendee sync stores durable accommodation signals beyond raw payload JSON"
    - "Same-order attendees are auto-grouped in a family or party model without blocking future manual cross-order linking"
    - "Operators can inspect normalized signals such as gender, location, remarks, and priority reason in attendee surfaces"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PostgreSQL-friendly attendee signal and family grouping data model"
      contains: "customAnswers"
    - path: "lib/domain/ticket-tailor/custom-answers.ts"
      provides: "Central custom-question extraction and priority derivation rules"
      exports: ["extractCustomAnswers", "detectPriorityFromAnswers"]
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Sync enrichment of attendee signals and same-order family linking"
      exports: ["runTicketTailorSync"]
    - path: "lib/domain/finance/attendees.ts"
      provides: "Attendee list query exposing signal fields for operator views"
      exports: ["getAttendeeLedger"]
    - path: "lib/domain/finance/attendee-detail.ts"
      provides: "Attendee detail query exposing signal and family-group context"
      exports: ["getAttendeeDetail"]
  key_links:
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "prisma.ticketTailorAttendee"
      via: "sync-time attendee enrichment"
      pattern: "customAnswers|genderType|allocationPriority"
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "prisma.attendeeFamilyGroup"
      via: "same-order family auto-grouping"
      pattern: "attendeeFamilyGroup|attendeeFamilyMember"
    - from: "app/api/dashboard/attendees/route.ts"
      to: "lib/domain/finance/attendees.ts"
      via: "protected signal-aware attendee query"
      pattern: "getAttendeeLedger"
---

<objective>
Normalize attendee accommodation signals and make them visible to operators.

Purpose: Satisfy TT-05 and ACC-04 by turning Ticket Tailor attendee custom questions and ticket metadata into durable, queryable fields that support downstream allocation logic.
Output: Schema and sync updates, signal extraction rules, same-order family auto-grouping, and attendee API/UI surfaces that expose the normalized signals.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-smart-allocation-attendee-signals/07-CONTEXT.md
@.planning/phases/05-room-allocation-operator-flow-polish/05-SUMMARY.md
@prisma/schema.prisma
@lib/integrations/ticket-tailor/sync.ts
@lib/domain/ticket-tailor/custom-answers.ts
@lib/domain/finance/attendees.ts
@lib/domain/finance/attendee-detail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden attendee signal storage in the schema</name>
  <files>prisma/schema.prisma, prisma/migrations/*_attendee_signal_foundation/migration.sql</files>
  <action>Refine the attendee schema so allocation-relevant fields are explicit, indexed, and migration-friendly. Keep SQLite-compatible types now while staying PostgreSQL-friendly later. Ensure the model can represent gender, location, remarks, priority reason/category, family-group provenance, and future manual linking without requiring JSON-only querying.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>Attendee allocation signals and family-group foundations are durable and queryable in the Prisma schema.</done>
</task>

<task type="auto">
  <name>Task 2: Refine sync-time extraction of attendee accommodation signals</name>
  <files>lib/domain/ticket-tailor/custom-answers.ts, lib/integrations/ticket-tailor/sync.ts, tests/ticket-tailor/*.test.ts</files>
  <action>Improve attendee custom-question extraction so gender comes from attendee custom questions, location/remarks remain durable, and priority derivation can safely identify elderly, disability/accessibility, and young-children cases from remarks and other custom answers. Keep same-order family auto-grouping reliable and idempotent.</action>
  <verify>npm run test && npm run typecheck</verify>
  <done>Running Ticket Tailor sync produces stable attendee signal fields and family-group links without regressing order/attendee import behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Expose normalized signals in attendee list and detail surfaces</name>
  <files>lib/domain/finance/attendees.ts, lib/domain/finance/attendee-detail.ts, app/api/dashboard/attendees/route.ts, app/api/dashboard/attendees/[attendeeId]/route.ts, app/dashboard/attendees/page.tsx, app/dashboard/attendees/[attendeeId]/page.tsx</files>
  <action>Update attendee domain queries, protected APIs, and dashboard attendee surfaces so operators can see normalized signals and family-group context directly instead of inferring from raw payload JSON. Preserve current finance and room-state fields while adding clear signal visibility for future allocation decisions.</action>
  <verify>npm run test && npm run typecheck && npm run build</verify>
  <done>Operators can inspect normalized attendee accommodation signals and same-order family context in attendee list/detail views.</done>
</task>

</tasks>

<verification>
1. Run Ticket Tailor sync and confirm attendee rows persist normalized signal fields beyond raw JSON.
2. Confirm same-order attendees are linked into one family group and re-sync remains idempotent.
3. Open attendee list and detail screens and confirm operators can see gender, location, remarks, and priority reason without digging into raw payloads.
4. Verify unauthenticated attendee APIs still return the established 401 contract.
</verification>

<success_criteria>
- TT-05 satisfied: attendee accommodation signals are extracted from Ticket Tailor custom questions and ticket metadata.
- ACC-04 satisfied for same-order groups: family or party grouping exists as a durable backend concept.
- Phase 7 plan 2 can build smart filters and allocation rules without reparsing provider JSON everywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-allocation-attendee-signals/07-01-SUMMARY.md`
</output>
