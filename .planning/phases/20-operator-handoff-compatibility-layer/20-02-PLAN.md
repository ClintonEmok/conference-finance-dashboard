---
phase: 20-operator-handoff-compatibility-layer
plan: 02
type: execute
wave: 2
depends_on: [20-01]
requirements: [OPS-02]
files_modified:
  - app/dashboard/accommodation/page.tsx
  - app/dashboard/accommodation/filter-state.ts
  - convex/events.ts
  - lib/domain/finance/order-ledger.ts
  - lib/domain/finance/attendees.ts
autonomous: true
must_haves:
  truths:
    - "Operators can triage unresolved submissions first and inspect rooming notes in a side-panel detail view."
    - "Primary operator list behavior remains source-agnostic while source metadata is hidden by default and visible on demand."
    - "Dashboard event filter/read adapters accept both integration and internal event records without breaking legacy integration paths."
  artifacts:
    - path: "app/dashboard/accommodation/page.tsx"
      provides: "Submission-first operator handoff UX with detail side panel"
      contains: "unresolved-first queue + metadata/details toggle"
    - path: "convex/events.ts"
      provides: "Source-agnostic event list query for dashboard consumers"
      contains: "integration + internal compatible event projection"
    - path: "lib/domain/finance/order-ledger.ts"
      provides: "Event adapter consumption that tolerates mixed source events"
      contains: "compatible event filter mapping"
    - path: "lib/domain/finance/attendees.ts"
      provides: "Attendee ledger event source compatibility bridge"
      contains: "source-safe availableEvents usage"
  key_links:
    - from: "app/dashboard/accommodation/page.tsx"
      to: "/api/dashboard/accommodation/assignments"
      via: "board fetch and detail panel rendering"
      pattern: "fetch\("/api/dashboard/accommodation/assignments"
    - from: "lib/domain/finance/order-ledger.ts"
      to: "api.events"
      via: "availableEvents fetch"
      pattern: "convexQuery\(api\.events"
    - from: "lib/domain/finance/attendees.ts"
      to: "api.events"
      via: "availableEvents fetch"
      pattern: "convexQuery\(api\.events"
---

<objective>
Implement operator-facing handoff UX and source-agnostic event adapter updates so internal signup submissions and integration events coexist in dashboard read flows.

Purpose: Satisfies OPS-02 while honoring context decisions (submission-first grouping, side-panel detail, hidden-by-default source labeling).
Output: Accommodation operator UI updated for handoff cues plus mixed-source event adapters in finance/attendee domain reads.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-CONTEXT.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-RESEARCH.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-01-SUMMARY.md
@app/dashboard/accommodation/page.tsx
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/attendees.ts
@convex/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add submission-first handoff UI with side-panel details and unresolved priority cues</name>
  <files>app/dashboard/accommodation/page.tsx, app/dashboard/accommodation/filter-state.ts</files>
  <read_first>app/dashboard/accommodation/page.tsx, app/dashboard/accommodation/filter-state.ts, .planning/phases/20-operator-handoff-compatibility-layer/20-CONTEXT.md</read_first>
  <action>
    Update accommodation operator page to consume Plan 20-01 payload and enforce handoff UX decisions:
    - Render unresolved assignment queue rows before normal rows and include a visible unresolved badge/state reason in the queue list.
    - Add a right-side detail panel (not modal-only) for selected attendee/submission row showing booking/submission metadata, assignment intent, rooming notes, roommate preference/avoid, and dietary/location fields.
    - Keep source label hidden in default list rows; show source metadata only in detail panel or expandable metadata section.
    - Preserve existing assignment/unassignment controls and signal filter URL synchronization behavior.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Accommodation page includes a side-panel detail container tied to selected queue row state.
    - Queue render logic explicitly sorts/prioritizes unresolved rows before standard rows.
    - Source label is absent from default primary row text and present in metadata/detail section.
    - Existing filter URL sync helper functions (`syncSignalFiltersToSearchParams`/`appendSignalFiltersToQuery`) remain in use.
  </acceptance_criteria>
  <done>Operators can immediately triage unresolved submissions and inspect rooming context in a submission-first side-panel workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce source-agnostic dashboard event adapter and wire finance attendee/order domains</name>
  <files>convex/events.ts, lib/domain/finance/order-ledger.ts, lib/domain/finance/attendees.ts</files>
  <read_first>convex/events.ts, lib/domain/finance/order-ledger.ts, lib/domain/finance/attendees.ts, convex/schema.ts, .planning/phases/18-dual-source-event-signup-platform/18-01-SUMMARY.md</read_first>
  <action>
    Implement a source-agnostic event adapter while preserving integration compatibility:
    - Add a new Convex query in `convex/events.ts` returning dashboard-usable event options across both integration (`ticketTailorEvents`) and internal canonical events (`events` + `eventSources`) with stable fields: `eventRef`, `providerEventId` (nullable for internal), `name`, `sourceKind`.
    - Update `lib/domain/finance/order-ledger.ts` and `lib/domain/finance/attendees.ts` to consume the new event query and normalize filter comparisons so integration event IDs still work and internal events are represented without breaking existing screens.
    - Keep existing Ticket Tailor/Tikkie order/payment query calls unchanged in this plan; only update event-option adapters and source-safe filter plumbing.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `convex/events.ts` exports a dedicated source-agnostic dashboard events query.
    - Order ledger and attendee domain files reference the new events query for `availableEvents` population.
    - Existing order/payment Convex function references (e.g., `api.orders.getOrdersWithFilters`) remain unchanged.
    - Mixed-source event options include a `sourceKind` field and still provide integration-compatible identifiers.
  </acceptance_criteria>
  <done>Dashboard event adapters become source-agnostic without regressing existing integration-backed finance flows.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- accommodation`
</verification>

<success_criteria>

- OPS-02 is met: internal and integration events share source-agnostic dashboard read adapters and operator UI behavior.
- Primary operator rooming view remains focused on actionable rows while source metadata stays optional-on-inspection.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-operator-handoff-compatibility-layer/20-02-SUMMARY.md`
</output>
