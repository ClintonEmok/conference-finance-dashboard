---
phase: 20-operator-handoff-compatibility-layer
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
requirements: [OPS-01, OPS-02]
files_modified:
  - app/api/dashboard/accommodation/assignments/route.test.ts
  - lib/domain/finance/order-ledger.test.ts
  - lib/domain/finance/attendees.test.ts
autonomous: false
must_haves:
  truths:
    - "Submitted signup records are visible in operator accommodation workflow with unresolved-first triage and detail metadata."
    - "Mixed-source event adapters keep integration-backed finance pages functional while allowing internal events in shared filter lists."
    - "Compatibility guarantees are locked by automated tests before manual operator verification."
  artifacts:
    - path: "app/api/dashboard/accommodation/assignments/route.test.ts"
      provides: "Route-level regression coverage for mixed-source handoff board payload"
      contains: "unresolved-first ordering + submission metadata assertions"
    - path: "lib/domain/finance/order-ledger.test.ts"
      provides: "Order ledger mixed-source event adapter compatibility coverage"
      contains: "integration IDs preserved + internal source options accepted"
    - path: "lib/domain/finance/attendees.test.ts"
      provides: "Attendee ledger mixed-source event adapter compatibility coverage"
      contains: "source-agnostic availableEvents + filter compatibility checks"
  key_links:
    - from: "app/api/dashboard/accommodation/assignments/route.test.ts"
      to: "app/api/dashboard/accommodation/assignments/route.ts"
      via: "GET route behavior assertions"
      pattern: "describe\(|it\("
    - from: "lib/domain/finance/order-ledger.test.ts"
      to: "lib/domain/finance/order-ledger.ts"
      via: "adapter normalization tests"
      pattern: "getOrderLedger"
    - from: "lib/domain/finance/attendees.test.ts"
      to: "lib/domain/finance/attendees.ts"
      via: "adapter normalization tests"
      pattern: "getAttendeeLedger"
---

<objective>
Lock Phase 20 behavior with regression coverage and complete operator handoff verification from public signup submission through accommodation workflow consumption.

Purpose: Ensures OPS-01/OPS-02 remain stable and prevents regressions as internal-source data enters shared operational paths.
Output: Automated compatibility tests + human verification checkpoint for operator handoff UX.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-CONTEXT.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-RESEARCH.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-01-SUMMARY.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-02-SUMMARY.md
@app/api/dashboard/accommodation/assignments/route.ts
@lib/domain/finance/order-ledger.ts
@lib/domain/finance/attendees.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accommodation route regression coverage for submission handoff payload behavior</name>
  <files>app/api/dashboard/accommodation/assignments/route.test.ts</files>
  <read_first>app/api/dashboard/accommodation/assignments/route.ts, app/api/dashboard/attendees/route.ts, app/api/signup/submit/route.test.ts</read_first>
  <action>
    Create route tests for `GET /api/dashboard/accommodation/assignments` validating Phase 20 handoff expectations:
    - Mock auth success and domain adapter response containing mixed integration/internal rows.
    - Assert unresolved rows are returned first and include `unresolvedReason`.
    - Assert internal submission rows carry `source`, `submissionId`, `bookingRef`, and rooming-note fields in response JSON.
    - Assert auth failure and invalid-parameter cases still map to existing `UNAUTHORIZED` and `BAD_REQUEST` contracts.
  </action>
  <verify>npm run test -- dashboard/accommodation/assignments</verify>
  <acceptance_criteria>
    - New route test file exists and imports the accommodation assignments route module.
    - Tests include assertions for unresolved-first behavior and submission metadata fields.
    - Unauthorized and bad-request contract tests are present and passing.
  </acceptance_criteria>
  <done>Accommodation API contract is regression-locked for mixed-source handoff payloads and compatibility errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add mixed-source adapter compatibility tests for order + attendee ledgers</name>
  <files>lib/domain/finance/order-ledger.test.ts, lib/domain/finance/attendees.test.ts</files>
  <read_first>lib/domain/finance/order-ledger.ts, lib/domain/finance/attendees.ts, lib/domain/finance/matched-payments.ts</read_first>
  <action>
    Add domain tests to protect source-agnostic adapter behavior:
    - `order-ledger.test.ts`: assert available-event options include integration and internal source rows while preserving existing integration filter behavior.
    - `attendees.test.ts`: assert mixed-source event options do not drop integration attendees and internal event options are represented in the returned filter metadata.
    - Mock only Convex query boundaries; do not change order/payment matching logic.
  </action>
  <verify>npm run test -- finance</verify>
  <acceptance_criteria>
    - `order-ledger.test.ts` contains at least one mixed-source availableEvents assertion.
    - `attendees.test.ts` contains at least one mixed-source availableEvents/filter compatibility assertion.
    - No tests mutate payment matching behavior; assertions focus on adapter/read compatibility.
  </acceptance_criteria>
  <done>Mixed-source event adapter behavior is test-protected for both attendee and order finance read models.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify operator handoff flow in UI</name>
  <what-built>End-to-end operator handoff path from submitted signup data to accommodation operator queue + detail side-panel, with source-agnostic compatibility adapters and automated regressions.</what-built>
  <how-to-verify>
    1. Start app (`npm run dev`) and ensure local data includes at least one internal submission and one integration-backed attendee/event.
    2. Open `/dashboard/accommodation`.
    3. Confirm unresolved entries are listed before normal entries.
    4. Select an unresolved entry and confirm side panel shows booking reference, source metadata, assignment intent, and rooming note fields.
    5. Confirm primary list remains readable without source label noise.
    6. Open attendee/order dashboard filters and confirm both internal and integration event options appear without breaking existing integration results.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- accommodation`
- `npm run test -- finance`
</verification>

<success_criteria>

- OPS-01 verified: submitted assignment + note fields are operator-visible in room/allocation workflow.
- OPS-02 verified: integration behavior remains stable while internal events use shared source-agnostic read adapters.
- Automated tests and manual verification confirm handoff/compatibility outcomes end-to-end.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-operator-handoff-compatibility-layer/20-03-SUMMARY.md`
</output>
