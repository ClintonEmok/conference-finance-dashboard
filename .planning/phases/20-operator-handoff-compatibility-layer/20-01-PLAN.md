---
phase: 20-operator-handoff-compatibility-layer
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [OPS-01]
files_modified:
  - convex/accommodation.ts
  - lib/domain/accommodation/assignments.ts
  - app/api/dashboard/accommodation/assignments/route.ts
autonomous: true
must_haves:
  truths:
    - "Operator accommodation board includes canonical signup submission attendees without manual data reshaping."
    - "Assignment intent and rooming notes from submitted signup data are visible in operator board payloads."
    - "Unresolved assignment situations are emitted as queue-priority records before normal rows."
  artifacts:
    - path: "convex/accommodation.ts"
      provides: "Source-aware operator board query merging integration attendees and canonical submission attendees"
      contains: "submissionAttendees + submissionAssignments + submissions joins"
    - path: "lib/domain/accommodation/assignments.ts"
      provides: "Typed RoomAllocationBoard contract updated for submission-derived handoff metadata"
      contains: "bookingRef/submissionId/source/assignmentIntent/unresolvedReason fields"
    - path: "app/api/dashboard/accommodation/assignments/route.ts"
      provides: "Dashboard API contract exposing enriched mixed-source board"
      contains: "GET contract preserving BAD_REQUEST/INTERNAL_ERROR behavior"
  key_links:
    - from: "convex/accommodation.ts"
      to: "submissionAttendees/submissionAssignments/submissions"
      via: "read-time join inside getRoomAllocationBoard"
      pattern: "query\(\"submissionAttendees\"\)|query\(\"submissionAssignments\"\)|query\(\"submissions\"\)"
    - from: "lib/domain/accommodation/assignments.ts"
      to: "api.accommodation.getRoomAllocationBoard"
      via: "convexQuery adapter and typed payload shaping"
      pattern: "convexQuery\(api\.accommodation\.getRoomAllocationBoard"
    - from: "app/api/dashboard/accommodation/assignments/route.ts"
      to: "getRoomAllocationBoard"
      via: "GET request handling"
      pattern: "const board = await getRoomAllocationBoard"
---

<objective>
Extend the accommodation operator read model so canonical signup submission data (assignments + notes) appears directly in the existing room/allocation workflow payload.

Purpose: Satisfies OPS-01 by eliminating post-submit manual reshaping between public signup and operator rooming operations.
Output: Enriched mixed-source room allocation board contract exposed through Convex + domain + dashboard API boundaries.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-CONTEXT.md
@.planning/phases/20-operator-handoff-compatibility-layer/20-RESEARCH.md
@.planning/phases/18-dual-source-event-signup-platform/18-02-SUMMARY.md
@.planning/phases/18-dual-source-event-signup-platform/18-03-SUMMARY.md
@convex/schema.ts
@convex/signupSubmission.ts
@convex/accommodation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge canonical submission attendees into operator room allocation read model</name>
  <files>convex/accommodation.ts</files>
  <read_first>convex/accommodation.ts, convex/schema.ts, convex/signupSubmission.ts, convex/attendees.ts, convex/_generated/ai/guidelines.md</read_first>
  <action>
    Update `getRoomAllocationBoard` in `convex/accommodation.ts` so the board becomes source-aware and submission-compatible:
    - Read bounded canonical submission tables (`submissions`, `submissionAttendees`, `submissionAssignments`) and accommodation slot mappings (`accommodationSlots`) scoped by optional `eventId`.
    - Build unified queue rows that preserve existing integration attendee fields and add canonical submission metadata: `source` (`integration|internal`), `submissionId`, `bookingRef`, `submissionNotes`, `assignmentIntent` (`assign|skip`), `roommatePreference`, `roommateAvoid`, `dietaryRestrictions`, `bookerName`, and `unresolvedReason`.
    - Emit unresolved assignment rows first (e.g. slot missing, skipped intent, missing assignable room target) and keep deterministic ordering by `allocationPriority`, `submittedAt`/`_creationTime`, then attendee key.
    - Keep existing room occupancy and assignment mutation behavior unchanged (no write-path refactor in this plan).
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `convex/accommodation.ts` contains bounded reads from `submissionAttendees`, `submissionAssignments`, and `submissions` inside `getRoomAllocationBoard`.
    - Board payload rows include `source`, `submissionId`, `bookingRef`, `assignmentIntent`, and `unresolvedReason` fields for canonical signup attendees.
    - Queue ordering code explicitly prioritizes unresolved rows before non-unresolved rows.
    - Existing `assignRoomToAttendee`/`unassignRoomFromAttendee` mutation handlers remain present and unchanged in behavior.
  </acceptance_criteria>
  <done>Canonical submission attendees and notes flow into operator board read payloads with unresolved assignment prioritization.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate enriched handoff contract through domain + dashboard API adapter</name>
  <files>lib/domain/accommodation/assignments.ts, app/api/dashboard/accommodation/assignments/route.ts</files>
  <read_first>lib/domain/accommodation/assignments.ts, app/api/dashboard/accommodation/assignments/route.ts, app/dashboard/accommodation/page.tsx</read_first>
  <action>
    Update domain/API contract layers to carry the new mixed-source fields without breaking existing consumers:
    - Extend `RoomAllocationBoard` and nested attendee/occupant types in `lib/domain/accommodation/assignments.ts` to include `source`, `submissionId`, `bookingRef`, `submissionNotes`, `assignmentIntent`, `roommatePreference`, `roommateAvoid`, and `unresolvedReason`.
    - Preserve current filter normalization behavior (`eventId`, `search`, room filters, signal filters) and keep backward-compatible defaults.
    - Keep `app/api/dashboard/accommodation/assignments/route.ts` response shape stable while passing through the new fields from the domain adapter and preserving existing auth/error contracts.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `RoomAllocationBoard` type definitions include new mixed-source handoff metadata fields.
    - `getRoomAllocationBoard` adapter in domain file still calls `api.accommodation.getRoomAllocationBoard` and returns enriched data.
    - API route GET handler still returns `NextResponse.json(board)` and retains `BAD_REQUEST` and `INTERNAL_ERROR` mappings.
    - No new required query params are introduced for existing route consumers.
  </acceptance_criteria>
  <done>Operator board API now serves a typed, enriched mixed-source handoff contract consumable by existing UI routes.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm run test -- accommodation`
</verification>

<success_criteria>

- OPS-01 contract baseline is met at read-model level: submission assignment + note data is available to operators via existing accommodation API.
- Mixed-source board payload remains backward-compatible for current UI consumer pathways.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-operator-handoff-compatibility-layer/20-01-SUMMARY.md`
</output>
