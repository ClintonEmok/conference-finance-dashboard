---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/providers.tsx
  - components/ui/input.tsx (add if missing)
  - components/ui/label.tsx (add if missing)
  - components/ui/table.tsx (add)
  - components/ui/skeleton.tsx (add)
  - components/ui/badge.tsx (add)
  - components/ui/separator.tsx (add)
  - components/ui/scroll-area.tsx (add)
  - components/ui/dropdown-menu.tsx (add)
  - components/ui/avatar.tsx (add)
  - app/dashboard/attendees/page.tsx
  - app/dashboard/orders/page.tsx
  - app/dashboard/reconciliation/page.tsx
autonomous: true

must_haves:
  truths:
    - "Tables use shadcn Table component with consistent styling"
    - "Form inputs use shadcn Input/Textarea components"
    - "Loading states use Skeleton components"
    - "Status badges use Badge component variants"
artifacts:
  - path: "components/ui/table.tsx"
    provides: "Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TableCaption"
  - path: "components/ui/badge.tsx"
    provides: "Badge with variants (secondary, outline, destructive)"
  - path: "components/ui/skeleton.tsx"
    provides: "Skeleton loading placeholder"
key_links:
  - from: "app/dashboard/attendees/page.tsx"
    to: "components/ui/table"
    via: "Table, TableRow, TableCell imports"
---

<objective>
Redesign the application UI using shadcn/ui components. This is a comprehensive refactor to modernize the dashboard's appearance, improve consistency, and leverage shadcn's design system.
</objective>

<context>
@app/dashboard/attendees/page.tsx
@app/dashboard/orders/page.tsx
@app/dashboard/reconciliation/page.tsx
@components/ui/button.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
<name>Install core UI components</name>
<files>components/ui/</files>
<action>
Install the following shadcn components needed for tables and data display:

```bash
npx shadcn@latest add table skeleton badge separator scroll-area dropdown-menu avatar input label
```

These will be used across all dashboard pages for consistent data display and form inputs.
</action>
<verify>ls components/ui/table.tsx components/ui/skeleton.tsx components/ui/badge.tsx components/ui/separator.tsx components/ui/scroll-area.tsx components/ui/dropdown-menu.tsx components/ui/avatar.tsx components/ui/input.tsx components/ui/label.tsx</verify>
<done>Core shadcn components installed for UI redesign</done>
</task>

<task type="auto">
<name>Refactor attendees table to use shadcn Table</name>
<files>app/dashboard/attendees/page.tsx</files>
<action>
In `app/dashboard/attendees/page.tsx`, replace the HTML table with shadcn Table components:

1. Add imports:

```tsx
import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"
import { Badge } from "@/components/ui/badge"
import { Skeleton } from "@/components/ui/skeleton"
```

2. Replace `<table className="...">` with `<Table>`, `<thead>` with `<TableHeader>`, `<tbody>` with `<TableBody>`, `<tr>` with `<TableRow>`, `<th>` with `<TableHead>`, `<td>` with `<TableCell>`

3. Replace status spans with `<Badge variant="secondary">` or appropriate variant

4. Replace loading `<div>` placeholders with `<Skeleton className="h-4 w-[100px]" />` patterns

5. Use proper semantic colors: `text-muted-foreground` instead of hardcoded grays
   </action>
   <verify>npm run build 2>&1 | tail -20</verify>
   <done>Attendees table uses shadcn Table, Badge, Skeleton components</done>
   </task>

<task type="auto">
<name>Refactor orders table to use shadcn Table</name>
<files>app/dashboard/orders/page.tsx</files>
<action>
Apply the same shadcn Table refactoring pattern to `app/dashboard/orders/page.tsx`:
- Import Table, Badge, Skeleton components
- Replace HTML table elements with shadcn Table components
- Convert status displays to Badge components
- Replace loading states with Skeleton components
- Use semantic color tokens
</action>
<verify>npm run build 2>&1 | tail -20</verify>
<done>Orders table uses shadcn Table, Badge, Skeleton components</done>
</task>

<task type="auto">
<name>Refactor reconciliation page to use shadcn components</name>
<files>app/dashboard/reconciliation/page.tsx</files>
<action>
In `app/dashboard/reconciliation/page.tsx`:
- Import Table, Badge, Skeleton, Card components as needed
- Apply shadcn Table for data display
- Use Card components for summary sections if not already present
- Replace any custom styled elements with shadcn equivalents
- Apply semantic color tokens throughout
</action>
<verify>npm run build 2>&1 | tail -20</verify>
<done>Reconciliation page uses shadcn components</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- Tables render with consistent shadcn styling
- Status badges use Badge component variants
- Loading states use Skeleton components
- No raw HTML table elements remain in refactored pages
</verification>

<success_criteria>

- At least 3 dashboard pages refactored to use shadcn Table component
- All status displays use Badge component
- All loading states use Skeleton component
- Consistent semantic color usage across pages
- No regression in functionality
  </success_criteria>

<output>
After completion, create `.planning/quick/260322-0dv-redesign-the-entire-application-ui-using/260322-0dv-SUMMARY.md`
</output>
