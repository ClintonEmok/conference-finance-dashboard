---
phase: quick-260326-di7-fix-the-ticket-tailor-sync-feature
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/ticket-tailor/sync/route.ts
  - app/dashboard/ticket-tailor/sync/page.tsx
  - convex/sync.ts
  - lib/integrations/ticket-tailor/client.ts
  - lib/integrations/ticket-tailor/sync.ts
  - tests/ticket-tailor/client.test.ts
  - tests/ticket-tailor/sync-route.test.ts
autonomous: true
must_haves:
  truths:
    - "An authenticated operator can trigger Ticket Tailor sync from `/api/ticket-tailor/sync` and receive a stable success or failure payload."
    - "A sync run starts, completes, and records diagnostics in `ticketTailorSyncRuns` instead of failing mid-pipeline without status updates."
    - "Scoped Ticket Tailor event/order/attendee imports still upsert through the Convex sync contract and surface usable counts/diagnostics in the dashboard sync page."
  artifacts:
    - path: "app/api/ticket-tailor/sync/route.ts"
      provides: "Protected manual sync endpoint with stable JSON contract"
    - path: "lib/integrations/ticket-tailor/sync.ts"
      provides: "Ticket Tailor sync orchestration using generated `api.sync.*` refs"
    - path: "convex/sync.ts"
      provides: "Convex sync run, event/order/attendee upsert, and diagnostics persistence"
    - path: "tests/ticket-tailor/sync-route.test.ts"
      provides: "Regression coverage for route contract and sync failure handling"
  key_links:
    - from: "app/api/ticket-tailor/sync/route.ts"
      to: "lib/integrations/ticket-tailor/sync.ts"
      via: "`runTicketTailorSync(scope)`"
      pattern: "runTicketTailorSync"
    - from: "lib/integrations/ticket-tailor/sync.ts"
      to: "convex/sync.ts"
      via: "generated `api.sync.*` mutations and queries"
      pattern: "api\\.sync\\."
    - from: "lib/integrations/ticket-tailor/client.ts"
      to: "lib/integrations/ticket-tailor/sync.ts"
      via: "paginated event/order fetch + attendee fallback extraction"
      pattern: "fetchTicketTailor.*|extractAttendeeItems"
---

<objective>
Repair the Ticket Tailor sync flow without widening scope beyond the broken operator workflow.

Purpose: Restore trust in manual Ticket Tailor sync after the recent Convex contract cleanup.
Output: A working scoped sync path, preserved route/UI contract, and regression coverage for the bug that caused sync to fail.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@.planning/phases/13-rebuild-convex-mutation-and-api-layer-from-clean-contracts/13-05-SUMMARY.md
@app/api/ticket-tailor/sync/route.ts
@app/dashboard/ticket-tailor/sync/page.tsx
@convex/sync.ts
@lib/integrations/ticket-tailor/client.ts
@lib/integrations/ticket-tailor/sync.ts
@tests/ticket-tailor/sync-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace and repair the broken Ticket Tailor sync contract</name>
  <files>lib/integrations/ticket-tailor/sync.ts, convex/sync.ts, lib/integrations/ticket-tailor/client.ts, app/api/ticket-tailor/sync/route.ts</files>
  <action>Reproduce the current Ticket Tailor sync failure from the manual sync path, then fix the smallest broken boundary in the existing flow. Preserve the current Clerk-protected route contract and the generated `api.sync.*` integration pattern from Phase 13. Keep `runTicketTailorSync()` responsible for starting/completing a sync run, upserting events/orders/attendees, and returning the same summary shape already consumed by the route and dashboard page. If the failure sits in Ticket Tailor pagination or canonical attendee fallback handling, fix that in `lib/integrations/ticket-tailor/client.ts`; if it sits in Convex validators or run completion wiring, fix that in `convex/sync.ts`. Do not redesign the pipeline or change endpoint semantics unless the bug makes the current contract impossible.</action>
  <verify>Run the smallest reproduction that currently fails, then re-run it after the fix; run `npx convex codegen` if any Convex function signatures change; run `npm run typecheck`.</verify>
  <done>The manual sync path can execute end-to-end again, `ticketTailorSyncRuns` records a terminal status with diagnostics, and the route still returns the expected success/failure JSON shape.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the fix with route and provider regression coverage</name>
  <files>tests/ticket-tailor/sync-route.test.ts, tests/ticket-tailor/client.test.ts</files>
  <action>Update the existing sync route tests and add or extend provider-client tests so the exact bug cannot regress. Keep route coverage focused on auth, request parsing, and stable JSON responses; keep provider/client coverage focused on the failing fetch/parsing case or sync helper edge case uncovered in Task 1. Prefer targeted tests over broad rewrites, and keep mocks aligned with the current `api.sync.*` contract and Clerk auth helpers.</action>
  <verify>Run `npm test -- tests/ticket-tailor/sync-route.test.ts tests/ticket-tailor/client.test.ts`.</verify>
  <done>The failing case is covered by an automated regression test, and the existing manual sync route contract remains explicitly asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the operator sync screen still matches the repaired backend contract</name>
  <files>app/dashboard/ticket-tailor/sync/page.tsx</files>
  <action>Update the dashboard sync page only if the repaired backend response exposes fields the UI currently misreads or omits. Keep the page contract-compatible with the route response, surface the repaired counts/diagnostics clearly, and avoid cosmetic redesign. This task is only for wiring and display correctness after the backend fix, not a UX overhaul.</action>
  <verify>Run `npm run typecheck` and `npm test -- tests/ticket-tailor/sync-route.test.ts tests/ticket-tailor/client.test.ts`; if a local dev server is available, trigger one sync from `/dashboard/ticket-tailor/sync` and confirm the result card renders the returned scope/counts/diagnostics without client errors.</verify>
  <done>The operator page can submit a sync request and render the repaired response shape without broken fields, missing counts, or stale assumptions about the backend payload.</done>
</task>

</tasks>

<verification>
- `npx convex codegen` if any Convex signature or validator changed.
- `npm run typecheck`
- `npm test -- tests/ticket-tailor/sync-route.test.ts tests/ticket-tailor/client.test.ts`
- If the app is running locally, perform one manual sync from `/dashboard/ticket-tailor/sync` and confirm a terminal result renders.
</verification>

<success_criteria>

- `/api/ticket-tailor/sync` remains Clerk-protected and returns stable JSON for success, validation errors, and sync failures.
- `runTicketTailorSync()` completes the repaired event/order/attendee import flow and always closes its sync run with a final status.
- The bug that broke Ticket Tailor sync is covered by targeted regression tests.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-di7-fix-the-ticket-tailor-sync-feature/260326-di7-SUMMARY.md`
</output>
