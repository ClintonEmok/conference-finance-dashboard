---
phase: quick
plan: "260321-s9e"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
---

<objective>
Replace the placeholder root `/` page with a branded landing page that checks auth status and redirects authenticated users to `/dashboard`, or shows a professional landing page with a sign-in CTA for unauthenticated visitors.
</objective>

<context>
@app/page.tsx
@app/dashboard/page.tsx
@middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Replace root page with branded landing + auth-aware redirect</name>
  <files>app/page.tsx</files>
  <action>
Replace the contents of `app/page.tsx` entirely with a client component that:

1. Imports `useEffect` from React and `useRouter` from `next/navigation`
2. Imports `authClient` from `@/lib/auth-client`
3. On mount, calls `authClient.getSession()` to check if a session exists
4. If session exists (no error, session is truthy), uses `router.replace("/dashboard")` to redirect
5. If no session, renders a branded landing page matching the dashboard's visual style (deep purple gradient, Outfit font, shadcn/ui components):
   - Hero section with the same purple gradient card style used in the dashboard (`bg-[linear-gradient(145deg,rgba(113,84,255,0.97),rgba(83,56,171,0.94))]`)
   - Headline: "Conference Finance Command Center"
   - Subline: "One trusted dashboard for church conference finance operations."
   - A prominent "Sign in to dashboard" button (using `Button` from `@/components/ui/button`) that links to `/login`
   - Minimal, centered layout with the content in a `max-w-md` column, vertically centered (`min-h-svh`, `items-center`)
   - Use the same `HandCoins` or `ArrowRight` icon from lucide-react for the CTA
6. While checking session (loading state), show a minimal loading indicator (e.g. a small spinner or "Loading..." text) to avoid a flash of content

Keep all existing imports and the `Outfit` font from `layout.tsx` available (they're already loaded at the root layout level). The page should use `"use client"` directive.
</action>
<verify>`npm run build` completes without errors and `app/page.tsx` is the only modified file in the diff</verify>
<done>Landing page renders at `/` for unauthenticated users with sign-in CTA; authenticated users are silently redirected to `/dashboard`</done>
</task>

</tasks>

<verification>
`npm run build` passes cleanly with no TypeScript errors or import issues.
</verification>

<success_criteria>

- Root `/` page renders a branded landing page matching dashboard aesthetics
- No session = sign-in CTA present and clickable
- Active session = redirect to `/dashboard` (verified by checking session logic)
- No console errors, clean build
  </success_criteria>

<output>
After completion, create `.planning/quick/260321-s9e-add-a-landing-page-on-the-root-route/260321-s9e-SUMMARY.md`
</output>
