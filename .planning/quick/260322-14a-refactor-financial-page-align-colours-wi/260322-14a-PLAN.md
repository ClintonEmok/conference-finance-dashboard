---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/financial/page.tsx
autonomous: true
---

<objective>
Refactor the financial page to use shadcn semantic colors instead of hardcoded hex values. Ensure visual consistency with the rest of the dashboard.
</objective>

<context>
@app/dashboard/financial/page.tsx
</context>

<tasks>

<task type="auto">
<name>Replace hardcoded colors with shadcn semantic tokens</name>
<files>app/dashboard/financial/page.tsx</files>
<action>
In `app/dashboard/financial/page.tsx`, replace all hardcoded colors with shadcn semantic tokens:

**Replace hardcoded blues:**

- `#0d5ea8` → `text-primary` or `text-foreground`
- `#0a4f8d` → `text-primary/90`
- `#2c5da8` → `text-primary`
- `#e9f2ff` → `bg-primary/10` or `bg-accent`
- `#d7e6ff` → `border-border` or `border-input`
- `#f8fbff` → `bg-muted/30` or `bg-accent`

**Replace slates:**

- `text-slate-950` → `text-foreground`
- `text-slate-600` → `text-muted-foreground`
- `text-slate-500` → `text-muted-foreground`
- `text-slate-400` → `text-muted-foreground/60`
- `bg-slate-50/70` → `bg-muted/50`
- `border-slate-100`, `border-slate-200` → `border-border`

**Replace gradients:**

- `bg-[linear-gradient(180deg,#eff6ff_0%,#ffffff_100%)]` → `bg-card` (or remove gradient for cleaner look)
- Custom shadows like `shadow-[0_24px_70px_rgba(15,23,42,0.06)]` → `shadow-sm` or `shadow-md`

**Button updates:**

- `bg-[#0d5ea8]` → `bg-primary`
- `hover:bg-[#0a4f8d]` → `hover:bg-primary/90`
- `border-[#c9daf5]` → `border-input`
- `text-[#0d5ea8]` → `text-primary` or `text-foreground`

**Use Card component:**

- Wrap cards in `Card` component from `@/components/ui/card` for consistent styling
- Use `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`

**Key principles:**

- Use `cn()` from `@/lib/utils` for conditional classes if needed
- Use `gap-*` not `space-y-*` for spacing
- Keep layout structure but change colors only
  </action>
  <verify>npm run build 2>&1 | tail -20</verify>
  <done>Financial page uses shadcn semantic colors, no hardcoded hex values remain</done>
  </task>

</tasks>

<verification>
- `npm run build` passes
- No hardcoded colors like `#0d5ea8`, `#d7e6ff`, etc. remain
- All text uses semantic tokens (text-foreground, text-muted-foreground)
- All backgrounds use semantic tokens (bg-card, bg-muted, bg-accent)
- Card component used for consistent styling
</verification>

<success_criteria>

- Hardcoded hex colors replaced with semantic shadcn tokens
- Visual appearance remains professional and cohesive
- No regression in layout or functionality
  </success_criteria>

<output>
After completion, create `.planning/quick/260322-14a-refactor-financial-page-align-colours-wi/260322-14a-SUMMARY.md`
</output>
