---
phase: quick-260326-it5-allow-hotels-to-be-deleted-if-there-aree
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/accommodation.ts
  - app/api/dashboard/accommodation/room-types/[roomTypeId]/route.ts
  - app/dashboard/accommodation/inventory/page.tsx
autonomous: true
must_haves:
  truths:
    - "Operators can delete a hotel when none of that hotel's rooms have assigned attendees."
    - "Hotel deletion is blocked with a clear message when any room in the hotel still has assigned attendees."
    - "Operators can delete an unused room type from the accommodation inventory UI."
    - "Room type deletion is blocked with a clear message when rooms still exist for that type."
  artifacts:
    - path: "convex/accommodation.ts"
      provides: "Guard logic for hotel and room-type delete mutations based on assignment/usage constraints"
    - path: "app/api/dashboard/accommodation/room-types/[roomTypeId]/route.ts"
      provides: "Protected DELETE endpoint for room type removal with operator-facing BAD_REQUEST mapping"
    - path: "app/dashboard/accommodation/inventory/page.tsx"
      provides: "Inventory UI actions and feedback for room type deletion and updated hotel delete guidance"
  key_links:
    - from: "app/dashboard/accommodation/inventory/page.tsx"
      to: "/api/dashboard/accommodation/room-types/[roomTypeId]"
      via: "room type delete button action"
      pattern: "fetch\(`/api/dashboard/accommodation/room-types/\${roomTypeId}`"
    - from: "app/api/dashboard/accommodation/room-types/[roomTypeId]/route.ts"
      to: "lib/domain/accommodation/inventory.ts -> convex/accommodation.ts"
      via: "deleteRoomType domain call"
      pattern: "deleteRoomType"
    - from: "convex/accommodation.ts"
      to: "ticketTailorAttendees"
      via: "assignedRoomId lookup before hotel delete"
      pattern: "withIndex\(\"assignedRoomId\""
---

<objective>
Allow accommodation cleanup to match real operator intent: hotels should be deletable when no attendees are assigned to their rooms, and room types should also be deletable from inventory when unused.

Purpose: remove unnecessary deletion blockers while preserving assignment safety.
Output: adjusted hotel delete guard, room type delete API surface, and inventory UI delete action for room types.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@.planning/quick/260326-ib1-for-room-inventory-we-need-to-be-to-able/260326-ib1-SUMMARY.md
@convex/accommodation.ts
@lib/domain/accommodation/inventory.ts
@app/api/dashboard/accommodation/room-types/route.ts
@app/dashboard/accommodation/inventory/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align hotel delete guard with attendee assignment truth</name>
  <files>convex/accommodation.ts</files>
  <action>Update `deleteHotel` so blocking is determined by actual attendee assignments (`ticketTailorAttendees.assignedRoomId`) for the hotel's rooms, not by potentially stale `occupiedBeds` counters. Keep destructive safety strict: if any attendee is assigned to any room in the hotel, throw `Cannot delete hotel with assigned attendees`; otherwise continue deleting the hotel's rooms, event-hotel links, and hotel record in one mutation. Preserve existing mutation args and return shape so route/UI contracts do not change.</action>
  <verify>Run `npm run typecheck`.</verify>
  <done>Hotels with zero assigned attendees across all hotel rooms delete successfully; hotels with at least one assigned attendee are blocked with the existing explicit error.</done>
</task>

<task type="auto">
  <name>Task 2: Add protected room-type DELETE endpoint with consistent error mapping</name>
  <files>app/api/dashboard/accommodation/room-types/[roomTypeId]/route.ts</files>
  <action>Create a dynamic API route for `DELETE /api/dashboard/accommodation/room-types/[roomTypeId]` using `requireApiUser()` and domain `deleteRoomType`. Map `Invalid...`, `...not found`, and `Cannot delete...` errors to `BAD_REQUEST`; keep unexpected failures as `INTERNAL_ERROR`. Match response structure used by other accommodation delete routes (`{ ok: true }` on success) so front-end handling can stay simple.</action>
  <verify>Run `npm run typecheck` and call `DELETE /api/dashboard/accommodation/room-types/{id}` with an authenticated session (success + blocked cases).</verify>
  <done>Room type delete is available via protected API and returns actionable operator-safe errors for usage conflicts.</done>
</task>

<task type="auto">
  <name>Task 3: Expose room-type delete action in inventory UI</name>
  <files>app/dashboard/accommodation/inventory/page.tsx</files>
  <action>Add room-type deletion controls in the inventory screen (where room types are already visible), including confirmation prompt, in-flight disable state, and per-room-type inline error feedback. Wire action to the new DELETE endpoint and refresh inventory after success. Update hotel delete confirmation copy to reflect the real rule (blocked only when attendees are assigned), not generic "all rooms are empty" wording.</action>
  <verify>Run `npm run typecheck` and manually validate inventory deletes on `/dashboard/accommodation/inventory`.</verify>
  <done>Operators can delete unused room types from the UI; blocked deletes show server reason; hotel delete messaging matches backend behavior.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Manual dashboard checks:
  1. Delete a hotel that has rooms but no assigned attendees -> succeeds.
  2. Delete a hotel where at least one room has assigned attendees -> blocked with `Cannot delete hotel with assigned attendees`.
  3. Delete a room type with no linked rooms -> succeeds and list refreshes.
  4. Delete a room type that still has linked rooms -> blocked with clear `Cannot delete...` message.
</verification>

<success_criteria>

- Hotel deletion behavior now matches assignment reality (not derived occupancy counters).
- Room type deletion is available end-to-end (UI -> API -> domain mutation).
- Operators receive clear, actionable feedback for both allowed and blocked delete attempts.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-it5-allow-hotels-to-be-deleted-if-there-aree/260326-it5-SUMMARY.md`
</output>
