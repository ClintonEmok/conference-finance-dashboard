---
phase: quick-260326-ib1-for-room-inventory-we-need-to-be-to-able
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/accommodation.ts
  - app/api/dashboard/accommodation/rooms/[roomId]/route.ts
  - app/api/dashboard/accommodation/hotels/[hotelId]/route.ts
  - app/dashboard/accommodation/inventory/page.tsx
  - app/dashboard/accommodation/rooms/[roomId]/page.tsx
autonomous: true
must_haves:
  truths:
    - "Operators can delete a room from inventory only when no attendees are assigned to that room."
    - "Operators can delete a hotel only when it has no room inventory and no active event scope links."
    - "When delete is blocked, the UI shows a clear reason instead of silently failing or unassigning people automatically."
  artifacts:
    - path: "convex/accommodation.ts"
      provides: "Deletion guard rules for room and hotel removal"
    - path: "app/api/dashboard/accommodation/rooms/[roomId]/route.ts"
      provides: "DELETE endpoint for room deletion with user-facing error mapping"
    - path: "app/api/dashboard/accommodation/hotels/[hotelId]/route.ts"
      provides: "DELETE endpoint for hotel deletion with user-facing error mapping"
    - path: "app/dashboard/accommodation/inventory/page.tsx"
      provides: "Inventory UI delete actions for hotels and refreshed state handling"
    - path: "app/dashboard/accommodation/rooms/[roomId]/page.tsx"
      provides: "Room-level delete action and post-delete navigation"
  key_links:
    - from: "app/dashboard/accommodation/inventory/page.tsx"
      to: "/api/dashboard/accommodation/hotels/[hotelId]"
      via: "delete button action with reload/error handling"
      pattern: "fetch\(`/api/dashboard/accommodation/hotels/\${hotelId}`"
    - from: "app/dashboard/accommodation/rooms/[roomId]/page.tsx"
      to: "/api/dashboard/accommodation/rooms/[roomId]"
      via: "room quick-action delete flow"
      pattern: "method:\s*\"DELETE\""
    - from: "app/api/dashboard/accommodation/*"
      to: "convex/accommodation.ts"
      via: "domain mutation guards for destructive operations"
      pattern: "Cannot delete"
---

<objective>
Add safe delete support for accommodation inventory so operators can remove unused rooms and hotels without breaking assignment integrity.

Purpose: unblock room-inventory cleanup while preserving assignment safety rules (no automatic unassign/cascade on destructive actions).
Output: guarded delete mutations, protected API delete routes, and UI delete actions for room/hotel inventory management.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/accommodation.ts
@app/dashboard/accommodation/inventory/page.tsx
@app/dashboard/accommodation/rooms/[roomId]/page.tsx
@app/api/dashboard/accommodation/rooms/[roomId]/route.ts
@lib/domain/accommodation/inventory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce backend delete guards and expose protected DELETE endpoints</name>
  <files>convex/accommodation.ts, app/api/dashboard/accommodation/rooms/[roomId]/route.ts, app/api/dashboard/accommodation/hotels/[hotelId]/route.ts</files>
  <action>Update `deleteRoom` in `convex/accommodation.ts` so it rejects deletion when attendees are still assigned (return a clear domain error like "Cannot delete room with assigned attendees") instead of auto-unassigning and deleting. Update `deleteHotel` so it rejects deletion when the hotel still has rooms and/or linked event scopes (clear error messages for each blocking condition), then deletes only when truly unassigned. In API routes, add `DELETE` support for rooms and hotels using existing `requireApiUser()` and domain helpers, mapping domain "Cannot delete..." and validation/not-found errors to `BAD_REQUEST` responses with actionable messages for operators.</action>
  <verify>Run `npm run typecheck`.</verify>
  <done>Delete attempts now follow business safety constraints: rooms/hotels with active assignments are blocked with clear API errors; unassigned records delete successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add inventory UI delete actions for rooms and hotels with clear feedback</name>
  <files>app/dashboard/accommodation/inventory/page.tsx, app/dashboard/accommodation/rooms/[roomId]/page.tsx</files>
  <action>Add explicit delete actions in operator-facing inventory surfaces: (a) hotel delete action in inventory cards, and (b) room delete action in room detail quick actions. Wire each action to the new DELETE endpoints, include confirmation prompts, disable during in-flight mutation, and show API error messages when deletion is blocked by assignments. On successful delete, refresh inventory state; for room detail deletion, redirect back to `/dashboard/accommodation/inventory` with a success-safe flow. Keep existing layout and inventory creation/edit flows intact.</action>
  <verify>Run `npm run typecheck` and manually test delete flows in the dashboard.</verify>
  <done>Operators can delete unused rooms/hotels from the UI, blocked deletes show clear reasons, and successful deletes update the dashboard without stale state.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Manual checks in local app:
  1. Open `/dashboard/accommodation/rooms/[roomId]` for a room with occupants and attempt delete -> blocked with clear message.
  2. Open `/dashboard/accommodation/rooms/[roomId]` for an empty room and delete -> success and redirect back to inventory.
  3. Open `/dashboard/accommodation/inventory` and delete a hotel with rooms or event scope -> blocked with clear message.
  4. Delete a hotel with zero rooms and no event scope links -> success and inventory refreshes without the hotel card.
</verification>

<success_criteria>

- Room deletion is allowed only for unassigned rooms; assigned rooms are never auto-unassigned by delete.
- Hotel deletion is allowed only when the hotel has no dependent room/event assignments.
- Inventory UI exposes delete actions that communicate blocked/success states clearly and keep the page data consistent after actions.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-ib1-for-room-inventory-we-need-to-be-to-able/260326-ib1-SUMMARY.md`
</output>
