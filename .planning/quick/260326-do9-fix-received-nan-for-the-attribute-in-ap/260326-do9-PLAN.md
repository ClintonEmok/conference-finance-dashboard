---
phase: quick-260326-do9-fix-received-nan-for-the-attribute-in-ap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/accommodation/inventory/page.tsx
  - lib/dashboard/accommodation/inventory-metrics.ts
  - tests/accommodation/inventory-metrics.test.ts
autonomous: true
must_haves:
  truths:
    - "The accommodation inventory page never renders `NaN` in the Available beds column."
    - "Hotel room-block totals stay finite even when the inventory payload contains missing or non-finite bed counts."
    - "Operators can still load `/dashboard/accommodation/inventory` and see the same room-capacity totals without a React children warning."
  artifacts:
    - path: "app/dashboard/accommodation/inventory/page.tsx"
      provides: "Inventory page wired to sanitized room metrics before rendering grouped hotel blocks"
    - path: "lib/dashboard/accommodation/inventory-metrics.ts"
      provides: "Pure helpers that coerce room capacity, occupied beds, and available beds to safe finite integers"
    - path: "tests/accommodation/inventory-metrics.test.ts"
      provides: "Regression coverage for NaN-prone room metric aggregation cases"
  key_links:
    - from: "app/dashboard/accommodation/inventory/page.tsx"
      to: "lib/dashboard/accommodation/inventory-metrics.ts"
      via: "room payload normalization and grouped room-block aggregation"
      pattern: "sanitize|normalize|group"
    - from: "tests/accommodation/inventory-metrics.test.ts"
      to: "lib/dashboard/accommodation/inventory-metrics.ts"
      via: "finite-number regression assertions for available beds"
      pattern: "availableBeds|Number\.isFinite|0"
---

<objective>
Fix the accommodation inventory page so Available beds can never surface as `NaN` in the React tree.

Purpose: Remove a user-facing runtime warning on the room inventory screen without changing the broader accommodation workflow.
Output: A small room-metrics sanitization helper, the inventory page wired to it, and a regression test covering the broken numeric edge case.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@app/dashboard/accommodation/inventory/page.tsx
@app/api/dashboard/accommodation/inventory/route.ts
@lib/domain/accommodation/inventory.ts
@convex/accommodation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract finite room-metric sanitization for inventory rendering</name>
  <files>lib/dashboard/accommodation/inventory-metrics.ts, tests/accommodation/inventory-metrics.test.ts</files>
  <action>Create a tiny pure helper module that accepts the room-level numbers used by `app/dashboard/accommodation/inventory/page.tsx` and returns safe finite integers for `capacity`, `occupiedBeds`, and `availableBeds`. Clamp invalid, missing, or non-finite values to `0`, and ensure grouped hotel block totals are derived from sanitized room values instead of raw payload fields. Keep this helper narrowly scoped to inventory-page rendering math; do not redesign the API payload or Convex contracts for this quick fix. Add targeted Vitest coverage for the exact regression shape: one or more rooms where `availableBeds` is `undefined`, `null`, or `NaN`, and the grouped output still reports `0` or the corrected finite total.</action>
  <verify>Run `npm test -- tests/accommodation/inventory-metrics.test.ts`.</verify>
  <done>The helper returns finite numbers for every room metric case the page consumes, and the regression test proves grouped Available beds totals cannot become `NaN`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the inventory page to sanitized totals and preserve the existing UI</name>
  <files>app/dashboard/accommodation/inventory/page.tsx</files>
  <action>Update `app/dashboard/accommodation/inventory/page.tsx` to use the new helper anywhere the page derives room totals, especially the grouped hotel block that renders “Available beds”. Sanitize room data before summing and rendering so `block.availableBeds`, capacity utilization inputs, and any adjacent derived counters stay finite. Preserve the current layout, labels, and fetch flow; this is a defensive rendering fix, not a UX rewrite. If the safest implementation is to normalize the fetched payload immediately after `loadInventory()`, keep that normalization local to this page and continue rendering the same operator-facing values when the backend sends valid data.</action>
  <verify>Run `npm run typecheck` and `npm test -- tests/accommodation/inventory-metrics.test.ts`.</verify>
  <done>`/dashboard/accommodation/inventory` renders the existing inventory cards and hotel blocks without ever passing `NaN` to React for Available beds or related capacity counters.</done>
</task>

</tasks>

<verification>
- `npm test -- tests/accommodation/inventory-metrics.test.ts`
- `npm run typecheck`
- If the app is running locally, open `/dashboard/accommodation/inventory` and confirm every “Available beds” value renders as a number (including problematic rooms) with no console warning about `Received NaN for the \`children\` attribute`.
</verification>

<success_criteria>

- The accommodation inventory page never renders `NaN` in the “Available beds” field.
- Grouped room totals are calculated from sanitized finite values, so malformed payload numbers degrade to safe zeros instead of propagating into the UI.
- A focused automated regression test covers the numeric edge case that previously produced the React warning.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-do9-fix-received-nan-for-the-attribute-in-ap/260326-do9-SUMMARY.md`
</output>
