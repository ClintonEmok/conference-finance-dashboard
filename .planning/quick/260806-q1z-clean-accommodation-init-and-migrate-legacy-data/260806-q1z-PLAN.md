---
phase: quick-260806-q1z-clean-accommodation-init-and-migrate-legacy-data
plan: 260806-q1z
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/init.ts
  - convex/accommodation.ts
  - tests/accommodation/accommodation-init.test.ts
autonomous: true
must_haves:
  truths:
    - "convex/init.ts seeds only the reusable accommodation catalog (categories, cot option, room types with real counts) and never auto-creates a night-before stay, per-event rates, event options, or resources for any event."
    - "Re-running init is idempotent: catalog rows upsert, stale superior_upgrade catalog rows and their event-option rows are removed, and eligibilityAgeBandCode is cleared from eventAccommodationOptions."
    - "Existing orders, selections, snapshots, payments, assignments, and the single existing event's stay/rates/resources are left intact — init only reconciles catalog rows and removes now-obsolete v5 option data."
    - "The dev deployment (acoustic-tiger-876) is migrated by running init, and a handler test locks the clean catalog + stale-row-removal contract so the seed cannot regress."
  artifacts:
    - path: "convex/init.ts"
      provides: "Reusable catalog seed + idempotent removal of obsolete superior_upgrade option data and legacy eligibilityAgeBandCode."
      contains: "handler"
    - path: "convex/accommodation.ts"
      provides: "No runtime behavior change; stale LOCKED_OPTION_SEMANTICS super_upgrade entry removed."
    - path: "tests/accommodation/accommodation-init.test.ts"
      provides: "Handler-level proof that init seeds the catalog, removes superior_upgrade, clears eligibilityAgeBandCode, and leaves per-event config untouched."
  key_links:
    - from: "convex/init.ts"
      to: "convex/schema.ts"
      via: "seeds accommodationCategories/accommodationOptions/accommodationRoomTypes; removes obsolete option rows"
      pattern: "accommodationOptions"
    - from: "convex/init.ts"
      to: "convex/finance.ts"
      via: "confirmed orders and legacy boolean selections keep pricing from their own snapshots/bridge, not from the removed option row"
      pattern: "optionsByKey"
---

<objective>
Clean up the accommodation seed and migrate the already-deployed legacy data on the `feat/simple-accommodation-dashboard` branch.

Purpose: `convex/init.ts` still auto-configures every accommodation-enabled event with a forced night-before stay, hardcoded rates, the cot option, and resources — behavior the v6 milestone explicitly rejected (accommodation must be optional and event-owned). The dev deployment already contains stale v5 rows from the earlier run of the old seed: a `superior_upgrade` catalog option and its event-option row, `eligibilityAgeBandCode: "under_3"` on the cot event option, and a cot catalog description still referencing the removed age-band model. This quick task turns init into a clean reusable-catalog-only seeder, makes re-running it migrate the deployment idempotently, and adds a handler test that locks the contract.

Output: A cleaned `convex/init.ts`, a removed stale `LOCKED_OPTION_SEMANTICS` entry, a new `tests/accommodation/accommodation-init.test.ts`, the dev deployment migrated, and a `260806-q1z-SUMMARY.md`. No change to signup, finance, allocation, or public-track-payment behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@convex/_generated/ai/guidelines.md
@convex/init.ts
@convex/schema.ts
@convex/accommodation.ts
@convex/finance.ts
@convex/signupCatalog.ts
@lib/domain/finance/accommodation-amounts.ts
@vitest.convex.config.ts

Branch: `feat/simple-accommodation-dashboard` (checkpoint `2f14ef7`, prune `878f738`).
Deployment: dev `acoustic-tiger-876` (`CONVEX_DEPLOYMENT=dev:acoustic-tiger-876`), URL `https://acoustic-tiger-876.convex.cloud`. Inspected live data on 2026-08-06: `accommodationOptions` has `superior_upgrade` + `cot`; `eventAccommodationOptions` has 2 rows (one `superior_upgrade`, one `cot` with `eligibilityAgeBandCode: "under_3"`); `eventAccommodationConfig` has 1 row (auto night-before, `allowExtendedStayBefore: true`); 4 rates, 11 resources; zero `orderAccommodationSelections` and zero child option rows.

Working-tree constraint: keep the unrelated untracked `screenshots*` files out of the commit. Make no change to money math, signup, allocation, public-track-payment, or the existing event's stay/rates/resources.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite convex/init.ts as a reusable-catalog-only, idempotent seeder with stale-option cleanup</name>
  <files>convex/init.ts</files>
  <action>
    Remove the per-event auto-configuration loop entirely (config singleton creation with the night-before stay, per-event rates, per-event cot option, per-event room/cot resources). Keep seeding only the reusable catalog: categories (standard/superior/family with labels, descriptions, sortOrder), the cot option (label/description updated to the new generic model — no "under 3 age band" wording; kind addon, unit per_night), and the room types with the real physical counts and capacities. After seeding, add an idempotent cleanup phase: delete every `accommodationOptions` row whose `code` is `"superior_upgrade"` and delete every `eventAccommodationOptions` row that references such an option; patch every remaining `eventAccommodationOptions` row to clear `eligibilityAgeBandCode` (set undefined) so no event option carries the removed age-band field. Do not touch orders, selections, snapshots, payments, assignments, tickets, or the existing event's `eventAccommodationConfig`/`eventAccommodationRates`/`eventAccommodationResources`. Keep the mutation idempotent (upsert catalog rows by code, delete stale rows only when present, return counts). Preserve `requireIdentity`-free internal mutation semantics and bounded indexed reads. Add a concise comment explaining the catalog-only contract and the stale-option reconciliation. Return a shape such as `{ categories, options, roomTypes, removedSuperiorUpgradeCatalog, removedSuperiorUpgradeEventOptions, clearedEligibilityAgeBand }`.
  </action>
  <verify>npx convex codegen && npx convex dev --once && npm run typecheck</verify>
  <done>init seeds only the reusable catalog, never auto-configures an event, and idempotently removes stale superior_upgrade rows and legacy eligibilityAgeBandCode.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the stale superior_upgrade entry from LOCKED_OPTION_SEMANTICS</name>
  <files>convex/accommodation.ts</files>
  <action>
    Remove the `superior_upgrade` entry from `LOCKED_OPTION_SEMANTICS` so the module no longer encodes the removed option's kind/unit as a locked catalog semantics. Keep the `cot` entry. No runtime pricing, selection, or admin behavior changes. Run typecheck and the convex handler test suite to confirm nothing referenced the removed entry.
  </action>
  <verify>npm run typecheck && npx vitest run --config vitest.convex.config.ts</verify>
  <done>No code references the removed superior_upgrade locked semantics; all convex handler tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add a handler test locking the init catalog-only + stale-cleanup contract</name>
  <files>tests/accommodation/accommodation-init.test.ts</files>
  <action>
    Create a `convex-test` handler suite (edge-runtime, `vitest.convex.config.ts`, `import.meta.glob` over `./**/*.ts`, fresh schema instances) that seeds a legacy fixture mirroring the dev deployment before running init: insert `accommodationOptions` rows for `superior_upgrade` and `cot`, an `eventAccommodationOptions` row for cot with `eligibilityAgeBandCode: "under_3"`, one `accommodationCategories` standard row, one `accommodationRoomTypes` row, and an `eventAccommodationConfig` + `eventAccommodationRates` + `eventAccommodationResources` row set for a sample event (simulating the existing deployed event). Then call the init internal mutation and assert: (a) the reusable catalog contains standard/superior/family categories, the cot option, and all seeded room types; (b) no `accommodationOptions` row with code `superior_upgrade` remains; (c) no `eventAccommodationOptions` row references it; (d) no `eventAccommodationOptions` row carries `eligibilityAgeBandCode`; (e) the pre-existing `eventAccommodationConfig`, `eventAccommodationRates`, and `eventAccommodationResources` rows are untouched (same ids/values); (f) re-running init is idempotent (same result, no duplicate rows, no further deletions). Use `ctx.db` bounded indexed reads and assert via a read-back query.
  </action>
  <verify>npx vitest run --config vitest.convex.config.ts tests/accommodation/accommodation-init.test.ts</verify>
  <done>A handler test proves init seeds only the catalog, removes stale superior_upgrade data, clears eligibilityAgeBandCode, leaves existing event config untouched, and is idempotent.</done>
</task>

</tasks>

<verification>
Run the full validation matrix and then migrate the dev deployment:
1. `npx convex codegen`
2. `npx convex dev --once`
3. `npm run typecheck`
4. `npm test`
5. `npx vitest run --config vitest.convex.config.ts`
6. `npx vitest run --config vitest.components.config.ts`
7. `npm run build`
8. Run init against the dev deployment: `npx convex run init '{}' --typecheck disable --codegen disable` (the default export internal mutation), then verify via a one-off read-only query that `accommodationOptions` contains only `cot`, no `eventAccommodationOptions` carries `eligibilityAgeBandCode` or references `superior_upgrade`, and the existing event config/rates/resources are unchanged.

Confirm the working tree contains no UI, finance, allocation, or public-track-payment production changes beyond the two files above, and that screenshots files remain uncommitted.
</verification>

<success_criteria>
- `convex/init.ts` is a reusable-catalog-only seeder with idempotent stale-option reconciliation and no per-event auto-configuration.
- The dev deployment is migrated: `superior_upgrade` is gone from the catalog and event options, and no event option carries `eligibilityAgeBandCode`.
- Existing event config, rates, resources, orders, selections, snapshots, payments, and assignments are untouched.
- A handler test locks the catalog-only + stale-cleanup contract and idempotency.
- Full validation matrix green.
</success_criteria>

<output>
After completion, create `.planning/quick/260806-q1z-clean-accommodation-init-and-migrate-legacy-data/260806-q1z-SUMMARY.md`.
</output>
