---
quick_task: 260815-0g5
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/emailBroadcasts.ts
  - convex/emailTemplates.ts
  - convex/_generated/api.js
  - convex/_generated/api.d.ts
  - convex/_generated/dataModel.d.ts
  - convex/_generated/server.js
  - convex/_generated/server.d.ts
  - components/dashboard/communications/communications-workspace.tsx
  - components/dashboard/communications/broadcasts-panel.tsx
  - lib/dashboard/communications/drafts.ts
  - tests/communications/communications-workspace.test.ts
  - convex/email-broadcast.handlers.test.ts
  - convex/email-templates.handlers.test.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "An operator can compose an announcement in a two-column workspace and see the real AnnouncementEmail render update in a sticky iframe preview without sending it."
    - "An operator can filter, search, and progressively reveal the matching audience up to 200 recipients, with ticket filters and confirmation summaries using ticket labels rather than Convex IDs."
    - "An operator can test-send or explicitly confirm an asynchronous broadcast, then inspect a single master-detail panel with progress, timestamps, stored filters, and status-filtered recipients, including cancel and retry-failed actions."
    - "Compose drafts restore per event from debounced localStorage, can be discarded, and saved announcement templates can be loaded, created, updated, and deleted for the current event."
    - "Existing authorization and delivery invariants remain intact: scheduleEmailBroadcast receives authorize: true, no synchronous browser confirmation or polling loop is introduced, and the existing diagnostic test-send action remains wired."
  artifacts:
    - path: "convex/schema.ts"
      provides: "emailTemplates table with event-scoped lookup index"
      contains: "emailTemplates"
    - path: "convex/emailTemplates.ts"
      provides: "Authenticated template list, upsert, and delete Convex functions"
      exports: ["getTemplatesForEvent", "saveTemplate", "deleteTemplate"]
    - path: "components/dashboard/communications/communications-workspace.tsx"
      provides: "Two-column compose/audience/send orchestration, preview, autosave, and template controls"
      contains: "WorkspaceFrame"
    - path: "components/dashboard/communications/broadcasts-panel.tsx"
      provides: "Unified broadcast history master-detail and recipient tracking UI"
      contains: "getBroadcastRecipients"
    - path: "lib/dashboard/communications/drafts.ts"
      provides: "Event-keyed localStorage draft persistence helpers"
      contains: "localStorage"
    - path: "convex/email-templates.handlers.test.ts"
      provides: "convex-test coverage for the new template API"
      contains: "convexTest"
  key_links:
    - from: "components/dashboard/communications/communications-workspace.tsx"
      to: "lib/email/templates/announcement.tsx"
      via: "render(AnnouncementEmail(...)) feeding iframe srcDoc after a debounce"
      pattern: "render.*AnnouncementEmail|srcDoc"
    - from: "components/dashboard/communications/communications-workspace.tsx"
      to: "convex/emailTemplates.ts"
      via: "useQuery/useMutation through api.emailTemplates"
      pattern: "api\.emailTemplates\.(getTemplatesForEvent|saveTemplate|deleteTemplate)"
    - from: "components/dashboard/communications/communications-workspace.tsx"
      to: "convex/emailBroadcasts.ts"
      via: "reactive audience/history/detail queries and authorized async scheduling"
      pattern: "scheduleEmailBroadcast|authorize: true|previewAudience|getBroadcastHistory|getBroadcastById|getBroadcastRecipients"
    - from: "components/dashboard/communications/broadcasts-panel.tsx"
      to: "convex/emailBroadcasts.ts"
      via: "selected broadcast ID and optional recipient status query"
      pattern: "statusFilter|getBroadcastRecipients"

---

<objective>
Redesign the Communications Center into a focused composer and tracking workspace while adding durable event-scoped drafts and saved announcement templates.

Purpose: Operators need to understand exactly what will be sent, who will receive it, and how delivery is progressing without leaving the event workspace. The implementation must preserve the existing authenticated, scheduler-driven broadcast contract.

Output: Updated Convex schema/API, a two-column CommunicationsWorkspace with live AnnouncementEmail preview and audience drill-down, a unified broadcasts-panel master-detail component, draft/template persistence, rewritten structural and Convex tests, and green validation gates.
</objective>

<execution_context>
@AGENTS.md
@convex/_generated/ai/guidelines.md
@.planning/STATE.md
@.planning/quick/260815-0g5-redesign-the-communications-center-two-c/260815-0g5-CONTEXT.md
@components/dashboard/communications/communications-workspace.tsx
@convex/emailBroadcasts.ts
@convex/schema.ts
@lib/email/templates/announcement.tsx
@app/dashboard/email-preview/page.tsx
@convex/email-broadcast.handlers.test.ts
</execution_context>

<dependency_graph>
Task 1 creates the emailTemplates schema/API and the 200-recipient backend contract. Task 2 depends on those generated API references and consumes the existing broadcast queries/mutations. Task 3 depends on the final UI and Convex contracts to rewrite structural tests and add convex-test coverage. Task 4 depends on all implementation and test changes and runs the complete validation gate.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Add event-scoped announcement templates and expand the audience preview contract</name>
  <files>convex/schema.ts; convex/emailTemplates.ts; convex/emailBroadcasts.ts; convex/_generated/api.js; convex/_generated/api.d.ts; convex/_generated/dataModel.d.ts; convex/_generated/server.js; convex/_generated/server.d.ts</files>
  <action>
    Add an `emailTemplates` table to `convex/schema.ts` with `eventId`, `name`, `title`, `message`, `eventName`, `eventDate`, `eventLocation`, optional `paymentUrl` and `nightBeforeNote`, plus `createdAt` and `updatedAt`; add the `by_eventId` index using the repository's Convex index naming convention. Create `convex/emailTemplates.ts` with validators on every public function and `requireIdentity` gates: `getTemplatesForEvent` returns a bounded, newest-first list for one event; `saveTemplate` inserts when no template ID is supplied and patches an existing template when an ID is supplied, rejecting an ID from another event; `deleteTemplate` authenticates, verifies event ownership, and deletes only the requested template. Keep this announcement-only and do not introduce a second template type or unbounded query.

    Change `previewAudience` so its default preview limit is 200 while retaining a hard `Math.min(..., 200)` cap. Do not alter the scheduler, authorization, audience filter validator, or broadcast persistence contract. Run Convex code generation after the schema/API change so client references and data-model types are current; do not hand-edit generated output.
  </action>
  <verify>Run `npx convex codegen` and `npx convex dev --once`; confirm generated `api.emailTemplates` and `Id<"emailTemplates">` references exist and the Convex validation command completes without schema or function errors.</verify>
  <done>The schema contains the indexed emailTemplates table; all three authenticated template functions are exposed with event scoping and upsert/delete behavior; previewAudience defaults to at most 200; generated Convex files are refreshed and the required Convex commands pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stacked workspace with the composer, preview, audience drill-down, autosave, templates, and master-detail tracking</name>
  <files>components/dashboard/communications/communications-workspace.tsx; components/dashboard/communications/broadcasts-panel.tsx; lib/dashboard/communications/drafts.ts</files>
  <action>
    Rewrite the workspace while preserving `WorkspaceFrame`, `WorkspaceTabs`, the canonical `communicationsHref(slug)` route, and the current `useEventDashboard` event context. Keep the existing compose state shape (`title`, `message`, `eventName`, `eventDate`, `eventLocation`, `paymentUrl`, `nightBeforeNote`) and defaults, but organize the page as a responsive two-column composer: left side contains compose fields, template picker/save-as/discard-template affordances, all five audience filters, audience count/list, test-send, and the explicit `Confirm broadcast` flow; right side contains a sticky live email preview. Use `render()` from the installed `@react-email/render` with `AnnouncementEmail`, the placeholder URLs `${origin}/booking/BK-EXAMPLE/manage` and `${origin}/signup/${slug}`, and a roughly 300ms debounce with stale-render protection before assigning HTML to iframe `srcDoc`. Do not render a hand-built preview or use `window.confirm`/`setInterval`.

    Add client-side audience search across name, email, and booking reference. Request up to 200 from `previewAudience`, show the first 25 initially, and reveal additional rows in chunks until all returned rows (maximum 200) are visible; reset the reveal/search state when filters change. Keep the five filter categories (status, location, submitted date range represented by from/to controls, accommodation, and ticket type) and resolve the selected ticket type ID to its human label when building the confirmation summary. Keep `sendAnnouncementTest`, `scheduleEmailBroadcast` with `authorize: true`, and cancel/retry mutations wired.

    Extract the former `ActiveBroadcastCard` and `HistoryCard` into `broadcasts-panel.tsx`. Make it a master-detail panel with a status-filterable history list (human-readable status labels, timestamp, recipient count), selected-broadcast detail (progress bar, sent/failed/pending counts, created/started/completed/cancelled timestamps, stored filter summary), and recipient table with pending/sent/failed filtering. Preserve reactive `getBroadcastHistory`, `getBroadcastById`, and `getBroadcastRecipients` queries plus cancel/retry actions; select a sensible initial history item without introducing client polling.

    Create `lib/dashboard/communications/drafts.ts` with a typed compose-draft shape and safe event-keyed `localStorage` read/write/remove helpers. In the workspace, restore the event's draft on mount, debounce writes on compose changes, avoid browser storage access during SSR, and expose a discard-draft action that clears storage and resets to event defaults. Wire template query/mutations to the current event, load a selected template into compose state, save the current state under a required template name, and delete only the selected saved template.
  </action>
  <verify>Run `npm run typecheck`; inspect the rendered source to confirm `srcDoc`, `AnnouncementEmail`, `render`, `api.emailTemplates`, `setTimeout` debounce, all four broadcast queries, `authorize: true`, and no `window.confirm` or `setInterval`. If a browser is available, open `/dashboard/events/{slug}/communications` and verify the two-column layout, preview updates, search/reveal behavior, draft restore/discard, template load/save/delete, and master-detail status filtering.</verify>
  <done>The route presents one responsive two-column composer with a sticky real-template preview; audience rows search and reveal through 200; ticket summaries show labels; drafts restore and discard per event; templates are event-scoped and usable from compose; broadcasts are represented by one master-detail panel with progress and recipient status filters; all prior send/cancel/retry safety invariants remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Communications structural tests and add Convex template/limit coverage</name>
  <files>tests/communications/communications-workspace.test.ts; convex/email-broadcast.handlers.test.ts; convex/email-templates.handlers.test.ts</files>
  <action>
    Replace assertions tied to `ComposeCard`, `AudienceCard`, `HistoryCard`, and `ActiveBroadcastCard` with assertions for the new workspace and `broadcasts-panel.tsx` structure: `WorkspaceFrame`/Broadcast tab, `AnnouncementEmail` + `render` + iframe `srcDoc`, draft/template wiring, audience search and reveal-to-200 behavior, human-readable filter summary, and master-detail progress/status-filtered recipients. Preserve the existing navigation tests and all required invariants: `scheduleEmailBroadcast`, `authorize: true`, no `window.confirm`, no `setInterval`, no `/api/dashboard/`, `sendAnnouncementTest`, cancel/retry, all five audience filter categories, and the four Convex broadcast queries.

    Add `convex/email-templates.handlers.test.ts` following the existing `convex-test` module-map pattern. Cover anonymous rejection for list/save/delete; authenticated event-scoped listing; insert via `saveTemplate`; update/upsert by template ID without creating a duplicate; cross-event ID rejection; and delete. Extend `convex/email-broadcast.handlers.test.ts` with coverage that the audience preview returns more than the old 100-row default and caps an oversized requested limit at 200, while keeping existing authorization and async delivery tests green. Use seeded events/bookers and avoid production calls or assumptions about generated IDs.
  </action>
  <verify>Run `npx vitest run tests/communications/communications-workspace.test.ts convex/email-broadcast.handlers.test.ts convex/email-templates.handlers.test.ts`; all rewritten structural assertions and Convex tests pass.</verify>
  <done>Tests describe the new Communications Center rather than removed component names, retain every locked safety invariant, and prove the new template functions plus the 200-row preview behavior against the Convex schema.</done>
</task>

<task type="auto">
  <name>Task 4: Run the complete implementation verification gate</name>
  <files>convex/schema.ts; convex/emailTemplates.ts; convex/emailBroadcasts.ts; components/dashboard/communications/communications-workspace.tsx; components/dashboard/communications/broadcasts-panel.tsx; lib/dashboard/communications/drafts.ts; tests/communications/communications-workspace.test.ts; convex/email-broadcast.handlers.test.ts; convex/email-templates.handlers.test.ts</files>
  <action>Run the repository validation after all edits: regenerate Convex artifacts and perform the required one-shot Convex dev validation, then run the full Vitest suite, TypeScript check, and production build. Review failures for regressions in existing email broadcast behavior rather than weakening tests or removing the explicit authorization contract. Do not deploy production Convex functions, send real broadcasts, or automate the operator-gated production communication flow.</action>
  <verify>Run `npx convex codegen && npx convex dev --once`, `npm test`, `npm run typecheck`, and `npm run build`; each command exits successfully. Confirm the final diff contains no `window.confirm` or `setInterval` in the Communications Center and no production deployment or broadcast side effect.</verify>
  <done>Convex generation/dev validation, focused and full tests, typecheck, and production build all pass, with the Communications Center ready for the operator's manual visual walkthrough.</done>
</task>

</tasks>

<verification>
1. Convex schema/API validation passes and generated references include the new emailTemplates functions/table.
2. Focused Communications and Convex handler tests pass, followed by the full Vitest suite.
3. `npm run typecheck` and `npm run build` pass.
4. Manual browser walkthrough confirms live preview, audience search/reveal, explicit confirmation, draft/template workflows, and master-detail broadcast tracking without real broadcast automation.
</verification>

<success_criteria>
- The Communications Center is a responsive two-column composer using the real AnnouncementEmail template in a debounced iframe preview.
- Audience preview is bounded at 200, searchable client-side, progressively revealed from 25, and summarized with ticket labels.
- Broadcast history and active tracking are one master-detail surface with progress, timestamps, stored filters, recipient status filtering, cancel, and retry-failed actions.
- Drafts persist per event with debounce and discard; announcement templates persist in Convex with authenticated event-scoped CRUD and test coverage.
- The existing async, explicitly authorized broadcast contract is unchanged and all validation gates are green.
</success_criteria>

<output>
After completion, create `.planning/quick/260815-0g5-redesign-the-communications-center-two-c/260815-0g5-SUMMARY.md` with the implementation summary, validation results, and any deferred visual walkthrough notes.
</output>
