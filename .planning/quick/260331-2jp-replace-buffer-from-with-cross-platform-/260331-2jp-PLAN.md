---
plan: 260331-2jp
type: quick
files_modified:
  - convex/autoSync.ts
autonomous: true
must_haves:
  truths:
    - "Ticket Tailor API Basic auth header is correctly base64-encoded"
    - "autoSyncTicketTailor action runs without Buffer runtime errors"
  artifacts:
    - path: convex/autoSync.ts
      provides: "Ticket Tailor HTTP client with auth"
      min_lines: 848
  key_links:
    - from: "convex/autoSync.ts:ttHeaders()"
      to: "Ticket Tailor API"
      via: "Basic Authorization header"
      pattern: "btoa\\(TT_API_KEY\\)"
---

<objective>
Replace `Buffer.from()` with cross-platform `btoa()` for base64 encoding in `convex/autoSync.ts`.

**Problem:** Line 64 uses `Buffer.from(TT_API_KEY).toString("base64")` but `Buffer` is not available in Convex's default V8 action runtime.

**Fix:** Use `btoa()` which is the web standard available in all JavaScript runtimes (browser, Node.js, V8, Deno, Bun).

**Output:** `convex/autoSync.ts` with cross-platform base64 encoding.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@convex/_generated/ai/guidelines.md
</context>

<tasks>
<task type="auto">
<name>Replace Buffer.from with btoa in ttHeaders function</name>
<files>convex/autoSync.ts</files>
<action>
In `convex/autoSync.ts`, line 64, replace:

```typescript
const encoded = Buffer.from(TT_API_KEY).toString("base64")
```

With:

```typescript
const encoded = btoa(TT_API_KEY)
```

**Why:** `btoa()` is the web standard for base64 encoding and is available in all JavaScript runtimes including Convex's V8 action runtime. `Buffer` is a Node.js-specific API that does not exist in V8.

**Context:** The `ttHeaders()` function creates HTTP headers for Ticket Tailor API requests. The `Authorization: Basic <base64>` header requires the API key to be base64-encoded. This is the only change needed.
</action>
<verify>
Run TypeScript check to confirm no errors:

```bash
npx tsc --noEmit convex/autoSync.ts
```

</verify>
<done>
Line 64 reads `const encoded = btoa(TT_API_KEY)` and the file has no TypeScript errors.
</done>
</task>
</tasks>

<verification>
- TypeScript compilation passes for `convex/autoSync.ts`
- No `Buffer` references remain in the file
- The `ttHeaders()` function still returns a valid headers object with `Authorization: Basic <encoded>`
</verification>

<success_criteria>

- `convex/autoSync.ts` compiles without errors
- `btoa()` used instead of `Buffer.from()` on line 64
- Ticket Tailor sync functionality preserved (same auth header format)
  </success_criteria>

<output>
After completion, create `.planning/quick/260331-2jp-replace-buffer-from-with-cross-platform-/260331-2jp-SUMMARY.md`
</output>
