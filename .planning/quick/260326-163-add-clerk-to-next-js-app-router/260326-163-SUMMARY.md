---
phase: quick-260326-163-add-clerk-to-next-js-app-router
plan: 01
subsystem: auth
tags: [clerk, nextjs, app-router, convex, keyless]
requires:
  - phase: 11-use-convex
    provides: Existing Next.js app shell with Convex, query, and theme providers.
provides:
  - Clerk App Router middleware via project-root proxy.ts
  - ClerkProvider wrapping the existing root provider stack
  - Clerk-powered landing page using Show, SignInButton, SignUpButton, and UserButton
affects: [auth, landing-page]
tech-stack:
  added: ["@clerk/nextjs"]
  patterns:
    [
      "Use Clerk keyless App Router quickstart with proxy.ts and ClerkProvider",
      "Keep the existing Convex/query/theme provider nesting inside ClerkProvider",
    ]
key-files:
  created:
    [
      proxy.ts,
      lib/convex/api.ts,
      .planning/quick/260326-163-add-clerk-to-next-js-app-router/260326-163-SUMMARY.md,
    ]
  modified:
    [
      package.json,
      package-lock.json,
      app/layout.tsx,
      app/page.tsx,
      lib/convex/hooks/accommodation.ts,
      lib/convex/hooks/attendees.ts,
      lib/convex/hooks/events.ts,
      lib/convex/hooks/orders.ts,
      lib/convex/hooks/payments.ts,
      lib/convex/hooks/sync.ts,
      lib/convex/hooks/tikkie.ts,
    ]
key-decisions:
  - "Use Clerk's current proxy.ts + clerkMiddleware quickstart instead of the legacy middleware/authMiddleware pattern."
  - "Wrap ClerkProvider around the existing root provider tree so Convex, query, and theme wiring stay intact."
  - "Remove the old Better Auth middleware to avoid conflicting auth gates after switching the root entry UI to Clerk."
patterns-established:
  - 'Clerk App Router auth entry uses Show when="signed-in" and when="signed-out" instead of SignedIn/SignedOut.'
  - "Quickstart validation can rely on Clerk keyless headers from a local dev boot before env vars are configured."
duration: 15 min
completed: 2026-03-26
---

# Phase Quick 260326-163 Plan 01: Add Clerk to Next.js App Router Summary

**Clerk keyless App Router wiring now boots through proxy.ts, wraps the existing provider shell, and replaces the Better Auth landing CTA with Clerk auth primitives.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-03-25T23:53:10Z
- **Completed:** 2026-03-26T00:08:29Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- Installed `@clerk/nextjs` and added `proxy.ts` with Clerk's App Router matcher.
- Wrapped the existing Convex, query, and theme providers in `ClerkProvider` without changing their nesting.
- Replaced the root page Better Auth session check with Clerk `Show`, `SignInButton`, `SignUpButton`, and `UserButton` UI.

## Task Commits

Each task was committed atomically:

1. **Task 1: Install Clerk and wire root App Router integration** - `f09cc48` (feat)
2. **Task 2: Replace the landing page auth entry with Clerk components** - `0629e57` (feat)

**Plan metadata:** pending

## Files Created/Modified

- `package.json` - added Clerk and retained the required Convex dependency for local type safety.
- `package-lock.json` - recorded Clerk install and dependency graph updates.
- `proxy.ts` - added Clerk App Router middleware and matcher.
- `app/layout.tsx` - wrapped the existing provider stack in `ClerkProvider`.
- `app/page.tsx` - swapped Better Auth session loading for Clerk auth controls.
- `middleware.ts` - removed the legacy Better Auth middleware so it does not conflict with Clerk routing.
- `lib/convex/api.ts` - added a temporary local shim around generated Convex api bindings.
- `lib/convex/hooks/*.ts` - corrected local hook imports and removed unnecessary `any` casts.

## Decisions Made

- Used Clerk's current `proxy.ts` + `clerkMiddleware()` quickstart so the app stays on the supported App Router path.
- Kept `ClerkProvider` outside the existing providers to preserve the current Convex, query, and theme shell unchanged.
- Removed the legacy Better Auth middleware because leaving it in place would keep old cookie-based redirects active after the Clerk landing-page swap.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Restored working Convex client imports for verification**

- **Found during:** Task 1 (Install Clerk and wire root App Router integration)
- **Issue:** `npm run typecheck` failed because local Convex hook imports pointed at a missing `convex/functions/_generated/api` path.
- **Fix:** Added a small `lib/convex/api.ts` shim and updated the local Convex hooks to use it.
- **Files modified:** `lib/convex/api.ts`, `lib/convex/hooks/accommodation.ts`, `lib/convex/hooks/attendees.ts`, `lib/convex/hooks/events.ts`, `lib/convex/hooks/orders.ts`, `lib/convex/hooks/payments.ts`, `lib/convex/hooks/sync.ts`, `lib/convex/hooks/tikkie.ts`
- **Verification:** `npm run typecheck` passed after the hook import cleanup.
- **Committed in:** `f09cc48`

**2. [Rule 3 - Blocking] Kept the required Convex package dependency while adding Clerk**

- **Found during:** Task 1 (Install Clerk and wire root App Router integration)
- **Issue:** The repo's tracked code imports `convex/react`, so verification fails if the local `convex` dependency is omitted while updating package files.
- **Fix:** Preserved the `convex` dependency alongside the Clerk install in `package.json` and `package-lock.json`.
- **Files modified:** `package.json`, `package-lock.json`
- **Verification:** `npm run typecheck` passed with both dependencies present.
- **Committed in:** `f09cc48`

**3. [Rule 2 - Missing Critical] Removed conflicting legacy Better Auth route middleware**

- **Found during:** Task 2 (Replace the landing page auth entry with Clerk components)
- **Issue:** The old `middleware.ts` still enforced Better Auth cookie redirects, which would conflict with the new Clerk App Router flow.
- **Fix:** Deleted `middleware.ts` so Clerk's `proxy.ts` is the only active auth middleware entrypoint.
- **Files modified:** `middleware.ts`
- **Verification:** Local dev boot returned `x-clerk-auth-status: signed-out` and rendered `/` through `proxy.ts` on port 4123.
- **Committed in:** `0629e57`

---

**Total deviations:** 3 auto-fixed (1 missing critical, 2 blocking)
**Impact on plan:** All deviations were required to complete the planned Clerk wiring cleanly and keep verification working in the current repo state.

## Issues Encountered

- `npm run lint` still fails because of pre-existing repo-wide lint errors in unrelated API/domain files. The Clerk changes themselves lint clean when targeted directly with `npx eslint` on the touched files.
- `npm run typecheck` briefly failed after local Next.js generated stale `.next/dev/types` validator files; clearing `.next` resolved it.

## User Setup Required

None - Clerk keyless mode boots without local auth env vars.

## Next Phase Readiness

- Root Clerk primitives are in place and verified in keyless mode, so follow-up auth migration work can build on the new provider and middleware entrypoints.
- Dashboard route protection and Convex auth bridging still need future phase work; this quick task only swaps the App Router shell and landing entry.

---

_Phase: quick-260326-163-add-clerk-to-next-js-app-router_
_Completed: 2026-03-26_
