---
phase: quick-260326-163-add-clerk-to-next-js-app-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - proxy.ts
  - app/layout.tsx
  - app/page.tsx
autonomous: true
must_haves:
  truths:
    - "App Router runs Clerk middleware from project-root proxy.ts without using deprecated authMiddleware()."
    - "Root layout provides Clerk context while preserving existing Convex, query, and theme providers."
    - "Landing page shows Clerk auth entry/actions using Show, SignInButton, SignUpButton, and UserButton instead of custom Better Auth entry UI."
  artifacts:
    - path: "package.json"
      provides: "@clerk/nextjs dependency installed with existing npm workflow"
      contains: "@clerk/nextjs"
    - path: "proxy.ts"
      provides: "Clerk middleware for App Router"
      contains: "clerkMiddleware"
    - path: "app/layout.tsx"
      provides: "ClerkProvider wrapped around existing app shell"
      contains: "ClerkProvider"
    - path: "app/page.tsx"
      provides: "Clerk-powered auth landing page"
      contains: "SignInButton"
  key_links:
    - from: "proxy.ts"
      to: "@clerk/nextjs/server"
      via: "clerkMiddleware import and default export"
      pattern: "clerkMiddleware"
    - from: "app/layout.tsx"
      to: "@clerk/nextjs"
      via: "ClerkProvider wrapping body contents"
      pattern: "ClerkProvider"
    - from: "app/page.tsx"
      to: "@clerk/nextjs"
      via: "Show-based signed-in/signed-out rendering"
      pattern: "Show"
---

<objective>
Add Clerk’s current Next.js App Router quickstart wiring in keyless mode so the app can boot with Clerk auth primitives without env setup, deprecated APIs, or Pages Router patterns.

Purpose: Replace the current custom root auth entry with Clerk’s supported App Router integration while keeping the existing app shell/providers intact.
Output: Installed Clerk dependency plus updated `proxy.ts`, `app/layout.tsx`, and `app/page.tsx` wired to Clerk quickstart components.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@package.json
@app/layout.tsx
@app/page.tsx
@convex/_generated/ai/guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Clerk and wire root App Router integration</name>
  <files>package.json, proxy.ts, app/layout.tsx</files>
  <action>Use the existing npm workflow to add `@clerk/nextjs`. Create `proxy.ts` at the repository root (not under `src/`) using `clerkMiddleware()` imported from `@clerk/nextjs/server` and the standard matcher pattern from Clerk’s App Router quickstart. Update `app/layout.tsx` to import `ClerkProvider` from `@clerk/nextjs` and wrap the current body contents with it while preserving the existing `ConvexClientProvider`, `QueryProvider`, and `ThemeProvider` nesting. Do not introduce `_app.tsx`, `authMiddleware()`, or any Pages Router setup.</action>
  <verify>`npm install` completes with `@clerk/nextjs` present, `npm run typecheck` passes, and `proxy.ts` exists with `clerkMiddleware` export.</verify>
  <done>`@clerk/nextjs` is installed, Clerk middleware is active from `proxy.ts`, and the root layout provides Clerk context without breaking the existing provider stack.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the landing page auth entry with Clerk components</name>
  <files>app/page.tsx</files>
  <action>Refactor `app/page.tsx` away from the current Better Auth session check and `/login` CTA so it uses Clerk App Router primitives only. Import and use `Show`, `SignInButton`, `SignUpButton`, and `UserButton` from `@clerk/nextjs`. Preserve the existing landing-page visual feel where practical, but render signed-out actions with `Show when="signed-out"` and signed-in controls with `Show when="signed-in"`. Keep the page App Router-compatible and do not use `<SignedIn>` or `<SignedOut>`.</action>
  <verify>`npm run lint` and `npm run typecheck` pass, and `app/page.tsx` contains `Show`, `SignInButton`, `SignUpButton`, and `UserButton` with no `authClient.getSession()` call remaining.</verify>
  <done>The root page is Clerk-driven: signed-out visitors get sign-in/sign-up actions, signed-in users get a `UserButton`, and no deprecated Clerk or legacy Better Auth landing-page flow remains.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`.
- Run `npm run lint`.
- Start the app and confirm it renders in Clerk keyless mode without requiring local env vars or manual signup before load.
</verification>

<success_criteria>

- `@clerk/nextjs` is installed via npm.
- `proxy.ts` exists at the project root and exports `clerkMiddleware()` with the quickstart matcher.
- `app/layout.tsx` wraps the existing provider tree in `ClerkProvider`.
- `app/page.tsx` uses `Show`, `SignInButton`, `SignUpButton`, and `UserButton` only.
- No `_app.tsx`, `authMiddleware()`, `<SignedIn>`, or `<SignedOut>` are introduced.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-163-add-clerk-to-next-js-app-router/260326-163-SUMMARY.md`
</output>
