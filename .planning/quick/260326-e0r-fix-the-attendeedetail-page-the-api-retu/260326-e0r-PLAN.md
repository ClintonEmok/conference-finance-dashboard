---
phase: quick-260326-e0r-fix-the-attendeedetail-page-the-api-retu
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/dashboard/attendees/[attendeeId]/route.ts
  - tests/attendees/attendee-detail-route.test.ts
autonomous: true
must_haves:
  truths:
    - "Opening `/dashboard/attendees/[attendeeId]` no longer fails with a 405 from `/api/dashboard/attendees/[attendeeId]`."
    - "The attendee detail page can load and render attendee, order, finance, and room data from the existing detail payload."
    - "Saving a Tikkie override still uses the same PATCH endpoint without regression."
  artifacts:
    - path: "app/api/dashboard/attendees/[attendeeId]/route.ts"
      provides: "Protected attendee detail route supporting both GET detail reads and PATCH override updates on the same URL"
    - path: "tests/attendees/attendee-detail-route.test.ts"
      provides: "Regression coverage proving GET returns detail payloads and PATCH behavior stays intact"
  key_links:
    - from: "app/dashboard/attendees/[attendeeId]/page.tsx"
      to: "app/api/dashboard/attendees/[attendeeId]/route.ts"
      via: "client fetch on initial page load"
      pattern: "fetch\(`/api/dashboard/attendees/\$\{targetAttendeeId\}`\)"
    - from: "app/api/dashboard/attendees/[attendeeId]/route.ts"
      to: "lib/domain/finance/attendee-detail.ts"
      via: "GET handler delegates to existing attendee detail domain builder"
      pattern: "getAttendeeDetail"
---

<objective>
Fix the attendee detail API so the detail page can actually load attendee data instead of returning 405 Method Not Allowed.

Purpose: Restore the operator flow from attendee list to attendee detail without changing the existing page design or PATCH override contract.
Output: The attendee detail route supports GET for page loads, preserves PATCH for override updates, and has regression coverage for the broken endpoint behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@app/dashboard/attendees/[attendeeId]/page.tsx
@app/api/dashboard/attendees/[attendeeId]/route.ts
@lib/domain/finance/attendee-detail.ts
@tests/ticket-tailor/sync-route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a GET handler for attendee detail reads on the existing route</name>
  <files>app/api/dashboard/attendees/[attendeeId]/route.ts</files>
  <action>Add `GET` to `app/api/dashboard/attendees/[attendeeId]/route.ts` using the same `requireApiUser()` gate as PATCH. Reuse the existing `attendeeId` normalization pattern, then delegate the read path to the already-existing domain function in `lib/domain/finance/attendee-detail.ts` so the page receives the full attendee detail payload it already expects. Return the payload directly with `NextResponse.json(...)`, map invalid ids to 400, missing attendees to 404, and keep PATCH behavior unchanged in the same file. Do not redesign the page payload shape, split the route, or move this read into a new endpoint; the current page already fetches this exact URL.</action>
  <verify>Run `npm run typecheck`.</verify>
  <done>`GET /api/dashboard/attendees/{attendeeId}` returns the attendee detail JSON contract for valid ids and no longer responds with 405 when the page loads.</done>
</task>

<task type="auto">
  <name>Task 2: Add a focused regression test for GET detail loading and PATCH coexistence</name>
  <files>tests/attendees/attendee-detail-route.test.ts</files>
  <action>Create a Vitest route test that mocks `requireApiUser()` plus the attendee detail read/update boundary used by the route. Cover at least: unauthenticated GET returns the shared unauthorized response, authenticated GET returns a representative attendee detail payload, and PATCH still remains available on the same route after adding GET. Keep the test narrow to route contract behavior; do not spin up the full page or real Convex calls.</action>
  <verify>Run `npm test -- tests/attendees/attendee-detail-route.test.ts`.</verify>
  <done>The test suite proves the attendee detail endpoint supports GET for page loads and still supports PATCH for override updates without route-method regression.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/attendees/attendee-detail-route.test.ts`
- If the app is running locally, open `/dashboard/attendees/{attendeeId}` from the attendee list and confirm the detail screen loads data instead of showing a failed-load message caused by a 405 response.
</verification>

<success_criteria>

- The attendee detail page's initial fetch to `/api/dashboard/attendees/{attendeeId}` succeeds with GET instead of returning 405.
- The existing detail page can render real attendee detail data without any page-level API URL change.
- The shared attendee route continues to support PATCH override updates after the GET handler is added.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-e0r-fix-the-attendeedetail-page-the-api-retu/260326-e0r-SUMMARY.md`
</output>
