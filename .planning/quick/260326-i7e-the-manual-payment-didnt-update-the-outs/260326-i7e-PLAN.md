---
phase: quick-260326-i7e-the-manual-payment-didnt-update-the-outs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/payments/manual-entry-form.tsx
  - components/payments/assign-dialog.tsx
  - lib/domain/finance/payments.ts
  - lib/domain/finance/reconciliation.ts
  - app/api/payments/route.ts
  - tests/payments/payments-route.test.ts
  - tests/reconciliation/reconciliation-outstanding.test.ts
autonomous: true
must_haves:
  truths:
    - "After recording a manual bank/cash payment for a selected order, reconciliation outstanding amount decreases on refresh instead of staying unchanged."
    - "The payments table shows the linked Ticket Tailor order id for manually linked payments instead of an empty order column."
    - "Manual linking paths (new manual entry + assign dialog) use providerOrderId consistently so order linkage is stable across reconciliation and payment views."
  artifacts:
    - path: "lib/domain/finance/reconciliation.ts"
      provides: "Outstanding calculation that subtracts matched payments by linked order identifier"
    - path: "app/api/payments/route.ts"
      provides: "Payment list payload enriched with resolved order details (including providerOrderId)"
    - path: "components/payments/manual-entry-form.tsx"
      provides: "Manual entry submit payload sends providerOrderId from selected order"
    - path: "components/payments/assign-dialog.tsx"
      provides: "Assign flow sends providerOrderId for payment-to-order linking"
    - path: "tests/reconciliation/reconciliation-outstanding.test.ts"
      provides: "Regression coverage for outstanding reduction after linked payments"
    - path: "tests/payments/payments-route.test.ts"
      provides: "Regression coverage for payment list order enrichment and identifier fallback"
  key_links:
    - from: "components/payments/manual-entry-form.tsx"
      to: "lib/domain/finance/payments.ts:createBankTransferPayment/createCashPayment"
      via: "POST body orderId using selectedOrder.providerOrderId"
      pattern: "orderId:\s*selectedOrder\.providerOrderId|orderId:\s*values\.orderId"
    - from: "components/payments/assign-dialog.tsx"
      to: "app/api/payments/[id]/assign/route.ts"
      via: "PATCH payload uses selectedOrder.providerOrderId"
      pattern: "body:\s*JSON\.stringify\(\{\s*orderId: selectedOrder\.providerOrderId"
    - from: "lib/domain/finance/reconciliation.ts"
      to: "api.payments.getPayments"
      via: "matched-payment aggregation before outstanding totals are returned"
      pattern: "api\.payments\.getPayments"
    - from: "app/api/payments/route.ts"
      to: "api.orders.getOrderByProviderId|api.orders.getOrderById"
      via: "order payload enrichment for payment rows"
      pattern: "api\.orders\.(getOrderByProviderId|getOrderById)"
---

<objective>
Fix manual payment linking so reconciliation outstanding values reflect manual payments and payment rows display the related Ticket Tailor order id.

Purpose: Remove operator confusion where a manual payment appears recorded but outstanding totals do not change and order linkage is invisible.
Output: Provider-order-id aligned manual linkage flow, payment-aware reconciliation outstanding math, and regression tests for both contracts.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@convex/_generated/ai/guidelines.md
@.planning/quick/260326-hgy-fix-the-manual-payment-entry-select-orde/260326-hgy-SUMMARY.md
@components/payments/manual-entry-form.tsx
@components/payments/assign-dialog.tsx
@lib/domain/finance/payments.ts
@lib/domain/finance/reconciliation.ts
@app/api/payments/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align manual payment order linkage to providerOrderId across UI + domain entry points</name>
  <files>components/payments/manual-entry-form.tsx, components/payments/assign-dialog.tsx, lib/domain/finance/payments.ts</files>
  <action>Update manual-linking entry points so selected orders pass Ticket Tailor `providerOrderId` as the `orderId` payload (manual entry and assign dialog), and keep domain normalization strict (trim + required checks). Preserve existing UX states from 260326-hgy and do not alter endpoint paths or auth flow. Add compatibility in domain mapping logic where needed so pre-existing payments with legacy Convex document ids are still readable while new manual links use provider ids.</action>
  <verify>Run `npm run typecheck`.</verify>
  <done>New manual bank/cash entries and manual assignment requests link payments using `providerOrderId`, with no regression in form validation or submission flow.</done>
</task>

<task type="auto">
  <name>Task 2: Make reconciliation outstanding payment-aware with provider-order matching</name>
  <files>lib/domain/finance/reconciliation.ts, tests/reconciliation/reconciliation-outstanding.test.ts</files>
  <action>Revise reconciliation row derivation so outstanding uses order amount minus matched payment totals for that order (at minimum statuses `manual_assignment` and `auto_matched`), keyed by provider order id. Keep existing reason semantics (`pending-payment`, etc.) and row filtering behavior, but ensure outstanding drops to zero when matched payments cover the order amount. Include a compatibility branch for legacy stored order ids where feasible (without schema migration) so existing data is not silently ignored.</action>
  <verify>Run `npm test -- tests/reconciliation/reconciliation-outstanding.test.ts`.</verify>
  <done>Reconciliation payload totals and row `outstandingMinor` values decrease after linked manual payments, and regression tests lock this behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Enrich payments API rows with related order details and add route regression coverage</name>
  <files>app/api/payments/route.ts, tests/payments/payments-route.test.ts</files>
  <action>Update `/api/payments` GET mapping to resolve each linked payment's order and return `{ order: { id, providerOrderId, buyerName, totalAmountMinor } }` instead of always `order: null`. Resolve by providerOrderId first, with fallback to Convex id lookup for backward compatibility. Keep existing pagination/auth/error contracts unchanged. Add focused route tests mocking `requireApiUser` + `convexQuery` to verify order enrichment, provider-id lookup, and fallback behavior.</action>
  <verify>Run `npm test -- tests/payments/payments-route.test.ts`.</verify>
  <done>Payment list API returns the related order block for linked payments, so the UI order column shows provider order id and buyer context.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- tests/reconciliation/reconciliation-outstanding.test.ts`
- `npm test -- tests/payments/payments-route.test.ts`
- Manual check: open `/dashboard/reconciliation` → Add Payment for a known pending order → submit and refresh; confirm outstanding value decreases and the payment appears with its Ticket Tailor order id in the Payments table.
</verification>

<success_criteria>

- Manual payment linkage uses provider order ids consistently for new writes.
- Reconciliation outstanding reflects linked manual payments instead of ignoring them.
- Payments list displays related order details (including providerOrderId) for linked rows.
- Automated tests prevent regressions in outstanding math and `/api/payments` order-enrichment behavior.
  </success_criteria>

<output>
After completion, create `.planning/quick/260326-i7e-the-manual-payment-didnt-update-the-outs/260326-i7e-SUMMARY.md`
</output>
