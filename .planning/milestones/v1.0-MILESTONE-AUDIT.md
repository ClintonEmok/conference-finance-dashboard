---
milestone: v1.0
audited: 2026-03-27T12:41:10Z
status: gaps_found
score: 8/9 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 8/9
  gaps_closed:
    - "Accommodation apply flow now forwards gender/priority/location signal filters and reload reacts to applied signal state"
    - "Family badge contract is aligned to `hasFamily` via shared filter-state helper"
  gaps_remaining:
    - "Accommodation family-group filtering is not operator-complete in the UI (no control to set/clear `familyGroupId`)"
    - "`Priority only` unchecked state is serialized as `hasPriority=false`, causing unintended inverse filtering"
  regressions: []
gaps:
  - truth: "Accommodation signal filters and smart allocation satisfy family/gender-aware requirements"
    status: failed
    reason: "Core backend/filter plumbing is present, but the accommodation UI still cannot intentionally set/clear family-group scope and applies an unintended `hasPriority=false` filter when the checkbox is not checked."
    artifacts:
      - path: "app/dashboard/accommodation/page.tsx"
        issue: "`applyFilters()` always sends `hasPriority: hasPriorityFilter`; unchecked checkbox submits `false` instead of neutral/null."
      - path: "app/dashboard/accommodation/page.tsx"
        issue: "No family-group filter input/state setter exists (`familyGroupId` only persists from URL), so operators cannot actively filter by family grouping from the UI."
      - path: "app/dashboard/accommodation/page.tsx"
        issue: "Reset flow does not clear `familyGroupId` because `syncUrlState(...)` call omits that field."
    missing:
      - "Serialize `hasPriority` as `null` when 'Priority only' is unchecked (reserve `false` for explicit inverse filter mode if UI supports it)."
      - "Add explicit family-group filter control/state (or equivalent operator action) and pass `familyGroupId` through apply/reset URL sync."
      - "Ensure reset clears family-group scope by setting `familyGroupId: null`."
---

# Milestone v1.0 Audit Report

**Milestone Goal (from v1 requirements):** Deliver a protected finance operations dashboard with reliable Ticket Tailor ingestion, finance reconciliation visibility, attendee detail, and accommodation workflows.
**Audited:** 2026-03-27T12:41:10Z
**Status:** gaps_found
**Re-verification:** Yes — after gap-closure implementation

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                      | Status     | Evidence                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Admin-only access gates dashboard pages and core operator APIs                             | ✓ VERIFIED | `proxy.ts` still matches `/dashboard(.*)`; `app/dashboard/layout.tsx` uses `requirePageUser`; protected API routes still import/use `requireApiUser` |
| 2   | Integration credentials can be validated/inspected at runtime                              | ✓ VERIFIED | `app/dashboard/integrations/page.tsx` still renders integration status via runtime check path                                                        |
| 3   | Operator can run scoped Ticket Tailor re-sync safely from dashboard                        | ✓ VERIFIED | Sync page POSTs `/api/ticket-tailor/sync`; route still guards with `requireApiUser()`                                                                |
| 4   | Ticket/order status normalization drives downstream reporting                              | ✓ VERIFIED | `runTicketTailorSync` still uses `normalizeTicketTailorStatus` and persists `normalizedStatus`                                                       |
| 5   | Revenue overview and trend reporting are available                                         | ✓ VERIFIED | Dashboard page fetches `/api/dashboard/revenue`; revenue route calls `getRevenueOverview()` and returns JSON payload                                 |
| 6   | Order drilldown and CSV export work from same filter scope                                 | ✓ VERIFIED | Orders page uses `/api/dashboard/orders` and export path `/api/dashboard/orders/export` from the same query state                                    |
| 7   | Outstanding balances/reconciliation list is available                                      | ✓ VERIFIED | Reconciliation page fetches `/api/dashboard/reconciliation`; API route calls `getReconciliationRows()`                                               |
| 8   | Attendee detail shows finance + room status and supports follow-up updates                 | ✓ VERIFIED | Attendee detail page reads `/api/dashboard/attendees/[id]` and sends PATCH updates                                                                   |
| 9   | Accommodation signal filters and smart allocation satisfy family/gender-aware requirements | ✗ FAILED   | Smart-allocation compatibility logic is present, but family-group UI filtering remains incomplete and `hasPriority` filter semantics are miswired    |

**Score:** 8/9 truths verified

### Required Artifacts

| Artifact                                                 | Expected                                              | Status     | Details                                                                                                                                                      |
| -------------------------------------------------------- | ----------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `app/api/ticket-tailor/sync/route.ts`                    | Protected operator sync endpoint                      | ✓ VERIFIED | Exists (132 lines), substantive, and still calls `requireApiUser()` before sync execution                                                                    |
| `lib/domain/accommodation/assignments.ts`                | Signal filter plumbing + compatibility-aware proposal | ✓ VERIFIED | Exists (598 lines), substantive, forwards signal filters (`familyGroupId`, `location`, `genderType`, `allocationPriority`, `hasPriority`) and proposal logic |
| `convex/accommodation.ts`                                | Real signal filtering and family signal derivation    | ✓ VERIFIED | Exists (1200 lines), substantive; `attendeeMatchesSignalFilters()` and `hasFamilySignal()` remain wired                                                      |
| `app/dashboard/accommodation/filter-state.ts`            | Shared signal-filter normalization and URL sync       | ✓ VERIFIED | Exists (144 lines), substantive; normalizes/syncs signal filters and family badge rendering helper uses `hasFamily`                                          |
| `app/dashboard/accommodation/page.tsx`                   | Filter controls wired to API and state refresh        | ⚠️ PARTIAL | Apply/reload wiring improved, but no explicit family-group control and `hasPriority` unchecked state serializes as `false`                                   |
| `app/api/dashboard/accommodation/auto-allocate/route.ts` | Protected auto-allocation endpoint                    | ✓ VERIFIED | Auth guard + domain proposal call remains wired                                                                                                              |

### Key Link Verification

| From                                       | To                                           | Via                          | Status     | Details                                                                                                                         |
| ------------------------------------------ | -------------------------------------------- | ---------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `TicketTailorSyncPage`                     | `/api/ticket-tailor/sync`                    | `fetch` on submit            | ✓ WIRED    | POST body + response handling present; backend auth guard confirmed                                                             |
| `/api/ticket-tailor/sync`                  | Ticket Tailor sync domain                    | `runTicketTailorSync(scope)` | ✓ WIRED    | Route parses scope and returns sync summary                                                                                     |
| `AccommodationPage`                        | `/api/dashboard/accommodation/assignments`   | `loadWorkspace()` fetch      | ⚠️ PARTIAL | Signal query wiring exists, but UI cannot intentionally set/clear `familyGroupId`; unchecked priority sends `hasPriority=false` |
| `/api/dashboard/accommodation/assignments` | `getRoomAllocationBoard`                     | Domain call                  | ✓ WIRED    | Route forwards all signal filters including `familyGroupId`/`location`/`hasPriority`                                            |
| Domain board query                         | `api.accommodation.getRoomAllocationBoard`   | `convexQuery` args           | ✓ WIRED    | Convex query receives and applies signal filters                                                                                |
| `AccommodationPage`                        | `/api/dashboard/accommodation/auto-allocate` | POST on button click         | ✓ WIRED    | Proposal request/response path remains connected                                                                                |
| Auto-allocate API                          | `generateAllocationProposal`                 | Domain call                  | ✓ WIRED    | Compatibility-aware proposal logic still reachable and used                                                                     |

### Requirements Coverage (v1 requirements)

| Requirement | Status        | Blocking Issue                                                                 |
| ----------- | ------------- | ------------------------------------------------------------------------------ |
| FOUND-01    | ✓ SATISFIED   | —                                                                              |
| FOUND-02    | ✓ SATISFIED   | —                                                                              |
| SEC-01      | ✓ SATISFIED   | Protected dashboard/page/API auth scaffolding remains in place                 |
| TT-01       | ✓ SATISFIED   | —                                                                              |
| TT-02       | ✓ SATISFIED   | Manual scoped sync UI/API path exists and remains protected                    |
| TT-03       | ✓ SATISFIED   | —                                                                              |
| TT-04       | ✓ SATISFIED   | —                                                                              |
| TT-05       | ✓ SATISFIED   | Signal extraction fields still present in accommodation payload                |
| DASH-01     | ✓ SATISFIED   | —                                                                              |
| DASH-02     | ✓ SATISFIED   | —                                                                              |
| DASH-03     | ✓ SATISFIED   | —                                                                              |
| DASH-04     | ✓ SATISFIED   | —                                                                              |
| ACC-01      | ✓ SATISFIED   | —                                                                              |
| ACC-02      | ✓ SATISFIED   | —                                                                              |
| ACC-03      | ✓ SATISFIED   | —                                                                              |
| ACC-04      | ✓ SATISFIED   | `hasFamilySignal` derivation + family cohesion scoring remain implemented      |
| ACC-05      | ✗ BLOCKED     | Family-group filter not fully operator-wired; unchecked priority semantics bug |
| ACC-06      | ✓ SATISFIED   | Compatibility-aware proposal flow remains implemented                          |
| FLOW-01     | ? NEEDS HUMAN | Structural links exist; full UX flow quality requires interactive validation   |

### Anti-Patterns Found

| File                                   | Line(s)    | Pattern                                                                           | Severity   | Impact                                                                 |
| -------------------------------------- | ---------- | --------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------- |
| `app/dashboard/accommodation/page.tsx` | 561-564    | `applyFilters()` always serializes `hasPriority` boolean (`false` when unchecked) | 🛑 Blocker | Unchecked "Priority only" applies an inverse filter instead of neutral |
| `app/dashboard/accommodation/page.tsx` | 966-978    | Reset URL sync omits `familyGroupId`                                              | 🛑 Blocker | Family-group scope cannot be reliably cleared from normal filter flow  |
| `app/dashboard/accommodation/page.tsx` | n/a (grep) | No dedicated family-group filter control/state setter in filter form              | ⚠️ Warning | ACC-05 is only partially exposed to operators                          |

### Human Verification Required

1. **Operator flow coherence across finance → attendee → accommodation**  
   **Test:** Run reconciliation row → attendee detail → room assignment → back to reconciliation.  
   **Expected:** Context remains clear, links preserve intent, no dead-end routes.  
   **Why human:** Requires UX judgment and interactive navigation validation.

2. **Visual clarity of accommodation signal indicators**  
   **Test:** Review mixed-gender, mixed-priority, and family-linked queues on accommodation board.  
   **Expected:** Operators can immediately identify priority, family, and compatibility cues.  
   **Why human:** Visual semantics/readability cannot be fully proven statically.

### Gaps Summary

Re-verification confirms the prior implementation gap was partially closed:

1. Signal filter apply/reload wiring now includes the previously missing gender/priority/location paths.
2. Family badge contract mismatch is fixed (`hasFamily` is now used consistently in the UI helper).

However, v1.0 still has a blocking ACC-05 gap:

- Family-group filtering is not fully operable from the accommodation UI (no explicit control and reset does not clear it).
- Priority filter semantics are incorrect when unchecked (`hasPriority=false` is still sent).

Until these are corrected, milestone v1.0 cannot be fully certified as complete.

---

_Audited: 2026-03-27T12:41:10Z_  
_Auditor: Claude (gsd-verifier)_
