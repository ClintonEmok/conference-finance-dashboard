# v4.0 Research: Stack

- No new framework or data library is required.
- Keep the existing Next.js App Router event layout and route segments.
- Use existing Convex React hooks with explicit loading, error, empty, and populated states.
- Keep large ledgers paginated and dashboard aggregates bounded.
- Compose existing shadcn/ui Sidebar, Tabs, Cards, Tables, Select, Sheet, and Drawer primitives.

Official documentation checked via Context7 on 2026-07-29: Next.js `/vercel/next.js`, Convex `/get-convex/convex-backend`, and shadcn/ui `/shadcn-ui/ui`.
